import KlarnerConstant.GeometricQEndpoint

/-!
# The complete geometric `Q` recurrence

Compatibility facade for the split `Q` development.  The dependency order is
`GeometricQCore`, `GeometricQGeometry`, then `GeometricQEndpoint`.
-/
