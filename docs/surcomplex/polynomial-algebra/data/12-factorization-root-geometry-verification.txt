SURCOMPLEX POLYNOMIAL ALGEBRA — EXACT FINITE CHECKS
Python 3.14.4; SymPy 1.14.0
All computations use exact rational/symbolic arithmetic; no numerical root approximation is used.

PASS: generic monic degrees 1..6: residue dual bases, inverse Gram matrices, determinants, Bezout kernels, Euler/derivative identities, trace identities on every basis vector, Newton sums through 2n, and norms of X^2+2X+3. Generic trace-discriminant determinants checked for degrees 1..4.
PASS: degree-7 factored polynomial, including infinite, finite, infinitesimal, and repeated roots: 27 center/scale charts (centers 0,t,1; integer scales -3..5). Exact weights, closed/open counts, all residue multiplicities, and derivative reductions checked.
PASS: cubic two-scale reductions, three displayed root truncations (residual orders >=9, >=9, >=13), and exact critical points. Quartic distance/derivative data, D=10 with leading coefficient 16, T(P)=7, three branching reductions, and all four leading shifts for P+t^8. Strict-boundary quadratic collision checked at h=3.
PASS: explicit coprime factor recursion through t^6 for (X-1)(X^2+1)+t(X^2+2X+3)+t^2(X-4). Parameter root factors of X^2-s^2+t checked through t^5 over Q(s), s nonzero.
PASS: Hermite CRT with multiplicities 2 and 3 at infinitesimally separated nodes; all idempotent and jet identities. Credited cubic residue matrix and repeated-root residue formula on monomials 0..6. Branch-value discriminant formula in degrees 2..5, including repeated critical points.
PASS: exact modulus coefficient-phase witness at z=1+2i with three independent weights; exact one-coefficient strict valuation witness at z=t^2. These are finite samples of the proved uncertainty formulas, not universal numerical verification.

PASS: 478 assertions completed in 11.20 seconds.

LIMITATIONS
These checks do not implement arbitrary surreal coefficients or arbitrary-rank Hahn fields.
They do not prove transfinite summability, real-closed transfer, the general root matching theorem, or the Nullstellensatz.
They are reproducible checks of finite identities and worked instances; the manuscript contains the general proofs.
