p = "C:/ProveIt/Analysis/ExponentialIdentities/Article/alaoglu-erdos-unified-report/alaoglu-erdos-unified-report.tex"
t = open(p, encoding="utf-8").read()
def rep(old, new):
    global t
    assert t.count(old) == 1, old[:70]
    t = t.replace(old, new)

rep(r"""If $2^x,3^x\in\Z$ and $2^x<8192$, then $x\in\Z$.  Equivalently, every nonintegral solution
satisfies $x\ge13$, and every candidate that is not a power of two is at least $8192$.
\end{theorem}

\begin{statusbox}{\statuslean{} ---
\lean{integer_of_two_three_rpow_integer_of_two_rpow_lt_8192},
\lean{thirteen_le_of_not_integer_of_two_three_rpow_integer},
\lean{le_of_twoBaseNonintegerCandidate_8192} in \lean{FiniteCheck8192}, extending
\lean{FiniteCheck4096} ($2^x<4096$, hence $x\ge12$), \lean{FiniteCheck256} ($2^x<256$,
hence $x\ge8$), and \lean{FiniteCheck} ($2^x<100$).  Statements elsewhere in this report that
quote the earlier kernel bound $x\ge12$ remain valid a fortiori; the $4096$ module is kept as
an independent, separately replayable certificate.}
\end{statusbox}
""", r"""If $2^x,3^x\in\Z$ and $2^x<16384$, then $x\in\Z$.  Equivalently, every nonintegral solution
satisfies $x\ge14$, and every candidate that is not a power of two is at least $16384$.
\end{theorem}

\begin{statusbox}{\statuslean{} ---
\lean{integer_of_two_three_rpow_integer_of_two_rpow_lt_16384},
\lean{fourteen_le_of_not_integer_of_two_three_rpow_integer},
\lean{le_of_twoBaseNonintegerCandidate_16384} in \lean{FiniteCheck16384}, extending
\lean{FiniteCheck8192} ($2^x<8192$, hence $x\ge13$), \lean{FiniteCheck4096} ($x\ge12$),
\lean{FiniteCheck256} ($x\ge8$), and \lean{FiniteCheck} ($2^x<100$).  Statements elsewhere
in this report that quote an earlier kernel bound remain valid a fortiori; each module is an
independent, separately replayable certificate.}
\end{statusbox}
""")

rep(r"""tier populations for $4096\le m<8192$ are $826$, $3120$, $71$, $12$, $33$, $2$, $2$, $28$,
$2$.  For the $4096$ table:
""", r"""tier populations for $4096\le m<8192$ are $826$, $3120$, $71$, $12$, $33$, $2$, $2$, $28$,
$2$.  The extension to $16384$ (\lean{FiniteCheck16384}) replays $8191$ rows in $39$ tiers
drawn from the semiconvergent ladder; its hardest value, $m=9329$, needs the enclosure
$5914137/3731405$ (exponents near $6\cdot10^6$, integers of $78$ Mbit), which the kernel's
GMP-backed \lean{Nat.pow} compares in about ten seconds, and the whole table replays in under
eight minutes.  The tables were generated by a single script (\lean{mpmath} floor, exact
big-integer re-verification of every row, tiers as distinct enclosure pairs), so each
further doubling is mechanical.  For the $4096$ table:
""")

rep(r"""\textbf{Kernel.}  $2^x<8192$ and $2^x,3^x\in\Z$ imply $x\in\Z$; a nonintegral solution has
$x\ge13$.  \statuslean{} \lean{FiniteCheck8192} (and, independently, $2^x<4096$ in
\lean{FiniteCheck4096}).""",
    r"""\textbf{Kernel.}  $2^x<16384$ and $2^x,3^x\in\Z$ imply $x\in\Z$; a nonintegral solution has
$x\ge14$.  \statuslean{} \lean{FiniteCheck16384} (and, independently, $2^x<8192$ and
$2^x<4096$ in \lean{FiniteCheck8192}, \lean{FiniteCheck4096}).""")

rep(r"""kernel-verified statement, that threshold is $12$ or $13$.

\paragraph{The $8192$ extension and the drafted $16384$ region.}
The kernel region was raised from $2^x<4096$ to $2^x<8192$ by a single further table of
$4095$ rows (\lean{FiniteCheck8192}, Section~\ref{co:finite}); the only new ingredient is a
semiconvergent enclosure for the two hardest values.  An earlier attempt at $2^x<16384$
exists in draft: it splits the enlarged table into four chunks, every chunk's Boolean
recursion and every row's exact comparison are accepted, but the per-chunk length lemmas
that glue the chunks into one covering statement exceeded the elaborator's recursion limit,
so no theorem is available from it.  \statusdraft{}  The $8192$ module shows that a single
unsplit table of $4096$ rows with exponents up to $4.8\cdot10^5$ replays comfortably, so the
natural next step is one more unsplit table for $[8192,16384)$, whose hardest rows will
again need semiconvergents rather than the $10^7$-denominator convergent.""",
    r"""kernel-verified statement, that threshold is $12$, $13$, or $14$.

\paragraph{The $8192$ and $16384$ extensions.}
The kernel region was raised from $2^x<4096$ to $2^x<8192$ and then to $2^x<16384$ by two
further unsplit tables (\lean{FiniteCheck8192}, \lean{FiniteCheck16384},
Section~\ref{co:finite}); the only new ingredient is the use of semiconvergent enclosures for
the hardest values.  An earlier chunked draft of the $16384$ region had failed on the
per-chunk length lemmas; the unsplit table avoids the problem entirely, and the $8191$-row
list literal and its length lemma are accepted with \lean{maxRecDepth} $200000$.  The next
doubling, $[16384,32768)$, will need the $10^7$-denominator convergent or its
semiconvergents for a handful of rows; the $m=9329$ timing shows this is minutes, not hours.""")

rep(r"""\item $x\ge13$, by the kernel-checked zero-free region $2^x<8192$.  \statuslean""",
    r"""\item $x\ge14$, by the kernel-checked zero-free region $2^x<16384$.  \statuslean""")
rep(r"""zero-free region $2^x<8192$; transcendence of every hypothetical counterexample; multiplicative""",
    r"""zero-free region $2^x<16384$; transcendence of every hypothetical counterexample; multiplicative""")
rep(r"""The zero-free region $2^{x}<8192$ is kernel-verified, while the current software frontier is
$2^{27}$ (\statuscomp{}) in Section~\ref{sec:finite27}.  The next target is one more
generated, kernel-replayed table for $[8192,16384)$; the semiconvergent device of
\lean{FiniteCheck8192} keeps its exponents far below the $10^7$ scale of the next convergent.""",
    r"""The zero-free region $2^{x}<16384$ is kernel-verified, while the current software frontier is
$2^{27}$ (\statuscomp{}) in Section~\ref{sec:finite27}.  Each further doubling is one
generated, kernel-replayed table; the semiconvergent device of \lean{FiniteCheck8192} and
\lean{FiniteCheck16384} keeps the exponents manageable.""")
rep(r"""$2^x<8192\Rightarrow x\in\Z$\ \ (so $x\ge13$) & \statuslean & \lean{FiniteCheck8192} \\""",
    r"""$2^x<8192\Rightarrow x\in\Z$\ \ (so $x\ge13$) & \statuslean & \lean{FiniteCheck8192} \\
$2^x<16384\Rightarrow x\in\Z$\ \ (so $x\ge14$) & \statuslean & \lean{FiniteCheck16384} \\""")
rep(r"""\lean{.../FiniteCheck8192} & zero-free region $2^x<8192$ (one unsplit \lean{decide} table,
nine tiers, one semiconvergent enclosure) \\""",
    r"""\lean{.../FiniteCheck8192} & zero-free region $2^x<8192$ (one unsplit \lean{decide} table,
nine tiers, one semiconvergent enclosure) \\
\lean{.../FiniteCheck16384} & zero-free region $2^x<16384$ ($8191$ rows, $39$ tiers,
exponents to $5.9\cdot10^6$) \\""")
rep(r"""the kernel-verified zero-free region moved from $2^x<4096$ to $2^x<8192$
(Theorem~\ref{co:thm-finite4096}), so a counterexample now has $x\ge13$ with kernel trust.""",
    r"""the kernel-verified zero-free region moved from $2^x<4096$ to $2^x<16384$
(Theorem~\ref{co:thm-finite4096}), so a counterexample now has $x\ge14$ with kernel trust.""")
rep(r"""The kernel-verified region was raised during this session
from $2^x<4096$ to $2^x<8192$ (Theorem~\ref{co:thm-finite4096}); the hardest rows need a
semiconvergent enclosure, not the next convergent, which keeps every exponent below
$5\cdot10^5$.  For $[8192,16384)$ the hardest value is $m=9329$, whose enclosure
$5914137/3731405$ the kernel checks in ten seconds (exact Python needs a hundred).  The
remaining gap to the software frontier $2^{27}$ is $14$ further doublings of purely
mechanical work.""",
    r"""The kernel-verified region was raised during this session
from $2^x<4096$ to $2^x<8192$ and then to $2^x<16384$ (Theorem~\ref{co:thm-finite4096});
the hardest rows need semiconvergent enclosures rather than the next convergent.  The
$16384$ table has $8191$ rows, its hardest value $m=9329$ uses the enclosure
$5914137/3731405$ (ten seconds in the kernel, a hundred in exact Python), and the whole table
replays in under eight minutes.  The remaining gap to the software frontier $2^{27}$ is
$13$ further doublings of mechanical work.""")
rep(r"""\item \textbf{Fourteen doublings.}""", r"""\item \textbf{Thirteen doublings.}""")
open(p, "w", encoding="utf-8", newline="\n").write(t)
print("ok")
