# Fabius paper coverage

This file maps the named mathematical results in the two source papers to
their proved Lean declarations.  It also audits the numbered equations and
substantive prose claims in the two local TeX drafts.  All cited Lean names
are available from the public import `FabiusFunction`.

For a human-readable synthesis rather than a declaration-by-declaration map,
see its
[LaTeX source](Fabius_Function_and_Rvachev_Up/Fabius_Function_and_Rvachev_Up.tex)
or [rendered PDF](Fabius_Function_and_Rvachev_Up/Fabius_Function_and_Rvachev_Up.pdf).

> **Artifact status (2026-09-04).**  The authoritative live lexical census and
> zero-gap result are recorded in the [documentation audit](DOCUMENTATION_AUDIT.md#findings)
> and pinned in `docs/doc_audit_baseline.json`; this overview does not duplicate
> their live numeric receipt.
> The frozen upstream tip's 957 modules and 11,920 declarations are a
> historical waypoint.  The retained unconditional public
> `complexQPochhammerInf_eq_qPochhammerInfIn` bridge in
> `RvachevPochhammerFactorization` gives the historical 957/11,921 merge-union
> checkpoint, and `exists_eq_in_residual_interval` in `MeanValueBracket`
> gives the historical 957/11,922 residual-certificate checkpoint.  Four Bell
> declarations, nine repeated-differential declarations, ten Cauchy
> declarations, and the reverse-row Stirling theorem are included in that
> authoritative receipt.
> The live audit reports no
> missing module headers or declaration comments; the exact-dyadic inverse,
> Jacobi two-square, Lagrange--Rvachev Matrix, geometric Richardson,
> Gaussian-binomial second-moment, fixed-column rate, and half-base root
> simplicity, Rvachev--Appell Hasse,
> arbitrary-space geometric-uniform
> realization, regular central q-binomial sum, and Lambert branch-gap
> Bernoulli additions, together with the geometric-uniform moment-polynomial,
> its real-MGF normalization bridge, Lagrange-nodes-only, greater-than-one
> Gaussian asymptotic, Thue--Morse Gamma-tower differential, local
> Thue--Morse corner-integral, central Rvachev--Legendre cancellation, and
> finite Legendre--Rvachev biorthogonality, finite-prefix Thue--Morse Appell
> collapse, the base-two Prouhet bridge, dyadic Fourier-boundary with its
> quotient form, strengthened Newman self-similarity, infinite central
> q-Vandermonde, q-Pochhammer Lambert-form, triangular-power-product, and
> mean-value-bracket
> leaves, together with the strengthened affine finite-polynomial functional
> and the complex geometric-uniform moment-product bridge with its public
> entire-function theorem, the exterior reciprocal-germ and combined
> reciprocity bridges, the sharp geometric-uniform moment-polynomial degree
> leaf, the global moment RatFunc, the centered-MGF Laurent-leading theorem,
> finite-prefix Appell recovery, and the strengthened closed up-tail and
> positive-moment closure, are included in
> that clean census.  This crosswalk records all
> 30 declarations in `LegendreGauntClosedForm.lean` and
> `FabiusLegendreGauntClosedForm.lean`, together with the prime-power,
> centered-Appell, arbitrary-phase synthesis, entire q-Pochhammer, and general
> finite/infinite q-Pochhammer dissection leaves.  The q-series inventory keeps
> `RvachevPochhammerFactorization` 1+10,
> `QPochhammerEntire` 0+5, `GeometricPochhammerNormalConvergence` 0+3,
> `QPochhammerDissection` 0+2,
> `QPochhammerInfinite` 1+29, `QPochhammerLambertForm` 0+5,
> `GaussianBinomialAtNegOneDerivative` 0+5,
> `HalfQBinomialRootSimplicity` 0+1,
> `GaussianBinomialContinuity` 0+3,
> `GaussianBinomialCumulants` 2+24,
> `GaussianBinomialPalindromic` 0+14,
> `GaussianBinomialPolynomialStructure` 0+5,
> `CentralQBinomialReduction` 0+6, `RegularCentralQBinomialSum` 2+1,
> `CentralQVandermondeInfinite` 0+4,
> `CyclotomicFactorization` 0+7,
> `JacobiTripleProduct` 2+25,
> `QBinomialTheoremInfinite` 1+27,
> `GaussianBinomialFixedColumnRate` 0+9,
> `GaussianBinomialGreaterOneAsymptotics` 0+2, `QPascalSummation` 0+4,
> `QuantumBinomial` 0+2, `RogersSzegoPolynomial` 1+9,
> `QPochhammerInfiniteBounds` 0+5, `HeineTransformation` 2+5,
> `QGaussSummation` 0+2, `QPochhammerComplexOrder` 1+4,
> `BasicHypergeometricSeries` 2+5, `QMultinomial` 1+9,
> `JacksonIntegral` 1+7,
> `QExponential` 3+8, `ThetaQuasiPeriodicity` 1+6,
> `JacobiCubic` 0+2,
> `QPochhammerLogDerivative` 0+10, `QPochhammerOrderDerivative` 0+3,
> `PrimitiveRootBlock` 0+3, `QLucas` 0+7,
> `CyclotomicDivisibility` 0+3, `QCatalan` 1+11,
> `NewtonInterpolation` 3+19, `QBetaIntegral` 1+8,
> `GaussianBinomialInteger` 1+10, `GaussianBinomialComplexOrder` 1+5,
> `QPfaffSaalschutz` 0+3, `TwoPhiOneReversal` 2+12,
> `QChuVandermonde` 0+10, `JacobiTwoSquareCount` 0+4,
> `QuantumMultinomial` 0+5, and `GaussianBinomialBounds` 0+6.  The
> geometric-interpolation union also includes
> `GeometricRichardsonGenerating` 3+7, `RvachevAppellHasse` 1+14,
> `RvachevLagrangeNodesOnly` 1+14, and `FinitePrefixAppellRecovery` 11+17.
> The Fourier/inverse union also includes `RvachevLaurentLeading` 1+6, and the
> Thue--Morse continuation union includes
> `ThueMorseGammaTowerDifferential` 0+3.
> The Lambert branch-gap union adds `LambertWBranchGapBernoulli` 0+5.  Its
> `summable_bernoulli_mul_pow_div_factorial_iff` theorem proves for every
> complex `z` that the Bernoulli exponential generating series is summable
> exactly when `‖z‖ < 2π`, hence diverges on the boundary and throughout the
> exterior.  Its new `hasSum_bernoulli_mul_pow_div_factorial_complex_iff`
> theorem gives the canonical removable complex value
> `(complexExpm1Div z)⁻¹` exactly on that disk, including value `1` at
> `z = 0`; it does not identify the sum with Lean's literal totalized quotient
> at zero or assert holomorphy.  The exhaustive theorem names, the real
> evaluation and branch hypotheses, and the remaining excluded claims appear
> in the Lambert Guide row below.  With the three finite branch-coordinate
> modules, the four-module Lambert union is 4+37, forty-one declarations.
> The geometric-uniform moment-polynomial union retains
> `GeometricUniformMomentPolynomial` 1+8,
> `GeometricUniformMomentPolynomialBridge` 0+1,
> `GeometricUniformComplexMomentProduct` 1+3,
> `GeometricUniformExteriorComplexMomentGerm` 1+2, and
> `GeometricUniformMomentPolynomialDegree` 0+3.  The historical target chronology closes
> at 923 modules and 11,610 declarations; retaining the unconditional public
> Pochhammer bridge yielded the `b899` 923/11,611 semantic union.
> The five-module polynomial q-calculus increment is retained exhaustively:
> `PolynomialQDerivative` 2+17, `PolynomialQLeibniz` 0+4,
> `QPochhammerDerivative` 0+3, `LambertSeriesLog` 0+4, and `QGamma` 2+10,
> totaling four definitions and thirty-eight theorems, forty-two declarations.
> The bounds module reuses `finiteQPochhammerIn_self_pos` from
> `GeneralQConditionNumber`; it does not redeclare it.  The rigorous forward
> q-monograph ledger is 181 Exact, 79 Partial, 14 None, and 8 interface
> rows; its source concordance is 103 Lean, 375 human, 60 N/A, and 9 conjecture
> rows, and the concordance extractor passes.  `prop:gaussian-bound` is Exact;
> `thm:q-lucas` and
> `cor:babbage-derivative` remain Partial at their stated polynomial-congruence
> and derivative boundaries.
>
> **Publication status.**  The fixed-26 publication check—14 fresh
> build cycles and 12 retained verified pairs—records the prior merge's
> user-directed deferral in the
> [draft manifest](semi-formalized-research-frontiers/drafts/MANIFEST.md#fixed-26-publication-checkpoint).
> A row still marked pending makes no synchronization claim.  The exact older
> receipts below remain historical evidence, and the fixed-26 table is an
> inventory rather than a merged-current parity receipt.  Fresh regeneration
> for the present merged source is in progress; parity requires final gates and receipts.
>
> Four exact direct receipts record the last synchronized pre-9135 source/PDF
> pairs and are now historical because the live sources include the new
> q-Chu/reversal, geometric-generating, Gaussian second-moment, Lambert
> branch-gap Bernoulli, and geometric-uniform moment-polynomial APIs.  The
> primary exposition's
> 14,037-line, 702,119-byte TeX has SHA-256
> `6a20e02cf300c0b29ba8d175831b4f86e4b336601cc5bd5f5752d5c5889be69a`,
> and its 197-page, 1,602,500-byte PDF has SHA-256
> `f083cd78308aba99d23d42372786c4b0a946ea8f5d47445c44d664fccfdde5e3`.
> The Lean walkthrough's 6,598-line, 465,231-byte TeX has SHA-256
> `796dd849fa423ba07413eaf0a1f30dc608355c5a3cd877aa7409ad089c54794e`,
> and its 149-page, 1,231,442-byte PDF has SHA-256
> `bc6e3e716a1a10daf24a065f6c97e2d00cbc95071ada777050a95f91598db4a0`.
> The canonical frontier's 17,954-line, 813,297-byte TeX has SHA-256
> `bcd9eefce2ead08e2cbb283e091a859aa31f36c67416543e994e10e8f9db3075`,
> and its 262-page, 1,885,642-byte PDF has SHA-256
> `7f7e1279e38c766a465e640638ea7e0079a942de0bc84a5c22be497af27c7bab`.
> The q-series master's 16,834-line, 837,715-byte TeX has SHA-256
> `4785625c1399558f3ca59481888fc76514e0a327a1faa16945c61851f874f3d5`,
> and its 395-page, 2,494,961-byte PDF has SHA-256
> `89159b2635f489a42d4c972fac95332808b1d637dee7921085db1ed7d6e055af`.
> Their exact successful three-pass page sequences were respectively
> 194→197→197, 144→149→149, 254→262→262, and 386→395→395.  Primary and
> walkthrough logs and publication gates are clean; the frontier retains only
> expected underfull diagnostics, and the q-series master retains one harmless,
> readable 32.5659 pt overfull line.  All page, metadata, font, render, text,
> and representative-visual gates passed.  These receipts certify their named
> pre-9135 pairs only and are superseded by the synchronized 2026-09-04 receipts
> below.
>
> The formerly independent Sequence publication receipt is historical: its
> material is now absorbed into the consolidated `Transseries_And_Inversion`
> root listed in the fixed-26 checkpoint.  The Lambert
> guide's preceding synchronized receipt is a 4,829-line, 174,423-byte TeX with SHA-256
> `724dfe5b1effcda29325a5bdfb066ff970eb74ab460f650185339fefce40ebc1`;
> its 69-page, 952,929-byte PDF has SHA-256
> `0b5f28dbfe590658e74150e8ccff6f023ecd0b8fb4e3e978ec275d9ddd244de6`.
> Its successful page sequence was 67→69→69; machine and visual gates passed,
> with expected underfull diagnostics and one harmless readable 0.825 pt
> internal overfull line.  The Bernoulli-series source overlay made that
> Lambert PDF historical; the synchronized 2026-09-04 receipt below supersedes
> it.  The Sequence inversion/transseries volume's
> 16,705-line, 778,477-byte TeX has SHA-256
> `4aa038c10ddd931b7c1248095ddfdf0ce8769c69cc0df4f344f6365d0e45e8e1`;
> its 205-page, 2,198,655-byte PDF has SHA-256
> `ec1f4d2ac608786f33be97d040fdfd03b6f74494dee74f044fd2e6631217d4fb`.
> Its successful page sequence was 198→205→205; corrected title/author metadata,
> machine gates, and extensive visual checks passed.  The final log retains one
> duplicate-page-destination notice, nine PDF-string notices, 47 overfull and
> 12 underfull diagnostics; sampled largest cases are clean and unclipped.
>
> **Historical synchronized publication receipts (2026-09-04).** Each pass
> tuple below is `pages/bytes`.  At their named source checkpoints, all six
> TeX/PDF pairs completed exactly three
> successful serial halt-on-error passes from absent sidecars.  Final-log
> reference/rerun/error checks, metadata, A4 rotation zero, every-page render
> and nonblank-text checks, embedded/subset fonts with Libertinus and no Type 3,
> and representative visual checks all passed; generated sidecars were cleaned
> and forbidden checksum-ledger basenames were absent.  These six pairs remain
> historical receipts for their named source checkpoints; the later historical
> `b899` receipts below superseded them where a rebuilt root was listed.
>
> - Primary exposition: TeX 14,328 lines / 715,760 bytes / SHA-256
>   `60c0a6ff4e75ec37e6928067859671d87622ad8f430a1006dd4c71c7e7b25674`;
>   passes 197/1,579,558 → 200/1,621,473 → 200/1,621,467; final PDF
>   200 pages / 1,621,467 bytes / SHA-256
>   `50febffeb7dda743330bd346b8f5fd45f85668db97c19fb52d4cd741d1692826`;
>   fonts 29 total / 6 Libertinus / 0 Type 3.
> - Lean walkthrough: TeX 6,855 lines / 482,759 bytes / SHA-256
>   `1c48c54b194eb9e99dae64ddca70e2aa5d2edd995160ee2d5bc6455b545683f7`;
>   passes 149/1,225,017 → 154/1,262,552 → 154/1,262,574; final PDF
>   154 pages / 1,262,574 bytes / SHA-256
>   `f9cba79348ffb81c41fc08b6523548effcc45ded9a2eb2b18e618ac9d59d0648`;
>   fonts 30 / 7 / 0.
> - Lambert Guide: TeX 4,876 lines / 177,511 bytes / SHA-256
>   `d852a345685dd61335a89fc4fd1092680bdc597a5d1e6ac612883946ad0d99ea`;
>   passes 68/963,230 → 70/986,865 → 70/986,865; final PDF 70 pages /
>   986,865 bytes / SHA-256
>   `0b8801649a6dd43d9f02dcfc2f60cac50b5c8f88bd782645bf97d30cc3dfbd41`;
>   fonts 42 / 5 / 0; one harmless readable 0.82504 pt overfull and 133
>   underfull diagnostics.
> - Canonical frontier: TeX 18,173 lines / 826,738 bytes / SHA-256
>   `844842bf699a24651f660bd7d81d814f6396b4fe6fc6de66a04908904221860b`;
>   passes 257/1,822,725 → 265/1,904,567 → 265/1,904,551; final PDF
>   265 pages / 1,904,551 bytes / SHA-256
>   `dcaa7ac1e5397912c97a474b4023521e49d0785eb6ef67d83d0ce002d9cbb6e6`;
>   fonts 40 / 8 / 0; one readable unclipped 9.43108 pt overfull at source
>   lines 1032–1043 and 299 underfull diagnostics.
> - Geometric q-frontier: TeX 27,598 lines / 1,270,870 bytes / SHA-256
>   `6db4e211b0588ed75a0e89e13d97306f1d5d38b42a2bf941914ea16b9ca93dae`;
>   passes 386/8,157,293 → 403/8,339,780 → 403/8,339,736; final PDF
>   403 pages / 8,339,736 bytes / SHA-256
>   `4d909b5e228e2053d473dc75da502382c7a4fe2b096f798e124e6530d3a15027`;
>   fonts 43 / 11 / 0; zero overfull and 37 underfull diagnostics.
> - Canonical q-series synthesis: TeX 16,910 lines / 842,514 bytes / SHA-256
>   `196f219d5e1efba463ebabb69659697b1afb28989ef1a8da6219226d3262ad32`;
>   passes 390/2,386,364 → 398/2,501,624 → 398/2,501,638; final PDF
>   398 pages / 2,501,638 bytes / SHA-256
>   `e8094b054f52b1fb71c7540f0834155fae0eac17887cb7cac1567848bd65d3b3`;
>   fonts 43 / 5 / 0.  Every pass's index run accepted 164 entries, rejected
>   none, produced 254 lines, and emitted no warning.  The sole retained
>   32.5659 pt overfull paragraph at source lines 590–598 is readable and
>   unclipped; the final log has zero underfull diagnostics.
>
> **Historical `b899` synchronized publication receipts (2026-09-04).** Each pass
> tuple below is `pages/bytes`.  All ten roots were frozen and built in exactly
> three serial halt-on-error passes from absent sidecars.  Subsequent merged
> source changes make these receipts historical; current rebuild state is
> recorded only in the fixed-26 checkpoint linked above.
>
> - Primary exposition: TeX 15,148 lines / 759,509 bytes / SHA-256
>   `721cb901de2254ef48991452c4831762f54a36e0a405b4bbeb7f812653e71754`;
>   passes 208/1,640,077 → 210/1,683,143 → 210/1,683,141; final PDF
>   210 pages / 1,683,141 bytes / SHA-256
>   `afe85efec5716fe85cc7d8a5d6af459fd72775526f4bda11df04e6ff275b36c9`;
>   fonts 29 total / 6 Libertinus / 0 Type 3.
> - Lean walkthrough: TeX 7,260 lines / 526,929 bytes / SHA-256
>   `2005d4a70a66a1d8f3eac9be6d83585ea70c9312b09b098f2c65025db9fca814`;
>   passes 160/1,281,609 → 165/1,319,585 → 165/1,319,594; final PDF
>   165 pages / 1,319,594 bytes / SHA-256
>   `b5e886d7c76db56fd9e9e1552bdd78fbd65ea9075ea1e04d62c00a7c04e948fb`;
>   fonts 30 / 7 / 0.
> - Geometric q-frontier: driver 27,671 lines / 1,275,367 bytes / SHA-256
>   `d47c0ad93eb359d13e7e9772668f16dbc98bcb4d880f3679366e1d461451bbcd`;
>   recursive TeX closure 8 files / 27,777 lines / 1,281,413 bytes / digest
>   `39f7cd41e706314f2cafb903c2da2e6e83d2b17f5bb0612492204d15c1a28d91`;
>   passes 388/8,163,847 → 405/8,346,265 → 405/8,346,247; final PDF
>   405 pages / 8,346,247 bytes / SHA-256
>   `fef7d8260543ad1d20d69e9e41fa0cfc31603de7961f6aeb97a50740aecd596c`;
>   fonts 43 / 11 / 0.
> - Canonical q-series synthesis: driver 17,265 lines / 864,659 bytes /
>   SHA-256
>   `4dd3f7fb22387d8e3d039e8d49cd870a63ebe0881f7f215c7074854825a27bb9`;
>   recursive TeX closure 14 files / 26,762 lines / 1,210,902 bytes / digest
>   `b567430fdd64f6d50bd24fcb070216c27f7e3e81e8b0c76c3228767ebdf980c6`;
>   passes 397/2,417,476 → 405/2,533,717 → 405/2,533,715; final PDF
>   405 pages / 2,533,715 bytes / SHA-256
>   `055eb1fc26467857394a5b3bd8cd327f6985ea5d2f966ab5f099ac20bb2b8fb2`;
>   fonts 43 / 5 / 0; every pass's index run accepted 164 entries, rejected
>   none, produced 254 lines, and emitted no warning.
> - Inverse-theory synthesis: driver 293 lines / 11,514 bytes / SHA-256
>   `92fab1fae38bbcf86a45b51bfe7ff34e2801361df9d2f3d6aa3de4dc966eaa3c`;
>   recursive TeX closure 17 files / 10,682 lines / 431,748 bytes / digest
>   `6e4e6fde424fd5046467b1f1cec0c19b6c10eb681fae4ba7cc53e14b6a5bf61e`;
>   passes 132/1,983,313 → 137/2,045,485 → 137/2,045,486; final PDF
>   137 pages / 2,045,486 bytes / SHA-256
>   `cee0de894656562fbdb75d6304055fc03fae06203985119419e465a5cd213995`;
>   fonts 31 / 6 / 0.
> - Comb-interpolation synthesis: driver 187 lines / 6,724 bytes / SHA-256
>   `a4c1e33165ff7291682cd890f23fe4af98e9f11f7ad1d9a7f8b68c78d53f9a56`;
>   recursive TeX closure 15 files / 12,597 lines / 477,163 bytes / digest
>   `9e22455b3f65eb48306ad21c57445b6052a56498cb363666ffb9b160f5cc8090`;
>   passes 153/2,383,950 → 160/2,467,995 → 160/2,468,000; final PDF
>   160 pages / 2,468,000 bytes / SHA-256
>   `ad8587049580e6fde371f534b6f8b4e56fa4c929173f87d3021ed369e5225d4c`;
>   fonts 33 / 7 / 0.
> - Lambert Guide: TeX 4,940 lines / 181,577 bytes / SHA-256
>   `2e6a4782fc4e4b945869f5fb45b39cf94e8dc34296866edf26b4cdfe19b1898b`;
>   passes 68/968,083 → 70/991,847 → 70/991,848; final PDF 70 pages /
>   991,848 bytes / SHA-256
>   `f802d78299f8f6aca7d31b935a4884f9343389a7307decb04c18b5159c8a4f04`;
>   fonts 42 / 5 / 0.
> - Up Polynomial Synthesis: driver 2,368 lines / 98,609 bytes / SHA-256
>   `95d293e34559e910cca2df4547e6e181a8d26bc8e8cf61c4445cf12c57ed8e0e`;
>   four-file TeX closure 5,434 lines / 211,270 bytes / digest
>   `62aa76428089cd164705b1d31e038d4e48545681eedc01cb491e6a94f07b0e41`;
>   passes 61/1,045,488 → 62/1,071,179 → 62/1,071,181; final PDF
>   62 pages / 1,071,181 bytes / SHA-256
>   `99c5d8256b983652755fe8e46ef015277e61b94941a4ca6c875bddaf0493b101`;
>   fonts 27 / 4 / 0.
> - Thue--Morse Atlas: TeX 10,553 lines / 481,614 bytes / SHA-256
>   `cced4128c359ec467baaf1a55c21c68424397f783a39ea7fe2af5a94975b9dd5`;
>   passes 139/1,681,559 → 144/1,739,891 → 144/1,739,884; final PDF
>   144 pages / 1,739,884 bytes / SHA-256
>   `1c81863b0976017fab1b7f5972c50cd541b3ffb05306bf85994548a56a782fc0`;
>   fonts 38 / 8 / 0.
> - Canonical semi-formalized frontier: TeX 18,651 lines / 858,502 bytes /
>   SHA-256
>   `140256058b7a01bcdb4f1592cfab9e6c2ac170f5f0863572627a9b2f93ab7793`;
>   passes 265/1,868,249 → 273/1,950,120 → 273/1,950,112; final PDF
>   273 pages / 1,950,112 bytes / SHA-256
>   `17525c7623bf774f515ecf1a949d533bbe125fde036356c4bb9f787eedad0322`;
>   fonts 40 / 8 / 0.
>
> Across all ten roots, required final-log error/reference/rerun
> gates, metadata, A4/rotation-zero checks, every-page render and nonblank-text
> checks, embedded/subset fonts with Libertinus and no Type 3, representative
> visual checks, sidecar cleanup, and the forbidden-checksum-basename search all
> passed. Diagnostics are clean except for five minor q-series horizontal boxes
> (maximum 10.14 pt), two inverse horizontal boxes (2.42 and 2.45 pt), one
> Lambert horizontal box (0.83 pt), and one canonical-frontier horizontal box
> (9.43108 pt), all nonblocking. The final aggregate TeX closure contains 76
> files / 140,223 lines / 6,439,569 bytes, with direct aggregate digest
> `ae8690ad8d160055cbae36eff96d858f87572d171e7aacf7540d67543998af21`.
> All ten rows were synchronized at the named `b899` checkpoint; they and the
> earlier receipts below remain historical provenance for their named sources.
>
> For provenance, the superseded pre-d8b pairs remain historical receipts:
> primary TeX/PDF `938517a92565685ac9f7194b879cfe752ce783f258bde8b7b685aee41aed13dc` /
> `bf26d78dd2cc49feb87a85413ef9c04c7a8a3dac4f793cf86e3436f7502cb2a7`
> (694,350 / 1,593,577 bytes; 195 PDF pages); walkthrough
> `e598aa02d4d10eda8bcfdafe3731f4a663bdcba58407f454485fae6796b41050` /
> `5ff79c24fbced37dfaa5eb9c34447d0e7661b2b2bc5a0597687e43f93d7e189a`
> (456,855 / 1,219,336 bytes; 145 pages); frontier
> `7dd140370a0ac68522364a83a3c6423df93570741eafaef2ee8c1fac17670e2f` /
> `9d38ab9d43befd6e26fd06ab9680b4a761365fb9d8a9f0de18489c243bd62d3e`
> (808,185 / 1,877,159 bytes; 260 pages); and q-series
> `d8f730b8eb6602d4d16112aea77a3e67dfbeadf46bcd28c1cdf3b12450b7d4fb` /
> `5d25df07e6df1cd32118ee87e64c1cc54ad32da7c578a182231f98dd9fee9d5c`
> (837,715 / 2,494,949 bytes; 395 pages).  Representation Frontiers and the
> Integration-and-Transform master retain exact historical 301- and 377-page
> receipts in the frontier manifest and their local group records; those
> receipts certify only their named source checkpoints.  Replacement parity is
> recorded only in the fixed-26 checkpoint linked above.
> Filed New Frontiers and the notation catalogue retain historical 41- and
> 88-page receipts; the comb-interpolation 158-page receipt is historical and
> was superseded by the later historical 160-page receipt above.  Checksum
> ledgers remain abolished and hardened
> repository-wide; no `SHA256SUMS*` files exist or participate in validation.
>
> **Post-`b899` upstream API chronology.**  The q-series union retains
> `QuantumMultinomial` 0+5, and
> `GaussianBinomialBounds` 0+6.  The geometric-interpolation union now also
> includes `GeometricRichardsonGenerating` 3+7.
> On the earlier local publication line, Gaussian checkpoint `71c908e` was
> 902/11,443, completed Lambert checkpoint `217a6b9` was 903/11,448, and
> fixed-column commit `581bf177f8e784ab5d4836acc2b4a47e285e6ce0` was
> 903/11,453 and synchronized the five source/PDF pairs recorded below.  The
> parallel upstream-only moment-polynomial checkpoint
> `3b6396deb6056523d944d79602d1bf7ecf18ec10` was 904/11,457.  These are
> historical checkpoints, not descriptions of the live facade.
>
> **Current merged-source publication receipts (10/10 accepted).** Primary:
> root `14628L/733516B/39d1b0b87cabbd75622b2c882db34bc842f9057561efa8d8a93918acaa97d7ea`;
> two-file aggregate
> `14912L/745351B/2f56e31a0545297886fd0b9f2c9005053f6d902a99f3b1b965b5d659277c71b2`;
> passes `197/199/199`; PDF
> `199pp/1645695B/fe3a27d09c19404a0b0827b047e57ed3d688c465487bf7054e10ca18c7f78cef`;
> log `1315L/52885B/d6e65808a1bd2bc9e3305154e883a2e4f892d4508b63b6da4b27f83b382e1800`.
> Walkthrough: root
> `7135L/514216B/0e726220d274add480fedd771af140d2d5da77497b64fba4bf14294c72817fa5`;
> two-file aggregate
> `7419L/526051B/9599230f56ce10be66991a4c7f2923a91806a988426053dfed69d01fa8c9830f`;
> passes `166/171/171`; PDF
> `171pp/1250845B/e731f6af11f4f00b65b827a7a713794010c0cdf846621df91254b9e2ff2fa0f5`;
> log `1311L/52881B/730df857bc56dab609d1599b66ac3710382b54331754b529a0b987c14d44a41f`.
> Canonical: root
> `18680L/851566B/8e0f799dead1a3d7d7df6a33d1b4cbe61a3fafcfc39bd68e6fb7f4fc6f63f31a`;
> two-file aggregate
> `18964L/863401B/07c65c06d64e0230f68cc644c4594bb47bc1f77fa43e35f04a5d4327124317d8`;
> passes `265/273/273`; PDF
> `273pp/1938114B/ad2362be615a8592a308abe920de982121b58e670409d3bc725e506dcae0a765`;
> log `3123L/105692B/1bedd5ffae49c5ad85f5ce2de3069fb7e7da432ec7d5095475376982fe6437d6`.
> Lambert: root
> `4961L/183269B/83301b4c66660713a70974263b6f191ea01f9ed8f5ae228495f644887b616568`;
> two-file aggregate
> `5245L/195104B/25141b9ee818b20ddf8349d88ec4f2dc977ff0ab35ca34e62cd62da64c2cf06a`;
> passes `68/70/70`; PDF
> `70pp/966637B/6c150ff18889030345de3e1a8581d5ea0ac75789a9720c1d5164ed4e7ec4b7fb`;
> log `1574L/57800B/9f995a50e3ab25256083edee745a1889027787194e3b3c6d1f12f60bf687145c`.
> q series: root
> `16812L/840316B/64dc18dedbd1966624162b64129128b24b51ca88d8a9e496c661cc1a46a24ba6`;
> 15-file aggregate
> `26593L/1198416B/762e6d6ca441de51db9679f95d6a01d8353e8044639f99803734719b8a65a5f8`;
> passes `393/401/401` plus three clean `makeindex` runs (`164/0/254/0`);
> PDF `401pp/2500131B/fd54459baf10845b5a89cc8b204f59ea33a0665b434ad270e738884072a1e6e1`;
> log `1230L/44401B/37dca6371ea8bf9285e5f104d550bd584a290f4aa92fcf5679b028c9dfd3079d`.
> Inverse: root
> `293L/11514B/92fab1fae38bbcf86a45b51bfe7ff34e2801361df9d2f3d6aa3de4dc966eaa3c`;
> 14-file aggregate
> `10909L/438542B/24bdab6491f5ca84fbb9e716f92c7923e8961b6acbc793d9aa5e0faa68852444`;
> passes `132/137/137`; PDF
> `137pp/2045463B/ca403c74e2b46923ce9ac1eda547ab1bcb5e71039b35c8ee394acdd2014c4f8e`;
> log `1569L/64081B/d4aa25579c958e11c59d914c74dfca331fc2bbccf7bba4715dcd18fa050e771f`;
> source-only 23-input closure digest
> `e07cb51f4fe072cd79a014cc891cb8cede62880593d7659b17da9377a21099bc`.
> Comb: root
> `187L/6724B/a4c1e33165ff7291682cd890f23fe4af98e9f11f7ad1d9a7f8b68c78d53f9a56`;
> nine-file aggregate
> `12773L/483551B/cef466ee56f6bb864faaac2244bccf1dbc2fd4032a717b6c81604551c0427309`;
> passes `153/160/160`; PDF
> `160pp/2468109B/bb714c8be4b82de2a888e0302da3aaf957b9e885f2c5f59466b3ea5d659e3f71`;
> log `1370L/58773B/8df53a7db51c85b7a046c5f58587319095b3d28c61b0091861bdeb1f43b342e3`.
> Up: root
> `2385L/99806B/81f3ba09894aca8331ae33c77e2a56f78c107fa3b04072878cff8ad60e815b5a`;
> five-file aggregate
> `11bd62d880f5ba4c63d872fb0ba5d801d10ba2a2337ef5d098643383639086dd`;
> passes `61/63/63`; PDF
> `63pp/1077921B/0903f2920d21f0ea8182822c31338e0be268d4d77bc8ddb7a2ff861ba2a6aa5f`;
> log `964L/38999B/52bd9d03864853f1ee31fa682fa96806345bb355582831c55fcc152c1acb2e7d`.
> Thue--Morse: root
> `10557L/482022B/8cef828c3d92a0017e22463ac90878a5e3a98e1138059d5f4793d47c04a88404`;
> two-file aggregate
> `10841L/493857B/79a43711c6989336166d6b2ed1faa306cc985b8d70557025ab139791d455723c`;
> passes `139/144/144`; PDF
> `144pp/1740015B/deb63fe66fc8f020bb072acbc4301e9b7c9f0559b165cbae2c076f261405c5be`;
> log `1503L/59417B/1219dbc87bc4f9920c40b24659182220b14e7908a45a9eb33d0f19390148c64b`.
> Geometric-q: root
> `27624L/1273010B/0839b42a3fb055d860b8e8a3d1ff5e84c2f4addce314d04707c5a067e81553d9`;
> seven-file aggregate
> `27997L/1288647B/18c4c6607e9b7564909ca7e647152a26e517f54d5007e157265b3f61adf8e4f0`;
> passes `387/404/404`; PDF
> `404pp/8341830B/a083b130a1568dc37af824294b033485f82c97dbeb30a4c4de4d463d04e99530`;
> log `2557L/114343B/4de474675a2dcde519c36ff1ac7067717c64b60b92bd40b999d0d117ba1f8df6`.
> All ten passed every recorded log, A4/rotation, PDF 1.5, encryption,
> font/subsetting, Libertinus, Type-3, and visual gate. For geometric-q,
> all 404 pages are A4 at rotation zero, the PDF is unencrypted PDF 1.5,
> all 43 font rows are embedded and subset, Libertinus has 11 rows, Type-3 has
> zero rows, and checksum basenames and PDF/source references are absent.
>
> **Earlier accepted receipts (history).** The following six tuples validate
> only their recorded pre-merge source snapshots:
>
> - primary exposition: source 13,748 lines / 686,081 bytes / SHA-256
>   `661ebb4e337f2ce79e8c3d5ca823bcdcce519fe220ff4b02d9dd5aef08d42cf9`;
>   passes 185/187/187; PDF 187 A4 pages / 1,581,617 bytes / SHA-256
>   `41e95844c6e5dc04933cc7256d285346c10eb29ff7eadf5a4165c472fa453eab`;
>   final log 1,309 lines / 52,641 bytes / SHA-256
>   `f177cb91c161f4dfc60017ddfd5efed1586ef28a4c80c68a9ae78cf19cf75da6`;
> - Lean walkthrough: source 6,638 lines / 460,643 bytes / SHA-256
>   `c44ab7ab38da46f8959a63916437b3d8ded628a3930f81dfa1a87996c7c66b8d`;
>   passes 146/151/151; PDF 151 A4 pages / 1,188,993 bytes / SHA-256
>   `907b3d2dbfc66192b63b86ada1015779229636855734815402ae5af9ea9bf015`;
>   final log 1,296 lines / 52,126 bytes / SHA-256
>   `0962c255aeb75bce3bcee89cbc1db9defe369e8c79ee6b6aa0435befdca410cf`;
> - canonical frontier: source 17,983 lines / 806,798 bytes / SHA-256
>   `a1cb1c0db2784116ca1f1d6fd1ce7e8b29afc52c64c93e7001bbffb91f775039`;
>   passes 252/260/260; PDF 260 A4 pages / 1,877,420 bytes / SHA-256
>   `78c19b361da06836c20c62fa5bd50131eb8fa47d3e89d37a7f089a51568953b3`;
>   final log 2,990 lines / 101,866 bytes / SHA-256
>   `4854a89ba1a3bb41248b9428cfed572f6ed593125b5a8fb43d5625d8d392dc0f`;
> - Lambert Guide: source 4,873 lines / 177,465 bytes / SHA-256
>   `90413f46373415edef411e9ea3b2d94006f7342bd2fa3ea931ca975d7f64b97e`;
>   passes 67/69/69; PDF 69 A4 pages / 958,516 bytes / SHA-256
>   `b159cd41f5b3e53060fa85fcfc4812d504cc1fff8400c03ecb7ef4018f37cd2c`;
>   final log 1,574 lines / 57,795 bytes / SHA-256
>   `5c81cd6b8dc2cf070c4b1995c98c0d046c54d28f0e682b78b5ad9e16c0a8caa7`;
> - q-Pochhammer/q-binomial monograph: master 16,448 lines / 816,185 bytes /
>   SHA-256 `a463abef7bb3c70e12a568a46fc192aac88a8ce240f8f781fff2b018a4aa086d`;
>   full graph 26,114 lines / 1,168,039 bytes / aggregate SHA-256
>   `d4c5b84cc07f6abb99279c5bba4fdf7404326426cbe81f4b33b72ba01e62739c`;
>   passes 383/391/391 with `makeindex` after each pass (164 accepted,
>   0 rejected, 0 warnings); PDF 391 A4 pages / 2,464,712 bytes / SHA-256
>   `a52eb90dec7b874cc29dea891a107b9eb2d55e6727eb8bd8943d0aab609c58a6`;
>   final log 1,231 lines / 44,343 bytes / SHA-256
>   `fb74b0c4cbd75d9022c78c1df5c1d567120bd67728d5b418697ddc5a2aa8f450`;
> - standalone geometric-q root: source 27,520 lines / 1,266,515 bytes /
>   SHA-256 `8292f10862334cb809139259eeb4906bb14f517d41b9600c9b7ad53bb21525b1`;
>   passes 385/402/402; PDF 402 A4 pages / 8,332,886 bytes / SHA-256
>   `d47431e4d3e721fccf12f90226db77f1898e44b477878954acca3a6e90127cf4`;
>   final log 2,557 lines / 114,331 bytes / SHA-256
>   `4d6f8c7974def4a3f9e6bc8ccdffefc3eef7ca8cb7c2f0145a075f95b82ff45e`.
>
> At those recorded source snapshots, all six passed the documented log, A4
> page, font, Type-3, extraction, and visual gates; the Lambert clipping defect
> and canonical page-176 running-head collision were repaired before
> acceptance.  The five `581bf` pairs retained
> below are historical receipts.  They are the historical 187-page,
> 1,578,751-byte primary PDF (SHA-256
> `7a93d9c47c22c62dc50cda6a64d030bfad5f44a9ef4cc5568b3b8b16b014bc8f`)
> from its 13,720-line, 684,413-byte source (SHA-256
> `7efb8d2294a15cf6b150bb0b04b35db74cf68db3f3cdb4bc5d765b7738504019`);
> the historical 150-page, 1,185,846-byte Lean-walkthrough PDF (SHA-256
> `8a5416061addfb480f410e8306340994fe7f5160927a928112087b6b1d5c0cf5`)
> from its 6,603-line, 457,421-byte source (SHA-256
> `4e6ea24897a31683c53e71c6cb97eb21f37eff3ae4e452add429bd9f47646ea6`);
> the historical 260-page, 1,875,190-byte canonical-frontier PDF (SHA-256
> `fee3b5af21b01b16c41cf8291ba0508d1e6c613bfd098cdef457cde44ea693c2`)
> from its 17,947-line, 804,625-byte source (SHA-256
> `ac6b28fb8a98d97ed4b7a0fe7a5ba2cdfab852fb6de1ee2ecfda4aed75804371`);
> the historical 70-page, 958,713-byte Lambert Guide PDF (SHA-256
> `24e8bf561283ffc5427297df6f656696a7e2538731e53d03d59e4268b50772fc`)
> from its 4,864-line, 176,796-byte source (SHA-256
> `3f3552983a73db2dab94f3625d10d054b747ee03fc2758e257af86f6216deab5`);
> and the historical 391-page, 2,464,122-byte q-series PDF (SHA-256
> `c0a00720685f40e0684b4858e7ce18ce134701529898fd4574d09b6c090e0e91`)
> from its 16,433-line, 815,194-byte master (SHA-256
> `f2aae6ddc3d7a399f9ed47806a0abe6458cbcab37bf2aac9f55ad3913b5a0e2d`).
> The preceding 257-page canonical-frontier artifact remains historical.
> Representation Frontiers (301 pages), filed New
> Frontiers (41), the notation catalogue (88), the Integration-and-Transform
> master (377) remain historical or unsynchronized under their package notices.
> The 158-page comb artifact is historical and is superseded by the accepted
> 160-page receipt above.
> The geometric-interpolation and decoder union also includes
> `RvachevAppellHasse` 1+14, `RvachevLagrangeNodesOnly` 1+14, and
> `FinitePrefixAppellRecovery` 11+17.  The Fourier/inverse union now also
> includes `RvachevLaurentLeading` 1+6.
> The Thue--Morse continuation union also includes the exhaustive
> `ThueMorseGammaTowerDifferential` 0+3 leaf.
> The four-module Lambert branch-gap union is 4+37, forty-one declarations,
> including the exhaustive `LambertWBranchGapBernoulli` 0+5 leaf.
> The RatFunc leaf gave the historical 924/11,615 checkpoint; the two new
> `ProbabilityLaplaceMoments` theorems give 924/11,617, and the exhaustive 1+1
> `RvachevLegendreBiorthogonality` leaf gives the historical 925/11,619
> checkpoint.  Subsequent merged source work adds
> `QPochhammerLambertForm` 0+5, `CentralQVandermondeInfinite` 0+4,
> `ThueMorseNewmanSelfSimilarity` 1+12, `TriangularPowerProduct` 0+2, and
> `MeanValueBracket` 0+6, along with a net twenty-nine further public
> declarations in existing modules, reaching the immediate pre-reciprocity
> checkpoint 930/11,678.  The promoted complex-product differentiability
> theorem and exhaustive 1+5 `GeometricUniformMomentReciprocity` leaf add one
> module and seven declarations, giving the historical reciprocity checkpoint
> 931/11,685.  The subsequently merged `DyadicBoundaryIdentity` 0+2 and
> `FinitePrefixThueMorseCollapse` 0+8 leaves add two modules and ten
> declarations, giving the historical 933/11,695 checkpoint.  Finally,
> `ProuhetBaseTwoBridge` 0+6, one new theorem in `DyadicBoundaryIdentity`, and
> seven new theorems in `ThueMorseNewmanSelfSimilarity` add one module and
> fourteen declarations in total, giving the historical 934/11,709
> pre-transseries census.  The later transseries tranche raises the frozen
> upstream tree to 957/11,920; retaining the unconditional Pochhammer bridge
> gives the historical merge-union checkpoint 957/11,921, and the
> residual-existence certificate `exists_eq_in_residual_interval` is included
> in the live semantic union computed by `scripts/doc_audit.py` and pinned in
> `docs/doc_audit_baseline.json`.
> At their named historical checkpoints, the retained primary exposition, Lean
> walkthrough, canonical frontier, Representation Frontiers, filed New
> Frontiers, notation catalogue, comb-interpolation synthesis, and q-series
> synthesis PDFs contained respectively
> 183, 130, 257, 301, 41, 88, 158, and 389 pages.
> On the incoming branch the same tranche gives the historical 934/11,709
> census.
> The subsequent nine-module series/transseries overlay contributed 72 explicit
> declarations, with two additional Neumann names generated by `to_additive`;
> six concurrent declarations outside those modules produced the historical
> 943/11,787 checkpoint.  The later nine-module transseries tranche contributes
> 90 explicit declarations, and four integer-exponent theorems extend the
> existing `TransseriesDifferentialBlock` module.  This exact +9/+94 delta
> gave the historical incoming 952/11,881 checkpoint.  The two explicit
> OrderDual Neumann wrappers and Wright-omega analyticity then gave the
> historical exactness checkpoint 952/11,884.  The current fifteen-module
> tranche contributes 112 declarations, and five net declarations enter
> existing modules, giving the historical 967/12,001 checkpoint.  The next
> merge adds two modules and 47 declarations, giving the historical
> intermediate 969/12,048 checkpoint; `UnitSeriesPowerRecurrence` 0+3 then
> gives the historical 970/12,051 checkpoint.  Reconciliation gave the
> historical 970/12,056 checkpoint.  Seven subsequent modules—
> `AbelPolynomialSeries` 2+9, `AssociahedronFaceNumbers` 4+23,
> `BernoulliFormalLog` 0+5, `ExponentialRescaling` 0+4,
> `PochhammerFalling` 1+13, `RaneyNumbers` 4+12, and
> `UnitSeriesPowerRecurrence` 0+3—give the historical 977/12,133 checkpoint
> after accounting for three declarations moved from `NorlundDiagonal`.
> `GridEvaluationCertificate` 0+4 and `IntegerCRTCertificate` 0+5 give the
> historical 979/12,142 checkpoint.  The incoming successor adds
> `NorlundGeneralized` 3+18, `StirlingSymmetricFunctions` 0+4,
> `LagrangeInversionUniqueness` 0+6, `NewtonReciprocal` 1+5,
> `StirlingSecondReverseRowIdentity` 0+2, and
> `TransseriesWrightOmegaTerms` 0+10, plus eight declarations in existing
> modules, producing the historical 985/12,199 checkpoint.  The authoritative
> current union is computed by `scripts/doc_audit.py` and pinned in
> `docs/doc_audit_baseline.json`.
> At the later named publication checkpoint, the retained primary exposition,
> Lean walkthrough, canonical frontier,
> Representation Frontiers, filed New Frontiers, notation catalogue,
> comb-interpolation synthesis, and q-series synthesis PDFs contain respectively
> 210, 165, 273, 301, 41, 88, 160, and 389 pages.  Their current TeX sources
> contain post-render unions, so those PDFs are historical validation receipts
> rather than source/PDF-parity claims.  The Integration-and-Transform master
> likewise retains a historical 377-page checkpoint.  The fixed-26 table
> linked above records the user-deferred publication batch; pending rows make
> no merged-current parity claim.

The Wigner boundary in this crosswalk is literal: the directly defined
integer-index zero-row square datum is not a bridge to a separately implemented
general Wigner symbol.  There is no signed value or phase convention,
half-integer or nonzero-magnetic-index API, general `3j`/`6j`/`9j`,
orthogonality, recoupling, or named Wigner-symmetry theorem.  A Gaunt factorial
identity and product-coefficient nonnegativity/zero criteria can be composed
from the named declarations but are not separate wrappers.  Infinite Legendre
interchange, Christoffel reconstruction, roots/quadrature, Padé/J-fractions,
infinite Jacobi theory, and asymptotics remain open.

## arXiv:1702.05442

The exact version used by this audit is vendored as
[TeX source](papers/arXiv-1702.05442v1/09-Function.tex) and as the
[published PDF](papers/arXiv-1702.05442v1/1702.05442v1.pdf).

| Source result | Lean declaration(s) |
| --- | --- |
| Theorem 1 | `Fabius.IsOriginalFabius`, `Fabius.IsOriginalFabius.mk_of_derivative_law`, `Fabius.IsFabius.isOriginalFabius_rvachevUp`, `Fabius.isOriginalFabius_iff_eq_canonical`, `Fabius.rvachevUp_eq_iff_eqOn_Iic_one`, `Fabius.isFabius_iff_isOriginalFabius_rvachevUp_and_rightTail`, `Fabius.isOriginalFabius_iff_existsUnique_isFabius`, `Fabius.existsUnique_originalFabius`, `Fabius.originalFabius_eq_canonical` |
| Lemma 1 | `Fabius.finiteConvolutionProbability_tendsto`, `Fabius.finiteConvolutionProbability_tendsto_fabius`, `Fabius.integral_finiteConvolutionMeasure_tendsto`, `Fabius.integral_finiteConvolutionMeasure_complex_tendsto` |
| Theorem 2 | `Fabius.stepApproximant_tendsto_rvachevUp`, `Fabius.stepApproximant_tendsto_fabius` |
| Theorem 3 | `Fabius.ProbabilityRepresentation.rvachevUp_eq_weightedSum_probability`, `Fabius.ProbabilityRepresentation.ofReal_rvachevUp_eq_weightedSum_probability` |
| Probability proposition after Theorem 3 | `Fabius.ProbabilityRepresentation.independent_uniform_coordinates`, `Fabius.ProbabilityRepresentation.coordinate_has_uniform_law` |
| Theorem 4(a–c) | `Fabius.original_theorem_four_a`, `Fabius.original_theorem_four_b`, `Fabius.original_theorem_four_c` |
| Unnumbered corollary | `Fabius.rvachev_not_analyticAt` |
| Theorem 5, its Schwartz-decay strengthening, and its upper-bound-free support specializations | `Fabius.rvachev_poisson_summation`, `Fabius.rvachevFourier_real_iteratedDeriv_rapidDecay`, `Fabius.rvachevFourier_real_rapidDecay`, `Fabius.rvachev_poisson_support_specialization_unscaled_of_one_half_le`, `Fabius.rvachev_poisson_support_specialization_of_one_half_le` |
| Theorem 6 | `Fabius.original_theorem_six` |
| Theorem 7 | `Fabius.original_theorem_seven_global` |

The Theorem 3 row intentionally records the paper's source-facing statement,
whose input lies in `[-1,0]`.  The library also proves strictly stronger
all-real forms:

- `Fabius.ProbabilityRepresentation.weightedSumCDF_eq_fabiusReal` and
  `Fabius.ProbabilityRepresentation.fabiusReal_eq_weightedSum_probability`,
  together with
  `Fabius.ProbabilityRepresentation.ofReal_fabiusReal_eq_weightedSum_probability`,
  identify the bounded Fabius function with the random-series CDF for every
  real threshold in both measure codomains;
- `Fabius.ProbabilityRepresentation.weightedSumCDF_eq_intervalIntegral_of_le_half`
  proves the collapsed smoothing equation
  `H(x) = ∫ t in 0..2*x, H(t)` for every `x ≤ 1/2`, including negative
  thresholds where the oriented integral and CDF both vanish;
- `Fabius.ProbabilityRepresentation.rvachevUp_eq_weightedSumCDF`,
  `Fabius.ProbabilityRepresentation.rvachevUp_eq_weightedSum_probability_global`,
  and
  `Fabius.ProbabilityRepresentation.ofReal_rvachevUp_eq_weightedSum_probability_global`
  give the real-valued and `ℝ≥0∞` identities
  `up(x) = P[X ≤ 1 - |x|]` for every real `x`.

In these names, “global” means that the real input is unrestricted.  These are
identities for the bounded CDF and `rvachevUp`, not for the signed extension
`extendedFabius` or its canonical specialization `globalFabius`.

The construction and proof infrastructure also formalizes the key supporting
Fourier, recurrence, convolution, polynomial, Taylor, Poisson, moment, and
generating-function identities in proof-oriented forms.  In particular:

- `Fabius.polynomialMeasure_eq_finiteConvolutionMeasure` is the exact bridge
  between equations (12) and (14);
- `Fabius.intervalIntegral_stepApproximant_tendsto` is the weak-limit bridge
  used in Theorem 2;
- `Fabius.rvachev_partition_one_over_nat`,
  `Fabius.rvachev_partition_unity`, `Fabius.rvachev_cosine_series`, and the
  source-compatible `rvachev_poisson_support_specialization` theorems cover
  equations (26)–(32), while their `_of_one_half_le` companions show that
  the upper bound on the lattice spacing is unnecessary;
- `Fabius.rvachevDyadic_cast_global` gives the executable equality underlying
  the global form of Theorem 7.

The second Euler/Weierstrass product displayed in equation (9) is a proof-body
identity rather than a theorem or lemma environment.  It is not part of the
named-result coverage claim.

## arXiv:1702.06487v3

Version 3 is vendored as
[TeX source](papers/arXiv-1702.06487v3/157-Arithmetic-v3.tex) and as the
[published PDF](papers/arXiv-1702.06487v3/1702.06487v3.pdf).

| Source result | Lean declaration |
| --- | --- |
| Proposition 1 | `Fabius.proposition_one` |
| Proposition 2 | `Fabius.proposition_two` and `Fabius.proposition_two_real` |
| Proposition 3 | `Fabius.proposition_three` |
| Proposition 4 | `Fabius.proposition_four` |
| Question 5 | `Fabius.reshetnikovQuestion` |
| Proposition 6 | `Fabius.proposition_six` |
| Theorem 7 | `Fabius.theorem_seven` |
| Proposition 8 | `Fabius.proposition_eight` |
| Theorem 9 | `Fabius.theorem_nine` and `Fabius.theorem_nine_all` |
| Lemma 1 | `Fabius.lemma_one` |
| Proposition 10 | `Fabius.proposition_ten` |
| Definition 12 | `Fabius.dyadicDenominator` |
| Theorem 13 | `Fabius.theorem_thirteen` and `Fabius.theorem_thirteen_denominator_bound` |
| Proposition 15 and the resulting least-common-denominator valuation bound (using Theorem 21) | `Fabius.proposition_fifteen` and `Fabius.dyadicDenominator_padicVal_two_lower_bound` |
| Conjecture 16 and its following denominator formulas | The conjecture remains recorded as `Fabius.conjecture_sixteen`.  The displayed odd-index denominator formula is the unconditional identity `Fabius.dyadicDenominator_odd_eq_conjecturalH` (valid even at `n = 0` under natural-number subtraction); `Fabius.conjecture_sixteen_denominator_formulas` packages it with the even-index formula that still assumes the conjecture for `n ≥ 1`. |
| Theorem 17 | `Fabius.theorem_seventeen` |
| Proposition 18 | `Fabius.proposition_eighteen` |
| Proposition 19 | `Fabius.proposition_nineteen` |
| Theorem 20 | `Fabius.theorem_twenty` |
| Theorem 21 and its reduced-denominator form | `Fabius.theorem_twenty_one` and `Fabius.fabiusAtInverseTwoPow_den_padicVal_two` |
| Proposition 22 | `Fabius.proposition_twenty_two_initial`, `Fabius.proposition_twenty_two` |

`FabiusFunction.Paper06487Supplement` additionally exposes the non-environment
claims used in the paper's prose and proofs: positivity, flat derivatives,
the doubled half-moment recurrence, multiplier divisibility and valuations,
the shifted dyadic-grid formula, the reordered sum, and the conditional
post-Conjecture-16 denominator formulas.

## Additional requested identities

The reciprocal-Gamma crosswalk below is audited against formal source
checkpoint `71ab6f6728fceb753c88d8b0573077a59acf2682`.
The reciprocal-Gamma jet and Thue--Morse Gamma-tower rows are a current overlay:
the five-jet/eight-tower tranche is pinned to `0ba35abd4`, while the current
integrated tree adds the ninth tower declaration.
The current geometric-sinc overlay adds four theorems to
`GeometricReciprocalGamma.lean` and the four-theorem
`GeometricSincCharacteristicFunction.lean` bridge; the rows below exhaust all
eight additions and distinguish locally uniform convergence of the pure
complex sinc product from locally uniform convergence of the full phase-bearing
prefixes on the real frequency line and uniform convergence on its compact sets.

| Claim | Status | Lean declaration(s) |
| --- | --- | --- |
| Unit-series Bell conversion, powers/logarithms, and exponential/logarithmic jets (`p0:lem:bell-conversion`, `p0:lem:power-log`, `p0:cor:exp-log-jets`) | **All three Exact.**  `UnitSeriesBellCoefficients.lean` has no public definitions and exactly sixteen public theorems.  They prove the ordinary/exponential partial-Bell normalization, coefficient formulas for falling and negative-binomial substitution, formal logarithms, all displayed exponential Bell/partition forms, and the coefficient recurrence, with all-index strengthenings where the API permits.  This is formal power-series algebra over the indicated commutative rational algebras/fields.  It makes no analytic convergence statement and selects no analytic logarithm branch. | `Fabius.ordPartialBell_eq_factorialRatio_partialBell`, `Fabius.factorial_mul_ordPartialBell_eq_factorial_mul_partialBell`, `Fabius.coeff_fallingSeries_subst_eq_sum_ordPartialBell`, `Fabius.coeff_fallingSeries_subst_eq_sum_ordPartialBell_of_pos`, `Fabius.coeff_fallingSeries_subst_eq_sum_partialBell`, `Fabius.coeff_negBinomSeries_subst_eq_sum_ordPartialBell`, `Fabius.coeff_negBinomSeries_subst_eq_sum_ordPartialBell_of_pos`, `Fabius.coeff_logOf_eq_sum_ordPartialBell`, `Fabius.egfA_factorialDenormalize_coeff_eq`, `Fabius.bellWeightSeries_factorialDenormalize_coeff_eq`, `Fabius.coeff_logOf_eq_sum_partialBell`, `Fabius.coeff_exp_subst_eq_completeBell`, `Fabius.coeff_exp_subst_eq_partitionExpSum`, `Fabius.coeff_exp_subst_eq_sum_weightedPartitions`, `Fabius.coeff_exp_subst_eq_sum_div_weightedPartitions`, `Fabius.coeff_exp_subst_recurrence` |
| Dickson and Neumann well-basedness (`q0:lem:dickson`, `q0:lem:neumann`) | **Both Exact subject to the order convention.**  `TransseriesWellBased.lean` has seven explicitly written theorems and no definitions; `to_additive` generates two additional public twins.  The two explicit `OrderDual` wrappers state Neumann's lemma in the manuscript's orientation.  For total orders this is its reverse-well-order/no-strict-growth convention; for merely partial orders the exact formal claim is `Set.IsPWO` in the dual order, not a greatest-element characterization.  The results require an ordered cancel commutative monoid and prove closure plus finite factorizations/decompositions; they do not construct a Hahn-series type.  Generated names are real API but are absent from the lexical declaration tally. | Written: `Fabius.dickson_isPWO`, `Fabius.dickson_antichain_finite`, `Fabius.dickson_isPWO_pi`, `Fabius.neumann_isPWO`, `Fabius.neumann_finite_factorizations`, `Fabius.neumann_isPWO_orderDual`, `Fabius.neumann_finite_factorizations_orderDual`. Generated: `Fabius.neumann_add_isPWO`, `Fabius.neumann_finite_decompositions` |
| Elementary height comparisons (`q0:prop:height`) | **Exact for the proposition's two printed analytic comparisons.**  `TransseriesHeight.lean` has no definitions and exactly three theorems.  It proves, at the real `atTop` filter, that logarithmic powers lose to every positive power and that power--log products lose to every positive exponential.  It does not define or compare arbitrary recursively nested transmonomial heights/depths. | `Fabius.isLittleO_log_pow_rpow`, `Fabius.isLittleO_log_pow_id`, `Fabius.isLittleO_pow_mul_log_pow_exp` |
| Asymptotic scale, vector-valued Poincaré remainder, and coefficient uniqueness (`q0:def:scale`, `q0:def:poincare`, `q0:prop:uniqueness`) | **Exact in the sequence-indexed formulation.** `TransseriesScale.lean` is 3+6 over an arbitrary filter and normed scalar field, with coefficients and remainders in a normed vector space.  Uniqueness requires `[l.NeBot]`; no convergence or unordered maximal scale is asserted. | `Fabius.IsAsymptoticScale`, `Fabius.poincarePartialSum`, `Fabius.IsPoincareExpansion`, `Fabius.poincarePartialSum_zero`, `Fabius.poincarePartialSum_succ`, `Fabius.IsPoincareExpansion.isLittleO_succ_remainder`, `Fabius.IsPoincareExpansion.tendsto_coeff`, `Fabius.IsPoincareExpansion.tendsto_coeff_div`, `Fabius.IsPoincareExpansion.coeff_unique` |
| Lexicographic dominance of real power--log monomials (`plt:lem:mot-dominance`) | **Exact for the analytic trichotomy and integer-generator rule.**  `TransseriesScaleDominance.lean` has one definition and seven theorems.  It proves the zero/one/`atTop` quotient cases and the manuscript's `t=1/X`, `L=log X` generator order.  It does not turn the entire unordered monomial set into the manuscript's maximal-scale package. | `Fabius.plMonomial`, `Fabius.tendsto_plMonomial_atTop_zero`, `Fabius.plMonomial_div_eventuallyEq`, `Fabius.tendsto_plMonomial_div_atTop_zero`, `Fabius.tendsto_plMonomial_div_atTop_one`, `Fabius.plMonomial_pos`, `Fabius.tendsto_plMonomial_div_atTop`, `Fabius.plMonomial_generators_dominance` |
| Sequence-indexed power--log scales | **Exact sequence consequence; full `plt:def:mot-scale` remains Partial.**  `TransseriesPolyLogScale.lean` has no definitions and four theorems.  It proves little-o for lexicographically decreasing pairs, the corresponding scale for a decreasing exponent sequence, and the pure inverse-power and fixed-power/log ladders.  It does not formalize the definition's unordered-set or finite-maximal clause. | `Fabius.isLittleO_plMonomial`, `Fabius.isAsymptoticScale_plMonomial`, `Fabius.isAsymptoticScale_plMonomial_pow`, `Fabius.isAsymptoticScale_plMonomial_log` |
| Logarithmic-block antidifferentiation (`plt:lem:mot-block-antiderivative`) | **Exact for the polynomial operator dichotomy.**  `TransseriesBlockAntiderivative.lean` has three definitions and twelve theorems.  For nonzero `c`, its explicit finite inverse makes `∂_L-c` a degree-preserving bijection on `K[L]`; for `c=0`, its explicit primitive makes differentiation surjective and raises nonzero degree by one.  This does not construct the manuscript's full Laurent ambient ring `K[t,t⁻¹][L]`. | Definitions: `Fabius.blockOperator`, `Fabius.blockAntiderivative`, `Fabius.resonantAntiderivative`. Theorems: `Fabius.sum_sub_sum_shift`, `Fabius.blockOperator_zero`, `Fabius.blockOperator_sub`, `Fabius.blockOperator_blockAntiderivative`, `Fabius.blockOperator_surjective`, `Fabius.natDegree_C_mul_of_ne_zero`, `Fabius.natDegree_blockOperator`, `Fabius.blockOperator_injective`, `Fabius.blockOperator_bijective`, `Fabius.derivative_resonantAntiderivative`, `Fabius.derivative_surjective`, `Fabius.natDegree_resonantAntiderivative` |
| Differential bridge from `K[L]` to transseries blocks (`plt:eq:mot-block-derivative`, `plt:prop:dif-block`) | **Exact integer block law; larger proposition Partial.** `TransseriesDifferentialBlock.lean` is 0+12.  It combines natural-block and abstract-unit wrappers with generic Laurent existence and conditional uniqueness.  The concrete Laurent ring, faithful evaluation in that model, and resonant uniqueness-up-to-constants wrapper remain absent. | `Fabius.derivation_pow_t`, `Fabius.derivation_block`, `Fabius.derivation_zpow_block`, `Fabius.exists_zpow_block_primitive`, `Fabius.existsUnique_zpow_block_primitive`, `Fabius.exists_block_primitive`, `Fabius.derivation_block_zero`, `Fabius.exists_block_primitive_resonant`, `Fabius.derivation_val_inv`, `Fabius.derivation_pow_inv`, `Fabius.derivation_zpow_t`, `Fabius.derivation_block_zpow` |
| Flatness and the invisible-function problem (`q0:def:flat`, `q0:prop:invisible`) | **Exact, with vector-valued generalization and a sharper iff.** `TransseriesFlat.lean` is 4+22.  The vector 1+11 core handles normed-space-valued remainders; the retained scalar 3+11 layer packages the submodule, power scale, and compatibility wrappers.  Variable multiplication requires explicit scale absorption, and the power specialization assumes eventual nonvanishing. | Vector core: `Fabius.IsFlat`, `Fabius.isFlat_zero`, `Fabius.IsFlat.add`, `.neg`, `.sub`, `.const_smul`, `Fabius.isFlat_exp_neg_rpow_atTop`, `Fabius.IsPoincareExpansion.add_flat`, `.sub_same_coeff_isFlat`, `.iff_sub_isFlat`, `Fabius.IsFlat.smul_of_scale_absorption`, `.smul_of_isBigO_inv_pow`. Scalar layer: `Fabius.flatSubmodule`, `Fabius.AbsorbsScale`, `Fabius.powScale`, `Fabius.mem_flatSubmodule_iff`, `Fabius.IsFlat.mul_absorbsScale`, `Fabius.absorbsScale_const`, `Fabius.IsPoincareExpansion.add_isFlat`, `Fabius.isFlat_sub_of_isPoincareExpansion`, `Fabius.isPoincareExpansion_iff_isFlat_sub`, `Fabius.isPoincareExpansion_zero_iff`, `Fabius.powScale_eq_rpow`, `Fabius.absorbsScale_of_isBigO_pow`, `Fabius.isFlat_exp_neg`, `Fabius.isPoincareExpansion_add_exp_neg` |
| Wright omega (`plt:prop:mot-omega-basic`) | **Exact over the reals.**  `WrightOmega.lean` has one definition and thirteen theorems.  In addition to its defining equation, positive-domain inverse, principal-Lambert relation, strict monotonicity, positivity, anchor at one, two-sided envelope, global linear lower bound, and divergence at `+∞`, it proves `AnalyticAt ℝ wrightOmega X` for every real `X`.  This is formal real analyticity, not a complex branch or complex-holomorphy claim. | `Fabius.wrightOmega`, `Fabius.analyticAt_wrightOmega`, `Fabius.wrightOmega_pos`, `Fabius.wrightOmega_add_log`, `Fabius.principalLambertW_eq_wrightOmega_log`, `Fabius.wrightOmega_leftInverse`, `Fabius.wrightOmega_strictMono`, `Fabius.wrightOmega_one`, `Fabius.one_le_wrightOmega`, `Fabius.wrightOmega_le_self`, `Fabius.sub_log_le_wrightOmega`, `Fabius.wrightOmega_envelope`, `Fabius.add_one_div_two_le_wrightOmega`, `Fabius.tendsto_wrightOmega_atTop` |
| Two Wright-omega orders (`plt:prop:mot-two-orders`) | **Exact concluding equivalences; proposition Partial.** `WrightOmegaTwoOrders.lean` is 0+8 and proves `ω(X)-X∼-log X` and `ω(X)-X+log X∼(log X)/X` at `atTop`, together with the transfer limits used in the proof.  The four-term expansion and explicit quantitative envelope remain absent. | `Fabius.wrightOmega_lt_self`, `Fabius.tendsto_log_wrightOmega_div_atTop_zero`, `Fabius.tendsto_wrightOmega_div_atTop_one`, `Fabius.tendsto_log_wrightOmega_sub_log_atTop_zero`, `Fabius.tendsto_log_wrightOmega_div_log_atTop_one`, `Fabius.self_sub_wrightOmega_isEquivalent_log`, `Fabius.wrightOmega_sub_self_isEquivalent_neg_log`, `Fabius.wrightOmega_residual_isEquivalent` |
| Stirling-series coefficients (`q2:eq:stirling-cj`) | **Exact formal coefficient clause.** `StirlingSeriesCoefficients.lean` is 3+12 and proves the formal kernel, exponential recurrence, and `c₀=1`, `c₁=1/12`, `c₂=1/288`, `c₃=-139/51840`.  It does not prove the surrounding analytic Fubini-sector expansion. | Definitions: `Fabius.stirlingKernelCoeff`, `Fabius.stirlingKernel`, `Fabius.stirlingCoeff`. Theorems: `Fabius.coeff_stirlingKernel`, `Fabius.constantCoeff_stirlingKernel`, `Fabius.stirlingCoeff_zero`, `Fabius.stirlingCoeff_recurrence`, `Fabius.bernoulli_three`, `Fabius.bernoulli_four`, `Fabius.stirlingKernelCoeff_one`, `Fabius.stirlingKernelCoeff_two`, `Fabius.stirlingKernelCoeff_three`, `Fabius.stirlingCoeff_one`, `Fabius.stirlingCoeff_two`, `Fabius.stirlingCoeff_three` |
| Leading harmonic-increment asymptotic (`plt:lem:mot-harmonic`) | **Partial: leading order only.**  `TransseriesHarmonicIncrement.lean` has no definitions and exactly two theorems.  It proves the general Stolz--Cesàro implication from convergent increments to `w_n/n`, and derives the Exact leading law `w_n/n → c₀` from the manuscript's recurrence and error hypotheses.  The `(c₁/c₀) log n` correction, the limiting constant, and the final `o(1)` remainder remain unformalized. | `Fabius.tendsto_div_atTop_of_tendsto_sub`, `Fabius.tendsto_div_atTop_of_harmonic_increment` |
| Ordinary/exponential partial-Bell normalization (`plt:lem:bell-normalizations`) | **Exact generating-series bridge; full printed lemma Partial.**  `OrdinaryPartialBell.lean` has two definitions and four theorems.  It defines the ordinary family coefficientwise from the power of its ordinary series, proves that defining power-series identity, identifies the divided exponential Bell weight series with it, proves `n! B̂_(n,k)(x_j/j!) = k! B_(n,k)(x)`, and proves above-diagonal vanishing.  The explicit multinomial-sum presentation printed in the volume and the final identification with its convolution polynomial are not separate Lean theorems. | Definitions: `Fabius.ordinarySeries`, `Fabius.ordinaryPartialBell`. Theorems: `Fabius.ordinaryPartialBell_pow`, `Fabius.bellWeightSeries_eq_ordinarySeries`, `Fabius.factorial_mul_ordinaryPartialBell`, `Fabius.ordinaryPartialBell_eq_zero_of_lt` |
| Real linear--logarithmic core (`p0:thm:lambert-core`) | **Exact real algebra and branch rule; full theorem Partial.**  `LinLogCoreInversion.lean` has four definitions and eighteen theorems.  It proves the branch-free Lambert equivalence, the unique positive principal root for `b>0`, the sharp threshold equivalence and two distinct separated real roots for `b<0`, and both slope identities.  Only the large-`L` asymptotic clause and the complex general-branch interpretation remain unformalized. | Definitions: `Fabius.linLogCoreArg`, `Fabius.linLogCoreRoot`, `Fabius.linLogCoreThreshold`, `Fabius.linLogCoreRootLower`. Theorems: `Fabius.linLogCore_eq_iff`, `Fabius.principalLambertW_linLogCoreArg_pos`, `Fabius.linLogCoreRoot_pos`, `Fabius.linLogCore_linLogCoreRoot`, `Fabius.strictMonoOn_linLogCore`, `Fabius.linLogCoreRoot_unique`, `Fabius.hasDerivAt_linLogCore`, `Fabius.linLogCore_slope_eq`, `Fabius.linLogCore_critical`, `Fabius.linLogCoreArg_mem_Ioo_iff`, `Fabius.principalLambertW_linLogCoreArg_neg`, `Fabius.linLogCoreRoot_pos_of_neg`, `Fabius.linLogCoreRootLower_pos`, `Fabius.linLogCore_linLogCoreRoot_of_neg`, `Fabius.linLogCore_linLogCoreRootLower`, `Fabius.linLogCoreRoot_lt_critical`, `Fabius.critical_lt_linLogCoreRootLower`, `Fabius.linLogCoreRoot_ne_linLogCoreRootLower` |
| Real power--logarithmic core at `r=1` (`p6:eq:core-r1`, `p6:lem:core`) | **Equation Exact; general lemma Partial.**  `PowerLogCoreInversion.lean` has three definitions and six theorems.  It proves the exponential substitution, root logarithm, branch-independent solve law for any real Lambert-equation solution, principal-branch solve law, and derivative at an arbitrary point and at the root.  This exactly covers `r=1`, the case used for Gamma and the `K`-function.  General `r`, the associated root determination, and complex branches remain unformalized. | Definitions: `Fabius.powerLogCore`, `Fabius.powerLogCoreArg`, `Fabius.powerLogCoreRoot`. Theorems: `Fabius.powerLogCore_exp`, `Fabius.log_powerLogCoreRoot_sub`, `Fabius.powerLogCore_of_lambert`, `Fabius.powerLogCore_powerLogCoreRoot`, `Fabius.hasDerivAt_powerLogCore`, `Fabius.hasDerivAt_powerLogCore_root` |
| Controlled forward-remainder transport (`p0:thm:remainder-transport`) | **Exact.**  `RemainderTransport.lean` has no definitions and four theorems.  The first three give the derivative-to-Lipschitz bridge and division-free/divided transport bounds; `transport_first_order` proves part (2) with an explicit error under the weaker Lipschitz condition on the core derivative.  The perturbation's two-sided derivative bound remains distinct from the core's one-sided derivative floor. | `Fabius.lipschitzOn_of_abs_deriv_le`, `Fabius.transport_bound_mul`, `Fabius.transport_bound`, `Fabius.transport_first_order` |
| Backward-error residual transfer (`q3:prop:transfer`) | **Exact.** `BackwardErrorExistence.lean` is 1+6.  Composition with `MeanValueBracket` gives existence, location, and uniqueness; the slope condition is weaker than differentiability and the certified ball must fit inside `[a,b]`. | `Fabius.HasSlopeLowerBound`, `Fabius.hasSlopeLowerBound_of_le_deriv`, `Fabius.exists_eq_of_residual`, `Fabius.injOn_of_hasSlopeLowerBound`, `Fabius.existsUnique_eq_of_residual`, `Fabius.exists_eq_and_abs_sub_le`, `Fabius.exists_eq_of_le_deriv` |
| Real Cayley function and kernel (`p1:def:cayley`, `p1:def:cayley-kernel`) | **Partial / exact real content.** `CayleyTreeFunction.lean` is 1+8 and proves the functional equation, boundary values, monotonicity, and branch-conditioned uniqueness on `w≤e⁻¹` with `c≤1`; complex-disc analyticity remains absent. `CayleyKernel.lean` is 1+14 and proves exact real coefficient, summability, closed-form, and removable-origin identities, but no formal complex `Analytic` theorem. | Tree: `cayleyTree`, `principalLambertW_eq_neg_cayleyTree`, `cayleyTree_eq_mul_exp`, `cayleyTree_zero`, `cayleyTree_nonneg`, `cayleyTree_exp_neg_one`, `cayleyTree_strictMonoOn`, `cayleyTree_le_one`, `cayleyTree_unique`. Kernel: `cayleyKernelCoeff`, `factorial_pos_real`, `cayleyKernelCoeff_eq_factorial`, `cayleyKernelCoeff_zero`, `cayleyKernelCoeff_one`, `cayleyKernelCoeff_two`, `cayleyKernelCoeff_three`, `exp_eq_tsum_pow_div_factorial`, `tsum_exp_shift_one`, `tsum_exp_shift_two`, `cayleyKernelCoeff_mul_pow_eq_sub`, `summable_cayleyKernelTerm`, `tsum_cayleyKernelTerm_mul_sq`, `tsum_cayleyKernelTerm`, `tsum_cayleyKernelTerm_zero` |
| Cayley local coordinate (`p1:thm:omega`) | **Partial.** `CayleyLocalCoordinate.lean` is 1+7 and proves `υ=½v²Φ(v)` including `v=0` and the nonnegative-branch substitution.  Square-root reversion, the Bell master formula, and the Puiseux expansion remain absent. | `one_sub_mul_exp_le_one`, `cayleyUpsilon`, `cayleyUpsilon_zero`, `cayleyUpsilon_nonneg`, `cayleyUpsilon_eq_half_mul_kernel`, `cayleyUpsilon_eq_half_mul_sq_kernel`, `one_sub_upsilon_div_exp_one_le`, `cayleyTree_eq_one_sub` |
| Divisor transform and Lambert correction equation | `DivisorTransform.lean` is 2+9: `p1:def:divisor` is **Exact**, while `p1:lem:R-coefficients` remains **Partial** at composite numerical values, the Otter recurrence, and tree sequence. `LambertCorrectionEquation.lean` is 2+9; its explicit `B` values, derivative, and coefficient sum for `|uv|<1` are exact, but `q1:prop:B` remains **Partial** because the correction from the prior inverse reduction is not proved to satisfy `q=B_L(v)`. | Divisor: `divisorTransform`, `divisorTransform_zero`, `divisorTransform_eq_add_properDivisors`, `divisorTransform_sub`, `two_mul_le_of_mem_properDivisors`, `disturbanceCoeff`, `disturbanceCoeff_eq_divisorTransform`, `disturbanceCoeff_one`, `disturbanceCoeff_congr`, `disturbanceCoeff_prime`, `disturbanceCoeff_nonneg`. Correction: `corrB`, `corrCoeff`, `corrCoeff_zero`, `corrCoeff_one`, `corrB_zero`, `hasDerivAt_corrB`, `hasDerivAt_corrB_zero`, `deriv_corrB_zero_pos`, `summable_corrExpTerm`, `tsum_corrExpTerm`, `corrB_eq_tsum` |
| Exponential recurrence and Touchard operator | `ExpSeriesRecurrence.lean` is 1+3; its formal recurrence is exact, but `p1:lem:gamma-ratio` remains **Partial** at Bernoulli coefficients, explicit values, degree, and all-orders Stirling asymptotics. `TouchardEulerOperator.lean` is 2+8 and makes `q2:def:touchard` **Exact**, strengthened to `j=0` and all real `r`; normalization uses `r≠0` and `1+r>0`. | Recurrence: `expCoeff`, `expCoeff_zero`, `succ_mul_expCoeff_succ`, `natCast_mul_expCoeff`. Touchard: `touchardPolynomial_zero`, `coeff_touchardPolynomial`, `touchardPolynomial_succ_eq_X_mul_add_derivative`, `eulerOp`, `iterate_eulerOp_exp`, `exp_neg_mul_iterate_eulerOp_exp`, `eval_touchardPolynomial_succ_eq_sum_Icc`, `touchardQ`, `touchardQ_mul`, `touchardQ_one` |
| Fabius endpoint and sharp Laplace bounds | `FabiusEndpointTwoTerm.lean` is 0+2 and makes `plt:prop:mot-fabius-endpoint` **Exact** for every `0<x≤1/4`. `LaplaceMomentBoundsSharp.lean` is 0+7; it retains square-root denominators, gives the `O(n⁻³⁄²)` higher term, and exposes threshold 5329 with constants `3374/n` and `40537/(12n)`, without an optimality claim. | Endpoint: `fabiusReal_le_exp_endpoint`, `log_fabiusReal_le_endpoint`. Sharp bounds: `normalizedLaplaceMoment_three_le_sharp`, `normalizedLaplaceMoment_four_le_sharp`, `dyadicHigherLaplaceMoments_le_sharp`, `abs_negativeLaplaceTailError_nat_le_four_div`, `abs_dyadicEndpointLaplaceLogError_add_secondOrder_le_sharp`, `abs_dyadicEndpointLaplaceLogError_add_secondOrder_le_sharp'`, `abs_log_fabius_dyadic_sub_cumulantMain_le_sharp` |
| Lambert-shift concavity and certificate | `q1:prop:certificate` is **Exact** for `z,x≥0`, including the sign and sandwich. `q1:prop:brackets` remains **Partial** only at the one-sided endpoint derivative values; strict concavity on the closed domain and interior bounds are formal. | `strictConcaveOn_lambertShift`, `strictConcaveOn_lambertShift_Ici_zero`, `abs_sub_lambertShiftInv_sandwich`, `residual_sign_agrees`, `residual_eq_zero_iff` |
| Gevrey least-term index (`p0:thm:optimal-truncation`) | **Partial.** `LeastTermIndex.lean` is 1+6 and proves the exact ratio/unimodality mechanism; the Stirling envelope, transported inverse error, and exponential floor remain absent. | `gevreyTerm`, `gevreyTerm_succ`, `gevreyTerm_pos`, `gevreyTerm_succ_lt`, `gevreyTerm_lt_succ`, `gevreyTerm_antitoneOn`, `gevreyTerm_monotoneOn` |
| Power--log block classes and monomial uniqueness | `TransseriesBlockClasses.lean` is 0+3 and makes `plt:prop:mot-blocks` **Exact**, strengthened to real exponents, without a quotient type or whole normal form. `TransseriesMonomialUniqueness.lean` is 0+2 and proves positive-coefficient equivalence forces both exponents equal and coefficient one; `plt:cor:mot-both-generators-needed` remains **Partial** because the required three-term Wright-omega asymptotics and assembled scale statement are absent. | Classes: `plMonomial_zero_left`, `exists_log_bracket_iff`, `exists_log_bracket_div_iff`. Uniqueness: `tendsto_plMonomial_div_const_mul_one_iff`, `isEquivalent_plMonomial_const_mul_iff` |
| Smallest differential algebra (`plt:thm:mot-smallest-differential-algebra`) | **Partial.** `TransseriesDifferentialClosure.lean` is 2+9 and proves algebraic minimality clauses (i)--(iii) for an arbitrary commutative-ring/algebra derivation, plus a generic derivation-orbit closure.  The growth clause in (i) and algebraic-independence/unique-representation clause (iv) remain absent; part (v) is already exact through `TransseriesDifferentialBlock`. | `IsDerivationStable`, `isDerivationStable_top`, `isDerivationStable_iInf`, `isDerivationStable_adjoin`, `derivationOrbit`, `subset_derivationOrbit`, `derivation_mem_derivationOrbit`, `isLeast_adjoin_derivationOrbit`, `isLeast_adjoin_singleton_of_derivation_eq_one`, `isLeast_adjoin_pair_of_derivation_log`, `isLeast_adjoin_triple_of_derivation_log` |
| Staircase recovery and the half-error obstruction (`p0:thm:staircase`) | **Five recovery clauses Exact under their stated working hypotheses.**  `StaircaseInversion.lean` has no definitions and seven theorems.  It proves the least-ceiling identity, uncertainty separation, explicit arbitrarily-small failure of separation, unconditional half-unit rounding on the range, the least residue-class representative, and the local half-error obstruction for every continuous approximation across a unit jump.  The construction of admissible interpolations and the Fourier expansion of the periodic layer remain unformalized. | `Fabius.isLeast_ceil`, `Fabius.staircase_ceil`, `Fabius.staircase_separation`, `Fabius.staircase_separation_fails`, `Fabius.staircase_round`, `Fabius.isLeast_residue_class`, `Fabius.exists_half_error_of_jump` |
| Derangement defect and nearest-integer law (`p8:cor:nearest-integer`) | **Integer nearest-integer conclusion Exact; full surrounding result Partial.**  `DerangementNearestInteger.lean` has one definition and seven theorems.  It proves the defect integral recursion, strict integer-argument defect bounds, the signed identity `D_n-n!e^(-1)=(-1)^n C(n)`, the half-unit bound for `n≥1`, and `round (n!e^(-1))=D_n`.  The bound for arbitrary real `x>-1`, the branch-splitting theorem, and its interpolation family remain unformalized. | `Fabius.subfactorialDefect`, `Fabius.subfactorialDefect_zero`, `Fabius.subfactorialDefect_succ`, `Fabius.subfactorialDefect_pos`, `Fabius.subfactorialDefect_lt`, `Fabius.numDerangements_sub_eq`, `Fabius.abs_numDerangements_sub_lt_half`, `Fabius.round_factorial_mul_exp_neg_one` |
| Catalan quadratic core (`p6:prop:quadratic-core-catalan`, `p6:lem:quadratic-core`, `p6:thm:deepest-pole`) | `p6:prop:quadratic-core-catalan` is **Exact**.  `p6:lem:quadratic-core` is **Partial**: the coefficient family and its coefficientwise quadratic recursion are formalized, but not a packaged power-series existence/uniqueness theorem, square-root identity, or exact denominator-exponent clause.  `p6:thm:deepest-pole` is **Absent**: the Gamma/Barnes identification is not formalized.  `QuadraticCoreCatalan.lean` is a Mathlib-only characteristic-zero algebra leaf with three definitions and eight theorems; its finite Catalan recursion uses no analytic convergence. | Definitions: `Fabius.quadHalf`, `Fabius.halfBinom`, `Fabius.quadCoef`. Theorems: `Fabius.catalan_two_step`, `Fabius.quadHalf_zero`, `Fabius.quadHalf_antidiagonal`, `Fabius.halfBinom_step`, `Fabius.quadHalf_rat`, `Fabius.quadCoef_rat`, `Fabius.quadCoef_zero`, `Fabius.quadCoef_rec` |
| Half-base q-binomial root locus (`cor:halfbase-root-locus`) | **Exact by composition.**  `HalfQBinomialRootSimplicity.lean` has no public definitions and exactly one public theorem.  It proves over `ℚ` that every displayed root `2^j`, `j<n`, of the coefficientwise half-base Gaussian polynomial has multiplicity exactly one.  Combining it with the pre-existing complete rational zero classification and the Gaussian/half-q-binomial coefficient bridge gives precisely the manuscript roots `1,2,…,2^(n-1)`, no others, all simple.  The result is not an arbitrary-characteristic simplicity theorem or an arbitrary-base root classification. | `Fabius.halfQBinomial_sum_rootMultiplicity_two_pow`; composed with `Fabius.halfQBinomial_sum_eq_zero_iff` and `Fabius.gaussianBinomial_half_eq_halfQBinomial` |
| Leading Laurent coefficient of the reciprocal centered Rvachev MGF (`is:p2:thm:Laurent-leading`) | **Exact.**  `RvachevLaurentLeading.lean` has one public definition and exactly six public theorems.  It defines the manuscript-normalized centered MGF by `M(t)=centeredComplexGeneratingFunction F (2*t)` and proves `M(t)=Φ(i*t/(2π))`.  A reusable cofactor theorem gives the pole-cancelled reciprocal-product limit at every nonzero integer zero.  For `n=2^v*u` with odd signed `u`, the final wrapper proves order `v+1` at `T_n=2π*i*n` and leading coefficient `-T_n^(v+1)/M(π*i*u)`; the odd-core denominator is proved nonzero.  The limit is deliberately through the punctured neighborhood `𝓝[≠]`, since Lean totalizes inverse at the pole.  No lower Laurent coefficients, pole-shell summation, or resulting Appell asymptotics are included. | Definition: `Fabius.rvachevCenteredMGF`.  Theorems: `Fabius.rvachevCenteredMGF_eq_rvachevFourierProduct`, `Fabius.rvachevCenteredMGF_pi_mul_I_int`, `Fabius.rvachevCenteredMGF_pi_mul_I_int_ne_zero_of_odd`, `Fabius.tendsto_sub_pow_mul_inv_rvachevFourierProduct_int`, `Fabius.tendsto_rvachevCenteredMGF_laurent_int`, `Fabius.tendsto_rvachevCenteredMGF_laurent_two_pow_mul_odd` |
| Finite-prefix Appell expansions and exact recovery (`is:p2:thm:finite-prefix-expansion`, `is:p2:thm:exact-recovery`) | **Both Exact.**  `FinitePrefixAppellRecovery.lean` has eleven public definitions and exactly seventeen public theorems.  Its finite convolution construction gives the exact Kabaya--Iri expansion in powers of `2^-N` and the centered Rvachev expansion in even powers, equivalently powers of `4^-N`.  The outer polynomial-valued degrees in `Polynomial (Polynomial ℚ)` are exactly `n` and `⌊n/2⌋`.  This does not imply the same degree after specializing the inner variable to a fixed `x`; in particular the centered top coefficient can vanish at `x=0` for odd degree.  Existing geometric Lagrange weights then recover the full rational uncentered polynomial from every consecutive block of `n+1` prefixes at base `1/2`, and the full centered polynomial from every block of `⌊n/2⌋+1` prefixes at base `1/4`, exactly and without a limit.  This is rational coefficient algebra, not an analytic MGF-convergence result. | Definitions: `Fabius.unitUniformRawMomentRat`, `Fabius.centeredUnitUniformRawMomentRat`, `Fabius.dyadicPrefixScaleRat`, `Fabius.dyadicPrefixMomentRat`, `Fabius.uncenteredDyadicPrefixMomentRat`, `Fabius.centeredDyadicPrefixMomentRat`, `Fabius.kabayaIriAppellPolynomialRat`, `Fabius.uncenteredDyadicPrefixAppellPolynomialRat`, `Fabius.centeredDyadicPrefixAppellPolynomialRat`, `Fabius.uncenteredDyadicPrefixAppellScalePolynomialRat`, `Fabius.centeredDyadicPrefixAppellScalePolynomialRat`.  Theorems: `Appell.poly_binomialConv`, `Appell.binomialConv_dilate`, `Appell.dilate_dilate`, `Fabius.dyadicPrefixMomentRat_zero`, `Fabius.uncenteredDyadicPrefixMomentRat_zero`, `Fabius.centeredDyadicPrefixMomentRat_zero`, `Fabius.dyadicPrefixMomentRat_binomialConv_tail`, `Fabius.binomialConv_uncenteredDyadicPrefixMomentRat_tail`, `Fabius.binomialConv_centeredDyadicPrefixMomentRat_tail`, `Fabius.uncenteredDyadicPrefixAppellPolynomialRat_eq_sum`, `Fabius.centeredDyadicPrefixAppellPolynomialRat_eq_sum_even`, `Fabius.uncenteredDyadicPrefixAppellPolynomialRat_eq_eval_scale`, `Fabius.centeredDyadicPrefixAppellPolynomialRat_eq_eval_scale`, `Fabius.natDegree_uncenteredDyadicPrefixAppellScalePolynomialRat`, `Fabius.natDegree_centeredDyadicPrefixAppellScalePolynomialRat`, `Fabius.kabayaIriAppellPolynomialRat_eq_sum_prefix`, `Fabius.rvachevAppellPolynomialRat_eq_sum_prefix` |
| Complete finite-prefix Thue--Morse Appell collapses (`is:p2:thm:TM-uncentered`, `is:p2:cor:Prouhet-canonical`, `is:p2:thm:TM-centered`) | **All three Exact by the named compositions.**  `FinitePrefixThueMorseCollapse.lean` has no public definitions and exactly eight public theorems.  The generic theorem diagonalizes an arbitrary rational Appell polynomial against `thueMorsePowerSum`.  The uncentered theorem uses `thueMorseSign k` on `x+k/2^N` and returns `(-1)^N 2^{-((N+1).choose 2)} n.descFactorial N x^(n-N)`; its strict-low-degree and first-response corollaries return zero for `n<N` and `(-1)^N N! 2^{-((N+1).choose 2)}`.  The centered theorem uses `x+(1-2^-N)-2k/2^N` and returns the sign-free `2^{-(N.choose 2)} n.descFactorial N x^(n-N)`; its positive-depth successor form is the manuscript grid `x+(1-2^{-(m+1)})-k/2^m`, and its strict-low-degree and first-response corollaries return zero and `N! 2^{-(N.choose 2)}`.  The total primary and first-response forms include `N=0`, strengthening the printed positive-depth presentation.  The uncentered main theorem supplies `is:p2:thm:TM-uncentered`; its `_of_lt` and `_self` consequences supply `is:p2:cor:Prouhet-canonical`; the centered `_succ` theorem supplies the literal `is:p2:thm:TM-centered` grid, while the primary centered theorem adds depth zero.  This is exact rational coefficient-model algebra.  It introduces no random variable and no `HasLaw`, analytic-MGF, or Barnes-function identification. | `Appell.sum_thueMorseSign_mul_eval_poly`, `Fabius.sum_thueMorseSign_mul_uncenteredDyadicPrefixAppellPolynomialRat`, `Fabius.sum_thueMorseSign_mul_uncenteredDyadicPrefixAppellPolynomialRat_of_lt`, `Fabius.sum_thueMorseSign_mul_uncenteredDyadicPrefixAppellPolynomialRat_self`, `Fabius.sum_thueMorseSign_mul_centeredDyadicPrefixAppellPolynomialRat`, `Fabius.sum_thueMorseSign_mul_centeredDyadicPrefixAppellPolynomialRat_succ`, `Fabius.sum_thueMorseSign_mul_centeredDyadicPrefixAppellPolynomialRat_of_lt`, `Fabius.sum_thueMorseSign_mul_centeredDyadicPrefixAppellPolynomialRat_self` |
| Dyadic-boundary composition for the Rvachev sinc product | **Imported API; no additional status move recorded here.**  `DyadicBoundaryIdentity.lean` has no public definitions and exactly three public theorems.  The prefix/product identity and denominator-cleared entire identity remain total.  The added theorem states the printed norm-quotient formula on exactly `0 < z < 2^k`, using central-lobe positivity to prove its denominator nonzero. | `Fabius.prod_complexSinc_prefix_mul_rvachevFourierProduct`, `Fabius.rvachevFourierProduct_dyadic_boundary`, `Fabius.norm_rvachevFourierProduct_dyadic_boundary` |
| Base-two bridge from digit-weighted Prouhet sums to Thue--Morse power sums | **Imported API; existing statuses unchanged.**  `ProuhetBaseTwoBridge.lean` has no public definitions and exactly six public theorems over every commutative ring.  It identifies the base-two digit-weight at `ζ=-1` with the Thue--Morse sign and transfers the general Prouhet cancellation to the classical block.  Its sharp self-moment is the closed formula `(-1)^m 2^(m.choose 2) m!`; the final theorem records the same result in the general machine's division-free form.  This bridge does not introduce a new source-result status. | `Fabius.thueMorseSign_cast_eq_neg_one_pow_digits_sum`, `Fabius.digitPowerSum_neg_one_two`, `Fabius.sum_range_two_neg_one_pow`, `Fabius.digitPowerSum_neg_one_two_eq_zero_of_lt`, `Fabius.digitPowerSum_neg_one_two_self`, `Fabius.sub_one_pow_mul_thueMorsePowerSumRing_self` |
| Explicit recurrent values and oscillation of the Newman ratio | **API strengthening; existing status retained.**  `ThueMorseNewmanSelfSimilarity.lean` is now 1+19.  The seven added theorems identify `8^(logb 4 3)=3*sqrt 3`, evaluate the second recurrent ratio as `sqrt 3 / 3`, prove it is strictly below `2/3`, show both recurrent values occur frequently at `atTop`, and package the explicit oscillation. | `Fabius.eight_rpow_logb_four_three`, `Fabius.sqrt_three_div_three_lt_two_div_three`, `Fabius.newman_ratio_eight`, `Fabius.newman_ratio_eight_lt_two_div_three`, `Fabius.frequently_newmanRatio_eq_two_div_three`, `Fabius.frequently_newmanRatio_eq_sqrt_three_div_three`, `Fabius.newmanRatio_oscillates` |
| Up-tail representation and positive moments (`prop:up-tail`, `cor:up-moments`) | **Both Exact.**  The two newest theorems in `ProbabilityLaplaceMoments.lean` close exactly the clauses missing from the older survival/layer-cake API.  Atomlessness identifies `weightedSumDistribution.real (Ici t)` with `up(t)` for every `t >= 0`; combined with the global identities `up(y)=F(1-|y|)` and `up(t)=1-F(t)`, this proves every clause of `prop:up-tail` on domains stronger than printed.  For every natural `n >= 1`, the second theorem gives the full-line expectation identity `integral x^n dμ = n * integral_0^1 t^(n-1) up(t) dt`, exactly `cor:up-moments`.  The law is the canonical weighted-sum law representing the manuscript's `X`; no report-shaped conjunction or arbitrary-random-variable wrapper is introduced. | `Fabius.rvachevUp_eq_fabiusReal_one_sub_abs`, `Fabius.rvachevUp_eq_one_sub_fabiusReal_of_nonneg`, `Fabius.ProbabilityRepresentation.weightedSumDistribution_real_Ici_eq_rvachevUp_of_nonneg`, `Fabius.ProbabilityRepresentation.integral_pow_weightedSumDistribution_eq_mul_intervalIntegral_rvachevUp` |
| Central-binomial dyadic cancellation (`cor:leg-central-sum`) | **Exact/Complete.**  `RvachevLegendreCentralSum.lean` has no public definitions and exactly three public theorems.  For every `F : BoundedFabius` with `IsFabius F` and every `n`, it proves the printed `M=4^n` identity `Q_(2n)(0)+2∑_(0<k<M)Q_(2n)(k/M)up(k/M)=(-1)^n choose(2n,n)`, including `n=0`.  The proof evaluates the existing even-mode finite synthesis at zero, removes `M≤|k|<2M` by compact support, pairs the remaining nonzero indices using evenness of both the decoder and `up`, clears the mesh, and uses the exact central value of the Rodrigues-normalized Legendre polynomial.  Quantifying over any bounded Fabius representative is a harmless strengthening.  This closes only the central-sum corollary: it supplies no Jacobi closed form, all-degree decoder parity or rationality, reverse spectral closure, mesh minimality, or larger Lagrange right inverse. | `Fabius.eval_legendrePolynomial_even_zero`, `Fabius.eval_rvachevLegendreDeconvolutionPolynomial_even`, `Fabius.rvachevLegendreCentralSum` |
| Legendre--up dyadic biorthogonality (`thm:leg-biorthogonality`) | **Exact.**  `RvachevLegendreBiorthogonality.lean` has exactly one public definition and one public theorem.  Its analysis kernel is literally `((2*m+1)/2) * integral_(-1)^1 up(x-c) P_m(x) dx`.  For `F : BoundedFabius` with `IsFabius F`, natural `l,m`, a natural mesh `M != 0`, and exactly `l <= padicValNat 2 M`, the theorem proves `M⁻¹ * ∑_(|k|<2M) Q_l⁻(k/M) Lambda_m(k/M) = if m=l then 1 else 0`; its `Finset.Ioo (-(2*M)) (2*M)` is the manuscript's open integer block.  These are precisely the positive-mesh and valuation hypotheses, so the row is Exact.  The broader `thm:leg-Lambda` remains incomplete because the leaf does not prove support, smoothness, parity, origin values, a Fourier--Bessel representation, or dyadic rationality of the kernel.  Likewise `cor:leg-biorthogonal-matrices` remains incomplete: the scalar Kronecker identity is present, but no bundled finite-matrix projector or reverse spectral closure is exported. | Definition: `Fabius.rvachevLegendreAnalysisKernel`.  Theorem: `Fabius.rvachevLegendreBiorthogonality` |
| Total integer zero-row Wigner-square datum and closed Legendre Gaunt form | `LegendreGauntClosedForm.lean` has exactly two public definitions and twenty-five public theorems.  Its admissibility predicate means even `i+j+k` and the three weak triangle inequalities, equivalently `i=b+c`, `j=a+c`, `k=a+b` for naturals `a,b,c`.  With `C_n=choose (2*n) n` and `s=a+b+c`, the total rational square datum is zero off support and equals `C_a*C_b*C_c/((2*s+1)*C_s)` on the pairwise-add coordinates; its factorial form is `s!^2*(2*a)!*(2*b)!*(2*c)!/((2*s+1)!*a!^2*b!^2*c!^2)`.  The half-sum theorem assumes exactly `i+j+k=2*s` and `i≤s`, `j≤s`, `k≤s`.  The degenerate boundaries are `legendreGauntRat i j (i+j)=2*C_i*C_j/((2*(i+j)+1)*C_(i+j))=2*W²(i,j,i+j)` and `legendreGauntRat 0 j k = if j=k then 2/(2*j+1) else 0 = 2*W²(0,j,k)`.  At every natural triple, rational Gaunt is `2*W²`, real Gaunt is twice its real cast, the square and both Gaunt forms are nonnegative, positivity is equivalent to admissibility, and vanishing is equivalent to nonadmissibility.  The product-linearization coefficient is `(2*k+1)*W²` and is positive exactly on support.  Here `W²` abbreviates the directly defined `legendreWignerThreeJZeroSqRat`; it is only an integer-index zero-row square datum, with no signed/phase, half-integer, nonzero-magnetic-index, general `3j`/`6j`/`9j`, orthogonality, recoupling, or infinite-series claim. | Definitions: `Fabius.legendreGauntAdmissible`, `Fabius.legendreWignerThreeJZeroSqRat`.  Theorems: `Fabius.legendreGauntAdmissible_iff_exists_pairwise_add`, `Fabius.legendreGauntAdmissible_pairwise_add`, `Fabius.legendreWignerThreeJZeroSqRat_pairwise_add`, `Fabius.legendreWignerThreeJZeroSqRat_pairwise_add_factorial`, `Fabius.legendreWignerThreeJZeroSqRat_eq_factorial_of_halfSum`, `Fabius.legendreWignerThreeJZeroSqRat_eq_zero_of_not_admissible`, `Fabius.legendreGauntRat_add_boundary`, `Fabius.legendreGauntRat_add_boundary_eq_two_mul_wignerThreeJZeroSqRat`, `Fabius.legendreGauntRat_zero_left`, `Fabius.legendreGauntRat_zero_left_eq_two_mul_wignerThreeJZeroSqRat`, `Fabius.legendreGauntRat_pairwise_add_eq_two_mul_wignerThreeJZeroSqRat`, `Fabius.legendreGauntRat_eq_zero_of_not_admissible`, `Fabius.legendreGauntRat_eq_two_mul_wignerThreeJZeroSqRat`, `Fabius.legendreGaunt_eq_two_mul_wignerThreeJZeroSqRat`, `Fabius.legendreWignerThreeJZeroSqRat_pos_iff_admissible`, `Fabius.legendreWignerThreeJZeroSqRat_nonneg`, `Fabius.legendreWignerThreeJZeroSqRat_eq_zero_iff_not_admissible`, `Fabius.legendreGauntRat_pos_iff_admissible`, `Fabius.legendreGauntRat_eq_zero_iff_not_admissible`, `Fabius.legendreGaunt_pos_iff_admissible`, `Fabius.legendreGaunt_eq_zero_iff_not_admissible`, `Fabius.legendreGauntRat_nonneg`, `Fabius.legendreGaunt_nonneg`, `Fabius.legendreProductLinearizationCoeffRat_eq_mul_wignerThreeJZeroSqRat`, `Fabius.legendreProductLinearizationCoeffRat_pos_iff_admissible` |
| Finite Wigner-square sums for rational and real up-law Legendre Gram entries | `FabiusLegendreGauntClosedForm.lean` has zero public definitions and exactly three public theorems.  Unconditionally for natural `i,j`, `rvachevLegendreGramEntryRat i j` is `2 * ∑ r ∈ range ((i+j)/2+1), canonicalRvachevLegendreCoefficientRat r * legendreWignerThreeJZeroSqRat i j (2*r)`; the identical formula holds entrywise for `i j : Fin n` in the rational Gram matrix.  For `F : BoundedFabius` with exactly `hF : IsFabius F`, the real up-Gram entry is the same finite twice-sum using `rvachevLegendreCoefficient F r` and the real cast of the square datum.  This closes the finite Wigner-square Gram route only; it supplies no signed/general Wigner symbol and no infinite Legendre-series interchange, Christoffel reconstruction, root/quadrature, Padé/J-fraction, infinite-Jacobi, or asymptotic theorem. | `Fabius.rvachevLegendreGramEntryRat_eq_two_mul_sum_wignerThreeJZeroSqRat`, `Fabius.rvachevLegendreGramMatrixRat_apply_eq_two_mul_sum_wignerThreeJZeroSqRat`, `Fabius.upLegendreGramMatrix_apply_eq_two_mul_sum_wignerThreeJZeroSqRat` |
| Denominator-free geometric complete-homogeneous and Lagrange residual layer | The six `GeometricCompleteHomogeneous` theorems prove both orientations of the geometric principal specialization, its common-scale and range forms, and two denominator-free Gaussian symmetry laws over every commutative semiring.  The five `GeometricLagrangeCompleteHomogeneous` theorems retain report-facing aliases, specialize every offset residual moment, and connect the recursive Gaussian coefficient to the rational q-binomial API under exactly the stated field/injectivity or `0 < q < 1` hypotheses.  At target zero, `sum_lagrangeEvalWeight_mul_pow_card_add_zero` gives every higher moment of a nonempty distinct field-valued node family as the negative signed nodal product times its complete homogeneous function; nonemptiness excludes precisely the empty-row `0^0` exception. | `Fabius.completeHomogeneousEval_geometric`, `Fabius.completeHomogeneousEval_scaled_geometric`, `Fabius.completeHomogeneousEvalOn_range_pow_eq_gaussianBinomial`, `Fabius.completeHomogeneousEvalOn_range_pow_eq_gaussianBinomial_degree`, `Fabius.gaussianBinomial_add_symm`, `Fabius.gaussianBinomial_symm_via_completeHomogeneous`; `Fabius.completeHomogeneousEvalOn_geometric_range`, `Fabius.sum_geometricLagrangeWeight_mul_pow_succ_add_eq_gaussianBinomial`, `Fabius.geometricLagrangeQMoment_eq_residual_gaussianBinomial`, `Fabius.completeHomogeneousEvalOn_geometric_range_eq_qBinomial`, `Fabius.geometricLagrangeQMoment_eq_residual_qBinomial_via_completeHomogeneous`; `Fabius.sum_lagrangeEvalWeight_mul_pow_card_add_zero` |
| Combinatorial Coefficient Calculus fixed-column Stirling OGF (`thm:second-ogf`) | **Exact in its formal/algebraic reading after focused verification.**  The zero-definition/eight-theorem `StirlingCompleteHomogeneous.lean` leaf identifies the existing Stirling column power series with the complete-homogeneous generating series over every commutative ring, extracts `S(k+r,k)=h_r(1,…,k)` and the finite multiplicity sum over every commutative semiring, and supplies `Fin k` and Mathlib `hsymm` forms.  The `n-k` statement correctly assumes `k≤n`; `k=0` is included.  Over a field and exactly `x≠0`, the final two theorems clear and invert `x^(k+1)(1/x)_(k+1)` to the product of `1-jx`.  Lean's reciprocal is totalized, so an ordinary scalar reading must still exclude the product's poles; no scalar convergence or analytic-continuation theorem is claimed. | `Fabius.stirlingColumnOGF_eq_completeHomogeneousGeneratingSeriesOn`, `Fabius.stirlingSecond_add_eq_completeHomogeneousEvalOn`, `Fabius.stirlingSecond_eq_completeHomogeneousEvalOn_of_le`, `Fabius.stirlingSecond_add_eq_completeHomogeneousEval`, `Fabius.stirlingSecond_add_eq_eval_hsymm`, `Fabius.stirlingSecond_add_eq_sum_finsuppAntidiag`, `Fabius.pow_mul_descPochhammer_eval_inv_eq_prod_one_sub_natCast_mul`, `Fabius.prod_inv_one_sub_natCast_mul_eq_inv_pow_mul_descPochhammer_eval_inv`; reused existing OGF results: `Fabius.prod_one_sub_mul_X_mul_stirlingColumnOGF`, `Fabius.stirlingColumnOGF_eq_prod_mk_pow` |
| Exact finite polynomial filters | The five-theorem `FinitePolynomialFilterExactness` surface first evaluates an arbitrary finite coefficient filter on a baseline plus finitely many geometric modes, then derives mass-one/root exactness over every commutative semiring.  Its field-valued geometric specializations cancel the prescribed inverse or forward modes, preserve the baseline, and evaluate the first uncancelled forward mode as `(-1)^n q^(choose (n+1) 2)`, retaining the explicit nonzero-base and normalization-denominator assumptions. | `Fabius.polynomialFilter_response_eq`, `Fabius.polynomialFilter_exact`, `Fabius.normalizedGeometricRootPolynomial_filter_exact`, `Fabius.forwardGeometricRichardsonPolynomial_filter_exact`, `Fabius.forwardGeometricRichardsonPolynomial_filter_firstUncancelled` |
| Complete rational geometric-moment and finite-conditioning API | `GeometricLagrangeQMoments` exhaustively contains one definition and 37 theorems.  They identify the moment with the weight and forward-Richardson polynomials, prove exact zero and residual q-Pochhammer/q-binomial forms, positivity and sign on `0 < q < 1`, both Pascal orientations and the finite q-binomial theorem in the rational quotient notation, pointwise absolute weights and exact row variation, and all dedicated quarter-base forms.  These are finite algebraic identities; quotient statements retain their nonzero-denominator hypotheses and no analytic convergence or remainder estimate is asserted. | `Fabius.geometricLagrangeQMoment`; `Fabius.geometricLagrangeQMoment_eq_weightPolynomial_eval`, `Fabius.geometricLagrangeQMoment_eq_forwardRichardson_eval`, `Fabius.geometricRootPolynomial_inv_eval_pow_mul_signedPowers`, `Fabius.geometricRootPolynomial_inv_eval_pow_mul_triangular`, `Fabius.geometricRootPolynomial_inv_eval_one_mul_triangular`, `Fabius.geometricLagrangeQMoment_eq_qPochhammer`, `Fabius.geometricLagrangeQMoment_zero`, `Fabius.geometricLagrangeQMoment_eq_zero`, `Fabius.geometricRootPolynomial_inv_eval_pow_eq_qPochhammer_of_le`, `Fabius.geometricLagrangeQMoment_eq_residual_qPochhammer`, `Fabius.qPochhammer_self_add`, `Fabius.qPochhammer_self_pos_of_pos_of_lt_one`, `Fabius.qBinomial_pos_of_pos_of_lt_one`, `Fabius.gaussianBinomial_eq_qBinomial_of_pos_of_lt_one`, `Fabius.qPochhammer_pow_pos_of_pos_of_lt_one`, `Fabius.qPochhammer_tail_div_self_eq_qBinomial`, `Fabius.geometricLagrangeQMoment_eq_residual_qBinomial`, `Fabius.geometricLagrangeQMoment_firstUncancelled`, `Fabius.negOnePow_mul_geometricLagrangeQMoment_eq_positiveResidual`, `Fabius.negOnePow_mul_geometricLagrangeQMoment_pos`, `Fabius.qPochhammer_self_succ`, `Fabius.qBinomial_succ_succ_of_pos_of_lt_one'`, `Fabius.qBinomial_succ_succ_of_pos_of_lt_one`, `Fabius.qBinomial_theorem_of_pos_of_lt_one`, `Fabius.sum_qBinomial_triangular_succ_eq_neg_qPochhammer`, `Fabius.abs_geometricLagrangeWeight_eq_qBinomial`, `Fabius.abs_geometricLagrangeWeight_eq_sign_mul`, `Fabius.abs_geometricLagrangeWeight_complement_eq_qBinomial`, `Fabius.sum_abs_geometricLagrangeWeight_eq_qPochhammer_ratio`, `Fabius.neg_qPochhammer_div_self_eq_prod`, `Fabius.sum_abs_geometricLagrangeWeight_eq_prod`, `Fabius.quarterGeometricLagrangeQMoment_eq_qPochhammer`, `Fabius.quarterGeometricLagrangeQMoment_eq_zero`, `Fabius.quarterGeometricLagrangeQMoment_eq_residual_qPochhammer`, `Fabius.quarterGeometricLagrangeQMoment_eq_residual_qBinomial`, `Fabius.quarterGeometricLagrangeQMoment_firstUncancelled`, `Fabius.sum_abs_quarterGeometricLagrangeWeight_eq_qPochhammer_ratio` |
| `gq:thm:richardson-generating`: Euler factorization of the geometric Richardson triangle | **Exact.** `GeometricRichardsonGenerating.lean` has exactly three public definitions and seven public theorems.  Its commutative-ring layer defines the Euler kernel, q-Pochhammer-normalized data series, and lower-triangular transform, proves the two coefficient identities, and factors the formal generating series without topology or any regularity assumption on `q`; `Ring.inverse` gives a total expression even when a finite q-Pochhammer factor is not a unit.  Over every field with `q ≠ 0`, `geometricRichardsonTransform_eq_sum_lagrange` identifies the convolution coefficient with the repository's canonical totalized geometric Lagrange row, and `geometricLagrangeRichardson_generating` is the displayed manuscript identity.  Roots of unity need not be excluded for these algebraic equalities, but colliding nodes are not thereby a valid interpolation scheme; at `q = 0`, repeated nodes occur for `n ≥ 2` and the Lagrange closed form fails, so the report-facing identification correctly excludes it.  The two analytic companions work over a complete normed field under `‖q‖ < 1` and norm-summability of the normalized data at `z`; the Lagrange form again assumes `q ≠ 0` and proves `HasSum`.  They do not assert that arbitrary formal power series can be evaluated analytically. | Definitions: `Fabius.geometricRichardsonKernel`, `Fabius.qPochhammerNormalizedDataSeries`, `Fabius.geometricRichardsonTransform`.  Theorems: `Fabius.coeff_rescale_qPochhammerSeries_eq_geometricRichardsonKernel`, `Fabius.coeff_qPochhammerNormalizedDataSeries`, `Fabius.geometricRichardsonTransform_generating`, `Fabius.geometricRichardsonTransform_eq_sum_lagrange`, `Fabius.geometricLagrangeRichardson_generating`, `Fabius.hasSum_geometricRichardsonTransform_mul_pow`, `Fabius.hasSum_geometricLagrangeRichardson_mul_pow` |
| Banach-valued survival-kernel calculus (reusable foundation) | For any finite measure supported on an arbitrary compact interval `[a,b]`, every Bochner-integrable kernel with values in a complete real normed space satisfies `∫ (∫ₐˣ k(t)dt) dμ(x) = ∫ₐᵇ μ((t,∞)) • k(t)dt`.  More generally, for `c ∈ [a,b]`, clipping the primitive at `min(x,c)` clips the survival integral at `c`.  These statements require neither normalization nor continuity nor a preselected antiderivative, and include real and complex kernels.  For a probability law they are expectation identities; for the Fabius weighted-sum law the survival factor is exactly `rvachevUp`.  Scalar corollaries have the draft report's full- and partial-endpoint orientations, and the older continuously differentiable probability integration-by-parts theorem is now a short corollary of the kernel identity.  Fractional powers and complex Mellin continuation remain separate frontier claims. | `Fabius.integral_Icc_intervalIntegral_eq_intervalIntegral_smul_survival`, `Fabius.integral_Icc_intervalIntegral_min_eq_intervalIntegral_smul_survival`, `Fabius.ProbabilityRepresentation.integral_Icc_intervalIntegral_eq_intervalIntegral_smul_rvachevUp`, `Fabius.ProbabilityRepresentation.integral_Icc_intervalIntegral_min_eq_intervalIntegral_smul_rvachevUp`, `Fabius.ProbabilityRepresentation.intervalIntegral_mul_rvachevUp_eq_integral_Icc_intervalIntegral`, `Fabius.ProbabilityRepresentation.intervalIntegral_mul_rvachevUp_eq_integral_Icc_intervalIntegral_min`, `Fabius.integral_Icc_eq_left_add_intervalIntegral_deriv_mul_survival`, `Fabius.ProbabilityRepresentation.integral_unit_eq_zero_add_integral_deriv_mul_rvachevUp` |
| Fourier--Legendre expansion of Rvachev's up function, with the displayed finite formula for the coefficient of `P_(2n)` | Proved exactly on the natural domain `x ∈ [-1,1]`.  All odd coefficients vanish, and the even series converges absolutely and uniformly on the closed interval, hence pointwise at both endpoints. | `Fabius.rvachevFullLegendreCoefficient_odd_eq_zero`, `Fabius.canonical_rvachevLegendreCoefficient_eq_fabius_sum`, `Fabius.hasSum_canonical_rvachevLegendreSeries_formula`, `Fabius.tsum_canonical_rvachevLegendreSeries_formula`, `Fabius.hasSum_canonical_rvachevLegendreSeries_formula_uniform`, `Fabius.tsum_canonical_rvachevLegendreSeries_formula_uniform` |
| Least-squares optimality of every finite `up` Legendre partial sum | Proved with the exact Pythagorean error identity and uniqueness.  The sum through `P_(2N)` has degree at most `2N` and is the unique minimizer even among all real polynomials of degree at most `2N+1`; therefore it is in particular the best approximation of its corresponding visible degree. | `Fabius.rvachevLegendrePartialSumPolynomial_natDegree_le`, `Fabius.integral_rvachevUp_sub_partialSum_mul_polynomial_eq_zero`, `Fabius.rvachevLegendrePartialSum_pythagorean`, `Fabius.rvachevLegendrePartialSum_least_squares`, `Fabius.rvachevLegendrePartialSum_strict_least_squares`, `Fabius.rvachevLegendrePartialSum_error_eq_iff`, `Fabius.canonical_rvachevLegendrePartialSum_mem_and_isMinOn` |
| Legendre-block orthogonality, Parseval energy, exact coefficient tail, and the Legendre series for the Fabius square energy `A_2` | At compiled source checkpoint `ca66a1247`, proved exactly for the polynomial-form blocks `B_n = u_n*P_(2n)`: their inner product is zero off the diagonal and `2*u_n^2/(4n+1)` on it.  The same coefficient energies have both a full `HasSum` identity for the squared `L²[-1,1]` norm of `up` and a shifted-tail `HasSum` identity for every partial-sum squared error; the latter also has a `tsum` equality.  With `A_2 = integral (0..1), F(t)^2 dt`, reflection gives `A_2 = sum u_n^2/(4n+1)`.  At compiled source checkpoint `9d5f41c2c`, `FabiusLegendreRationalEnergy` adds three executable rational definitions and fifteen theorems: every finite partial sum has an exact positive rational representative, these representatives form a monotone sequence, and their real casts converge to `A_2`; the first four values are exactly `1/4`, `7/18`, `3271/8100`, and `3246043/8037225`. | `Fabius.rvachevLegendreBlock`, `Fabius.intervalIntegral_rvachevLegendreBlock_mul`, `Fabius.integral_sq_eval_rvachevLegendrePartialSumPolynomial`, `Fabius.hasSum_rvachevLegendreCoefficient_energy`, `Fabius.hasSum_rvachevLegendreCoefficient_energy_tail`, `Fabius.rvachevLegendreSquaredError_partialSum_eq_tsum_tail`, `Fabius.fabiusSquareEnergy`, `Fabius.integral_sq_rvachevUp_eq_two_mul_fabiusSquareEnergy`, `Fabius.hasSum_fabiusSquareEnergy_legendre`, `Fabius.fabiusSquareEnergy_eq_tsum_legendre`; `Fabius.canonicalRvachevLegendreCoefficientRat`, `Fabius.fabiusSquareEnergyTermRat`, `Fabius.fabiusSquareEnergyPartialSumRat`, `Fabius.canonicalRvachevLegendreCoefficientRat_cast`, `Fabius.canonicalRvachevLegendreCoefficientRat_zero`, `Fabius.fabiusSquareEnergyTermRat_cast`, `Fabius.fabiusSquareEnergyTermRat_nonneg`, `Fabius.fabiusSquareEnergyTermRat_zero`, `Fabius.fabiusSquareEnergyPartialSumRat_cast`, `Fabius.monotone_fabiusSquareEnergyPartialSumRat`, `Fabius.fabiusSquareEnergyPartialSumRat_pos`, `Fabius.fabiusSquareEnergyPartialSumRat_zero`, `Fabius.fabiusSquareEnergyPartialSumRat_one`, `Fabius.fabiusSquareEnergyPartialSumRat_two`, `Fabius.fabiusSquareEnergyPartialSumRat_three`, `Fabius.hasSum_fabiusSquareEnergy_ratCast`, `Fabius.fabiusSquareEnergy_eq_tsum_ratCast`, `Fabius.tendsto_fabiusSquareEnergyPartialSumRat_cast` |
| Fourier and dyadic sinc-product formulas for the Fabius square energy `A_2` | At compiled source checkpoint `b9b240bc0`, `FabiusSquareEnergyFourier` exports no definitions and exactly four theorems.  For every bounded Fabius solution, the full real-axis integral of the squared norm of its Rvachev Fourier transform is `2*A_2`, while the positive-half-line integral is `A_2`.  Substitution of the real dyadic sinc product gives both the report's `sinc_pi` product indexed from zero and, after `t = 2*pi*xi`, its `1/(2*pi)`-scaled sine-quotient product indexed from one. | `Fabius.integral_norm_sq_rvachevFourier_eq_two_mul_fabiusSquareEnergy`, `Fabius.fabiusSquareEnergy_eq_integral_Ioi_norm_sq_rvachevFourier`, `Fabius.fabiusSquareEnergy_eq_integral_Ioi_tprod_sinc_sq`, `Fabius.fabiusSquareEnergy_eq_scaled_integral_Ioi_tprod_sinc_sq` |
| Shifted dyadic polynomial comb exactness and normalized self-sampling quadrature | `PolynomialCombExactness` has zero definitions and exactly three public theorems.  For every bounded Fabius solution, natural level `m`, real phase `theta`, and arbitrary real weight function `g`, its sampled product with `up(2^-m * (theta+k))` has finite integer support.  Every real polynomial `P` with `P.natDegree <= m` satisfies the resulting whole-line shifted comb identity.  After physical-coordinate normalization, for every natural `N`, arbitrary real phase, and `P.natDegree <= N`, the integral of `P * up` equals `2^-N` times the integer sum over nodes `2^-N * (theta+k)` weighted by `up` at those nodes.  Level zero is included; compact support makes both sums finite, and no phase rationality, positivity, infinite-support summability, or optimal-mesh assertion is used. | `Fabius.finite_support_comb`, `Fabius.tsum_shifted_polynomial_eq_integral`, `Fabius.integral_polynomial_mul_rvachevUp_eq_dyadic_tsum` |
| Parity-selected one-extra-degree Rvachev quadrature and Appell synthesis | The current compiled `RvachevSuperconvergentSynthesis` module exports exactly one definition and eight theorems.  For every nonzero natural mesh `M`, its selected endpoint or quarter phases give monomial and polynomial exactness through degree `v₂(M)+1`, physical-coordinate quadrature, deconvolved-polynomial reconstruction, and the Appell monomial specialization.  At `M=2^N`, even `N` selects the exact representatives `0,1/2`, while odd `N` selects `1/4,3/4`; the arbitrary-`M` theorem is stronger than the dyadic-only manuscript claim.  The predicate is not a modulo-integer wrapper or a complete phase classification, and the module proves no maximality, positivity, or rationality result. | Definition: `Fabius.IsRvachevSuperconvergentPhase`.  Theorems: `Fabius.isRvachevSuperconvergentPhase_two_pow_iff`, `Fabius.tsum_quarter_monomial_eq_integral_of_even_deg`, `Fabius.tsum_three_quarters_monomial_eq_integral_of_even_deg`, `Fabius.tsum_shifted_monomial_eq_integral_superconvergent`, `Fabius.tsum_shifted_polynomial_eq_integral_superconvergent`, `Fabius.integral_polynomial_mul_rvachevUp_eq_normalized_tsum_superconvergent`, `Fabius.normalized_tsum_shifted_rvachevDeconvolvedPolynomial_mul_rvachevUp_superconvergent`, `Fabius.normalized_tsum_shifted_rvachevAppellPolynomial_mul_rvachevUp_superconvergent` |
| Exact polynomial and finite Legendre synthesis by shifted Rvachev atoms, literal outer translate-block convergence, and uniformly convergent one-mesh partial trains | In the current compiled tree, `RvachevPolynomialSynthesis` has no public definitions and five theorems.  For every nonzero natural mesh `M` and real polynomial of degree at most `v₂(M)`, it proves the global raw and normalized `tsum` formulas and their exact finite `k ∈ (-2M,2M)` forms on `[-1,1]`.  Its fifth theorem, `Fabius.normalized_tsum_shifted_rvachevDeconvolvedPolynomial_mul_rvachevUp`, samples `up` at `M⁻¹ * (θ + k)` and the deconvolved polynomial at `x - M⁻¹ * (θ + k)`, reconstructing `P.eval x` for arbitrary real phase `θ` and real `x`.  At `M = 2^N` this arbitrary-phase layer formalizes reproduction through every degree `n ≤ N`; the separate `RvachevSuperconvergentSynthesis` row strengthens it by one degree at parity-selected phases.  `FabiusLegendreTranslateBlocks` has six definitions and seven theorems: it specializes the mesh to `2^d` and `4^n`, proves `Q_d = D(P_d)` has exact natural degree `d` and leading coefficient `(1/2)^d * choose (2*d) d`, realizes each polynomial Legendre block as a literal finite shifted-up block, and proves block orthogonality and the atom-Gram double sum.  The focused-build `FabiusLegendreTranslateSeries` module has five definitions and twenty-five theorems.  It proves that `C_N = D(S_N)` has the same `natDegree` and leading coefficient as `S_N`, including degenerate visible-degree drops; absolute pointwise and supremum-norm summability of the literal blocks; their pointwise and uniform `HasSum`/`tsum` identities; the finite-mode formula for `C_N`; the expanded common-mesh coefficients; global and finite synthesis at mesh `4^N`; and equality of the resulting function with both the polynomial partial sum and the sum of the separately scaled blocks.  Its bundled common-mesh partial trains converge to `rvachevUp` in `C([-1,1])`, equivalently in the interval supremum norm; it also gives the raw `TendstoUniformlyOn` form, convergence of the supremum-norm error to zero, and the pointwise corollary on `[-1,1]`.  These APIs do not prove minimality or sharpness of the mesh, analytic reciprocal-MGF or differential-series identities, parity or the displayed closed forms for the deconvolved Legendre polynomials, rationality of the atom rows, equality of the fixed-scale and separate-block coefficient vectors, an unconditional `natDegree(S_N) = 2*N` theorem or nonvanishing of its top Legendre coefficient, or the later refinement/projector/asymptotic layers. | Polynomial synthesis: `Fabius.tsum_rvachevDeconvolvedPolynomial_mul_shifted_rvachevUp`, `Fabius.normalized_tsum_rvachevDeconvolvedPolynomial_mul_shifted_rvachevUp`, `Fabius.sum_Ioo_rvachevDeconvolvedPolynomial_mul_shifted_rvachevUp`, `Fabius.normalized_sum_Ioo_rvachevDeconvolvedPolynomial_mul_shifted_rvachevUp`, `Fabius.normalized_tsum_shifted_rvachevDeconvolvedPolynomial_mul_rvachevUp`.  Finite blocks: `Fabius.rvachevLegendreDeconvolutionPolynomial`, `Fabius.rvachevLegendreScale`, `Fabius.rvachevLegendreIndexSet`, `Fabius.rvachevLegendreAtomCoefficient`, `Fabius.rvachevLegendreTranslateBlock`, `Fabius.rvachevTranslateGram`, `Fabius.natDegree_rvachevLegendreDeconvolutionPolynomial`, `Fabius.leadingCoeff_rvachevLegendreDeconvolutionPolynomial`, `Fabius.eval_legendrePolynomial_eq_sum_rvachevUp`, `Fabius.eval_legendrePolynomial_even_eq_sum_rvachevUp`, `Fabius.rvachevLegendreTranslateBlock_eq_rvachevLegendreBlock`, `Fabius.intervalIntegral_rvachevLegendreTranslateBlock_mul`, `Fabius.sum_rvachevLegendreAtomCoefficient_mul_gram`.  Outer/fixed-scale module definitions: `Fabius.rvachevLegendrePartialSumDeconvolutionPolynomial`, `Fabius.rvachevLegendrePartialSumAtomCoefficient`, `Fabius.rvachevLegendrePartialSumTranslateBlock`, `Fabius.rvachevLegendreTranslateBlockOnInterval`, `Fabius.rvachevLegendrePartialSumTranslateBlockOnInterval`.  Its theorems: `Fabius.natDegree_rvachevLegendrePartialSumDeconvolutionPolynomial`, `Fabius.leadingCoeff_rvachevLegendrePartialSumDeconvolutionPolynomial`, `Fabius.summable_norm_rvachevLegendreTranslateBlock`, `Fabius.summable_rvachevLegendreTranslateBlock`, `Fabius.hasSum_rvachevLegendreTranslateBlock`, `Fabius.tsum_rvachevLegendreTranslateBlock`, `Fabius.rvachevLegendrePartialSumDeconvolutionPolynomial_eq_sum`, `Fabius.rvachevLegendrePartialSumAtomCoefficient_eq_sum`, `Fabius.eval_rvachevLegendrePartialSumPolynomial_eq_tsum_rvachevUp`, `Fabius.eval_rvachevLegendrePartialSumPolynomial_eq_sum_rvachevUp`, `Fabius.rvachevLegendrePartialSumTranslateBlock_eq_eval_partialSumPolynomial`, `Fabius.rvachevLegendrePartialSumTranslateBlock_eq_sum_translateBlock`, `Fabius.rvachevLegendreTranslateBlockOnInterval_apply`, `Fabius.rvachevLegendreTranslateBlockOnInterval_eq_smul`, `Fabius.summable_norm_rvachevLegendreTranslateBlockOnInterval`, `Fabius.summable_rvachevLegendreTranslateBlockOnInterval`, `Fabius.hasSum_rvachevLegendreTranslateBlock_uniform`, `Fabius.tsum_rvachevLegendreTranslateBlock_uniform`, `Fabius.rvachevLegendrePartialSumTranslateBlockOnInterval_apply`, `Fabius.rvachevLegendrePartialSumTranslateBlockOnInterval_eq_eval_partialSumPolynomial`, `Fabius.rvachevLegendrePartialSumTranslateBlockOnInterval_eq_sum`, `Fabius.tendsto_rvachevLegendrePartialSumTranslateBlockOnInterval`, `Fabius.rvachevLegendrePartialSumTranslateBlock_tendstoUniformlyOn`, `Fabius.tendsto_norm_rvachevLegendrePartialSumTranslateBlockOnInterval_sub`, `Fabius.tendsto_rvachevLegendrePartialSumTranslateBlock` |
| Generic finite-node Lagrange--Rvachev synthesis, componentwise biorthogonality, and exact interpolation loop | The current-tree `LagrangeRvachevSynthesis` module has exactly two public definitions and seven public theorems.  It samples the Rvachev deconvolution of each finite Lagrange basis polynomial, forms the normalized linear data-to-atom coefficient, bounds every basis and interpolant degree by `s.card - 1`, reconstructs each cardinal and the full interpolant on `[-1,1]` whenever `M ≠ 0` and `s.card - 1 ≤ v₂(M)`, proves the Kronecker-delta identity at distinct in-range nodes, and proves unit row mass for a nonempty distinct family.  The degree bounds and synthesis identities themselves allow colliding nodes.  This is a componentwise generic finite-node theorem, not a geometric Gaussian q-binomial/q-Pochhammer or elementary-symmetric closed form for decoder entries, a bundled matrix/right-inverse or stochastic-matrix wrapper, or an optimal/minimum-variation result. | Definitions: `Fabius.lagrangeRvachevDecoder`, `Fabius.lagrangeRvachevAtomCoefficient`.  Theorems: `Fabius.natDegree_lagrangeBasis_le_card_sub_one`, `Fabius.natDegree_lagrangeInterpolate_le_card_sub_one`, `Fabius.normalized_sum_Ioo_lagrangeRvachevDecoder_mul_shifted_rvachevUp`, `Fabius.normalized_sum_Ioo_lagrangeRvachevDecoder_eval_node`, `Fabius.lagrangeRvachevAtomCoefficient_eq_deconvolved_interpolate`, `Fabius.sum_Ioo_lagrangeRvachevAtomCoefficient_mul_shifted_rvachevUp`, `Fabius.sum_lagrangeRvachevDecoder_eq_one` |
| Finite Appell--Hasse formulas and geometric Gaussian decoder (`gq:prop:q-Appell-falling`, `gq:thm:gaussian-Appell-decoder`) | **Both Exact by composition.** `RvachevAppellHasse.lean` has one public definition and exactly fourteen public theorems.  Over every commutative semiring, its coefficientwise transform sends monomials to an arbitrary Appell family and is the finite Hasse-derivative operator.  The Rvachev specialization proves the formal reciprocal-moment sequence is even and reduces deconvolution to even Hasse derivatives.  Taylor/Vieta identify derivatives of finite root products with complementary elementary symmetric functions, giving the displayed q-falling formula without restrictions on `c,q`.  The field Lagrange factorization and total geometric nodal-weight calculation then produce the full Gaussian q-Pochhammer prefactor and finite symmetric sum for every decoder entry.  These formulas remain algebraically true at zero or colliding nodes through totalized inversion, but are not cardinal interpolation there; the manuscript's cardinal application uses `c>0`, `0<q<1`, a nonzero dyadic mesh, and the existing degree/interval hypotheses.  The separate `Fabius.normalized_sum_Ioo_rvachevDeconvolvedPolynomial_mul_shifted_rvachevUp` and generic Lagrange synthesis results supply the atom-reconstruction clauses.  Here `rvachevReciprocalMomentRat` is the formal reciprocal-moment sequence; no analytic reciprocal-MGF convergence, larger matrix right inverse, or decoder optimality is asserted. | Definition: `Fabius.Appell.polynomialTransform`. Theorems: `Fabius.Appell.polynomialTransform_apply`, `Fabius.Appell.polynomialTransform_monomial`, `Fabius.Appell.polynomialTransform_eq_sum_hasseDeriv_of_natDegree_lt`, `Fabius.Appell.polynomialTransform_eq_sum_hasseDeriv`, `Fabius.rvachevReciprocalMomentRat_odd`, `Fabius.rvachevDeconvolutionLinearMap_eq_appellPolynomialTransform`, `Fabius.rvachevDeconvolvedPolynomial_eq_sum_even_hasseDeriv`, `Fabius.eval_hasseDeriv_prod_X_sub_C_eq_elementarySymmetricEval`, `Fabius.eval_rvachevDeconvolvedPolynomial_prod_X_sub_C`, `Fabius.eval_rvachevDeconvolvedPolynomial_qFallingPower`, `Fabius.lagrangeBasis_eq_nodalWeight_mul_prod_X_sub_C`, `Fabius.lagrangeRvachevDecoder_eq_nodalWeight_mul_sum`, `Fabius.geometric_nodalWeight_eq_geometricQPochhammer`, `Fabius.geometric_lagrangeRvachevDecoder_eq` |
| Nodes-only Bernoulli--Bell amplitudes (`cor:lag-nodes-only`) | **Exact/Complete by composition.** `RvachevLagrangeNodesOnly.lean` has one public definition and exactly fourteen public theorems.  It proves the ordinary even-derivative Lagrange formula under `Set.InjOn v s`, the raw omitted-node elementary-symmetric/Appell expansion with `Lagrange.nodalWeight`, rational descent of the decoder polynomial, and the complete-Bell/Bernoulli--Mersenne coefficient link.  Rational nodes give a polynomial over `ℚ`; value rationality is claimed only at rational evaluation points such as `k/M`, not arbitrary irrational real points.  The lattice equality is total at `M=0`, while reconstruction keeps the separate nonzero/admissible-mesh hypotheses.  The Bell identity is formal coefficient algebra rather than analytic reciprocal-MGF convergence, and odd cumulants use the pre-existing centered-parity theorem.  No monolithic wrapper is claimed.  Independently, the decoder and atom-coefficient synthesis declarations make `thm:lag-cardinal` **Exact/Complete by assembly**.  The larger compound `thm:lag-right-inverse`, global atom synthesis, and decoder optimality remain unpromoted. | Definition: `Fabius.rvachevDeconvolvedPolynomialRat`.  Theorems: `Fabius.map_rvachevDeconvolvedPolynomialRat`, `Fabius.rvachevDeconvolvedPolynomial_eq_sum_appell`, `Fabius.eval_rvachevDeconvolvedPolynomial_eq_sum_even_iterateDerivative`, `Fabius.rvachevDeconvolvedPolynomial_prod_X_sub_C_eq_sum_appell`, `Fabius.eval_rvachevDeconvolvedPolynomial_lagrangeBasis_eq_sum_even_iterateDerivative`, `Fabius.eval_rvachevDeconvolvedPolynomial_lagrangeBasis_eq_nodalWeight_mul_sum_appell`, `Fabius.lagrangeRvachevDecoder_eq_nodalWeight_mul_sum_appell`, `Fabius.map_lagrangeBasis_ratCast`, `Fabius.map_rvachevDeconvolvedPolynomialRat_lagrangeBasis`, `Fabius.lagrangeRvachevDecoder_eq_ratCast`, `Fabius.rvachevRawMomentRat_eq_centeredRvachevFullMoment`, `Fabius.momentCumulant_rvachevRawMomentRat_eq_centeredRvachevFullCumulant`, `Fabius.momentCumulant_rvachevRawMomentRat_even_eq_bernoulliMersenne`, `Fabius.rvachevReciprocalMomentRat_eq_completeBellPolynomial_neg_centeredCumulant`; reused odd-parity theorem: `Fabius.centeredRvachevFullCumulant_odd` |
| Typed finite Lagrange--Rvachev Matrix/Markov layer and forced decoder sign under row overlap | `LagrangeRvachevMatrix.lean` has exactly four public definitions/abbreviations and six public theorems.  Its atom type is the exact finite integer block `Ioo (-(2*M)) (2*M)`.  The normalized encoder entry is `M⁻¹ * rvachevUp F (v i - k/M)` and the decoder entry is the sampled scalar `lagrangeRvachevDecoder`.  Encoder nonnegativity is unconditional in the node family and mesh; encoder row mass one requires `IsFabius F`, `M ≠ 0`, and that row's node in `[-1,1]`, but no distinctness.  Decoder row mass one requires a nonempty distinct node family and no Fabius or mesh-admissibility hypothesis.  The typed equation `encoder * decoder = 1` requires distinct nodes all in `[-1,1]`, `M ≠ 0`, and `s.card - 1 ≤ padicValNat 2 M`.  Generically, nonnegativity of `A`, row-unitality of `D`, `A * D = 1`, and one column strictly positive in two distinct rows force some negative entry of `D`; encoder row normalization is not used.  The Rvachev theorem specializes this implication but does not prove that an overlap exists.  No claim is made about the range, kernel, rank, trace, characteristic polynomial, or Cauchy--Binet data of `decoder * encoder`, nor about geometric closed forms or decoder optimality. | Definitions/abbreviations: `Fabius.rvachevAtomIndexSet`, `Fabius.RvachevAtomIndex`, `Fabius.lagrangeRvachevEncoderMatrix`, `Fabius.lagrangeRvachevDecoderMatrix`.  Theorems: `Fabius.lagrangeRvachevEncoderMatrix_nonneg`, `Fabius.sum_lagrangeRvachevEncoderMatrix_row_eq_one`, `Fabius.sum_lagrangeRvachevDecoderMatrix_row_eq_one`, `Fabius.lagrangeRvachevEncoderMatrix_mul_decoderMatrix`, `Fabius.exists_neg_entry_of_rightInverse_of_row_overlap`, `Fabius.exists_lagrangeRvachevDecoderMatrix_entry_neg_of_row_overlap` |
| Sharp universal shifted-comb threshold and least natural mesh | The focused-build `CompositeMeshSharpness` module has one definition and seven theorems.  Universal exactness through degree `d` means exactness at every real shift for every real polynomial of natural degree at most `d`, with a nonzero natural mesh `M`.  It holds exactly when `d ≤ v₂(M)`, equivalently when `2^d ∣ M`.  A real first-defect witness occurs for the monomial of degree `v₂(M)+1`; `2^d` is the least universally exact mesh, and the complete even-degree space through `2N` has least mesh `4^N`.  The quantifier over the whole polynomial space is essential: no individual Legendre polynomial, fixed `S_N`, or target-adapted mesh is claimed minimal. | `Fabius.exists_shift_tsum_shifted_monomial_ne_integral_nat_real`, `Fabius.rvachevCombExactThrough`, `Fabius.rvachevCombExactThrough_iff_padicValNat`, `Fabius.rvachevCombExactThrough_iff_pow_two_dvd`, `Fabius.rvachevCombExactThrough_two_pow`, `Fabius.two_pow_le_of_rvachevCombExactThrough`, `Fabius.isLeast_rvachevCombExactThrough`, `Fabius.isLeast_rvachevCombExactThrough_even` |
| Translated monomial representation of `FabiusF[x]` on `0 ≤ x ≤ 2` | Proved for the signed global Fabius function.  The exact nested finite sums retain the source order, all four binomial factors, and the literal integer exponent `k-j+2k^2-2n`; the inner dyadic values use the same global function.  The endpoint `x=0` follows the convention `0^0=1`. | `Fabius.legendrePolynomial_comp_one_sub_two_X`, `Fabius.eval_legendrePolynomial_even_sub_one`, `Fabius.hasSum_extendedFabius_translatedLegendreSeries`, `Fabius.hasSum_globalFabius_translatedLegendre_formula`, `Fabius.tsum_globalFabius_translatedLegendre_formula`, `Fabius.globalFabius_eq_tsum_translatedLegendre_formula` |
| Computability of the canonical bounded Fabius function | Proved in the Grzegorczyk sense.  The sequential clause uses uniform fast dyadic names and a certified primitive-recursive centered-spline evaluator with nearest rounding.  Effective uniform continuity uses the primitive-recursive positive modulus `d(n)=2n` for positive precision indices.  The analytic spline approximation is certified uniformly on all of `ℝ`, with signed-global error at most `2^-p`; its bounded/CDF specialization is uniform on `[0,1]`. | `Fabius.ComputableRealSequence`, `Fabius.SequentiallyComputable`, `Fabius.EffectivelyUniformContinuous`, `Fabius.abs_fabiusUniformSpline_sub_extendedFabius_le`, `Fabius.fabiusUniformSpline_tendstoUniformly_extendedFabius`, `Fabius.fabiusUniformSpline_tendstoUniformly_globalFabius`, `Fabius.fabiusUniformSpline_tendstoUniformlyOn_fabiusReal`, `Fabius.tmBitPR_primrec`, `Fabius.fabiusSplineApproxPR_computable`, `Fabius.fabiusSplineApproxPR_error`, `Fabius.fabius_sequentiallyComputable`, `Fabius.fabius_isComputableRealFunction` |
| Exact centered finite-spline geometry | Proved in every degree, including the degree-zero step spline.  On `[0,1]` the centered spline is exactly the midpoint-corrected finite CDF, takes values in `[0,1]`, and is nondecreasing.  It is already equal to one throughout its last half-cell `1 - 1 / 2^(p+1) ≤ x ≤ 1`, and in particular at `x=1`. | `Fabius.ProbabilityRepresentation.fabiusUniformSpline_zero_eq_centeredPartialCDF_of_mem_Icc`, `Fabius.ProbabilityRepresentation.fabiusUniformSpline_eq_centeredPartialCDF_all`, `Fabius.ProbabilityRepresentation.fabiusUniformSpline_mem_Icc_all`, `Fabius.ProbabilityRepresentation.monotoneOn_fabiusUniformSpline_all`, `Fabius.monotoneOn_fabiusUniformSpline_all`, `Fabius.ProbabilityRepresentation.uniformCenteredPartialCDF_eq_one_of_le`, `Fabius.fabiusUniformSpline_eq_one_of_le`, `Fabius.fabiusUniformSpline_one_eq_one` |
| Exact two-sided quarter cell of the finite Fabius spline | Proved for the report-indexed finite approximant `P_n` at every `n ≥ 3` and every `|z| ≤ 2^-n`: `P_n(1/4+z) = 5/72 + z + 4z^2 - (4/9)4^-n`.  On the same closed cell, reflection has difference `2z`, the centered second difference is `8z^2`, and `P_n` is strictly increasing.  This is an unconditional finite Thue--Morse/spline identity.  The displayed quantile identity is also available for any supplied local left inverse, but Lean neither constructs the report's finite inverse `G_n` nor identifies it with the inverse of the limiting Fabius function. | `Fabius.reportFiniteFabiusApproximant_quarter_twoSided`, `Fabius.reportFiniteFabiusApproximant_quarter_reflection`, `Fabius.reportFiniteFabiusApproximant_quarter_centralSecondDifference`, `Fabius.strictMonoOn_reportFiniteFabiusApproximant_quarter_twoSided`, `Fabius.reportFiniteFabiusApproximant_quarterPrefix_value`, `Fabius.reportFiniteFabiusApproximant_quarterPrefix_quantile` |
| Exact midpoint--endpoint transmutation, affine midpoint jet, and central primitive transfer | Proved for every bounded Fabius solution on the full closed half-cell `0 ≤ h ≤ 1/2`: `F(1/2+h) = 1/2+2h-F(h)` and `F(1/2-h) = 1/2-2h+F(h)`.  Both translated first derivatives equal `2-F'(h)`, including the endpoints, and every iterated derivative of order `k ≥ 2` vanishes at `1/2`.  The centered oriented-integral formula `∫_[1/2-a,1/2+a] F = a` is proved for every real oriented radius `a`, strengthening the draft's half-cell statement; for positive radius the ordinary interval average is `1/2`.  Arbitrary weights transport the right and left midpoint defects to `-F` and `F`; the unweighted draft identity and every Cauchy kernel `(a-h)^n/n!` for anchored repeated primitives are named corollaries.  This module introduces no recursively defined primitive operator; its Cauchy kernels are compatible with the separately formalized `normalizedVolterra` API.  It also makes no local-affineness, analytic-continuation, inverse-defect asymptotic, or finite-spline claim. | `Fabius.deriv_fabiusReal_one_sub`, `Fabius.fabiusReal_midpoint_add_eq`, `Fabius.fabiusReal_midpoint_sub_eq`, `Fabius.deriv_fabiusReal_midpoint_add_eq`, `Fabius.deriv_fabiusReal_midpoint_sub_eq`, `Fabius.iteratedDeriv_fabiusReal_half_eq_zero_of_two_le`, `Fabius.intervalIntegral_fabiusReal_centered`, `Fabius.intervalIntegral_mul_fabiusReal_midpoint_add_defect_eq_neg`, `Fabius.intervalIntegral_mul_fabiusReal_midpoint_sub_defect_eq`, `Fabius.intervalIntegral_fabiusReal_midpoint_add_defect_eq_neg`, `Fabius.intervalIntegral_repeatedPrimitiveKernel_fabiusReal_midpoint_add_defect_eq_neg`, `Fabius.fabius_half`, `Fabius.deriv_fabiusReal_half`, `Fabius.fabius_analyticAt_iff` |
| Exact inverse-midpoint offset and defect fixed points | With `h(δ)=F⁻¹(1/2+δ)-1/2` and `E(δ)=h(δ)-δ/2`, proved on `0 ≤ δ ≤ 1/2` that `δ=2h(δ)-F(h(δ))`, `h(δ)=δ/2+F(h(δ))/2`, and `E(δ)=F(h(δ))/2=F(δ/2+E(δ))/2`.  The exact enclosures are `0 ≤ h(δ) ≤ 1/2` and `0 ≤ E(δ) ≤ 1/4`; both are sharp, with `h(0)=E(0)=0`, `h(1/2)=1/2`, and `E(1/2)=1/4`.  The offset enclosure remains true for every `δ ≥ 0` because the inverse is totalized by clamping.  Both `h` and `E` are globally odd, including the clamped tails.  These are finite identities and bounds, not an all-orders defect estimate, asymptotic equivalence, logarithmic expansion, or Lambert-W transfer. | `Fabius.fabiusInvMidpointOffset`, `Fabius.fabiusInvMidpointDefect`, `Fabius.fabiusInvMidpointOffset_zero`, `Fabius.fabiusInvMidpointDefect_zero`, `Fabius.fabiusInvMidpointOffset_half`, `Fabius.fabiusInvMidpointDefect_half`, `Fabius.fabiusInvMidpointOffset_mem_Icc`, `Fabius.fabiusInvMidpointOffset_equation`, `Fabius.fabiusInvMidpointOffset_fixedPoint`, `Fabius.fabiusInvMidpointDefect_eq_half_fabiusReal`, `Fabius.fabiusInvMidpointDefect_fixedPoint`, `Fabius.fabiusInvMidpointDefect_mem_Icc`, `Fabius.fabiusInvMidpointOffset_neg`, `Fabius.fabiusInvMidpointDefect_neg` |
| Fixed-length Fabius increments and exact inverse modulus | The exhaustive `InverseModulus` surface is one definition and 31 theorems.  For every bounded Fabius solution and nonnegative length, fixed-length increments are reflection-invariant, vanish on both constant tails, and have global weak shape.  At every nondegenerate length `0 < h <= 1`, they are strictly monotone on the maximal branches `[-h,(1-h)/2]` and `[(1-h)/2,1]`; inside `[0,1]`, only the two endpoint intervals minimize mass.  This yields the exact equality cases for the least-mass and constrained-superadditivity inequalities.  Transport through the globally clamped inverse gives ordered and order-free gap inequalities, global subadditivity, absolute and metric self-moduli, saturation at one, and attained exact `IsGreatest` and `sSup` moduli at every nonnegative radius over all real inputs and over `[0,1]`.  On `[0,1]`, the ordered inverse bound is an equality exactly for a degenerate interval or one meeting an endpoint, and the absolute bound is an equality exactly for coincident inputs or an endpoint input.  These equality classifications are not claimed globally, where clamping creates extra cases.  Strict and closed effective-injectivity bounds, their contrapositive separation form, and the exact strict-threshold biconditional complete the surface.  This structural module alone does not construct an explicit recursive denominator; that effective layer is supplied by `FabiusInverseEffectiveContinuity`. | `Fabius.fabiusIntervalMass`; `Fabius.fabiusIntervalMass_reflect`, `Fabius.fabiusIntervalMass_eq_zero_of_add_nonpos`, `Fabius.fabiusIntervalMass_eq_zero_of_one_le`, `Fabius.monotoneOn_fabiusIntervalMass_firstHalf`, `Fabius.antitoneOn_fabiusIntervalMass_secondHalf`, `Fabius.strictMonoOn_fabiusIntervalMass_firstHalf`, `Fabius.strictAntiOn_fabiusIntervalMass_secondHalf`, `Fabius.fabiusReal_sub_le_sub`, `Fabius.fabiusReal_lt_fabiusIntervalMass_of_mem_Ioo`, `Fabius.fabiusIntervalMass_eq_fabiusReal_iff`, `Fabius.fabiusReal_sub_lt_sub`, `Fabius.fabiusReal_sub_eq_sub_iff`, `Fabius.fabiusReal_add_le`, `Fabius.fabiusReal_add_eq_iff`, `Fabius.fabiusInv_sub_le_sub_of_mem_Icc`, `Fabius.fabiusInv_sub_eq_sub_iff_of_mem_Icc`, `Fabius.fabiusInv_sub_le_sub_of_le`, `Fabius.fabiusInv_sub_le_sub`, `Fabius.fabiusInv_add_le`, `Fabius.abs_fabiusInv_sub_le`, `Fabius.abs_fabiusInv_sub_eq_iff_of_mem_Icc`, `Fabius.dist_fabiusInv_le`, `Fabius.fabiusInv_min_one`, `Fabius.isGreatest_abs_fabiusInv_sub`, `Fabius.sSup_abs_fabiusInv_sub_eq`, `Fabius.isGreatest_abs_fabiusInv_sub_Icc`, `Fabius.sSup_abs_fabiusInv_sub_Icc_eq`, `Fabius.abs_fabiusInv_sub_lt_of_abs_sub_lt_fabiusReal`, `Fabius.abs_fabiusInv_sub_le_of_abs_sub_le_fabiusReal`, `Fabius.fabiusReal_le_abs_sub_of_le_abs_fabiusInv_sub`, `Fabius.forall_abs_fabiusInv_sub_lt_iff` |
| Effective dyadic continuity of the totalized inverse | The exhaustive `FabiusInverseEffectiveContinuity` surface is two definitions and 12 theorems.  For `r > 0`, its one-term recurrence estimate is `1 / (2^(r.choose 2) * (r+1)! * (2^r-1)) ≤ F(2^-r)`.  Replacing `2^r-1` by `2^r` gives the factorial denominator `D!(r)=2^((r+1).choose 2)*(r+1)!`; the report's elementary denominator is `DDelta(r)=2^((r+1).choose 2)*(r+1)^(r+1)`.  Both denominator functions are primitive recursive, `D!(r) ≤ DDelta(r)`, and both reciprocal endpoint-mass bounds hold.  For either denominator, the strict input threshold implies inverse-output error `< 2^-r`, and the closed threshold implies error `≤ 2^-r`; taking the stronger factorial witness at `r=n` proves `EffectivelyUniformContinuous (fabiusInv F hF)`.  This module itself does not construct the downstream sequential realizer or combined `IsComputableRealFunction` theorem; those are supplied by `EffectiveMonotoneInverse` and `FabiusInverseComputable`.  It also does not assert logarithmic `r(n)`, an exact least/ceiling denominator, or an input-bit asymptotic. | `Fabius.inverseFabiusFactorialDenominator`, `Fabius.inverseFabiusDeltaDenominator`; `Fabius.inverseFabiusFactorialDenominator_eq`, `Fabius.inverseFabiusFactorialDenominator_primrec`, `Fabius.inverseFabiusDeltaDenominator_primrec`, `Fabius.fabiusReal_inverse_two_pow_one_term_lower_bound`, `Fabius.inv_inverseFabiusFactorialDenominator_le_fabiusReal`, `Fabius.inverseFabiusFactorialDenominator_le_deltaDenominator`, `Fabius.inv_inverseFabiusDeltaDenominator_le_fabiusReal`, `Fabius.abs_fabiusInv_sub_lt_inverse_two_pow_of_lt_factorialDenominator`, `Fabius.abs_fabiusInv_sub_le_inverse_two_pow_of_le_factorialDenominator`, `Fabius.abs_fabiusInv_sub_lt_inverse_two_pow_of_lt_deltaDenominator`, `Fabius.abs_fabiusInv_sub_le_inverse_two_pow_of_le_deltaDenominator`, `Fabius.fabiusInv_effectivelyUniformContinuous` |
| Logarithmic reciprocal modulus for the totalized inverse | The exhaustive `FabiusInverseLogarithmicModulus` surface is three definitions and 15 theorems.  Its primitive-recursive selector is exactly `r(n)=Nat.log2 n+1`; for `n>0` this is the least natural `r` with `n<2^r`, and `r(n)≤n`.  The factorial and Delta logarithmic denominators equal their dyadic predecessors evaluated at `r(n)` for positive `n`, take value `1` at zero, are primitive recursive, and satisfy the pointwise factorial-to-Delta comparison.  For either denominator, a strict or closed input threshold gives the strict output estimate `<1/n`, since the intermediate bound is at most `2^-r(n)<1/n`.  Both the stronger factorial denominator and the report-exact Delta denominator separately witness `EffectivelyUniformContinuous (fabiusInv F hF)`.  The tolerant-bisection realizer, sequential computability, and combined `IsComputableRealFunction` theorem are supplied downstream by `EffectiveMonotoneInverse` and `FabiusInverseComputable`; the exact endpoint-mass ceiling denominator is supplied by the next leaf, while input-bit asymptotics remain outside Lean. | `Fabius.inverseFabiusLogarithmicOrder`, `Fabius.inverseFabiusLogarithmicFactorialDenominator`, `Fabius.inverseFabiusLogarithmicDeltaDenominator`; `Fabius.inverseFabiusLogarithmicOrder_eq_succ_log2`, `Fabius.inverseFabiusLogarithmicOrder_primrec`, `Fabius.inverseFabiusLogarithmicOrder_isLeast`, `Fabius.inverseFabiusLogarithmicOrder_le_self`, `Fabius.inverseFabiusLogarithmicFactorialDenominator_of_pos`, `Fabius.inverseFabiusLogarithmicDeltaDenominator_of_pos`, `Fabius.inverseFabiusLogarithmicFactorialDenominator_primrec`, `Fabius.inverseFabiusLogarithmicDeltaDenominator_primrec`, `Fabius.inverseFabiusLogarithmicFactorialDenominator_le_deltaDenominator`, `Fabius.abs_fabiusInv_sub_lt_inv_nat_of_lt_logarithmicFactorialDenominator`, `Fabius.abs_fabiusInv_sub_lt_inv_nat_of_le_logarithmicFactorialDenominator`, `Fabius.abs_fabiusInv_sub_lt_inv_nat_of_lt_logarithmicDeltaDenominator`, `Fabius.abs_fabiusInv_sub_lt_inv_nat_of_le_logarithmicDeltaDenominator`, `Fabius.fabiusInv_effectivelyUniformContinuous_logarithmic`, `Fabius.fabiusInv_effectivelyUniformContinuous_logarithmicDelta` |
| Sharp exact dyadic reciprocal modulus for the totalized inverse | The exhaustive `FabiusInverseExactDyadicModulus` surface is two definitions and ten theorems.  The fixed-order denominator is the natural ceiling of the reciprocal of the exact rational endpoint value `F(2^-r)`.  It is positive, primitive recursive, and least among positive integers whose reciprocal is at most that mass.  The induced strict inverse modulus is sharp: every smaller positive denominator fails at the explicit endpoint pair `0, F(2^-r)`, so the ceiling is also least among strict integer moduli for the fixed dyadic output target `2^-r`.  The logarithmic wrapper is primitive recursive, takes value `1` at zero, and, for positive `n`, uses that fixed-target ceiling at the least dyadic order `r(n)`; it gives output error `<1/n`.  This last conclusion is witness-only: no leastness is asserted for the weaker target `1/n`, and the zero value is only a convention with no modulus conclusion.  The recursive-denominator clause is therefore closed; no input-bit asymptotic is exported. | `Fabius.inverseFabiusExactDyadicDenominator`, `Fabius.inverseFabiusExactLogarithmicDenominator`; `Fabius.inverseFabiusExactDyadicDenominator_primrec`, `Fabius.inverseFabiusExactLogarithmicDenominator_primrec`, `Fabius.inverseFabiusExactDyadicDenominator_pos`, `Fabius.inv_inverseFabiusExactDyadicDenominator_le_fabiusAtInverseTwoPow`, `Fabius.inverseFabiusExactDyadicDenominator_isLeast`, `Fabius.abs_fabiusInv_sub_lt_inverse_two_pow_of_lt_exactDyadicDenominator`, `Fabius.exists_fabiusInv_gap_of_lt_exactDyadicDenominator`, `Fabius.inverseFabiusExactDyadicDenominator_isLeast_strictModulus`, `Fabius.inverseFabiusExactLogarithmicDenominator_of_pos`, `Fabius.abs_fabiusInv_sub_lt_inv_nat_of_lt_exactLogarithmicDenominator` |
| Generic tolerant monotone inversion on the unit interval | `EffectiveMonotoneInverse.lean` has exactly two public definitions and six public theorems.  At requested precision `p`, its natural-number realizer performs exactly `p` dyadic halvings while carrying a bracket index and an optional accepted numerator.  Exact signed-code comparisons take the left branch when `U+4<L`, the right branch when `L+4<U`, and otherwise certify that the midpoint already has inverse error `<2^-p`; accepted numerators are doubled through later depths, so every final code has denominator `2^p`.  The final bracket endpoint handles the no-hit case and gives error `≤2^-p`.  The abstract theorem concludes unit-interval sequential computability from a supplied computable positive reciprocal inverse modulus; the adjacent gap module supplies a reusable way to construct that certificate. | Definitions: `Fabius.SequentiallyComputableOn`, `Fabius.unitClamp`.  Theorems: `Fabius.unitClamp_sequentiallyComputable`, `Fabius.tolerantDifference_error`, `Fabius.tolerantDifference_safe_updates`, `Fabius.tolerantDifference_inconclusive`, `Fabius.tolerantBisection_correct`, `Fabius.effectiveInversionOn_Icc`. |
| Effective inversion from computable positive rational dyadic gaps | `EffectiveGapInverse.lean` has exactly four public definitions and four public theorems.  A `ComputablePositiveRationalSequence` supplies computable positive natural numerators and denominators; its computable reciprocal denominator `denominator p / numerator p + 1` is positive and has reciprocal strictly below `value p`.  For a strict increasing inverse pair `f,g` on `[0,1]`, the hypothesis `α.value p ≤ f (x + 2^-p) - f x` for every `x ∈ [0,1-2^-p]` yields the inverse modulus.  Adding a computable dyadic approximation of `f` and both interval maps proves `SequentiallyComputableOn g [0,1]` and `EffectivelyUniformContinuousOn g [0,1]`.  The final theorem proves `IsComputableRealFunction (fun x => g (unitClamp x))`; it agrees with `g` on the unit interval but does not constrain the unclamped values of `g` outside it.  Together with `EffectiveMonotoneInverse` 2+6 and `FabiusInverseComputable` 0+1, the effective-inverse union is three modules and seventeen declarations. | `Fabius.EffectivelyUniformContinuousOn`, `Fabius.ComputablePositiveRationalSequence`, `Fabius.ComputablePositiveRationalSequence.value`, `Fabius.ComputablePositiveRationalSequence.reciprocalDenominator`, `Fabius.ComputablePositiveRationalSequence.reciprocalDenominator_spec`, `Fabius.inverseModulus_of_positiveRationalGap`, `Fabius.effectiveInversionOn_Icc_of_computablePositiveRationalGap`, `Fabius.clampedEffectiveInversion_of_computablePositiveRationalGap` |
| Total computability of the inverse Fabius function | `FabiusInverseComputable.lean` has zero public definitions and exactly one public theorem.  It instantiates the generic fixed-depth realizer with the centered-spline dyadic oracle for `fabiusReal` and the computable positive `inverseFabiusDeltaDenominator`, clamps arbitrary input names without changing the totalized inverse, and combines total sequential computability with the logarithmic-Delta effective-uniform-continuity witness.  The result is the complete `IsComputableRealFunction` certificate for `fabiusInv`, not a practical running-time or input-bit complexity claim. | `Fabius.fabiusInv_isComputableRealFunction` |
| Exact bounded-Fabius derivative values on every matched dyadic grid | Proved for all `k,m ∈ ℕ` with `m < 2^k`.  At `m/2^k`, the `k`th derivative is zero exactly when `m` is even; after deleting those zeros, the surviving values are `2^C(k+1,2)` times the Thue--Morse word `thueMorseSign (m/2)`.  Every odd numerator therefore attains the already-proved sharp global derivative bound.  The cases `m=0`, `(k,m)=(1,1)`, and the even numerator `m=2^(k-1)` respectively recover endpoint flatness, the maximal midpoint slope, and all higher midpoint-jet vanishings.  The theorem is for the strict grid below one; the bounded function's endpoint and exterior constant-tail behavior remain governed by the separate global derivative API. | `Fabius.iteratedDeriv_fabiusReal_dyadicGrid_eq_ite`, `Fabius.iteratedDeriv_fabiusReal_dyadicGrid_eq_zero_iff`, `Fabius.abs_iteratedDeriv_fabiusReal_dyadicGrid_of_odd`, `Fabius.iteratedDeriv_fabiusReal_zero`, `Fabius.abs_iteratedDeriv_fabiusReal_le`, `Fabius.isGreatest_abs_iteratedDeriv_fabiusReal` |
| Exact dyadic derivative filtration for the up-function | `DyadicDerivativeFiltration.lean` has zero public definitions and exactly six public theorems.  It proves support vanishing outside `(-1,1)`, vanishing above the reduced dyadic depth, the signed Thue--Morse value at the critical depth, and exact detection of that depth as the greatest nonvanishing derivative order.  Its two newest theorems complete the below-depth case: for every order `m` and `x<1`, `up^(m)(x)=2^((m+1).choose 2) F_ext(2^m(x+1))`; at `a/2^n`, under `m<n` and `a<2^n`, this becomes the report's denominator-`2^(n-m)` formula. | `Fabius.rvachevUp_eq_zero_of_one_le_abs`, `Fabius.iteratedDeriv_rvachevUp_dyadic_eq_zero`, `Fabius.iteratedDeriv_rvachevUp_dyadic_critical`, `Fabius.dyadic_depth_eq_max_nonzero_iteratedDeriv`, `Fabius.iteratedDeriv_rvachevUp_eq_extendedFabius`, `Fabius.iteratedDeriv_rvachevUp_dyadic_below` |
| Centered Thue--Morse sinc prefix and full product shell | Proved as a denominator-cleared identity at every real seed, including the removable point `w = 0`.  At nonzero `w`, vanishing of the finite centered prefix is equivalent to vanishing of its Thue--Morse sine polynomial, hence to a zero in one of the first `m+1` sine factors.  The full standalone Rvachev product obeys the corresponding dyadic shell identity and the same sharp finite-factor zero criterion.  These declarations concern the product model; they do not by themselves establish a new Fourier-integral identity or a complex q-Pochhammer factorization.  The separate `RvachevPochhammerFactorization` row below records the now-formal general geometric and dyadic complex factorizations. | `Fabius.thueMorseSinePolynomial_eq_prod_sin_two_pow`, `Fabius.centeredSincPartialProduct_dyadic_eq_thueMorse`, `Fabius.centeredSincPartialProduct_dyadic_eq_zero_iff_exists`, `Fabius.rvachevFourierProduct_dyadic_eq_thueMorse`, `Fabius.rvachevFourierProduct_dyadic_eq_zero_iff_thueMorse`, `Fabius.rvachevFourierProduct_dyadic_eq_zero_iff_exists` |
| Exact dyadic derivative cells, signed natural moments, and signed/absolute value distributions | `RvachevDerivativeDistribution.lean` has exactly one public definition and 18 public theorems.  Its cell coordinate is total; the cell-range theorem assumes exactly `k < 2^n` and `y ∈ [-1,1]`, and the derivative/distribution results take `F : BoundedFabius` with `hF : IsFabius F`.  Cell profiles and midpoint values hold at every derivative order, including order zero and both endpoints, with their exact Thue--Morse signs, and arbitrary continuous Banach-valued tests satisfy the all-order finite signed-mixture law.  Writing `T_n=(n+1).choose 2`, for every `n m : ℕ` the hypothesis-free signed moment formula multiplies the base moment by `((2 : ℝ)^n)⁻¹ * (1 + (-1 : ℝ)^m)^n * ((2 : ℝ)^T_n)^m`; at `n=0` and odd `m`, the Boolean-cube factor is deliberately `0^0=1`, so the result is the generally nonzero original odd moment.  The even corollary assumes exactly `Even m` and holds for every `n`, including `n=0` and `m=0`; the odd vanishing corollary assumes exactly `0<n` and `Odd m`.  At every positive derivative order, `intervalIntegral_comp_normalized_iteratedDeriv_rvachev` gives the sharply normalized symmetric half-mixture of `up` and `-up` for a continuous test `H : ℝ → E` valued in any real Banach space, with derivative normalization `((2 : ℝ)^T_n)⁻¹` and mixture scalar `(2 : ℝ)⁻¹`.  Under the same exact hypothesis `0<n`, `map_normalized_iteratedDeriv_rvachev_restrict_Icc` identifies the corresponding Borel pushforward of `volume.restrict (Set.Icc (-1) 1)` with the half-weighted sum of the `up` law and its reflection, using measure scalar `(2 : NNReal)⁻¹`.  This positivity hypothesis is essential: at `n=0` the normalized signed derivative has the unsymmetrized original `rvachevUp` law.  Distinctly, after absolute value and sharp normalization the two restricted-Lebesgue pushforward measures on `[-1,1]` are literally equal at every order, yielding every nonnegative real `Real.rpow` moment.  The generic order-one inverse layer-cake transport that can underlie a beta specialization is formalized separately.  A separate product-space Rademacher realization, the `eLpNorm`/general rearrangement-invariant norm ladder, inverse-Fabius level sets, and the packaged beta/norm/spectral layers remain frontier targets. | Definition: `Fabius.rvachevDerivativeCell`.  Theorems: `Fabius.rvachevDerivativeCell_eq_div_add`, `Fabius.rvachevDerivativeCell_one_eq_succ_neg_one`, `Fabius.rvachevDerivativeCell_zero_neg_one`, `Fabius.rvachevDerivativeCell_two_pow_neg_one`, `Fabius.rvachevDerivativeCell_mem_Icc`, `Fabius.iteratedDeriv_rvachev_cell`, `Fabius.iteratedDeriv_rvachev_cell_zero`, `Fabius.abs_iteratedDeriv_rvachev_cell`, `Fabius.intervalIntegral_comp_iteratedDeriv_rvachev`, `Fabius.intervalIntegral_iteratedDeriv_rvachev_pow`, `Fabius.intervalIntegral_iteratedDeriv_rvachev_pow_of_even`, `Fabius.intervalIntegral_iteratedDeriv_rvachev_pow_eq_zero_of_odd`, `Fabius.intervalIntegral_comp_normalized_iteratedDeriv_rvachev`, `Fabius.intervalIntegral_comp_abs_iteratedDeriv_rvachev`, `Fabius.intervalIntegral_comp_normalized_abs_iteratedDeriv_rvachev`, `Fabius.map_normalized_abs_iteratedDeriv_rvachev_restrict_Icc`, `Fabius.map_normalized_iteratedDeriv_rvachev_restrict_Icc`, `Fabius.intervalIntegral_abs_iteratedDeriv_rvachev_rpow` |
| Rademacher sine signs and the Thue--Morse product | `RademacherSine.lean` has exactly nine public theorems.  The factored identity for `sin(πx)` is unconditional; positivity of the fractional sine and its sign formula assume exactly `fract x ≠ 0`.  At the half-shifted dyadic point the floor and nonintegrality are exact for every natural `n,j`, the factor is the corresponding Thue--Morse bit, and it is `+1` under `n < 2^j`.  The finite product assumes `n < 2^m`; the total `tprod` formula holds for every `n` because the late factors are proved to be one.  Thus the infinite notation is finite-support bookkeeping, not an analytic convergence theorem or an unshifted sine identity. | `Fabius.sin_pi_mul_eq_neg_one_zpow_floor`, `Fabius.sin_pi_mul_fract_pos`, `Fabius.sign_sin_pi_mul`, `Fabius.floor_rademacherPoint`, `Fabius.fract_rademacherPoint_ne_zero`, `Fabius.sign_sin_rademacherPoint`, `Fabius.sign_sin_rademacherPoint_eq_one_of_lt`, `Fabius.thueMorseSign_eq_prod_sign_sin`, `Fabius.thueMorseSign_eq_tprod_sign_sin` |
| Rvachev Fourier transform as the sinc product, with total dyadic shells and a strict global peak | The public facade identifies the actual transform of every bounded Fabius solution with the standalone sinc product.  It transfers the canonical product, two-sided decay gauge, extremal ray, lobe signs and maxima, and exact positive-integer punctured real-axis zero-order limits.  The cross-multiplied real shell identity is valid at every seed, including `y = 0` and the empty shell `k = 0`; on the real axis the modulus is one exactly at the origin and is strictly smaller elsewhere. | `Fabius.rvachevFourier_eq_rvachevFourierProduct`, `Fabius.rvachevFourier_eq_canonical`, `Fabius.rvachevFourier_two_pow_mul`, `Fabius.norm_rvachevFourier_two_pow_mul_cross`, `Fabius.norm_rvachevFourier_le_decayGauge_abs`, `Fabius.norm_rvachevFourier_peak_ray_envelope`, `Fabius.norm_rvachevFourier_lt_one_of_ne_zero`, `Fabius.norm_rvachevFourier_eq_one_iff`, `Fabius.existsUnique_isMaxOn_rvachevFourier_lobe`, `Fabius.rvachevFourier_eq_thueMorse_sign_mul_norm`, `Fabius.tendsto_norm_rvachevFourier_div_pow_zero_order` |
| Euler product transform with natural multiplicities | Proved over every countable base index.  The sigma-index summability equivalence assumes a nonnegative real family; the product and sum transfers assume the corresponding sigma-indexed family is multipliable or summable.  The Euler transform assumes `∀ i, ‖f i‖ < 1` and `Summable fun i => (c i : ℝ) * ‖f i‖`.  Multiplicity is carried by `Σ i, Fin (c i)`, so the result is branch-free and asserts no logarithm-of-a-power or principal-log identity. | `Fabius.summable_sigma_fin_iff`, `Fabius.tprod_sigma_fin_eq_tprod_pow`, `Fabius.tsum_sigma_fin_eq_tsum_nsmul`, `Fabius.tprod_one_sub_pow_eq_cexp_powerSum` |
| Natural-weight linearity and the all-depth shift--refinement law for generalized Rvachev products | `WeightLinearityProducts.lean` has no public definitions and exactly nine public theorems.  Admissibility is closed under natural scaling, addition, and arbitrary finite natural linear combinations; `Φ` carries those operations to powers and finite products.  For every `a : ℕ → ℕ` with exactly `Summable fun h : ℕ => (a h : ℝ) / 2 ^ h`, every `m : ℕ`, and every `z : ℂ`, `Φ_a(2^m z) = (∏ h ∈ range m, complexSinc(π * (2^(m-h) * z))^(a h)) * Φ_(S^m a)(z)`, with `S^m a(h) = a(h+m)` and a summable shifted weight.  There is no frequency restriction.  At `m=0` the finite product is empty and the theorem is the identity `Φ_a(z)=Φ_a(z)`, so zero depth and `z=0` are included.  The final factorization theorem applies a natural-valued admissible family satisfying the stated nonnegative Gregory--Newton expansion of the shifted weight.  Because all exponent sequences and coefficients here are natural, no signed-difference analytic-germ or cancellation-across-poles statement is asserted. | Exhaustive theorem inventory: `Fabius.summable_weight_natMul`, `Fabius.summable_weight_add`, `Fabius.summable_weight_linearCombination`, `Fabius.generalizedRvachevProduct_natMul`, `Fabius.generalizedRvachevProduct_linearCombination`, `Fabius.shiftExponent_iterate`, `Fabius.summable_shiftExponent_iterate`, `Fabius.generalizedRvachevProduct_two_pow_mul`, `Fabius.generalizedRvachevProduct_shift_factorization` |
| Dyadic order-divisor identifiability for generalized Rvachev products | `GeneralizedRvachevIdentifiability.lean` has no public definitions and exactly six public theorems.  Over every additive cancellative commutative monoid and every natural base `b > 1`, equality of the weighted scale multiplicities at all `b^n` determines the weight sequence.  For admissible natural exponent sequences, the analytic order at `2^n` is the inclusive prefix through `n`; the order at `1` recovers the head, consecutive dyadic orders recover every successor exponent after `ENat.toNat`, equality of all dyadic orders forces equality of sequences, and equality of the entire generalized products is equivalent to equality of sequences.  This is identifiability from the multiplicity/order divisor, not from the bare zero set or the point values on that zero set: `a` and `2 • a`, for example, have the same zero locations.  Identifiability from spectral-zeta quotients, cumulant samples, or generalized probability laws remains open. | Exhaustive theorem inventory: `Fabius.weightSequence_eq_of_weightedScaleMultiplicity_base_pow_eq`, `Fabius.analyticOrderAt_generalizedRvachevProduct_two_pow`, `Fabius.exponent_zero_eq_toNat_analyticOrderAt_generalizedRvachevProduct`, `Fabius.exponent_succ_eq_toNat_analyticOrderAt_generalizedRvachevProduct`, `Fabius.exponentSequence_eq_of_analyticOrderAt_two_pow_eq`, `Fabius.generalizedRvachevProduct_eq_iff` |
| General-weight Euler--zeta expansion of the generalized sinc product | The definition `weightedScaleSeries a k` is total for natural weights and natural `k`.  Every theorem assumes admissibility `Summable fun h : ℕ => (a h : ℝ) / 2 ^ h`; the two scale-series summability theorems and the weighted scale-collapse theorem additionally require `k ≠ 0`.  The pair power-sum identity holds for every complex `z` and natural `r`, while the generalized-product exponential expansion assumes exactly `‖z‖ < 1`.  This proves the analytic exponential side only: no principal-log identity, characteristic-function model, probabilistic cumulants, or support theorem is asserted. | `Fabius.weightedScaleSeries`, `Fabius.summable_weightedScaleSeries_real`, `Fabius.summable_weightedScaleSeries`, `Fabius.tsum_weighted_div_two_pow_even_pow`, `Fabius.weighted_sinc_pair_powerSum`, `Fabius.generalizedRvachevProduct_eq_cexp` |
| Alternating Newton Euler--zeta kernel | The two closed kernel evaluations hold for every natural `d` and require `k ≠ 0`; the explicit exponential expansion holds for every natural `d` under exactly `‖z‖ < 1`.  The natural weight is defined for every `d`, but its agreement with the source volume's signed generalized-binomial convention requires even `d`.  The declarations establish analytic series identities only, not a characteristic-function, probabilistic-cumulant, or variance interpretation. | `Fabius.tsum_alternatingNewtonWeight_inv_four_pow`, `Fabius.weightedScaleSeries_alternatingNewton`, `Fabius.alternatingNewton_eq_cexp` |
| Infinite products at summable scales (reusable analytic foundation) | For a normed scalar field, normed space, and normed-ring target, `∑ ‖a n‖ < ∞` and `f(z)-1 = O(z)` at zero give pointwise summability of factor deviations.  Compact-uniform convergence takes values in a complete commutative normed ring with a normed unit and assumes continuity of `f`; local uniform convergence additionally assumes a locally compact domain.  The holomorphic form takes values in a complete commutative normed ring carrying a normed complex-algebra structure and assumes complex differentiability; the normalized wrapper uses `f 0 = 1` instead of an explicit big-O premise.  Exact no-hidden-zero results use the complete commutative normed-ring/normed-unit hypotheses plus a multiplicative norm, but require neither continuity nor local compactness. | `Fabius.summable_norm_scaled_sub_one`, `Fabius.hasProdUniformlyOn_scaled`, `Fabius.multipliableUniformlyOn_scaled`, `Fabius.hasProdLocallyUniformly_scaled`, `Fabius.multipliableLocallyUniformly_scaled`, `Fabius.continuous_tprod_scaled`, `Fabius.differentiable_tprod_scaled`, `Fabius.differentiable_tprod_scaled_of_eq_one`, `Fabius.tprod_scaled_ne_zero`, `Fabius.tprod_scaled_eq_zero_iff` |
| Geometric reciprocal-Gamma products, Mahler equations, zeros, reflection, and geometric-sinc convergence | `GeometricReciprocalGamma.lean` now has exactly 29 public declarations, six definitions and 23 theorems.  For every complex `q` with `‖q‖ < 1`—including `q = 0`—the product `G_q(z)=∏ n, Γ(1+q^n z)⁻¹` is genuinely multipliable and entire, is one at zero, satisfies `G_q(z)=Γ(1+z)⁻¹ G_q(qz)`, and vanishes exactly when `1+q^n z=-m` for some naturals `n,m`.  Its pointwise inverse is meromorphic and satisfies the Gamma-side Mahler equation.  The matching factors `sinc(π q^n z)` now have an explicit locally uniform product, pointwise `Multipliable` and `HasProd` forms, and an entire tprod `S_q(z)=geometricSincProduct q z`; reflection gives `G_q(z)G_q(-z)=S_q(z)`, and at `q=1/2` this is exactly the standalone Rvachev product and its Gamma-denominator form.  Mathlib totalizes `Complex.Gamma` to zero at classical poles, so the Gamma-side functions are pointwise inverses whose poles are recorded by meromorphic order.  No equality with the raw tprod `∏ Γ(1+q^n z)` away from poles, arithmetic Weierstrass regrouping, logarithmic/digamma/Malmsten/heat/integral formula, D-finiteness result, or general-base collision order is asserted. | `Fabius.shiftedReciprocalGamma`, `Fabius.shiftedReciprocalGamma_zero`, `Fabius.shiftedReciprocalGamma_differentiable`, `Fabius.shiftedReciprocalGamma_sub_one_isBigO`, `Fabius.shiftedReciprocalGamma_eq_zero_iff`, `Fabius.shiftedReciprocalGamma_mul_neg`, `Fabius.summable_norm_qpow`, `Fabius.geometricReciprocalGamma`, `Fabius.geometricReciprocalGammaFactors_multipliable`, `Fabius.geometricReciprocalGamma_differentiable`, `Fabius.geometricReciprocalGamma_zero`, `Fabius.geometricReciprocalGamma_mahler`, `Fabius.geometricReciprocalGamma_eq_zero_iff`, `Fabius.geometricGamma`, `Fabius.geometricGamma_meromorphic`, `Fabius.geometricGamma_mahler`, `Fabius.geometricSincProduct`, `Fabius.hasProdLocallyUniformly_geometricSincProduct`, `Fabius.geometricSincProductFactors_multipliable`, `Fabius.hasProd_geometricSincProduct`, `Fabius.geometricReciprocalGamma_mul_neg`, `Fabius.geometricSincProduct_differentiable`, `Fabius.dyadicReciprocalGamma`, `Fabius.dyadicGamma`, `Fabius.dyadicReciprocalGamma_differentiable`, `Fabius.dyadicReciprocalGamma_zero`, `Fabius.geometricSincProduct_inv_two`, `Fabius.dyadicReciprocalGamma_mul_neg`, `Fabius.rvachevFourierProduct_eq_one_div_dyadicGamma_mul` |
| Complex infinite `q`-Pochhammer symbol and global geometric-sinc spectral factorization | The complex symbol `(a;q)_∞` is a total `tprod`; for every complex `a` and exactly `‖q‖ < 1`, its factor family is multipliable, has the named product, and its finite `finiteQPochhammerIn` prefixes converge to it.  For every complex strict contraction `q` and every `z`, the paired Euler perturbations are absolutely summable, their scale--zero product is legitimate, and `geometricSincProduct q z = ∏' k, complexQPochhammerInf (z²/(k+1)²) (q²)` globally, including `q = 0` and individual zero factors.  The final two theorems specialize this identity to the dyadic Rvachev product with nome `1/4` and transfer it to every bounded Fabius witness satisfying `IsFabius`.  This closes the general contracting `q²`-Pochhammer rearrangement but not a named centered characteristic/MGF wrapper, the reciprocal `|q| > 1` formula or zero--pole exchange; the separate GeometricPochhammerNormalConvergence row supplies that outer locally uniform product. | Exhaustive `RvachevPochhammerFactorization` surface (one definition and ten theorems): `Fabius.complexQPochhammerInf`; `Fabius.complexQPochhammerInf_eq_tprod`, `Fabius.complexQPochhammerInf_eq_qPochhammerInfIn`, `Fabius.multipliable_one_sub_mul_pow_complex`, `Fabius.hasProd_complexQPochhammerInf`, `Fabius.tendsto_finiteQPochhammerIn_complex`, `Fabius.summable_norm_sineTerm_qpow_pair`, `Fabius.geometricSincProduct_eq_tprod_pair`, `Fabius.geometricSincProduct_eq_tprod_complexQPochhammerInf`, `Fabius.rvachevFourierProduct_eq_tprod_complexQPochhammerInf`, `Fabius.rvachevFourier_eq_tprod_complexQPochhammerInf` |
| Entire fixed-nome complex `q`-Pochhammer symbol, exact zero lattice, and simple zeros | `QPochhammerEntire.lean` has no public definitions and exactly five public theorems.  For every fixed complex nome with exactly `‖q‖ < 1`, the defining factors converge locally uniformly as functions of `a` on all of `ℂ`, and `a ↦ (a;q)_∞` is entire.  Its zeros are exactly the displayed factor zeros `∃ j, 1-a*q^j=0`; for `q ≠ 0` this is equivalently the exact reciprocal-power lattice `∃ j, a=(q^j)⁻¹`.  Every zero has analytic order one.  The division-free factor spelling is the total statement and includes `q=0`, where only the index-zero factor vanishes, at `a=1`, again simply.  The proof splits off the unique vanishing factor and proves that the finite-prefix/tail cofactor is holomorphic and nonzero.  This closes the fixed-`q` single-symbol zero and multiplicity problem only.  Joint holomorphy in `(a,q)`, an outside-disk reciprocal formula, and centered characteristic-function/MGF wrappers remain outside the result; outer local-uniform/normal convergence is supplied by the separate row below. | `Fabius.hasProdLocallyUniformly_complexQPochhammerInf`, `Fabius.complexQPochhammerInf_differentiable`, `Fabius.complexQPochhammerInf_eq_zero_iff`, `Fabius.complexQPochhammerInf_eq_zero_iff_eq_inv_pow`, `Fabius.analyticOrderAt_complexQPochhammerInf_of_eq_zero` |
| Locally uniform/normal convergence of the outer geometric spectral `q`-Pochhammer product | `GeometricPochhammerNormalConvergence.lean` has no public definitions and exactly three public theorems.  For every complex strict contraction `q`, including `q=0`, the factors `z ↦ complexQPochhammerInf (z^2/(k+1)^2) (q^2)` have a locally uniform product on the whole complex plane equal to `geometricSincProduct q`.  The remaining two theorems specialize this `HasProdLocallyUniformly` statement to the dyadic nome `1/4`, first for `rvachevFourierProduct` and then for every bounded Fabius witness satisfying `IsFabius`.  No nonvanishing premise is imposed, so zeros of individual factors are included.  This closes the outer-convergence clause only: the compound `qF` spectral theorem remains partial because the centered characteristic-function/MGF packaging, outside-disk reciprocal formula, pole divisor, and zero--pole exchange are not formalized. | `Fabius.hasProdLocallyUniformly_geometricSincProduct_complexQPochhammerInf`, `Fabius.hasProdLocallyUniformly_rvachevFourierProduct_complexQPochhammerInf`, `Fabius.hasProdLocallyUniformly_rvachevFourier_complexQPochhammerInf` |
| Classical limits of finite `q`-Pochhammer symbols | `FabiusFunction.ClassicalPochhammerLimit` | Exhaustive zero-definition/five-theorem surface: `ascPochhammer_eval_eq_prod_range`, `tendsto_one_sub_div_one_sub_of_hasDerivAt`, `tendsto_finiteQPochhammerIn_div_pow_of_hasDerivAt`, `tendsto_finiteQPochhammerIn_cpow_div_pow`, and `tendsto_finiteQPochhammerIn_rpow_div_pow`.  The rising-factorial product identity holds over every commutative semiring.  Over a nontrivially normed field, `HasDerivAt f c 1` and `f 1 = 1` imply `(f(q);q)_n/(1-q)^n → (c)_n` through the punctured neighborhood of one, for every natural `n`.  The final two theorems specialize to complex principal powers and real powers for arbitrary exponent and finite order.  No rate, uniformity in the exponent or order, infinite-product limit, or classical-gamma limit is claimed. |
| Universal Gaussian-binomial polynomial | `FabiusFunction.GaussianBinomialUniversal` | Exhaustive zero-definition/two-theorem surface: `gaussianBinomial_eq_eval₂_universal` and `gaussianBinomial_eq_eval_map_universal`.  Over every commutative semiring and for arbitrary `q,n,k`, `[n,k]_q` is evaluation of the universal polynomial `[n,k]_X ∈ ℕ[X]`, either directly by `eval₂` or after mapping its natural coefficients into the target.  This proves polynomiality with natural coefficients, including out-of-range columns, but adds no degree formula, coefficientwise closed form, unimodality, log-concavity, or cyclotomic factorization. |
| Polynomial `q`-Taylor calculus | `FabiusFunction.PolynomialQTaylor` | Exhaustive public surface (two definitions and eighteen theorems): `qFactorial`, `qFallingPower`; `qFactorial_zero`, `qFactorial_succ`, `qDerivative_one`, `qDerivative_iterate_add`, `qDerivative_iterate_C_mul`, `qDerivative_iterate_sum`, `qDerivative_iterate_C_mul_X_pow`, `qDerivative_iterate_eq_zero_of_natDegree_le`, `qFallingPower_zero`, `qFallingPower_succ`, `qFallingPower_succ'`, `qFallingPower_monic`, `qFallingPower_natDegree`, `qFallingPower_eval_self`, `qDerivative_qFallingPower_succ`, `qDerivative_iterate_qFallingPower`, `prod_qInt_sub_eq_qFactorial`, and `qTaylor`.  The factorial and iterated-derivative algebra are over commutative semirings; falling powers and their Jackson derivative use commutative rings; exact degree additionally needs a nontrivial ring.  Over a field, if `[j]_q ≠ 0` for every `1 ≤ j ≤ N`, every polynomial of natural degree at most `N` has the displayed finite `q`-Taylor expansion at arbitrary base point `a`.  No topology, analytic-function expansion, remainder estimate, convergence, or `q→1` theorem is asserted. |
| Finite geometric `q`-partial fractions | `FabiusFunction.QPartialFractions` | Exhaustive public surface (one definition and five theorems): `partialFractionCoeff`; `prod_erase_one_sub_inv_pow_mul_pow`, `finiteQPochhammerIn_self_ne_zero_of_le`, `partialFractionCoeff_mul_prod_eq_one`, `sum_partialFractionCoeff_mul_prod_erase`, and `one_div_finiteQPochhammerIn_eq_sum`.  Over a field, nonvanishing of `(q;q)_n` gives the exact division-free polynomial interpolation identity; this hypothesis includes the degenerate base `q = 0`.  The pointwise reciprocal formula additionally assumes every pole factor `1-zq^j` is nonzero.  The erased-product and diagonal normalization lemmas state their explicit `q ≠ 0` and index bounds.  No infinite partial-fraction expansion, repeated-root case, analytic continuation, residue theorem, or convergence claim is present. |
| Integer-index finite `q`-Pochhammer calculus | `FabiusFunction.QPochhammerIntegerIndex` | Exhaustive public surface (two definitions and fifteen theorems): `qIntervalProd`, `finiteQPochhammerZ`; `Ico_int_eq_image_range`, `prod_Ico_int_eq_prod_range`, `qIntervalProd_of_le`, `qIntervalProd_of_lt`, `qIntervalProd_self`, `qIntervalProd_symm`, `qIntervalProd_trans_of_le`, `qIntervalProd_trans`, `finiteQPochhammerZ_natCast`, `finiteQPochhammerZ_neg_natCast`, `prod_Ico_add_zpow`, `qIntervalProd_add_eq`, `finiteQPochhammerZ_add`, `finiteQPochhammerZ_add_one`, and `finiteQPochhammerZ_neg_natCast_eq`.  Over a field, the signed interval product is the forward product on increasing integer intervals and the inverse reversed product on decreasing ones.  The ordered cocycle is unconditional; the all-order cocycle assumes its two factors nonzero.  Natural and negative-natural indices, symmetry, and empty intervals are included.  Translation and integer-index concatenation require `q ≠ 0`, concatenation also requires both factors nonzero, the one-step shift requires the displayed two nonzero factors, and the closed negative-index formula requires `a ≠ 0` and `q ≠ 0`.  No infinite-index limit, meromorphic continuation in the integer index, or root-of-unity singularity regularization is asserted. |
| Universal `q`-multinomial coefficients | `FabiusFunction.QMultinomial` | Exhaustive public surface: the definition `qMultinomial` and exactly nine theorems, `qMultinomial_nil`, `qMultinomial_cons`, `gaussianBinomial_diag`, `qMultinomial_singleton`, `qMultinomial_pair`, `map_qMultinomial`, `qMultinomial_eq_eval₂_universal`, `finiteQPochhammerIn_self_sum_eq_qMultinomial_mul`, and `qMultinomial_eq_div`.  The definition, empty-list law, and recursion need only an arbitrary semiring.  The diagonal, singleton, pair, naturality, and universal-polynomial laws use commutative semirings; the pair law assumes `k ≤ n`.  The factorial identity is division-free over commutative rings; the field quotient form assumes exactly `(q;q)_(sum l) ≠ 0`.  This proves polynomiality with natural coefficients, not a coefficient formula, symmetry under arbitrary permutations, or analytic convergence. |
| General basic hypergeometric terms and convergence domains | `FabiusFunction.BasicHypergeometricSeries` | Exhaustive public surface: the two definitions `basicHypergeometricTerm`, `basicHypergeometric` and exactly five theorems, `qPochhammerInfIn_neg_norm_nonneg`, `twoPhiOneTerm_eq_basicHypergeometricTerm`, `norm_basicHypergeometricTerm_le`, `summable_basicHypergeometricTerm_of_le`, and `summable_basicHypergeometricTerm_of_eq`.  Over a complete normed field with `‖q‖ < 1` and nonvanishing denominator products, the uniform bound gives absolute convergence for every `z` when `r ≤ s`, and on `‖z‖ < 1` when `r = s + 1`; `₂φ₁` is identified termwise with the general definition.  No boundary convergence, analytic continuation, summation formula, or convergence claim with a vanishing denominator is asserted. |
| Continuity and the classical limit of Gaussian binomial coefficients | `GaussianBinomialContinuity.lean` has zero public definitions and three public theorems.  Continuity and `[n,k]_q → C(n,k)` as `q → 1` hold in every topological semiring for every natural `n,k`, including degree zero and out-of-range columns.  The finite q-Pochhammer quotient form instead works over a field and assumes exactly `k ≤ n` and `(q;q)_k ≠ 0`.  No differentiability, convergence rate, or quotient statement at a vanishing denominator is asserted. | `Fabius.continuous_gaussianBinomial`, `Fabius.tendsto_gaussianBinomial_nhds_one`, `Fabius.gaussianBinomial_eq_finiteQPochhammerIn_div` |
| q-Pascal row summation and Gaussian-coefficient commutation | `QPascalSummation.lean` has zero public definitions and four public theorems.  The first q-Pascal row split holds in every semiring and the second in every commutative semiring, total in the row index and including `n = 0`.  In an arbitrary semiring, Gaussian coefficients commute in either orientation with every element that commutes with `q`.  These are finite algebraic statements with no subtraction, division, topology, or convergence hypothesis. | `Fabius.sum_gaussianBinomial_succ_mul`, `Fabius.sum_gaussianBinomial_succ_mul'`, `Commute.gaussianBinomial_left`, `Commute.gaussianBinomial_right` |
| Noncommutative q-binomial theorem for the quantum plane | `QuantumBinomial.lean` has zero public definitions and two public theorems.  In every possibly noncommutative semiring, the relation `Y*X = q*(X*Y)` and the displayed commutation of `q` with `X` and `Y` give both `Y*X^m = q^m*(X^m*Y)` and the ordered Gaussian expansion of `(X+Y)^n`.  Both results include exponent zero and need no inverses or subtraction; `q` need not be central outside the stated variables. | `Fabius.quantumPlane_mul_pow`, `Fabius.quantum_binomial` |
| Euler identities, effective Gaussian bounds, and the infinite q-binomial theorem | `QBinomialTheoremInfinite.lean` has one public definition and 27 public theorems.  It packages Tannery's theorem, comparison products, uniform Gaussian bounds, arbitrary fixed-shift convergence and geometric rates, Euler's product, the infinite q-binomial theorem, and Euler's reciprocal expansion under the displayed strict-contraction hypotheses. | Definition: `Fabius.gaussianMajorant`. Added names: `Fabius.norm_finiteQPochhammerIn_pow_sub_one_le_exp_of_norm_le_one`, `Fabius.isBigO_finiteQPochhammerIn_pow_sub_one`, `Fabius.tendsto_gaussianBinomial_add_const_atTop`, `Fabius.isBigO_gaussianBinomial_sub_inv`, `Fabius.isBigO_gaussianBinomial_add_sub_inv`; the retained 22-name inventory is unchanged. |
| Rogers--Szegő polynomials, finite recurrences, and generating function | `RogersSzegoPolynomial.lean` has one public definition and nine public theorems.  The polynomial, its zero-degree and `z = 1` evaluations, and its two-term recurrence are finite algebra over every commutative semiring; the adjacent-column, dilation, and three-term identities use a commutative ring and include every natural index.  Over a normed field, the absolute-summability helper assumes `‖q‖ < 1` and norm below one for its series variable, while the finite antidiagonal coefficient identity assumes only `‖q‖ < 1`.  Over a complete normed field, the generating function assumes exactly `‖q‖ < 1`, `‖t‖ < 1`, and `‖z*t‖ < 1`, including `q = 0` and `t = 0`.  No unit-circle boundary, orthogonality measure, zero-location, or asymptotic theorem is asserted. | Definition: `Fabius.rogersSzego`.  Theorems: `Fabius.rogersSzego_zero`, `Fabius.rogersSzego_one`, `Fabius.rogersSzego_succ`, `Fabius.one_sub_pow_succ_mul_gaussianBinomial_succ_succ`, `Fabius.rogersSzego_dilation`, `Fabius.rogersSzego_three_term`, `Fabius.summable_norm_pow_div_finiteQPochhammerIn_self`, `Fabius.sum_antidiagonal_euler_mul_euler`, `Fabius.hasSum_rogersSzego_generating` |
| Jacobi triple product and Euler pentagonal theorem | `JacobiTripleProduct.lean` has two public definitions and 25 public theorems.  Its integer theta and pentagonal exponents are total.  The finite polynomial triple product holds over every commutative ring for arbitrary `q,z,N`, including `N = 0`; the Laurent form works over a field under exactly `z ≠ 0`.  Over every complete normed field, both Jacobi bilateral forms assume `‖q‖ < 1` and `z ≠ 0`, while the two pentagonal forms assume only `‖q‖ < 1` and explicitly include `q = 0`.  The current edit only renames the unused proof binder `hi` to `_hi` in `thetaCoeff_pair_eq`; it changes no public type, hypothesis, result, or count.  No norm-one boundary, modular transformation, theta functional equation, or analytic continuation is claimed. | Definitions: `Fabius.thetaExponent`, `Fabius.pentagonalExponent`.  Theorems: `Fabius.two_dvd_mul_sub_one`, `Fabius.mul_sub_one_nonneg`, `Fabius.two_mul_thetaExponent`, `Fabius.thetaExponent_natCast`, `Fabius.thetaExponent_neg_natCast`, `Fabius.choose_two_add_choose_two_succ_eq`, `Fabius.sum_mul_sum_eq_sum_antidiagonal`, `Fabius.thetaCoeff_pair_eq`, `Fabius.sum_antidiagonal_thetaCoeff`, `Fabius.finite_triple_product_poly`, `Fabius.pow_mul_finiteQPochhammerIn_div`, `Fabius.neg_one_zpow_natCast_sub`, `Fabius.finite_triple_product`, `Fabius.norm_gaussianBinomialInt_le`, `Fabius.tendsto_gaussianBinomialInt_central`, `Fabius.summable_pow_thetaExponent_mul_zpow`, `Fabius.hasSum_jacobi_triple_product`, `Fabius.hasSum_jacobi_triple_product'`, `Fabius.mul_three_mul_sub_one_nonneg`, `Fabius.two_dvd_mul_three_mul_sub_one`, `Fabius.two_mul_pentagonalExponent`, `Fabius.pentagonalExponent_eq`, `Fabius.pentagonalExponent_pos`, `Fabius.hasSum_pentagonal`, `Fabius.hasSum_pentagonal_pairs` |
| Finite `q`-Pochhammer residue-class dissections (exhaustive foundational API) | `QPochhammerDissection.lean` has no public definitions and exactly two public theorems.  Over every commutative ring and for arbitrary `a,q,r,n`, the exact-multiple identity sorts `(a;q)_(r*n)` into the `r` products `(a*q^s;q^r)_n`.  Under only `u ≤ r`, the remainder identity gives length `n+1` to residues `s<u` and length `n` to `u≤s<r`.  Both endpoint cases `u=0,r` are literal; no topology, convergence, division, cancellation, or nonvanishing hypothesis occurs. | `Fabius.finiteQPochhammerIn_dissection`, `Fabius.finiteQPochhammerIn_dissection_remainder` |
| General infinite `q`-Pochhammer algebra, zeros, regularity, and simple-zero derivatives (exhaustive foundational API) | `QPochhammerInfinite.lean` defines the total `qPochhammerInfIn` in every topological commutative ring and exports exactly 29 theorems.  A complete commutative normed ring with `‖1‖=1` and `‖q‖<1` has multipliability, finite-prefix convergence, prefix/tail concatenation, first- and arbitrary-factor removal, and positive-modulus infinite dissection.  With a multiplicative norm, zeros are exactly the raw factors `a*q^j=1` and `(q;q)_∞≠0`.  In a complete normed field, `q≠0` gives the inverse-power parametrization; local compactness gives local uniformity and continuity in `a` for fixed `q`.  Over `ℂ` the function of `a` is entire; the module gives the derivative and its nonvanishing at every raw factor zero, analytic order one at every zero, and, under `q≠0`, its closed inverse-power value and coefficient nonvanishing.  It claims neither joint holomorphy in `q` nor global growth/order/type/asymptotics.  The separate five-theorem `QPochhammerEntire.lean` compatibility API covers `complexQPochhammerInf`, including its nonzero-nome reciprocal-power spelling; the unconditional bridge `complexQPochhammerInf_eq_qPochhammerInfIn` is canonically owned by `RvachevPochhammerFactorization`. | Definition: `Fabius.qPochhammerInfIn`.  Theorems: `Fabius.qPochhammerInfIn_eq_tprod`, `Fabius.summable_norm_mul_pow`, `Fabius.one_sub_ne_zero_of_norm_lt_one`, `Fabius.norm_mul_pow_self_lt_one`, `Fabius.finiteQPochhammerIn_self_ne_zero`, `Fabius.multipliable_one_sub_mul_pow_of_norm_lt_one`, `Fabius.hasProd_qPochhammerInfIn`, `Fabius.tendsto_finiteQPochhammerIn_qPochhammerInfIn`, `Fabius.qPochhammerInfIn_eq_finite_mul_shift`, `Fabius.qPochhammerInfIn_succ_shift`, `Fabius.qPochhammerInfIn_eq_factor_mul`, `Fabius.qPochhammerInfIn_dissection`, `Fabius.qPochhammerInfIn_ne_zero`, `Fabius.qPochhammerInfIn_eq_zero_iff`, `Fabius.qPochhammerInfIn_self_ne_zero`, `Fabius.qPochhammerInfIn_eq_tprod_smul`, `Fabius.summable_norm_pow_of_norm_lt_one`, `Fabius.isBigO_one_sub_sub_one`, `Fabius.differentiable_finiteQPochhammerIn`, `Fabius.qPochhammerInfIn_eq_zero_iff_exists_inv_pow`, `Fabius.hasProdLocallyUniformly_qPochhammerInfIn`, `Fabius.continuous_qPochhammerInfIn`, `Fabius.pow_sq_mul_finiteQPochhammerIn_inv_pow_self`, `Fabius.differentiable_qPochhammerInfIn`, `Fabius.hasDerivAt_qPochhammerInfIn_of_mul_pow_eq_one`, `Fabius.hasDerivAt_qPochhammerInfIn_inv_pow`, `Fabius.deriv_qPochhammerInfIn_inv_pow_ne_zero`, `Fabius.deriv_qPochhammerInfIn_ne_zero_of_mul_pow_eq_one`, `Fabius.analyticOrderAt_qPochhammerInfIn_of_eq_zero` |
| Euler expansions and the infinite q-binomial theorem (exhaustive foundational API) | `QBinomialTheoremInfinite.lean` has one public definition and exactly 27 public theorems.  It retains its comparison, majorant, Tannery, Euler, and q-binomial API and adds the five effective fixed-column declarations named above. | Definition and exhaustive additions are given in the preceding q-binomial row. |
| Effective fixed-column Gaussian convergence (`thm:fixed-column-limit`; exhaustive companion API) | **Exact.** `GaussianBinomialFixedColumnRate.lean` has no public definitions and exactly nine public theorems after ownership transfer.  Its ring-level product-defect/denominator-free bounds and field-level fixed/shifted relative/additive estimates retain their hypotheses; the stronger fixed-shift limit is `QBinomialTheoremInfinite.tendsto_gaussianBinomial_add_const_atTop`. | `Fabius.norm_finiteQPochhammerIn_pow_sub_one_le_exp'`, `Fabius.norm_finiteQPochhammerIn_pow_sub_one_le`, `Fabius.norm_finiteQPochhammerIn_self_mul_gaussianBinomial_sub_one_le`, `Fabius.norm_gaussianBinomial_sub_inv_finiteQPochhammerIn_le`, `Fabius.norm_gaussianBinomial_add_sub_inv_finiteQPochhammerIn_le`, `Fabius.gaussianBinomial_fixedColumn_relativeError_isBigO`, `Fabius.gaussianBinomial_shifted_fixedColumn_relativeError_isBigO`, `Fabius.gaussianBinomial_fixedColumn_error_isBigO`, `Fabius.gaussianBinomial_shifted_fixedColumn_error_isBigO` |
| Greater-than-one Gaussian asymptotics (`cor:qgreaterone`) | **Exact.** `GaussianBinomialGreaterOneAsymptotics.lean` has no public definitions and exactly two theorems.  For real `q` under exactly `1<q`, the fixed-column theorem controls `(q⁻¹;q⁻¹)_k * (q^(k*(n-k)))⁻¹ * [n,k]_q - 1` by `O((q⁻¹)^(n-k+1))`; natural subtraction is total and reciprocity is used only eventually when `k≤n`.  The central theorem gives `[2m,m]_q ~ q^(m*m) * (q⁻¹;q⁻¹)_∞⁻¹`.  Together with `Fabius.gaussianBinomial_inv`, these close the printed corollary without asserting a shifted-central or wider-domain result. | `Fabius.gaussianBinomial_gt_one_fixedColumn_relativeError_isBigO`, `Fabius.gaussianBinomial_gt_one_central_isEquivalent`; reused reciprocity: `Fabius.gaussianBinomial_inv` |
| Jacobi triple product and Euler pentagonal theorem (exhaustive companion API) | `JacobiTripleProduct.lean` has two public definitions and exactly 25 public theorems.  The finite triple-product polynomial identity is ring-level and division-free; its Laurent form is field-level and assumes `z≠0`.  The bilateral Jacobi identities hold in every complete normed field for exactly `‖q‖<1` and `z≠0`; Euler's pentagonal theorem follows by the cubic-nome specialization and positive-modulus q-Pochhammer dissection.  No boundary `‖q‖=1`, `z=0` Laurent value, modular transformation, or theta-functional-equation theorem is asserted. | Definitions: `Fabius.thetaExponent`, `Fabius.pentagonalExponent`.  Theorems: `Fabius.two_dvd_mul_sub_one`, `Fabius.mul_sub_one_nonneg`, `Fabius.two_mul_thetaExponent`, `Fabius.thetaExponent_natCast`, `Fabius.thetaExponent_neg_natCast`, `Fabius.choose_two_add_choose_two_succ_eq`, `Fabius.sum_mul_sum_eq_sum_antidiagonal`, `Fabius.thetaCoeff_pair_eq`, `Fabius.sum_antidiagonal_thetaCoeff`, `Fabius.finite_triple_product_poly`, `Fabius.pow_mul_finiteQPochhammerIn_div`, `Fabius.neg_one_zpow_natCast_sub`, `Fabius.finite_triple_product`, `Fabius.norm_gaussianBinomialInt_le`, `Fabius.tendsto_gaussianBinomialInt_central`, `Fabius.summable_pow_thetaExponent_mul_zpow`, `Fabius.hasSum_jacobi_triple_product`, `Fabius.hasSum_jacobi_triple_product'`, `Fabius.mul_three_mul_sub_one_nonneg`, `Fabius.two_dvd_mul_three_mul_sub_one`, `Fabius.two_mul_pentagonalExponent`, `Fabius.pentagonalExponent_eq`, `Fabius.pentagonalExponent_pos`, `Fabius.hasSum_pentagonal`, `Fabius.hasSum_pentagonal_pairs` |
| q-Pascal row summation and Gaussian-coefficient commutation (exhaustive foundational API) | `QPascalSummation.lean` has no public definitions and exactly four public theorems.  Its first row split holds over arbitrary semirings, the reflected split over commutative semirings, and the two root-namespace `Commute` results state that a Gaussian coefficient commutes with anything commuting with the nome.  These are finite identities with no division or convergence premise. | `Fabius.sum_gaussianBinomial_succ_mul`, `Fabius.sum_gaussianBinomial_succ_mul'`, `Commute.gaussianBinomial_left`, `Commute.gaussianBinomial_right` |
| Gaussian continuity, classical limit, and quotient form (exhaustive companion API) | `GaussianBinomialContinuity.lean` has no public definitions and exactly three public theorems.  Continuity in the nome and convergence to the ordinary binomial coefficient at `q→1` hold in every topological semiring.  The finite q-Pochhammer quotient form is field-level and assumes exactly the displayed denominator `(q;q)_k≠0`; it is not used to redefine the division-free Gaussian coefficient. | `Fabius.continuous_gaussianBinomial`, `Fabius.tendsto_gaussianBinomial_nhds_one`, `Fabius.gaussianBinomial_eq_finiteQPochhammerIn_div` |
| Noncommutative quantum binomial theorem (exhaustive companion API) | `QuantumBinomial.lean` has no public definitions and exactly two public theorems.  In an arbitrary semiring, `Y*X=q*X*Y` and commutation of `q` with `X,Y` give `Y*X^m=q^m*X^m*Y` and the ordered Gaussian expansion of `(X+Y)^n`.  The ambient semiring need not be commutative; centrality beyond the two stated commutations is not assumed. | `Fabius.quantumPlane_mul_pow`, `Fabius.quantum_binomial` |
| Rogers--Szegő polynomials and generating series (exhaustive companion API) | `RogersSzegoPolynomial.lean` has one public definition and exactly nine public theorems.  Boundary values and the two-term recurrence are commutative-semiring identities; dilation and the three-term recurrence use a commutative ring.  The generating series is proved over every complete normed field under exactly `‖q‖<1`, `‖t‖<1`, and `‖z*t‖<1`, by the Cauchy product of two Euler reciprocal series.  No orthogonality measure, root-location theorem, or boundary-convergence claim is made. | Definition: `Fabius.rogersSzego`.  Theorems: `Fabius.rogersSzego_zero`, `Fabius.rogersSzego_one`, `Fabius.rogersSzego_succ`, `Fabius.one_sub_pow_succ_mul_gaussianBinomial_succ_succ`, `Fabius.rogersSzego_dilation`, `Fabius.rogersSzego_three_term`, `Fabius.summable_norm_pow_div_finiteQPochhammerIn_self`, `Fabius.sum_antidiagonal_euler_mul_euler`, `Fabius.hasSum_rogersSzego_generating` |
| Latest q-calculus closure: infinite-product bounds, Heine/q-Gauss, complex order, basic hypergeometric series, and q-multinomials | The exhaustive module counts are `QPochhammerInfiniteBounds` 0+5, `HeineTransformation` 2+5, `QGaussSummation` 0+2, `QPochhammerComplexOrder` 1+4, `BasicHypergeometricSeries` 2+5, and `QMultinomial` 1+9.  Together they contribute six definitions and thirty theorems.  Their strict-contraction, nonvanishing, and denominator hypotheses are retained; the recursive q-multinomial and cleared product formula are division-free. | Principal entry points: `Fabius.norm_qPochhammerInfIn_le`, `Fabius.heine_transformation`, `Fabius.q_gauss_summation`, `Fabius.qPochhammerC`, `Fabius.basicHypergeometric`, `Fabius.qMultinomial` |
| Gaussian palindromicity, q-exponentials, Jackson integration, and theta quasi-periodicity | Exhaustive counts are `GaussianBinomialPalindromic` 0+14, `QExponential` 3+8, `JacksonIntegral` 1+7, and `ThetaQuasiPeriodicity` 1+6: five definitions and thirty-five theorems.  The tranche proves Gaussian degree, monicity, coefficient reversal, the division-free mean identity, and the total linear-coefficient classifier; defines the q-derivative and two q-exponentials with product and eigenfunction laws; proves both halves of Jackson's fundamental theorem and integration by parts; and gives the bilateral-theta product, quasi-periodicity, and exact zero criterion.  All analytic convergence, strict-contraction, nonzero-variable, and nonvanishing premises remain explicit. | Principal entry points: `Fabius.reflect_gaussianBinomial`, `Fabius.two_mul_derivative_gaussianBinomial_eval_one`, `Fabius.coeff_gaussianBinomial_one_of_pos_of_lt`, `Fabius.coeff_gaussianBinomial_one`, `Fabius.qExp`, `Fabius.qDeriv_qExp`, `Fabius.jacksonIntegral`, `Fabius.jacksonIntegral_mul_qDeriv`, `Fabius.bilateralTheta_eq_prod`, `Fabius.bilateralTheta_mul_left`, `Fabius.bilateralTheta_eq_zero_iff` |
| Universal Gaussian structure, q-Pochhammer derivatives, and Jacobi's cubic identity | Exhaustive counts are `GaussianBinomialPolynomialStructure` 0+5, `QPochhammerLogDerivative` 0+10, `QPochhammerOrderDerivative` 0+3, and `JacobiCubic` 0+2: twenty theorems and no definitions.  The results give exact universal Gaussian degree, monicity, constant coefficient, and coefficient reversal over `ℕ[X]`; differentiate the infinite q-Pochhammer product and identify its Lambert-series logarithmic derivative for `‖q‖<1`, `‖a‖<1`; differentiate the complex-order symbol under `q≠0` and `‖a*q^α‖<1`; and prove Jacobi's cubic `HasSum` identity for a strict-contraction nome. | Principal entry points: `Fabius.natDegree_gaussianBinomial_universal`, `Fabius.gaussianBinomial_universal_reflect`, `Fabius.hasDerivAt_qPochhammerInfIn_lambert`, `Fabius.hasDerivAt_qPochhammerC`, `Fabius.hasSum_jacobi_cubic` |
| Cyclotomic factorization and central Gaussian reduction | `CyclotomicFactorization` 0+7 and `CentralQBinomialReduction` 0+6 contribute thirteen theorems and no definitions.  The former factors `(X;X)_n` over commutative rings and `[n,k]_X` over integral domains into the exact cyclotomic exponents.  The latter proves sign pairing, even--odd dissection, finite-symbol naturality, and the division-free central reduction over commutative rings; only its quotient form requires the two displayed denominators to be nonzero. | Principal entry points: `Fabius.finiteQPochhammerIn_X_eq_prod_cyclotomic`, `Fabius.gaussianBinomial_X_eq_prod_cyclotomic`, `Fabius.finiteQPochhammerIn_mul_neg`, `Fabius.central_gaussianBinomial_sq_mul`, `Fabius.central_gaussianBinomial_sq_div` |
| Primitive-root blocks, q-Lucas, cyclotomic carries, and q-Catalan polynomials | Exhaustive counts are `PrimitiveRootBlock` 0+3, `QLucas` 0+7, `CyclotomicDivisibility` 0+3, and `QCatalan` 1+11: one definition and twenty-four theorems.  They prove the complete primitive-root block, the q-Lucas formula, the exact cyclotomic carry/divisibility criterion and multiple-index primitive-root value, and MacMahon's integral q-Catalan quotient with degree `n*(n-1)` and Catalan value at one.  Positive order, primitive-root, commutative-integral-domain, and index-bound hypotheses remain explicit.  QLucas's local `two_mul_choose_two` helper is private; the public theorem of that name belongs to `QChuVandermonde`. | Principal entry points: `Fabius.finiteQPochhammerIn_isPrimitiveRoot`, `Fabius.gaussianBinomial_q_lucas`, `Fabius.cyclotomic_dvd_gaussianBinomial_iff`, `Fabius.gaussianBinomial_mul_isPrimitiveRoot`, `Fabius.qCatalan`, `Fabius.qCatalan_eval_one` |
| Newton interpolation and the Jackson q-beta integral | Exhaustive counts are `NewtonInterpolation` 3+19 and `QBetaIntegral` 1+8: four definitions and twenty-seven theorems.  The first gives triangular Newton coefficients, the node polynomial, interpolation/uniqueness, divided differences, and geometric-grid formulas over a field, together with the seven-name `newtonInterpolant` compatibility API.  The second evaluates the real Jackson q-beta integral by infinite products and q-Gamma and proves symmetry, positivity, and recurrences for `0 < q < 1` and positive arguments. | Principal entry points: `Fabius.newtonCoeff`, `Fabius.nodeNewtonPoly`, `Fabius.newtonInterpolant`, `Fabius.eval_nodeNewtonPoly`, `Fabius.eval_newtonPoly`, `Fabius.newtonCoeff_eq_sum`, `Fabius.newtonCoeff_pow_eq_sum`, `Fabius.qBeta`, `Fabius.qBeta_eq_prod`, `Fabius.qBeta_eq_qGamma` |
| Integer/complex upper Gaussian coefficients and q-Pfaff--Saalschütz | Exhaustive counts are `GaussianBinomialInteger` 1+10, `GaussianBinomialComplexOrder` 1+5, and `QPfaffSaalschutz` 0+3: two definitions and eighteen theorems.  Integer upper indices satisfy both q-Pascal laws and negative-index reflection and yield the reciprocal finite q-binomial series.  Principal-branch complex upper indices give the generalized reciprocal and finite q-binomial series.  The terminating balanced `₃φ₂` identity is algebraic over a field; its nonzero parameters and all finite-product denominators are explicit. | Principal entry points: `Fabius.gaussianBinomialZ`, `Fabius.gaussianBinomialZ_neg_natCast`, `Fabius.hasSum_reciprocal_finiteQPochhammerIn`, `Fabius.gaussianBinomialC`, `Fabius.hasSum_qPochhammerC_inv`, `Fabius.hasSum_qPochhammerC`, `Fabius.q_pfaff_saalschutz` |
| Terminating `₂φ₁` reversal (exhaustive API; `lem:2phi1-reversal`) | `TwoPhiOneReversal.lean` has two public definitions and exactly twelve public theorems.  The result is stated for the actual `twoPhiOne` tsum, not merely a finite proxy; the finite-support bridge requires no analytic convergence bounds.  Reflection is proved involutive and applying the reversal twice cancels both prefactors.  Thus `lem:2phi1-reversal` is **Exact**.  The reversal retains `q,a,c,z ≠ 0` and nonvanishing of `(q;q)_n`, `(c;q)_n`, and `(q^{1-n}/a;q)_n`; the separate `n=0` bridge also covers `q=0`. | Definitions: `Fabius.twoPhiOneFinite`, `Fabius.twoPhiOneReflection`.  Theorems: `Fabius.choose_two_add_succ_choose_two`, `Fabius.finiteQPochhammerIn_sub_eq`, `Fabius.finiteQPochhammerIn_reversal_ne_zero`, `Fabius.finiteQPochhammerIn_inv_pow_self`, `Fabius.twoPhiOneReflection_involutive`, `Fabius.twoPhiOneFinite_reversal`, `Fabius.twoPhiOneFinite_reversal_twice`, `Fabius.twoPhiOneFinite_eq_sum_twoPhiOneTerm`, `Fabius.twoPhiOne_eq_twoPhiOneFinite_inv_pow`, `Fabius.twoPhiOne_reversal`, `Fabius.twoPhiOne_reversal_twice`, `Fabius.twoPhiOne_one_eq_twoPhiOneFinite_zero` |
| The two q-Chu--Vandermonde sums (exhaustive API; `cor:q-chu`, `prop:qchu2-by-reversal`) | `QChuVandermonde.lean` has no public definitions and exactly ten public theorems.  `cor:q-chu` is **Exact**: both identities have actual-`twoPhiOne` wrappers on the full displayed rational domain `q ≠ 0`, `A ≠ 0`, `(q;q)_n ≠ 0`, `(C;q)_n ≠ 0`; the second needs neither `C ≠ 0` nor `(A;q)_n ≠ 0`.  `prop:qchu2-by-reversal` is **Partial**: its provenance theorem proves the second identity by reversal only after additionally assuming `C ≠ 0` and `(A;q)_n ≠ 0`.  The stronger full-domain finite theorem and actual-tsum wrapper instead use a direct denominator-cleared q-Cauchy proof; rational continuation and a cleared commutative-ring extension are not formalized. | `Fabius.two_mul_choose_two`, `Fabius.mul_sub_one_eq_mul_sub_add`, `Fabius.finiteQPochhammerIn_div_eq_sum_chu`, `Fabius.q_chu_vandermonde_first`, `Fabius.finiteQPochhammerIn_div_eq_sum_chu_second`, `Fabius.twoPhiOneFinite_mul_finiteQPochhammerIn_eq_chu_second`, `Fabius.q_chu_vandermonde_second`, `Fabius.q_chu_vandermonde_second_by_reversal`, `Fabius.twoPhiOne_q_chu_vandermonde_first`, `Fabius.twoPhiOne_q_chu_vandermonde_second` |
| Jacobi's two-square count and the two Lambert forms | `JacobiTwoSquareCount` is exhaustively 0+4.  For every nonzero natural `n`, the count is `r₂(n)=4 D(n)` in `ℤ`; the printed prime product follows under the explicit even-valuation condition at primes congruent to three modulo four.  The two analytic theorems instantiate the hypothesis-parameterized `TwoSquareTheorem` kernels and prove both the `χ₄` and alternating-odd Lambert forms unconditionally over any complete normed field under `‖q‖<1`. | `Fabius.sumSqRep_two_eq_four_mul_twoSquareDivisorSum`, `Fabius.sumSqRep_two_eq_four_mul_prod`, `Fabius.theta_sq_eq_chi4_lambert`, `Fabius.theta_sq_eq_odd_lambert` |
| Noncommutative q-multinomial theorem | `QuantumMultinomial` has no public definitions and exactly five public theorems.  In an arbitrary semiring, if `q` commutes with every variable and `x_j*x_i=q*(x_i*x_j)` for `i<j`, powers of the finite sum expand into increasing-index ordered monomials weighted by `qMultinomial`.  Tuple-antidiagonal decomposition, semiring Gaussian symmetry, and coefficient commutation are included.  The theorem is finite and division-free. | `Fabius.sum_antidiagonalTuple_succ`, `Fabius.gaussianBinomial_eq_evalRingHom_quantum`, `Fabius.gaussianBinomial_symm'`, `Commute.qMultinomial_left`, `Fabius.quantum_multinomial` |
| Gaussian reciprocity and real bounds | `GaussianBinomialBounds` has no public definitions and exactly six public theorems.  It evaluates reciprocity over fields, proves the constant-term lower bound and infinite-q-Pochhammer upper bound for nonnegative real strict contractions, and derives the dimension-dominant two-sided estimates for every real `Q > 1`; `k ≤ n` and all nonzero/order hypotheses remain explicit. | `Fabius.gaussianBinomial_inv`, `Fabius.one_le_gaussianBinomial`, `Fabius.finiteQPochhammerIn_pow_le_one`, `Fabius.gaussianBinomial_le_inv_qPochhammerInfIn`, `Fabius.pow_le_gaussianBinomial_of_one_lt`, `Fabius.gaussianBinomial_le_pow_div_of_one_lt` |
| Reciprocal-integer geometric reciprocal-Gamma zero set | For every natural base `b ≥ 2`, the entire product `G_(1/b)` vanishes exactly at the embedded strictly negative natural numbers, independently of `b`, and is nonzero at every embedded nonnegative natural number.  Its pointwise inverse is meromorphic.  These four theorems exhaust `ReciprocalIntegerGammaZeros.lean`; they identify the zero set but do not determine the general-base collision order. | `Fabius.norm_inv_natCast_lt_one`, `Fabius.geometricReciprocalGamma_inv_natCast_eq_zero_iff`, `Fabius.geometricReciprocalGamma_inv_natCast_ne_zero_of_nonneg`, `Fabius.geometricGamma_inv_natCast_meromorphic` |
| Exact dyadic reciprocal-Gamma zero set and zero/pole orders | The dyadic reciprocal product vanishes exactly at the strictly negative integers and is nonzero at every nonnegative integer.  At a negative integer `m`, its complex analytic order is `1 + padicValNat 2 m.natAbs`; equivalently, at `-n` for `n ≠ 0` it is `1 + padicValNat 2 n`.  The meromorphic inverse has the negative of that order, Mathlib's encoding of a pole of positive order `1+v₂(n)`.  At checkpoint `71ab6f6728fceb753c88d8b0573077a59acf2682` these theorems did not yet establish the separate simple-zero derivative coefficient of `1/Γ`; the next row records the later closure. | `Fabius.dyadicReciprocalGamma_eq_zero_iff`, `Fabius.dyadicReciprocalGamma_int_ne_zero_of_nonneg`, `Fabius.dyadicReciprocalGamma_nat_ne_zero`, `Fabius.dyadicGamma_meromorphic`, `Fabius.analyticOrderAt_dyadicReciprocalGamma_int_of_neg`, `Fabius.analyticOrderAt_dyadicReciprocalGamma_neg_nat`, `Fabius.meromorphicOrderAt_dyadicGamma_int_of_neg`, `Fabius.meromorphicOrderAt_dyadicGamma_neg_nat` |
| Reciprocal-Gamma first jets at all nonpositive integers | At source commit `0ba35abd4`, proved for every natural `r` with no additional hypothesis: `(Γ⁻¹)'(-r)=(-1)^r r!`, the reciprocal-Gamma zero at `-r` has analytic order one, and `Γ(s)⁻¹/(s+r)` tends to the same nonzero coefficient in the punctured neighborhood of `-r`; the origin theorem is retained as a compatibility name.  These declarations concern the entire reciprocal function `s ↦ (Complex.Gamma s)⁻¹`.  They do not assign a value or derivative to raw `Complex.Gamma` at one of its poles. | `Fabius.deriv_Gamma_inv_neg_nat`, `Fabius.hasDerivAt_Gamma_inv_neg_nat`, `Fabius.hasDerivAt_Gamma_inv_zero`, `Fabius.analyticOrderAt_Gamma_inv_neg_nat`, `Fabius.tendsto_Gamma_inv_div_add_nat` |
| Thue--Morse continuation first jets, GammaLog levels, and dyadic Gamma-tower laws | At source commit `0ba35abd4`, the first eight public declarations give, for every natural `r` and positive real `a`, `D'_a(-r)=(-1)^r r! · mellin (mellinKernel a) (-r)`, its real-integral form, `L_r(a)=2^r(L_r(a/2)-L_r((a+1)/2))`, and `T_r(a)=(T_r(a/2)/T_r((a+1)/2))^(2^r)`.  The definitions `L_r(a)=thueMorseGammaLog r a` and `T_r(a)=exp(L_r(a))` are total for every real `a`, but the identification and dyadic theorems assume exactly `0<a`.  `thueMorseGammaLog` is a chosen logarithmic coordinate; no identity with `Complex.log (thueMorseGammaTower r a)` is asserted.  The current integrated tree adds the ninth public declaration: for `a,b>0`, the complex embedding of `Real.exp (mpLimit a b)` equals `T_0(b)/T_0(a)`.  The completed parameter-differential layer is recorded in the next row. | `Fabius.hasDerivAt_dirichletMellinContinuation_neg_nat`, `Fabius.deriv_dirichletMellinContinuation_neg_nat`, `Fabius.thueMorseGammaLog`, `Fabius.thueMorseGammaTower`, `Fabius.thueMorseGammaLog_eq_mellin`, `Fabius.thueMorseGammaLog_eq_integral`, `Fabius.thueMorseGammaLog_dyadic`, `Fabius.thueMorseGammaTower_dyadic`, `Fabius.ofReal_exp_mpLimit_eq_gammaTower_div` |
| Positive-parameter GammaLog differential ladder (`p2:thm:gamma-tower`) | **Exact with the preceding row.** `ThueMorseGammaTowerDifferential.lean` has no public definitions and exactly three theorems.  For every complex Mellin exponent and positive real damping parameter, differentiation under the integral shifts `s` to `s+1`; specialization gives `L_(r+1)'(a)=(r+1)L_r(a)` and its falling-factorial iteration for exactly `k≤r`.  Every theorem retains `0<a` and concerns the chosen `thueMorseGammaLog` coordinate.  No identity with a branch or the principal value of `Complex.log (thueMorseGammaTower r a)`, and no nonpositive-parameter differential law, is claimed. | `Fabius.hasDerivAt_mellin_mellinKernel_parameter`, `Fabius.hasDerivAt_thueMorseGammaLog_succ`, `Fabius.iteratedDeriv_thueMorseGammaLog` |
| Generic absolutely summable uniform-coordinate series (reusable foundation) | On the product probability space `[0,1]^ℕ`, coordinate projections have independent uniform laws.  For every norm-summable vector-weight sequence in a complete real normed space, the weighted series is pointwise summable, continuous, and measurable in a Borel codomain; it has a head--tail split for arbitrary weights, reflects to the total vector weight minus itself, and has norm at most the sum of the weight norms.  Nonnegative real weights give the interval bound `[0, ∑ w_n]`, hence `[0,1]` at unit mass.  Scalar naturality has both the canonical and compatibility names listed below; the named geometric specialization is recorded separately. | `Fabius.ProbabilityRepresentation.independent_uniform_coordinates`, `Fabius.ProbabilityRepresentation.coordinate_has_uniform_law`, `Fabius.ProbabilityRepresentation.weightedUniformSeries`, `Fabius.ProbabilityRepresentation.summable_weightedUniformSeries_terms`, `Fabius.ProbabilityRepresentation.continuous_weightedUniformSeries`, `Fabius.ProbabilityRepresentation.measurable_weightedUniformSeries`, `Fabius.ProbabilityRepresentation.weightedUniformSeries_smul_weights`, `Fabius.ProbabilityRepresentation.weightedUniformSeries_split`, `Fabius.ProbabilityRepresentation.weightedUniformSeries_reflect`, `Fabius.ProbabilityRepresentation.norm_weightedUniformSeries_le`, `Fabius.ProbabilityRepresentation.weightedUniformSeries_mem_Icc`, `Fabius.ProbabilityRepresentation.weightedUniformSeries_mem_unitInterval`, `Fabius.ProbabilityRepresentation.weightedCoordinateSum_eq_weightedUniformSeries` |
| Generic weighted-uniform distribution laws and exact barycenters (reusable foundation) | The law `μ_w = (S_w)_* P` is total even when the series map is not known measurable.  A measurable tail statistic has the exact product joint law with the head coordinate.  In a complete Borel real normed space, norm-summable weights make `μ_w` a probability measure, give arbitrary-weight head--tail splitting and central reflection, and have exact barycenter `∫ x, x ∂μ_w = (1 / 2) • ∑' n, w n`.  The barycenter theorem uses no sign, order, independence expansion, or termwise integration hypothesis.  An independently chosen summable tail sequence `v` is allowed; only the later parent-law split uses `v n = w (n+1)`.  The dyadic `weightedSumDistribution` is literally the generic law at `w n = 1 / 2 / 2^n`, and its topological support is exactly `[0,1]`; its almost-everywhere unit-interval support and restriction invariance now live with the law in `ProbabilityRepresentation.lean`.  Nonnegative real weights also give pointwise and almost-sure carried-interval laws and restriction invariance.  For real laws, adding a nonzero scaled uniform coordinate to an arbitrary independent s-finite tail measure gives an absolutely continuous image.  The head-coordinate weighted theorem assumes `w 0 ≠ 0`; the sharp theorem assumes only norm summability and `∃ n, w n ≠ 0`, and the same hypotheses yield `NullSingletonClass`.  The compatibility layer provides alternative head--tail/probability names, reversed-orientation scalar law naturality, and unit-mass carried-interval corollaries; `WeightedUniformDistribution.lean` has exactly eleven public theorems and no public definitions.  The separate exact-support tranche is recorded in the next row. | `Fabius.ProbabilityRepresentation.uniformProduct_map_head_tail_comp`, `Fabius.ProbabilityRepresentation.weightedUniformDistribution`, `Fabius.ProbabilityRepresentation.isProbabilityMeasure_weightedUniformDistribution`, `Fabius.ProbabilityRepresentation.uniformProduct_map_head_tailWeightedUniformSeries`, `Fabius.ProbabilityRepresentation.weightedUniformDistribution_split`, `Fabius.ProbabilityRepresentation.weightedUniformDistribution_reflection`, `Fabius.ProbabilityRepresentation.ae_weightedUniformDistribution_mem_Icc`, `Fabius.ProbabilityRepresentation.weightedUniformDistribution_restrict_Icc`, `Fabius.ProbabilityRepresentation.weightedUniformDistribution_Icc`, `Fabius.ProbabilityRepresentation.weightedUniformDistribution_compl_Icc`, `Fabius.ProbabilityRepresentation.weightedSumDistribution_eq_weightedUniformDistribution`, `Fabius.ProbabilityRepresentation.ae_weightedSumDistribution_mem_Icc`, `Fabius.ProbabilityRepresentation.weightedSumDistribution_restrict_Icc`, `Fabius.ProbabilityRepresentation.weightedSumDistribution_support_eq_Icc`, `Fabius.ProbabilityRepresentation.uniformProduct_map_head_tail_function`, `Fabius.ProbabilityRepresentation.weightedUniformDistribution_isProbabilityMeasure`, `Fabius.ProbabilityRepresentation.integral_id_weightedUniformDistribution`, `Fabius.ProbabilityRepresentation.weightedUniformDistribution_smul_weights`, `Fabius.ProbabilityRepresentation.uniformProduct_map_head_tail_weightedUniformSeries`, `Fabius.ProbabilityRepresentation.weightedUniformDistribution_unitInterval`, `Fabius.ProbabilityRepresentation.weightedUniformDistribution_compl_unitInterval`, `Fabius.ProbabilityRepresentation.uniformScaledAdd_absolutelyContinuous`, `Fabius.ProbabilityRepresentation.weightedUniformDistribution_absolutelyContinuous_of_head_ne_zero`, `Fabius.ProbabilityRepresentation.weightedUniformDistribution_absolutelyContinuous`, `Fabius.ProbabilityRepresentation.weightedUniformDistribution_nullSingletonClass` |
| Arbitrary-space geometric-uniform realization (`thm:geometric-uniform-basic`) | **Exact on the theorem's stated `0<q<1` domain.**  `GeometricUniformRealization.lean` has one definition and exactly seventeen theorems.  For any supplied measurable space and measure carrying an `iIndepFun` family of `Icc 0 1`-valued coordinates with interval-volume marginal laws, its geometric series has the canonical geometric-uniform law, mean `1/2`, reflection symmetry, exact support and CDF, and the displayed independent-copy affine fixed-point identity.  Pointwise absolute convergence and splitting are included.  Law, mean, reflection, and general CDF transport hold under `|q|<1`; interval membership, support, and exterior CDF values use `0≤q<1`, and the CDF refinement integral uses `0<q<1`.  “Arbitrary-space” is a conditional law-transfer theorem, not existence of independent uniform coordinates on every probability space; the independent-copy form also retains `IsProbabilityMeasure P` and its explicit independence premise. | Definition: `Fabius.ProbabilityRepresentation.geometricUniformRealization`.  Theorems: `Fabius.ProbabilityRepresentation.geometricUniformRealization_eq_tsum`, `Fabius.ProbabilityRepresentation.geometricUniformRealization_split`, `Fabius.ProbabilityRepresentation.uniformProcess_hasLaw_uniformProduct`, `Fabius.ProbabilityRepresentation.weightedUniformSeries_hasLaw_of_iIndep_uniform`, `Fabius.ProbabilityRepresentation.geometricUniformRealization_hasLaw`, `Fabius.ProbabilityRepresentation.summable_norm_geometricUniformRealization_terms`, `Fabius.ProbabilityRepresentation.geometricUniformRealization_mem_Icc`, `Fabius.ProbabilityRepresentation.map_geometricUniformRealization_support_eq_Icc`, `Fabius.ProbabilityRepresentation.integral_geometricUniformRealization_eq_one_half`, `Fabius.ProbabilityRepresentation.one_sub_geometricUniformRealization_hasLaw`, `Fabius.ProbabilityRepresentation.geometricUniformRealization_identDistrib_one_sub`, `Fabius.ProbabilityRepresentation.affine_uniform_geometric_hasLaw`, `Fabius.ProbabilityRepresentation.geometricUniformRealization_identDistrib_affine`, `Fabius.ProbabilityRepresentation.measureReal_geometricUniformRealization_le_eq_cdf`, `Fabius.ProbabilityRepresentation.measureReal_geometricUniformRealization_le_eq_integral`, `Fabius.ProbabilityRepresentation.measureReal_geometricUniformRealization_le_eq_zero_of_nonpos`, `Fabius.ProbabilityRepresentation.measureReal_geometricUniformRealization_le_eq_one_of_one_le` |
| Exact support of weighted-uniform laws (reusable foundation) | An arbitrary product of open-positive probability measures is open-positive, and the uniform coordinate product is a canonical instance.  The pushforward of an open-positive measure through a continuous map has support equal to the closure of the map's range.  For norm-summable weights in a complete Borel real normed space, compactness of the coordinate cube removes that closure: the weighted law has support exactly the range of its series map.  For arbitrary signed real weights, connectedness and the extremal coordinate choices identify both range and support with `[ ∑ min (w_n) 0, ∑ max (w_n) 0 ]`; cancellation creates no gaps.  Constant coordinate paths give the nonnegative specialization `[0, ∑ w_n]`, including the degenerate zero-mass case, and unit-mass weights have support `[0,1]`. | `Fabius.ProbabilityRepresentation.isOpenPosMeasure_infinitePi`, `Fabius.ProbabilityRepresentation.uniformProduct_isOpenPosMeasure`, `Fabius.ProbabilityRepresentation.support_map_eq_closure_range_of_continuous`, `Fabius.ProbabilityRepresentation.weightedUniformSeries_constCoordinates`, `Fabius.ProbabilityRepresentation.weightedUniformDistribution_support_eq_range`, `Fabius.ProbabilityRepresentation.range_weightedUniformSeries_eq_Icc_min_max`, `Fabius.ProbabilityRepresentation.weightedUniformDistribution_support_eq_Icc_min_max`, `Fabius.ProbabilityRepresentation.range_weightedUniformSeries_eq_Icc`, `Fabius.ProbabilityRepresentation.weightedUniformDistribution_support_eq_Icc`, `Fabius.ProbabilityRepresentation.weightedUniformDistribution_support_eq_unitInterval` |
| Generic finite moment functionals and Hankel Gram forms | An arbitrary scalar sequence defines a polynomial moment functional and symmetric product pairing.  Their degree-bounded monomial-basis matrix is exactly the order-`n` Hankel matrix; leading-principal truncation and scalar base change are explicit, and over an integral domain the restricted pairing is nondegenerate exactly when its Hankel determinant is nonzero.  This generic finite algebra asserts no measure representation or positivity by itself. | `Fabius.momentFunctional`, `Fabius.momentFunctional_of_linearMap`, `Fabius.momentFunctional_map`, `Fabius.momentPairing`, `Fabius.momentHankelMatrix`, `Fabius.momentHankelMatrix_succ_submatrix`, `Fabius.momentHankelDet`, `Fabius.map_momentHankelDet`, `Fabius.finiteMomentPairing_toMatrix`, `Fabius.finiteMomentPairing_nondegenerate_iff` |
| Generic finite Gram--Stieltjes polynomial construction | The adjugate construction gives a fraction-free polynomial whose lower pure-power pairings vanish, whose top pairing is the next Hankel determinant, and whose self-pairing is the product of consecutive determinants.  Over a field, a nonzero order-`n` determinant gives the normalized monic degree-`n` polynomial, arbitrary lower-degree orthogonality, uniqueness, and self-pairing `Δ_(n+1) / Δ_n`. | `Fabius.gramStieltjesNumerator`, `Fabius.momentPairing_gramStieltjesNumerator_eq_coeff_mul_det`, `Fabius.momentPairing_gramStieltjesNumerator_self`, `Fabius.gramStieltjesPolynomial`, `Fabius.gramStieltjesPolynomial_isMonicOfDegree`, `Fabius.momentPairing_gramStieltjesPolynomial_eq_zero`, `Fabius.eq_gramStieltjesPolynomial_of_isMonicOfDegree_of_orthogonal`, `Fabius.momentPairing_gramStieltjesPolynomial_self` |
| Generic finite Jacobi recurrence | Over a field and under a nonzero order-`n` minor, the normalized Gram--Stieltjes self-pairing is the consecutive determinant quotient `h_n = Δ_(n+1) / Δ_n`.  The diagonal coefficient is `⟨X P_n,P_n⟩/h_n`, and the total-field subdiagonal coefficient is `h_(n+1)/h_n = Δ_(n+2)Δ_n/Δ_(n+1)^2`.  A nonzero first minor gives `X P_0 = P_1 + α_0 P_0`, while three consecutive nonzero minors give every higher exact monic three-term recurrence.  This is finite, measure-free algebra and asserts no positivity, root, quadrature, continued-fraction, or convergence theorem. | `Fabius.momentPairing_X_mul_left`, `Fabius.gramStieltjesNorm`, `Fabius.momentPairing_gramStieltjesPolynomial_self_eq_norm`, `Fabius.gramStieltjesJacobiDiagonal`, `Fabius.gramStieltjesJacobiSubdiagonal`, `Fabius.gramStieltjesJacobiSubdiagonal_eq_det_ratio`, `Fabius.gramStieltjesPolynomial_three_term_zero`, `Fabius.gramStieltjesPolynomial_three_term` |
| Polynomial-basis moment Gram determinants | `PolynomialMomentGramDeterminant.lean` has exactly two public definitions and seven public theorems.  For any coherent polynomial family with `natDegree (p k) ≤ k`, let `C` contain its monomial coefficients, `H` be the monomial Hankel matrix, and `G` its moment Gram matrix.  Direct expansion of the bilinear pairing proves `G = Cᵀ H C`.  The degree bound makes `C` upper triangular, so determinant multiplicativity gives `det G = (det C)^2 det H = (∏ k, coeff (p k) k)^2 det H`; nonzero diagonal coefficients then transport the existing zero-based Jacobi subdiagonal to a Gram-determinant cross-ratio.  This last Lean equality assumes no Hankel nonvanishing because field division is total: if the middle Hankel determinant is zero, both cross-ratios reduce to zero, which is not a genuine nonsingular Jacobi recurrence.  The coefficient matrix and entry formula are semiring-level; the Gram matrix, entry formula, and congruence are commutative-semiring-level; determinant identities are commutative-ring-level; and the quotient identity is field-level.  This generic module alone makes no measure, positivity, orthogonality, rational-entry, or Wigner-datum claim.  The downstream Gaunt and closed-form leaves supply the finite rational entry formulas and the total squared zero-row integer-index datum.  Signed `3j` phase, half-integer or nonzero magnetic indices, orthogonality, recoupling, Christoffel reconstruction, and infinite-Jacobi claims remain open. | Definitions: `Fabius.polynomialCoefficientMatrix`, `Fabius.polynomialMomentGramMatrix`.  Theorems: `Fabius.polynomialCoefficientMatrix_apply`, `Fabius.polynomialMomentGramMatrix_apply`, `Fabius.polynomialMomentGramMatrix_eq_transpose_mul_hankel_mul`, `Fabius.polynomialMomentGramMatrix_det_eq_coefficient_det_sq_mul`, `Fabius.polynomialCoefficientMatrix_det_eq_prod_coeff`, `Fabius.polynomialMomentGramMatrix_det_eq_prod_coeff_sq_mul`, `Fabius.gramStieltjesJacobiSubdiagonal_eq_polynomialMomentGramMatrix_det_ratio` |
| Scalar naturality of finite Gram--Stieltjes and Jacobi data | The moment pairing and fraction-free numerator commute with scalar ring homomorphisms at their semiring/ring boundaries; over fields, the normalized monic polynomial, norm quotient, diagonal coefficient, and subdiagonal coefficient commute with scalar base change as well.  This is the exhaustive zero-definition/six-theorem surface of `GramStieltjesNaturality.lean`; it supplies no measure, positivity, evaluator, root, quadrature, continued-fraction, or convergence result. | `Fabius.momentPairing_map`, `Fabius.map_gramStieltjesNumerator`, `Fabius.map_gramStieltjesPolynomial`, `Fabius.map_gramStieltjesNorm`, `Fabius.map_gramStieltjesJacobiDiagonal`, `Fabius.map_gramStieltjesJacobiSubdiagonal` |
| Exact rational native Jacobi system | The full rational raw-moment sequence defines, in every degree, a rational Hankel determinant, monic orthogonal polynomial, squared-norm quotient, and Jacobi subdiagonal.  Their real casts are the up-law objects for every bounded Fabius representative; the rational determinants, norms, and subdiagonals are positive; the polynomials are monic of exact degree and orthogonal for the rational moment pairing; symmetry kills the diagonal; and the determinant cross-ratio and centered three-term recurrence hold exactly over `ℚ`.  `rvachevJacobiSubdiagonalRat n` is zero-based and equals the conventional `β_(n+1)`.  These four definitions remain noncomputable finite-algebra objects, not native-code evaluators.  The downstream executable rational Legendre Gram and value modules now evaluate the report's displayed `H_4` and `β_4`; this module itself supplies their all-degree abstract rational objects and no evaluator.  Root/quadrature, Christoffel, continued-fraction, Padé, and asymptotic claims remain outside this layer. | Definitions: `Fabius.rvachevHankelDetRat`, `Fabius.rvachevOrthoPolynomialRat`, `Fabius.rvachevOrthoNormRat`, `Fabius.rvachevJacobiSubdiagonalRat`.  Theorems: `Fabius.upMoment_eq_rvachevRawMomentRat_cast`, `Fabius.rvachevHankelDetRat_cast`, `Fabius.rvachevHankelDetRat_pos`, `Fabius.rvachevOrthoPolynomialRat_cast`, `Fabius.rvachevOrthoPolynomialRat_isMonicOfDegree`, `Fabius.momentPairing_rvachevOrthoPolynomialRat_eq_zero`, `Fabius.rvachevOrthoNormRat_cast`, `Fabius.rvachevOrthoNormRat_pos`, `Fabius.gramStieltjesJacobiDiagonal_rvachevRawMomentRat_eq_zero`, `Fabius.rvachevJacobiSubdiagonalRat_cast`, `Fabius.rvachevJacobiSubdiagonalRat_pos`, `Fabius.rvachevJacobiSubdiagonalRat_eq_det_ratio`, `Fabius.rvachevOrthoPolynomialRat_three_term` |
| Executable rational ordinary Legendre polynomials (exhaustive companion API) | `LegendrePolynomialRational.lean` has exactly two public definitions and six public theorems.  `legendrePolynomialCoeffRat` is an executable finite coefficient sum; `legendrePolynomialRat` is its noncomputable polynomial wrapper.  The six unconditional natural-index theorems identify the wrapper's real cast and every coefficient, prove exact degree, evaluate and prove nonzero the top coefficient, and give the quotient of consecutive top coefficients.  The two private construction helpers are excluded. | Definitions: `Fabius.legendrePolynomialCoeffRat`, `Fabius.legendrePolynomialRat`.  Theorems: `Fabius.legendrePolynomialRat_cast`, `Fabius.coeff_legendrePolynomialRat`, `Fabius.natDegree_legendrePolynomialRat`, `Fabius.coeff_legendrePolynomialRat_self`, `Fabius.coeff_legendrePolynomialRat_self_ne_zero`, `Fabius.coeff_legendrePolynomialRat_self_div_succ` |
| Up-law Legendre Gram/Hankel determinant bridge | `FabiusLegendreHankelDeterminant.lean` has exactly two public definitions and seven public theorems.  For `D_n`, the determinant of the moment Gram matrix of `P_0,…,P_(n-1)`, and `L_j = 2^(-j) choose(2*j,j)`, the determinant identity `D_n = (∏ j<n, L_j^2) hankelDet(F,n)`, the empty-`0×0` convention `D_0 = 1`, the quotient `L_n/L_(n+1) = (n+1)/(2*n+1)`, and the real Gram formula `((n+1)/(2*n+1))^2 D_(n+2)D_n/D_(n+1)^2` all hold for arbitrary `F : BoundedFabius`.  The entry-as-integral identification, strict positivity, and bridge from the exact rational Rvachev subdiagonal require `IsFabius F`; positivity then turns the totalized real cross-ratio into a genuine nonsingular Jacobi formula.  Lean's index `n` is zero-based and denotes the report's `β_(n+1)`.  This module itself gives no Gaunt or Wigner-square entry formula or entry rationality by that route; the downstream Gaunt and closed-form modules below do.  Signed `3j` phase, half-integer or nonzero magnetic indices, recoupling, Christoffel reconstruction, and an infinite Jacobi product remain outside the development.  It is also distinct from `rvachevTranslateGram`, which pairs shifted-up atoms under unweighted interval integration. | Definitions: `Fabius.upLegendreGramMatrix`, `Fabius.upLegendreGramDet`.  Theorems: `Fabius.upLegendreGramMatrix_apply_eq_integral`, `Fabius.upLegendreGramDet_eq_prod_leadingCoeff_sq_mul_hankelDet`, `Fabius.upLegendreGramDet_zero`, `Fabius.upLegendreGramDet_pos`, `Fabius.coeff_legendrePolynomial_self_div_succ`, `Fabius.gramStieltjesJacobiSubdiagonal_upMoment_eq_upLegendreGramDet_ratio`, `Fabius.rvachevJacobiSubdiagonalRat_cast_eq_upLegendreGramDet_ratio` |
| Executable rational up-law Legendre Gram entries, matrices, and determinants | `FabiusLegendreRationalGram.lean` has exactly three public definitions and eleven public theorems.  Its entry is a bounded double sum of `legendrePolynomialCoeffRat` values and executable rational Rvachev moments; the finite matrix and determinant agree with the abstract polynomial-moment Gram constructions.  For `F : BoundedFabius` with `IsFabius F`, casting sends the rational entry, matrix, and determinant to their real up-law counterparts.  Over `ℚ`, the determinant is the rational Hankel determinant times the squared-leading-coefficient product, is `1` at order zero and strictly positive at every order; determinant ratios give the exact rational squared norm and the zero-based Jacobi subdiagonal `β_(n+1)`, the latter with prefactor `((n+1)/(2*n+1))^2`.  This is the executable rational coefficient/entry/matrix/determinant and cast-bridge layer.  The downstream Gaunt modules supply its finite Gaunt formulas, and `FabiusLegendreGauntClosedForm.lean` supplies rational entry/matrix and real matrix sums against the total squared zero-row integer datum.  This module itself makes no such identification.  Signed `3j` phase, half-integer or nonzero magnetic indices, recoupling, Christoffel reconstruction, root or quadrature theory, an infinite Jacobi product/continued fraction, and asymptotics remain outside the development. | Definitions: `Fabius.rvachevLegendreGramEntryRat`, `Fabius.rvachevLegendreGramMatrixRat`, `Fabius.rvachevLegendreGramDetRat`.  Theorems: `Fabius.rvachevLegendreGramEntryRat_eq_momentPairing`, `Fabius.rvachevLegendreGramMatrixRat_apply`, `Fabius.rvachevLegendreGramMatrixRat_eq_polynomialMomentGramMatrix`, `Fabius.rvachevLegendreGramEntryRat_cast`, `Fabius.rvachevLegendreGramMatrixRat_cast`, `Fabius.rvachevLegendreGramDetRat_cast`, `Fabius.rvachevLegendreGramDetRat_eq_prod_leadingCoeff_sq_mul_rvachevHankelDetRat`, `Fabius.rvachevLegendreGramDetRat_zero`, `Fabius.rvachevLegendreGramDetRat_pos`, `Fabius.rvachevOrthoNormRat_eq_rvachevLegendreGramDetRat_ratio`, `Fabius.rvachevJacobiSubdiagonalRat_eq_rvachevLegendreGramDetRat_ratio` |
| Executable rational Legendre Gaunt coefficients and finite product linearization | `LegendreGaunt.lean` has exactly four public definitions and twelve public theorems.  For every `n : ℕ`, `legendreLebesgueMomentRat n` is `2/(n+1)` at even `n` and zero at odd `n`, and its real cast is `∫ x in (-1)..1, x^n`.  For all `i,j,k : ℕ`, `legendreGauntRat i j k` is the bounded triple coefficient–moment sum for `P_i P_j P_k`; `legendreGaunt i j k` is its real integral on `[-1,1]`; and `legendreProductLinearizationCoeffRat i j k = ((2*k+1)/2) * legendreGauntRat i j k`.  The rational sum equals the moment functional and casts to the integral, is invariant under the displayed first-two and last-two swaps, and gives the exact real and rational finite identities `P_i P_j = ∑ k ∈ range(i+j+1), legendreProductLinearizationCoeffRat i j k • P_k`.  Only the three zero results have premises: `Odd (i+j+k)`, `i+j<k`, or the triangle violation `i+j<k ∨ i+k<j ∨ j+k<i`; all other public statements are unconditional at arbitrary natural indices.  This module itself proves only necessary support zeros and makes no converse-support, Wigner-square, factorial, positivity, or asymptotic claim; the next closed-form leaf proves the total squared zero-row integer-index case and sharpens support to an equivalence. | Definitions: `Fabius.legendreLebesgueMomentRat`, `Fabius.legendreGauntRat`, `Fabius.legendreGaunt`, `Fabius.legendreProductLinearizationCoeffRat`.  Theorems: `Fabius.legendreLebesgueMomentRat_even`, `Fabius.legendreLebesgueMomentRat_odd`, `Fabius.legendreLebesgueMomentRat_cast`, `Fabius.legendreGauntRat_eq_momentFunctional`, `Fabius.legendreGauntRat_cast`, `Fabius.legendreGauntRat_swap_left`, `Fabius.legendreGauntRat_swap_right`, `Fabius.legendrePolynomial_mul_eq_sum_gaunt`, `Fabius.legendrePolynomialRat_mul_eq_sum_gaunt`, `Fabius.legendreGauntRat_eq_zero_of_odd_sum`, `Fabius.legendreGauntRat_eq_zero_of_add_lt`, `Fabius.legendreGauntRat_eq_zero_of_triangle_violation` |
| Total zero-row Wigner-square Gaunt closed form, sharp support, positivity, and product coefficients | `LegendreGauntClosedForm.lean` has exactly two public definitions and twenty-five public theorems.  `legendreGauntAdmissible i j k` is even total degree plus the three weak triangle inequalities, equivalently the existence of natural pairwise-sum coordinates.  `legendreWignerThreeJZeroSqRat` is the total rational square datum: zero off that support and given there by the symmetric central-binomial quotient, with both pairwise-coordinate and half-sum factorial forms.  Boundary theorems cover `k=i+j` and a zero first index.  At arbitrary natural indices the rational and real Gaunt coefficients are twice the datum, their positivity is equivalent to admissibility, their zero locus is its complement, and they are nonnegative.  The coefficient of `P_k` in `P_i P_j` is `(2*k+1)` times the datum and has the same sharp positivity support.  This is only an integer-index zero-magnetic-row square datum defined internally by its rational formula: it makes no signed-symbol or phase choice and supplies no half-integer indices, nonzero magnetic indices, general `3j`/`6j`/`9j` symbols, Wigner orthogonality, or recoupling identity. | Definitions: `Fabius.legendreGauntAdmissible`, `Fabius.legendreWignerThreeJZeroSqRat`.  Theorems: `Fabius.legendreGauntAdmissible_iff_exists_pairwise_add`, `Fabius.legendreGauntAdmissible_pairwise_add`, `Fabius.legendreWignerThreeJZeroSqRat_pairwise_add`, `Fabius.legendreWignerThreeJZeroSqRat_pairwise_add_factorial`, `Fabius.legendreWignerThreeJZeroSqRat_eq_factorial_of_halfSum`, `Fabius.legendreWignerThreeJZeroSqRat_eq_zero_of_not_admissible`, `Fabius.legendreGauntRat_add_boundary`, `Fabius.legendreGauntRat_add_boundary_eq_two_mul_wignerThreeJZeroSqRat`, `Fabius.legendreGauntRat_zero_left`, `Fabius.legendreGauntRat_zero_left_eq_two_mul_wignerThreeJZeroSqRat`, `Fabius.legendreGauntRat_pairwise_add_eq_two_mul_wignerThreeJZeroSqRat`, `Fabius.legendreGauntRat_eq_zero_of_not_admissible`, `Fabius.legendreGauntRat_eq_two_mul_wignerThreeJZeroSqRat`, `Fabius.legendreGaunt_eq_two_mul_wignerThreeJZeroSqRat`, `Fabius.legendreWignerThreeJZeroSqRat_pos_iff_admissible`, `Fabius.legendreWignerThreeJZeroSqRat_nonneg`, `Fabius.legendreWignerThreeJZeroSqRat_eq_zero_iff_not_admissible`, `Fabius.legendreGauntRat_pos_iff_admissible`, `Fabius.legendreGauntRat_eq_zero_iff_not_admissible`, `Fabius.legendreGaunt_pos_iff_admissible`, `Fabius.legendreGaunt_eq_zero_iff_not_admissible`, `Fabius.legendreGauntRat_nonneg`, `Fabius.legendreGaunt_nonneg`, `Fabius.legendreProductLinearizationCoeffRat_eq_mul_wignerThreeJZeroSqRat`, `Fabius.legendreProductLinearizationCoeffRat_pos_iff_admissible` |
| Finite Gaunt sums for rational and real up-law Legendre Gram entries | `FabiusLegendreGaunt.lean` has exactly one public definition and eight public theorems.  `canonicalRvachevFullLegendreCoefficientRat k` is `canonicalRvachevLegendreCoefficientRat (k/2)` when `2 ∣ k` and zero otherwise, so its even restriction is the canonical sequence and its odd entries vanish.  Unconditionally in `k`, it is `((2*k+1)/2) * momentFunctional rvachevRawMomentRat (legendrePolynomialRat k)`.  For all natural `i,j`, `rvachevLegendreGramEntryRat i j` equals both the full sum `∑ k ∈ range(i+j+1), canonicalRvachevFullLegendreCoefficientRat k * legendreGauntRat i j k` and the even reindexing `∑ r ∈ range((i+j)/2+1), canonicalRvachevLegendreCoefficientRat r * legendreGauntRat i j (2*r)`; the latter is also the exact entry formula for `i j : Fin n`.  Only the cast theorem and real matrix formula require `F : BoundedFabius` with `hF : IsFabius F`; they identify the full rational coefficient with `rvachevFullLegendreCoefficient F k` and the real matrix entry with the same finite even sum using `rvachevLegendreCoefficient F r` and `legendreGaunt`.  This module itself proves finite polynomial and Gaunt-sum identities; the next leaf substitutes the total squared zero-row integer datum.  It chooses no signed `3j` phase and treats no half-integer/nonzero magnetic index, orthogonality, recoupling, infinite Legendre-series interchange, Christoffel reconstruction, named values for the displayed `G_3` entries, or new asymptotic result. | Definition: `Fabius.canonicalRvachevFullLegendreCoefficientRat`.  Theorems: `Fabius.canonicalRvachevFullLegendreCoefficientRat_even`, `Fabius.canonicalRvachevFullLegendreCoefficientRat_odd`, `Fabius.canonicalRvachevFullLegendreCoefficientRat_cast`, `Fabius.canonicalRvachevFullLegendreCoefficientRat_eq_normalized_moment`, `Fabius.rvachevLegendreGramEntryRat_eq_sum_full_gaunt`, `Fabius.rvachevLegendreGramEntryRat_eq_sum_gaunt`, `Fabius.rvachevLegendreGramMatrixRat_apply_eq_sum_gaunt`, `Fabius.upLegendreGramMatrix_apply_eq_sum_gaunt` |
| Finite zero-row Wigner-square sums for rational Rvachev and real up-law Legendre Gram entries | `FabiusLegendreGauntClosedForm.lean` has zero public definitions and exactly three public theorems.  Every executable rational Gram entry is twice the finite sum of `canonicalRvachevLegendreCoefficientRat r` times `legendreWignerThreeJZeroSqRat i j (2*r)`, and the finite rational matrix theorem is the same identity at `Fin n` indices.  For `F : BoundedFabius` with `IsFabius F`, the real up-law matrix entry is twice the corresponding finite sum with `rvachevLegendreCoefficient F r` and the real cast of the datum.  These are finite rational entry/matrix and real matrix sums against the total squared zero-row integer datum; they choose no signed `3j` phase and treat no half-integer or nonzero magnetic index, general Wigner-symbol API, orthogonality, or recoupling identity. | `Fabius.rvachevLegendreGramEntryRat_eq_two_mul_sum_wignerThreeJZeroSqRat`, `Fabius.rvachevLegendreGramMatrixRat_apply_eq_two_mul_sum_wignerThreeJZeroSqRat`, `Fabius.upLegendreGramMatrix_apply_eq_two_mul_sum_wignerThreeJZeroSqRat` |
| Exact low-order rational Legendre Gram and Jacobi values | `FabiusLegendreRationalGramValues.lean` has zero public definitions and exactly eleven public theorems.  `moment_four` gives the raw eighth moment `132809/32531625`; `rvachevLegendreGramDetRat_one`, `rvachevLegendreGramDetRat_two`, `rvachevLegendreGramDetRat_three`, `rvachevLegendreGramDetRat_four`, and `rvachevLegendreGramDetRat_five` give determinant orders one through five as `1`, `1/9`, `8/2025`, `39616/602791875`, and `16544275456/27453718922765625`.  `rvachevOrthoNormRat_four` proves `H_4 = 26727424/55791736875`, and the zero-based `rvachevJacobiSubdiagonalRat_three` proves the conventional `β_4 = 835232/4640643`.  The first eight results are unconditional rational computations.  `hankelRatio_four`, `integral_sq_upOrthoPolynomial_four`, and `hankelRatio_four_div_three` transport the norm, its squared integral, and the consecutive real Hankel-ratio quotient only for `F : BoundedFabius` with `IsFabius F`.  The separate Gaunt and closed-form modules close the finite entry and total zero-row-square expansions, but this values leaf supplies no named evaluation of the displayed `G_3` entries.  Signed `3j` phase, half-integer or nonzero magnetic indices, recoupling, Christoffel reconstruction, roots or quadrature, infinite Jacobi products/continued fractions, and asymptotics remain open. | `Fabius.moment_four`, `Fabius.rvachevLegendreGramDetRat_one`, `Fabius.rvachevLegendreGramDetRat_two`, `Fabius.rvachevLegendreGramDetRat_three`, `Fabius.rvachevLegendreGramDetRat_four`, `Fabius.rvachevLegendreGramDetRat_five`, `Fabius.rvachevOrthoNormRat_four`, `Fabius.rvachevJacobiSubdiagonalRat_three`, `Fabius.hankelRatio_four`, `Fabius.integral_sq_upOrthoPolynomial_four`, `Fabius.hankelRatio_four_div_three` |
| Generic/up-measure Gram--Stieltjes comparison | The polynomial functional of the up moments is exactly integration against the canonical up measure.  Consequently the generic and measure-specific Hankel matrices and determinants agree, the bordered-cofactor polynomial equals the last-adjugate-column numerator, and their monic normalizations are identical.  The generic norm is the up-specific Hankel ratio, symmetry kills the generic Jacobi diagonal, and the generic subdiagonal is the quotient of consecutive up-specific ratios. | `Fabius.momentFunctional_upMoment_eq_integral`, `Fabius.momentPairing_upMoment_eq_integral`, `Fabius.momentHankel_eq_momentHankelMatrix`, `Fabius.hankelDet_eq_momentHankelDet`, `Fabius.hankelOrthoPolynomial_eq_gramStieltjesNumerator`, `Fabius.upOrthoPolynomial_eq_gramStieltjesPolynomial`, `Fabius.hankelRatio_eq_gramStieltjesNorm`, `Fabius.gramStieltjesJacobiDiagonal_upMoment_eq_zero`, `Fabius.gramStieltjesJacobiSubdiagonal_upMoment_eq` |
| Fabius-measure Hankel positivity and finite orthogonal-polynomial recurrence | The measure-specific layer identifies the up moments, proves strict positive definiteness and positive Hankel determinants and ratios, constructs the unique monic orthogonal polynomials, proves parity and the symmetric three-term recurrence, and computes the first exact polynomials and Jacobi data.  Root location and simplicity, Gaussian/Lobatto quadrature, finite or infinite Jacobi/Stieltjes-fraction identification, and convergence are not supplied by this finite layer. | `Fabius.upMoment`, `Fabius.momentHankel_posDef`, `Fabius.hankelDet_pos`, `Fabius.hankelRatio_pos`, `Fabius.upOrthoPolynomial`, `Fabius.upOrthoPolynomial_monic`, `Fabius.integral_upOrthoPolynomial_sq`, `Fabius.eq_upOrthoPolynomial_of_monic_of_orthogonal`, `Fabius.upOrthoPolynomial_comp_neg_X`, `Fabius.upOrthoPolynomial_three_term`, `Fabius.upOrthoPolynomial_two`, `Fabius.upOrthoPolynomial_three`, `Fabius.upOrthoPolynomial_four` |
| Generic finite-measure Cauchy calculus and uniform affine fixed points | The oriented transform, its unnormalized kernel powers, the complement of complexified support, affine pushforward laws, openness, first derivative, and holomorphy are packaged for arbitrary finite real measures.  If a finite measure is carried by `K` and is fixed by `(u,x) ↦ c+a u+b x`, with both scales nonzero and `K` invariant, then named theorems give its transform DDE and every positive adjacent-power recurrence off the complexification of `K`.  No probability normalization, compactness, closedness, measurability, or nonemptiness of `K` is required; affine naturality and the integral definitions are total, and negative nonzero scales are allowed.  This row is the exhaustive public inventory of `MeasureCauchyTransform.lean` (three definitions and nine theorems). | `Fabius.measureCauchyDomain`, `Fabius.measureCauchyTransform`, `Fabius.measureCauchyPower`, `Fabius.measureCauchyTransform_apply`, `Fabius.measureCauchyPower_one`, `Fabius.measureCauchyPower_map_affine`, `Fabius.measureCauchyTransform_map_affine`, `Fabius.isOpen_measureCauchyDomain`, `Fabius.hasDerivAt_measureCauchyTransform`, `Fabius.analyticOn_measureCauchyTransform`, `Fabius.measureCauchyPower_succ_of_uniformAffineFixedPoint`, `Fabius.hasDerivAt_measureCauchyTransform_of_uniformAffineFixedPoint` |
| Generic centered moment/Laurent calculus for bounded finite real measures | The field-generic finite Cauchy-kernel identity is followed by an arbitrary-center moment and Laurent API over every `RCLike 𝕜`.  For a finite real measure, arbitrary `c z : 𝕜`, `0 ≤ R`, almost-everywhere support `‖(x : 𝕜) - c‖ ≤ R`, and `R < ‖z-c‖`, the coefficients have the positive report sign `measureCauchyMoment μ c k / (z-c)^(k+1)`, the zeroth coefficient is `(μ.real Set.univ : 𝕜)`, and the exact finite remainder has norm at most `μ.real Set.univ * (‖z-c‖-R)⁻¹ * (R/‖z-c‖)^N`.  The series is supplied in `Summable`, `tsum`, and `HasSum` forms, and four complex wrappers identify the direct integral with the named oriented `measureCauchyTransform`.  No probability normalization or topological-support theorem is used.  This row is the exhaustive public inventory of `MeasureCauchyMomentLaurent.lean` (one definition and fifteen theorems). | `Fabius.measureCauchyMoment`, `Fabius.inv_sub_eq_sum_range_add`, `Fabius.measureCauchyMoment_zero`, `Fabius.measurable_inv_sub_rclike`, `Fabius.integrable_centered_pow_div_sub_of_ae_norm_sub_le`, `Fabius.integrable_inv_sub_of_ae_norm_sub_le`, `Fabius.norm_measureCauchyMoment_le`, `Fabius.integral_inv_sub_eq_sum_range_measureCauchyMoment_add`, `Fabius.norm_integral_inv_sub_sub_sum_range_measureCauchyMoment_le`, `Fabius.summable_measureCauchyMoment_laurent`, `Fabius.integral_inv_sub_eq_tsum_measureCauchyMoment`, `Fabius.hasSum_measureCauchyMoment_laurent`, `Fabius.measureCauchyTransform_eq_sum_range_measureCauchyMoment_add`, `Fabius.norm_measureCauchyTransform_sub_sum_range_measureCauchyMoment_le`, `Fabius.measureCauchyTransform_eq_tsum_measureCauchyMoment`, `Fabius.hasSum_measureCauchyTransform_measureCauchyMoment_laurent` |
| Geometric-uniform Cauchy--Stieltjes hierarchy | For every real `q`, total definitions package the exact-support domain, oriented transform, and every unnormalized kernel power.  For `|q|<1` the transform is holomorphic off the exact topological support.  If also `q≠0`, named theorems give `S_q'(z)=((1-q)q)⁻¹(S_q(z/q)-S_q((z-(1-q))/q))` and every positive adjacent-power recurrence.  Negative ratios are included through the exact series-range carrier; the divided formulas deliberately exclude `q=0`.  This row is the exhaustive public inventory of `GeometricUniformCauchy.lean` (three definitions and six theorems). | `Fabius.geometricUniformStieltjesDomain`, `Fabius.geometricUniformStieltjesTransform`, `Fabius.geometricUniformStieltjesPower`, `Fabius.geometricUniformStieltjesTransform_apply`, `Fabius.geometricUniformStieltjesPower_one`, `Fabius.isOpen_geometricUniformStieltjesDomain`, `Fabius.analyticOn_geometricUniformStieltjesTransform`, `Fabius.hasDerivAt_geometricUniformStieltjesTransform_refinement`, `Fabius.geometricUniformStieltjesPower_succ` |
| Cauchy--Stieltjes calculus for the canonical Fabius--Rvachev laws | The report-oriented transforms use `(z-x)⁻¹`, hence are the negatives of Mathlib's `(x-z)⁻¹` resolvent transform.  The up-law and unit-interval transforms and power hierarchies have exact measure forms; the up transform has its density form; both transforms are holomorphic off `[-1,1]` and `[0,1]`; and their first derivatives are the negatives of the squared-kernel integrals.  The equality in law gives both transform orientations and the all-power bridge `S_n(z)=2^n R_n(2z-1)`.  Named theorems give the unit-interval DDE `S'(z)=4(S(2z)-S(2z-1))` and both adjacent-order recurrences on their complex slit domains.  Integral definitions and affine bridges are total under Lean's Bochner-integral convention; analytic statements use the named domains.  This row is the exhaustive public inventory of `CauchyTransform.lean` (six definitions and seventeen theorems). | `Fabius.rvachevCauchyDomain`, `Fabius.fabiusStieltjesDomain`, `Fabius.rvachevCauchyTransform`, `Fabius.fabiusStieltjesTransform`, `Fabius.rvachevCauchyPower`, `Fabius.fabiusStieltjesPower`, `Fabius.rvachevCauchyTransform_apply`, `Fabius.fabiusStieltjesTransform_apply`, `Fabius.rvachevCauchyPower_one`, `Fabius.fabiusStieltjesPower_one`, `Fabius.rvachevCauchyTransform_eq_integral_rvachevUp`, `Fabius.isOpen_rvachevCauchyDomain`, `Fabius.isOpen_fabiusStieltjesDomain`, `Fabius.hasDerivAt_rvachevCauchyTransform`, `Fabius.hasDerivAt_fabiusStieltjesTransform`, `Fabius.analyticOn_rvachevCauchyTransform`, `Fabius.analyticOn_fabiusStieltjesTransform`, `Fabius.fabiusStieltjesTransform_eq_two_mul_rvachevCauchyTransform`, `Fabius.rvachevCauchyTransform_eq_inv_two_mul_fabiusStieltjesTransform`, `Fabius.fabiusStieltjesPower_eq_two_pow_mul_rvachevCauchyPower`, `Fabius.hasDerivAt_fabiusStieltjesTransform_refinement`, `Fabius.fabiusStieltjesPower_succ`, `Fabius.rvachevCauchyPower_succ` |
| Atom-exact compact-support Cauchy integration by parts | For an arbitrary finite real measure, ordered compact support almost everywhere, and a spectral parameter off its complexification, direct named CDF and survival theorems give the first-power formulas with closed rays and therefore retain atoms.  `CauchyHigherPowers.lean` gives both orientations for every positive resolvent power.  The dyadic wrappers are `S(z)=(z-1)⁻¹-∫₀¹F(t)(z-t)⁻²dt` and `S(z)=z⁻¹+∫₀¹Up(t)(z-t)⁻²dt`.  These declarations do not by themselves identify the all-order Thue--Morse derivative orbit with one named higher-kernel formula. | `Fabius.integral_inv_sub_eq_mass_smul_sub_intervalIntegral_measureReal_Iic`, `Fabius.integral_inv_sub_eq_sub_intervalIntegral_cdf`, `Fabius.fabiusStieltjesTransform_eq_inv_sub_one_sub_intervalIntegral_fabiusReal`, `Fabius.intervalIntegral_inv_sub_sq`, `Fabius.integral_inv_sub_eq_mass_smul_add_intervalIntegral_measureReal_Ioi`, `Fabius.fabiusStieltjesTransform_eq_inv_add_intervalIntegral_rvachevUp`, `Fabius.hasDerivAt_pow_inv_sub`, `Fabius.intervalIntegral_pow_inv_sub`, `Fabius.integral_pow_inv_sub_eq_mass_smul_sub_intervalIntegral_measureReal_Iic`, `Fabius.integral_pow_inv_sub_eq_mass_smul_add_intervalIntegral_measureReal_Ioi` |
| Rvachev Cauchy renormalization and all-order Thue--Morse orbit | Both branches `z ↦ 2z+1` and `z ↦ 2z-1` preserve `rvachevCauchyDomain`.  For every bounded Fabius solution and every `z` in that domain, `R'(z)=2(R(2z+1)-R(2z-1))` holds in both `HasDerivAt` and derivative-value forms.  For every natural `n`, `iteratedDeriv n R z` is `2^((n+1).choose 2)` times the exact `2^n`-term Thue--Morse affine orbit. | `Fabius.mapsTo_rvachevCauchyDomain_two_mul_add_one`, `Fabius.mapsTo_rvachevCauchyDomain_two_mul_sub_one`, `Fabius.hasDerivAt_rvachevCauchyTransform_affineDifference`, `Fabius.deriv_rvachevCauchyTransform`, `Fabius.iteratedDeriv_rvachevCauchyTransform_eq_thueMorse_sum` |
| Real logarithmic and generalized-order resolvent hierarchy | Direct named theorems integrate the uniform digit to a real logarithm and give the Rvachev logarithmic fixed-point representation for real `z>1`; give the adjacent positive-integer resolvent hierarchy for real `z>1`; and lower every real order `α>1` by one at real `z>1`.  The complex-slit integer recurrences are separately exported as the direct named theorems `fabiusStieltjesPower_succ` and `rvachevCauchyPower_succ`; they strengthen the real integer domain but do not provide a complex logarithm/branch theorem or complex order. | `Fabius.integral_inv_sub_uniform_half`, `Fabius.integral_inv_sub_eq_integral_log`, `Fabius.integrable_pow_inv_sub_rvachevMeasure`, `Fabius.integral_pow_inv_sub_uniform_half`, `Fabius.integral_pow_inv_sub_succ`, `Fabius.integrable_rpow_inv_sub_rvachevMeasure`, `Fabius.integral_rpow_inv_sub_uniform_half`, `Fabius.integral_rpow_inv_sub_hierarchy` |
| Up-measure Laurent, Herglotz, and integrated inversion layers | The radius-one, center-zero up-measure specialization is connected to the generic centered Laurent calculus by an almost-everywhere unit-ball theorem and exact identification of the generic moments with `upMoment`.  It retains real exterior and complex `‖z‖>1` finite expansions, geometric bounds, and convergent `tsum`/`HasSum` forms under their established report-facing names.  Off the real axis the named transform respects conjugation, has the correct strict half-plane sign and quantitative bound, and has exact finite-height Poisson and conjugate-Poisson representations.  The Poisson layer culminates in integrated interval Stieltjes--Perron inversion.  These results do not assert pointwise or nontangential Sokhotski--Plemelj limits or a principal-value Hilbert-transform formula; a Laplace/Fourier transform representation is also not supplied by this tranche. | `Fabius.ae_norm_sub_zero_le_one_rvachevMeasure`, `Fabius.measureCauchyMoment_rvachevMeasure_zero`, `Fabius.integral_inv_sub_eq_sum_upMoment_add`, `Fabius.abs_integral_inv_sub_sub_sum_le`, `Fabius.integral_inv_sub_eq_tsum_upMoment`, `Fabius.hasSum_upMoment_laurent`, `Fabius.integral_inv_sub_complex_eq_sum_upMoment_add`, `Fabius.norm_integral_inv_sub_complex_sub_sum_le`, `Fabius.integral_inv_sub_complex_eq_tsum_upMoment`, `Fabius.hasSum_upMoment_laurent_complex`, `Fabius.rvachevCauchyTransform_eq_tsum_upMoment`, `Fabius.rvachevCauchyTransform_conj`, `Fabius.im_rvachevCauchyTransform_neg`, `Fabius.im_rvachevCauchyTransform_pos`, `Fabius.im_rvachevCauchyTransform_eq`, `Fabius.re_rvachevCauchyTransform_eq`, `Fabius.inv_pi_mul_neg_im_rvachevCauchyTransform_mem_Icc`, `Fabius.stieltjesPerron_inversion` |
| Initial exact Jacobi data | Additional exact up-law moments, the third and fourth Hankel determinants, the next Jacobi ratios, and the monic degree-three and degree-four orthogonal polynomials are direct named theorems, including pointwise and squared-norm corollaries.  The separate rational-Gram-values row above now supplies the next displayed `H_4` and `β_4`; neither finite tranche supplies the complete J-fraction/Padé convergence package or asymptotic Jacobi theory. | `Fabius.moment_two`, `Fabius.moment_three`, `Fabius.hankelDet_three`, `Fabius.hankelRatio_two`, `Fabius.integral_sq_upOrthoPolynomial_two`, `Fabius.upOrthoPolynomial_three`, `Fabius.upOrthoPolynomial_three_eval`, `Fabius.hankelRatio_three`, `Fabius.integral_sq_upOrthoPolynomial_three`, `Fabius.hankelDet_four`, `Fabius.upOrthoPolynomial_four`, `Fabius.upOrthoPolynomial_four_eval` |
| Generic naturality and operator-affine recursion (reusable foundation) | An almost-everywhere head--tail recursion `f ω = Φ (ω 0, g (tail ω))`, with measurable `g` and `Φ`, induces the product-law recursion without a separate measurability hypothesis on `f`; a pointwise wrapper is provided.  Continuous linear maps commute with norm-summable series when only the source is complete and with their Borel laws without requiring a complete target.  Pointwise scalar naturality needs neither completeness nor summability; its law form retains source completeness, Borel structure, and norm summability.  If `w (n+1) = A (w n)`, the shifted law is `A_* μ_w`, and the series and law obey the exact operator-affine recursion.  The scalar case `A = c • id` allows arbitrary real `c`.  These eleven declarations are generic; the named geometric law below discharges their hypotheses for `(1-q)q^n`. | `Fabius.ProbabilityRepresentation.uniformProduct_map_of_ae_head_tail_recursion`, `Fabius.ProbabilityRepresentation.uniformProduct_map_of_head_tail_recursion`, `Fabius.ProbabilityRepresentation.weightedUniformSeries_map`, `Fabius.ProbabilityRepresentation.weightedUniformSeries_smul`, `Fabius.ProbabilityRepresentation.weightedUniformDistribution_map`, `Fabius.ProbabilityRepresentation.weightedUniformDistribution_smul`, `Fabius.ProbabilityRepresentation.weightedUniformDistribution_map_eq_shift`, `Fabius.ProbabilityRepresentation.weightedUniformSeries_split_of_shift_eq_map`, `Fabius.ProbabilityRepresentation.weightedUniformDistribution_split_of_shift_eq_map`, `Fabius.ProbabilityRepresentation.weightedUniformSeries_geometric_split`, `Fabius.ProbabilityRepresentation.weightedUniformDistribution_geometric_split` |
| Contractive affine independent-copy laws (reusable foundation) | At compiled checkpoint `d312c0603`, `AffineIndependentCopy.lean` defines the convolution law of `digit D + q • X` for an arbitrary measurable digit space and a second-countable Borel real inner-product target.  It proves probability preservation, equality with the direct product pushforward, one-step characteristic-function factorization, and the fixed-point recurrence with its finite iterate.  For a measurable digit and probability digit/candidate laws, `|q| < 1` makes the fixed point unique; this includes `q=0` and negative `q` and assumes no support, density, or moments.  Completeness of the target occurs only in the characteristic-recurrence uniqueness theorem and its two fixed-point wrappers. | `ProbabilityTheory.affineIndependentCopyLaw`, `ProbabilityTheory.affineIndependentCopyLaw_isProbabilityMeasure`, `ProbabilityTheory.affineIndependentCopyLaw_eq_map_prod`, `ProbabilityTheory.charFun_affineIndependentCopyLaw`, `ProbabilityTheory.charFun_eq_mul_charFun_of_affineIndependentCopy_fixedPoint`, `ProbabilityTheory.charFun_iterate_of_affineIndependentCopy_fixedPoint`, `ProbabilityTheory.eq_of_charFun_affine_recurrence`, `ProbabilityTheory.affineIndependentCopyLaw_fixedPoint_unique`, `ProbabilityTheory.affineIndependentCopy_map_fixedPoint_unique` |
| Named geometric uniform-coordinate law | `GeometricUniformLaw.lean` exhaustively has 24 public declarations.  For every real `q`, the weights are `(1-q)q^n` with exact zero/successor formulas.  If the absolute value of `q` is less than one, they are norm-summable with total mass one; the resulting series is continuous and measurable, splits as `X_q = (1-q)U_0 + q X_q'`, and reflects to `1-X_q`.  Its law is a probability measure with exact mean `1 / 2`, the exact head--tail product law, affine self-similarity, reflection, absolute continuity with respect to Lebesgue measure, null singletons, and topological support equal to the exact range of `X_q`.  The mean and absolute-continuity statements require exactly `|q| < 1`, include `q=0` and negative `q`, and need no positivity assumption.  The wrapper in `GeometricUniformUniqueness.lean` says that, for `|q| < 1`, this self-similarity uniquely characterizes the law among all probability measures, likewise without support, density, or moment hypotheses.  Under `0 ≤ q < 1`, the weights are nonnegative, the series range and law support are exactly `[0,1]`; `q=0` is included.  At `q=1/2`, exact bridges identify both the random series and law with the classical dyadic construction.  The CDF and density layer is recorded in the next two rows. | `Fabius.ProbabilityRepresentation.geometricUniformWeight`, `Fabius.ProbabilityRepresentation.geometricUniformWeight_zero`, `Fabius.ProbabilityRepresentation.geometricUniformWeight_succ`, `Fabius.ProbabilityRepresentation.hasSum_geometricUniformWeight`, `Fabius.ProbabilityRepresentation.summable_norm_geometricUniformWeight`, `Fabius.ProbabilityRepresentation.geometricUniformWeight_nonneg`, `Fabius.ProbabilityRepresentation.geometricUniformSeries`, `Fabius.ProbabilityRepresentation.continuous_geometricUniformSeries`, `Fabius.ProbabilityRepresentation.measurable_geometricUniformSeries`, `Fabius.ProbabilityRepresentation.geometricUniformSeries_split`, `Fabius.ProbabilityRepresentation.geometricUniformSeries_reflect`, `Fabius.ProbabilityRepresentation.geometricUniformSeries_mem_Icc`, `Fabius.ProbabilityRepresentation.geometricUniformDistribution`, `Fabius.ProbabilityRepresentation.geometricUniformDistribution_isProbabilityMeasure`, `Fabius.ProbabilityRepresentation.geometricUniformDistribution_absolutelyContinuous`, `Fabius.ProbabilityRepresentation.geometricUniformDistribution_nullSingletonClass`, `Fabius.ProbabilityRepresentation.uniformProduct_map_head_tail_geometricUniformSeries`, `Fabius.ProbabilityRepresentation.geometricUniformDistribution_selfSimilar`, `Fabius.ProbabilityRepresentation.geometricUniformDistribution_reflection`, `Fabius.ProbabilityRepresentation.integral_id_geometricUniformDistribution_eq_one_half`, `Fabius.ProbabilityRepresentation.geometricUniformDistribution_Icc`, `Fabius.ProbabilityRepresentation.geometricUniformDistribution_compl_Icc`, `Fabius.ProbabilityRepresentation.geometricUniformDistribution_support_eq_range`, `Fabius.ProbabilityRepresentation.geometricUniformDistribution_support_eq_Icc`, `Fabius.ProbabilityRepresentation.eq_geometricUniformDistribution_of_selfSimilar`, `Fabius.ProbabilityRepresentation.weightedCoordinateSum_eq_geometricUniformSeries_one_half`, `Fabius.ProbabilityRepresentation.weightedSumDistribution_eq_geometricUniformDistribution_one_half` |
| Fixed half--quarter multisection of the normalized geometric-uniform law | With `evenCoordinates ω n = ω (2n)` and `oddCoordinates ω n = ω (2n+1)`, the ratio-`1/2` series is pointwise `(2/3)` times the even ratio-`1/4` series plus `(1/3)` times the odd one.  Under the product-uniform law the two coordinate processes are independent copies, so the half law is exactly the pushforward of the product of two quarter laws, equivalently the convolution of their `2/3`- and `1/3`-scaled pushforwards.  These fixed identities need no user hypotheses.  They do not assert a general ratio or general multisection theorem, MGF/cumulant compatibility, the centered-density convolution, or a Pochhammer/Fourier dissection. | `Fabius.ProbabilityRepresentation.evenCoordinates`, `Fabius.ProbabilityRepresentation.oddCoordinates`, `Fabius.ProbabilityRepresentation.geometricUniformSeries_one_half_multisection`, `Fabius.ProbabilityRepresentation.geometricUniformDistribution_one_half_multisection`, `Fabius.ProbabilityRepresentation.geometricUniformDistribution_one_half_conv_one_quarter` |
| Monograph `thm:qF-moment-polynomial` (algebraic, sharp-degree, real-MGF, inner complex-product, exterior complex-germ, and global RatFunc tranches) | **Exact by assembly.**  `GeometricUniformMomentPolynomial.lean` has exactly one public definition and eight public theorems.  Its recursively defined `ℚ[X]` family has `P_0=1`, the exact division-free residual-product recurrence, `natDegree P_n ≤ n.choose 2`, `P_n(0)=1/(n+1)!`, and the displayed values through `P_4`.  `GeometricUniformMomentPolynomialDegree.lean` has no public definitions and exactly three public theorems.  It proves `[q^(n.choose 2)]P_n=bernoulli' n/n!`, equivalently `(-1)^n B_n/n!`, for every `n`; for `n≥2` it proves `[q^(n.choose 2-1)]P_n=-bernoulli' n/n!+bernoulli' (n-1)/(2*(n-1)!)`.  Hence `natDegree P_n=n.choose 2` for `n=1` and even `n`, including `n=0`, while it is `n.choose 2-1` for odd `n>1`.  This makes canonical `prop:qF-P-degree-sharp` **Exact**, and together with the algebra leaf makes `p7:thm:Pn` **Exact**.  `GeometricUniformMomentPolynomialBridge.lean` has no public definitions and exactly one public theorem: for every real `q` with `|q|<1`, including `q=0` and negative contractions, it proves `P_n(q)=((q;q)_n/(1-q)^n)·(iteratedDeriv n M_q 0/n!)`, where `M_q` is the genuine geometric-uniform MGF.  `GeometricUniformComplexMomentProduct.lean` has exactly one public definition and three public theorems: `geometricUniformComplexMomentProduct`, `hasProdLocallyUniformly_geometricUniformComplexMomentProduct`, `differentiable_geometricUniformComplexMomentProduct`, and `geometricUniformMomentPolynomial_eval₂_eq_complexMomentProduct_taylorCoefficient`.  For every complex `q` with `‖q‖<1`, including `q=0`, it constructs `A_q(z)=∏' j, complexExpm1Div ((1-q)*q^j*z)`, proves locally uniform convergence on the whole complex plane, proves that the resulting function is entire, and proves the analogous Taylor-coefficient normalization; for nonreal `q`, this product is not a probability MGF.  `GeometricUniformExteriorComplexMomentGerm.lean` has exactly one public definition and two public theorems.  For every complex `q` with `1<‖q‖`, it defines the manuscript's actual reciprocal germ `M_q(z)=(A_{q⁻¹}(-z))⁻¹`, proves it analytic at zero, and proves the same normalized Taylor-coefficient identity; the total inverse carries no global holomorphy, pole-divisor, or boundary claim.  `GeometricUniformMomentRatFunc.lean` has exactly one public definition and four public theorems.  It defines the single global rational coefficient `a_n(q)=P_n(q)/[n]_q!`, proves the pole-clearing identity `[n]_X!·a_n=P_n` in `RatFunc ℚ`, identifies every safe inner value `‖q‖<1` with the complex-product Taylor coefficient, identifies every safe exterior value `1<‖q‖` with the reciprocal-germ Taylor coefficient, and proves `a_n(1)=P_n(1)/n!`; the inner theorem includes `q=0` and the specialization theorem includes `n=0`.  These algebraic and analytic tranches assemble the whole canonical `thm:qF-moment-polynomial`, so that label is **Exact**.  No value is assigned at a genuine pole, and there is no exact pole-divisor/order theorem, analytic continuation through the unit circle, or global holomorphy claim for the exterior reciprocal.  Label `thm:geometric-uniform-mgf` remains **Partial**: coefficient rationality and the `q=1` specialization are now packaged, but no public theorem packages the product's dilation/Mahler law, normalization, formal uniqueness, exact pole data, or direct equality of the complex product with the real MGF or characteristic function. | `Fabius.geometricUniformMomentPolynomial`, `Fabius.geometricUniformMomentPolynomial_zero`, `Fabius.geometricUniformMomentPolynomial_succ`, `Fabius.geometricUniformMomentPolynomial_natDegree_le`, `Fabius.geometricUniformMomentPolynomial_eval_zero`, `Fabius.geometricUniformMomentPolynomial_one`, `Fabius.geometricUniformMomentPolynomial_two`, `Fabius.geometricUniformMomentPolynomial_three`, `Fabius.geometricUniformMomentPolynomial_four`, `Fabius.coeff_geometricUniformMomentPolynomial_choose_two`, `Fabius.coeff_geometricUniformMomentPolynomial_choose_two_sub_one`, `Fabius.geometricUniformMomentPolynomial_natDegree_eq`, `Fabius.geometricUniformMomentPolynomial_eval₂_eq_mgf_taylorCoefficient`, `Fabius.geometricUniformComplexMomentProduct`, `Fabius.hasProdLocallyUniformly_geometricUniformComplexMomentProduct`, `Fabius.differentiable_geometricUniformComplexMomentProduct`, `Fabius.geometricUniformMomentPolynomial_eval₂_eq_complexMomentProduct_taylorCoefficient`, `Fabius.geometricUniformExteriorComplexMomentGerm`, `Fabius.analyticAt_geometricUniformExteriorComplexMomentGerm`, `Fabius.geometricUniformMomentPolynomial_eval₂_eq_exteriorComplexMomentGerm_taylorCoefficient`, `Fabius.geometricUniformMomentRatFunc`, `Fabius.qFactorial_mul_geometricUniformMomentRatFunc`, `Fabius.eval_geometricUniformMomentRatFunc_eq_complexMomentProduct_taylorCoefficient`, `Fabius.eval_geometricUniformMomentRatFunc_eq_exteriorComplexMomentGerm_taylorCoefficient`, `Fabius.eval_geometricUniformMomentRatFunc_one` |
| Monograph `thm:qF-reciprocity` (combined inner/exterior complex moment germ) | **Exact.**  `GeometricUniformMomentReciprocity.lean` has the exhaustive public surface of one definition and five theorems: `geometricUniformComplexMomentGerm`, `geometricUniformComplexMomentGerm_of_norm_lt_one`, `geometricUniformComplexMomentGerm_of_one_lt_norm`, `analyticAt_geometricUniformComplexMomentGerm`, `geometricUniformComplexMomentGerm_reciprocity`, and `geometricUniformComplexMomentGerm_moment_convolution`.  The combined function is the inner product for `‖q‖<1`, the exterior reciprocal germ for `1<‖q‖`, and analytic at zero for `‖q‖≠1`.  Under exactly `q≠0` and `‖q‖≠1`, the reciprocity theorem proves `M_q(z)·M_{q⁻¹}(-z)=1` locally as an `EventuallyEq` in `𝓝 0`; the convolution theorem proves at every order the exact binomial convolution of the two germs' iterated derivatives, equal to `if n=0 then 1 else 0`.  Locality is essential because the inner product may have zeros away from the origin and Lean's inverse is total.  No global reciprocal identity or unit-circle continuation is asserted. | `Fabius.geometricUniformComplexMomentGerm`, `Fabius.geometricUniformComplexMomentGerm_of_norm_lt_one`, `Fabius.geometricUniformComplexMomentGerm_of_one_lt_norm`, `Fabius.analyticAt_geometricUniformComplexMomentGerm`, `Fabius.geometricUniformComplexMomentGerm_reciprocity`, `Fabius.geometricUniformComplexMomentGerm_moment_convolution` |
| Geometric-uniform tail dictionary and sinc-prefix convergence | The predecessor dictionary proves finite measure, characteristic-function, MGF, and CGF tail factorizations.  The sinc module evaluates the scaled uniform digit for every real `q`; for `|q| < 1` it gives both the closed-factor and phase-collapsed finite identities `φ_q(t) = exp(i(1-q^m)t/2) · ∏_{k<m} sinc((1-q)q^k t/2) · φ_q(q^m t)`, then proves that the phase-bearing prefix without the residual converges pointwise to `φ_q(t)`.  The scope includes `q=0` and negative `q`; the dyadic results are exact wrappers for `weightedSumDistribution`.  This predecessor module itself remains pointwise; the next row records the named locally uniform real-frequency upgrade and its compact-set uniform corollary. | `Fabius.charFun_geometricUniformDigit`, `Fabius.charFun_geometricUniformDistribution_prefix`, `Fabius.charFun_geometricUniformDistribution_prefix_sinc`, `Fabius.tendsto_prefix_sinc_charFun`, `Fabius.charFun_weightedSumDistribution_prefix_sinc`, `Fabius.tendsto_prefix_sinc_charFun_weightedSumDistribution` |
| Geometric sinc/Gamma characteristic-function bridge and phase-prefix convergence | `GeometricSincCharacteristicFunction.lean` has exactly four public theorems and no public definitions.  For every real `q` with the sharp hypothesis `|q| < 1`, including `q=0` and negative `q`, and every real frequency `t`, set `z=(1-q)t/(2π)`.  The first two theorems prove `φ_q(t)=exp(it/2) S_q(z)` for `S_q=geometricSincProduct (q : ℂ)` and, by reflection, `φ_q(t)=exp(it/2) G_q(z)G_q(-z)` for `G_q=geometricReciprocalGamma (q : ℂ)`.  For `P_m(t)=exp(i(1-q^m)t/2) · ∏_{k<m} sinc((1-q)q^k t/2)`, the other two prove `P_m → φ_q` locally uniformly on `ℝ` and uniformly on every compact `K ⊆ ℝ`.  Together with the Gamma-side row, this closes the named `HasProd`, locally uniform pure-product, entire-product, characteristic-function, real-frequency locally uniform, and compact-real-uniform bridges.  No complex-frequency analogue for the full phase-bearing prefixes or uniform-convergence claim on all of `ℝ` is made.  General-`q` rapid-decay bounds and Fourier inversion, a separately packaged centered family and MGF wrapper, explicit Bernoulli-cumulant/Bell-moment formulas or asymptotics, further transform formulas, and shape theory remain outside Lean.  The fixed-dyadic Pochhammer factorization above is not a substitute for these remaining general-`q` statements. | `Fabius.charFun_geometricUniformDistribution_eq_phase_mul_geometricSincProduct`, `Fabius.charFun_geometricUniformDistribution_eq_phase_mul_geometricReciprocalGamma`, `Fabius.tendstoLocallyUniformly_prefix_sinc_charFun`, `Fabius.tendstoUniformlyOn_prefix_sinc_charFun` |
| Continuous CDF and density extraction for atomless real probability laws (reusable foundation) | Every real probability law with null singletons has a continuous CDF.  If such a law is invariant under `y ↦ a-y`, then its CDF satisfies `F(a-x)=1-F(x)`.  More strongly, if an arbitrary real probability CDF has the everywhere pointwise derivative `f`, then the law is exactly Lebesgue measure with density `ENNReal.ofReal ∘ f`.  This last theorem assumes neither continuity of `f`, null singletons, nor prior absolute continuity: CDF monotonicity supplies nonnegativity and local integrability. | `Fabius.continuous_cdf_of_nullSingleton`, `Fabius.cdf_reflection_sub`, `Fabius.measure_eq_withDensity_of_cdf_hasDerivAt` |
| Named geometric-uniform CDF and explicit density | The definition `F_q = cdf μ_q` is total in `q`, and its monotonicity, range bounds, and measurability are unconditional.  If the absolute value of `q` is less than one, then `F_q` is continuous, reflected about `1/2`, and equals `1/2` there.  Under `0 ≤ q < 1`, it is exactly zero on `x ≤ 0` and one on `1 ≤ x`.  For `0 < q < 1`, conditioning gives both the subtype and interval-integral equations, and the explicit difference quotient `f_q` is the derivative of `F_q` everywhere.  The total Lean definition deliberately exposes why this formula is not the density outside the positive range: it is identically zero at `q=0` and nonpositive for every `q<0`, so `volume.withDensity (ENNReal.ofReal ∘ f_q)` is the zero measure whenever `q≤0` and, on the contraction range, is provably not `μ_q`.  For `0<q<1`, by contrast, the law is exactly that `withDensity` measure; both `F_q` and `f_q` are `C∞`, and `f_q` is continuous, nonnegative, symmetric, zero off the open unit interval, topologically supported inside `[0,1]`, and compactly supported.  A Lean theorem for the paper-level corrected signed-ratio density at negative `q` remains open.  At `q=1/2`, `F_q` is the classical weighted-sum CDF and every bounded Fabius function globally, while `f_q(x)=2 up(2x-1)`.  The dyadic smoothing, continuity, exterior-value, and reflection proofs now specialize this general API; the characteristic-function sinc-prefix layer is recorded separately above. | `Fabius.ProbabilityRepresentation.geometricUniformCDF`, `Fabius.ProbabilityRepresentation.monotone_geometricUniformCDF`, `Fabius.ProbabilityRepresentation.geometricUniformCDF_nonneg`, `Fabius.ProbabilityRepresentation.geometricUniformCDF_le_one`, `Fabius.ProbabilityRepresentation.measurable_geometricUniformCDF`, `Fabius.ProbabilityRepresentation.continuous_geometricUniformCDF`, `Fabius.ProbabilityRepresentation.geometricUniformCDF_reflection`, `Fabius.ProbabilityRepresentation.geometricUniformCDF_one_half`, `Fabius.ProbabilityRepresentation.geometricUniformCDF_zero_of_nonpos`, `Fabius.ProbabilityRepresentation.geometricUniformCDF_one_of_one_le`, `Fabius.ProbabilityRepresentation.geometricUniformCDF_eq_integral`, `Fabius.ProbabilityRepresentation.geometricUniformCDF_eq_intervalIntegral`, `Fabius.ProbabilityRepresentation.geometricUniformDensity`, `Fabius.ProbabilityRepresentation.geometricUniformDensity_zero`, `Fabius.ProbabilityRepresentation.geometricUniformDensity_nonpos_of_neg`, `Fabius.ProbabilityRepresentation.volume_withDensity_geometricUniformDensity_eq_zero_of_nonpos`, `Fabius.ProbabilityRepresentation.geometricUniformDistribution_ne_withDensity_geometricUniformDensity_of_nonpos`, `Fabius.ProbabilityRepresentation.geometricUniformCDF_hasDerivAt`, `Fabius.ProbabilityRepresentation.deriv_geometricUniformCDF`, `Fabius.ProbabilityRepresentation.continuous_geometricUniformDensity`, `Fabius.ProbabilityRepresentation.geometricUniformDensity_nonneg`, `Fabius.ProbabilityRepresentation.geometricUniformDensity_zero_of_nonpos`, `Fabius.ProbabilityRepresentation.geometricUniformDensity_zero_of_one_le`, `Fabius.ProbabilityRepresentation.support_geometricUniformDensity_subset_Ioo`, `Fabius.ProbabilityRepresentation.support_geometricUniformDensity_subset_Icc`, `Fabius.ProbabilityRepresentation.tsupport_geometricUniformDensity_subset_Icc`, `Fabius.ProbabilityRepresentation.geometricUniformDensity_hasCompactSupport`, `Fabius.ProbabilityRepresentation.geometricUniformDensity_reflection`, `Fabius.ProbabilityRepresentation.geometricUniformDistribution_eq_withDensity`, `Fabius.ProbabilityRepresentation.contDiff_geometricUniformCDF`, `Fabius.ProbabilityRepresentation.contDiff_geometricUniformDensity`, `Fabius.ProbabilityRepresentation.weightedSumCDF_eq_geometricUniformCDF_one_half`, `Fabius.ProbabilityRepresentation.geometricUniformCDF_one_half_eq_fabiusReal`, `Fabius.ProbabilityRepresentation.geometricUniformDensity_one_half_eq_rvachevUp` |
| Support-free quantile transport, its compact specialization, and Fabius substitution | For an arbitrary real probability law `μ`, with no support assumption, a source-a.e. measurable `Q` pushes uniform measure on `[0,1]` to `μ` whenever, for every real threshold `x`, `Q(y) ≤ x ↔ y ≤ cdf μ x` holds for source-almost every `y`.  A compact theorem derives the same conclusion from a.e. support and a pointwise unit-square order characterization.  The latter specializes to the globally clamped Fabius inverse and the dyadic weighted-sum law.  Every strongly measurable Banach-valued statistic satisfies the corresponding integral identity without an integrability hypothesis: Lean's Bochner integral is totalized to zero on both sides in the non-integrable case.  Only the source measure is restricted to `[0,1]`.  These four quantile-transport declarations do not by themselves prove a two-function integration-by-parts identity; the separate layer-cake rows below record the exact first-order results now available. | `Fabius.map_quantile_eq`, `Fabius.map_inverseCDF_volume_restrict_Icc`, `Fabius.map_fabiusInv_restrict_Icc_eq_weightedSumDistribution`, `Fabius.integral_comp_fabiusInv_restrict_Icc_eq_weightedSumDistribution` |
| Weighted subgraph/supergraph Fubini and survival/lower-CDF layer cake (reusable Banach-valued foundation) | For arbitrary s-finite measures, a measurable height, an integrable real or complex scalar weight, and an integrable Banach-valued kernel, lower subgraph sections may be exchanged with upper sections, and closed supergraph sections with closed lower sections.  The latter convention preserves atoms exactly.  For a finite real measure, the two engines give support-free clamped survival and lower-CDF identities.  On the lower-CDF side, the partial terminal form needs only `c ≤ b` and the one-sided almost-everywhere bound `z ≤ b`; compact interval support gives the convenient partial corollary and the full identity.  The dyadic weighted-sum law specializes the survival side to partial and full Rvachev-`up` identities for every interval-integrable real-Banach-valued kernel, with no positivity assumption; `E = ℂ` supplies complex kernels. | `Fabius.integral_smul_setIntegral_subgraph`, `Fabius.integral_smul_setIntegral_supergraph`, `Fabius.intervalIntegral_survival_smul_eq_integral_clamp`, `Fabius.intervalIntegral_survival_smul_eq_integral_min_of_ae_mem_Icc`, `Fabius.intervalIntegral_survival_smul_eq_integral_of_ae_mem_Icc`, `Fabius.intervalIntegral_cdf_smul_eq_integral_clamp`, `Fabius.intervalIntegral_cdf_smul_eq_integral_max_of_ae_le`, `Fabius.intervalIntegral_cdf_smul_eq_integral_max_of_ae_mem_Icc`, `Fabius.intervalIntegral_cdf_smul_eq_integral_of_ae_mem_Icc`, `Fabius.ProbabilityRepresentation.intervalIntegral_rvachevUp_smul_eq_integral_min`, `Fabius.ProbabilityRepresentation.intervalIntegral_rvachevUp_smul_eq_integral` |
| Fractional lower-CDF layer cake and exact Fabius stopped-power moments | For a finite real measure, positive real order `α`, and `a ≤ x`, the fractional Volterra integral of `t ↦ μ.real (Iic t)` equals the Gamma-normalized clamped stopped-power moment.  An almost-everywhere lower support bound replaces the clamp by `max (x-z) 0`; for a probability measure the same result is exported directly with Mathlib's `ProbabilityTheory.cdf`; compact interval support gives the ordinary endpoint power.  For the dyadic weighted-sum law this yields, for every real `x`, both distribution-law and explicit product-sample-space forms of `I^α_0 F(x) = E[(x-X)₊^α] / Γ(α+1)`.  At `x=1`, reflection gives both law and sample-space forms of the ordinary moment `E[X^α] / Γ(α+1)`.  These are positive-real integral identities; no complex-order continuation, fractional derivative, Caputo theorem, or Rvachev/inverse-function specialization is asserted. | `Fabius.fractionalVolterra_measureReal_Iic_eq_integral_clamp`, `Fabius.fractionalVolterra_measureReal_Iic_eq_integral_posPart`, `Fabius.fractionalVolterra_cdf_eq_integral_posPart`, `Fabius.fractionalVolterra_measureReal_Iic_eq_integral_rpow_of_ae_mem_Icc`, `Fabius.ProbabilityRepresentation.fractionalVolterra_fabiusReal_eq_integral_posPart`, `Fabius.ProbabilityRepresentation.fractionalVolterra_fabiusReal_eq_uniformProduct_integral_posPart`, `Fabius.ProbabilityRepresentation.fractionalVolterra_fabiusReal_one_eq_integral_rpow`, `Fabius.ProbabilityRepresentation.fractionalVolterra_fabiusReal_one_eq_uniformProduct_integral_rpow` |
| Weighted inverse layer cake and Fabius inverse area | On arbitrary ordered compact rectangles, interval-preserving clocks satisfying the strict order equivalence exchange a scalar-weighted stopped primitive with the complementary scalar primitive.  The equivalence makes the two interval restrictions a Galois connection; hence the public explicit-primitive and pointwise-`C¹` theorems now derive the measurable clamped representative they need instead of assuming clock measurability.  Those forms allow real or complex weights and a Banach-valued primitive.  A stronger regularity interface accepts an arbitrary real or complex `L¹` weight and a real-valued absolutely continuous primitive: without endpoint normalization it integrates `φ(u) * (Ψ(Q(u)) - Ψ(a))`, and with `Ψ(a) = 0` it gives the displayed composition form.  Absolute continuity supplies derivative integrability, so no extra integrability premise is imposed on the complementary side; degenerate ordered rectangles are included.  Exact Fabius specializations provide all three primitive forms, the full weighted real inverse-area identity, equality of the unit-interval inverse and Rvachev-`up` areas, and the inverse mean `1/2`.  The separate absolutely-continuous row below supplies both full and variable-upper two-function compact-rectangle identities and the partial Fabius inverse-area identity.  Reversed endpoints, improper singular-endpoint extensions, complex-valued arbitrary-AC primitives, higher or fractional inverse primitives, and complex Mellin continuation remain frontier targets. | `Fabius.galoisConnection_Icc_restrict_of_lt_iff_lt`, `Fabius.intervalIntegral_smul_intervalIntegral_of_lt_iff_lt`, `Fabius.intervalIntegral_smul_comp_of_lt_iff_lt`, `Fabius.intervalIntegral_mul_comp_sub_of_lt_iff_lt_of_absolutelyContinuousOnInterval`, `Fabius.intervalIntegral_mul_comp_of_lt_iff_lt_of_absolutelyContinuousOnInterval`, `Fabius.intervalIntegral_smul_intervalIntegral_fabiusInv`, `Fabius.intervalIntegral_smul_comp_fabiusInv`, `Fabius.intervalIntegral_mul_comp_fabiusInv_of_absolutelyContinuousOnInterval`, `Fabius.intervalIntegral_mul_fabiusInv_eq`, `Fabius.intervalIntegral_fabiusInv_eq_intervalIntegral_rvachevUp`, `Fabius.intervalIntegral_fabiusInv_eq_one_half` |
| Absolutely-continuous two-function inverse-pair calculus (reusable foundation) | On arbitrary ordered compact rectangles, interval-preserving clocks satisfying `C x < u ↔ x < Q u` obey the two-function integration identity for arbitrary absolutely continuous real functions `A` and `B`.  Every `y ∈ [a,b]` also gives the exact restricted identity on `[a,y] × [c,C(y)]`.  No measurability hypothesis is imposed on either clock: the order relation forces monotonicity on the two closed intervals, and canonical clamped extensions supply the measurable representatives needed by layer cake.  Degenerate ordered intervals are included.  The full and variable-upper Fabius wrappers give the corresponding unit-interval identities; their identity-function specializations give the full area sum and the exact partial inverse-area formula.  Reversed endpoints require swapping the corresponding interval; improper singular endpoints, higher or fractional inverse primitives, complex-valued arbitrary-AC factors, and complex Mellin continuation are not asserted. | `Fabius.intervalIntegral_deriv_mul_comp_add_comp_mul_deriv_of_lt_iff_lt`, `Fabius.intervalIntegral_deriv_mul_comp_add_comp_mul_deriv_to_of_lt_iff_lt`, `Fabius.intervalIntegral_deriv_mul_fabiusReal_add_fabiusInv_mul_deriv`, `Fabius.intervalIntegral_deriv_mul_fabiusReal_add_fabiusInv_mul_deriv_to`, `Fabius.intervalIntegral_fabiusReal_add_fabiusInv_eq_one`, `Fabius.intervalIntegral_fabiusInv_to_eq_mul_sub_intervalIntegral_fabiusReal` |
| Literal oriented repeated integration (reusable Banach-valued foundation) | For maps from `ℝ` to an arbitrary real normed space, the first primitive and its literal iterates are defined with an arbitrary base point and oriented endpoint.  The order is an exact additive semigroup and every positive order vanishes at the base point.  In a complete codomain, a continuous input gives a continuous ladder with strict FTC, ordinary derivative recurrence, all bounded-index derivative cancellations, and `C^n` regularity at order `n`. | `Fabius.volterraPrimitive`, `Fabius.iteratedPrimitive`, `Fabius.iteratedPrimitive_zero`, `Fabius.iteratedPrimitive_succ`, `Fabius.iteratedPrimitive_add`, `Fabius.iteratedPrimitive_succ_self`, `Fabius.continuous_volterraPrimitive`, `Fabius.continuous_iteratedPrimitive`, `Fabius.iteratedPrimitive_succ_hasStrictDerivAt`, `Fabius.iteratedPrimitive_succ_hasDerivAt`, `Fabius.deriv_iteratedPrimitive_succ`, `Fabius.iteratedDeriv_iteratedPrimitive_add`, `Fabius.iteratedDeriv_iteratedPrimitive`, `Fabius.contDiff_iteratedPrimitive` |
| Normalized integer-order Volterra calculus (reusable normed-space foundation) | The order-zero identity and every positive Cauchy kernel are packaged as one operator over any real normed codomain, with arbitrary base point and oriented endpoint.  It is local on the unoriented endpoint interval and commutes with real scalars and finite integrable sums.  Division-free affine covariance holds for every real scale, including negative and zero scales; its inverse-smul form assumes only that the scale is nonzero.  Under interval integrability on the two local pieces, changing the base point is exactly a local Volterra tail plus the finite Taylor jet encoded by the lower-order old-base values, with no ordering of the three points.  If the input vanishes on the open unoriented interval from the new base point to the endpoint, every positive-order value collapses to that jet; endpoint values are irrelevant and coincident points are included.  These affine and base-point formulas do not require completeness.  The kernel-raising law is assumption-free; the Hasse-polynomial and monomial formulas need only interval integrability, include coincident or reversed endpoints, and convert every polynomial weight into an explicit finite combination of higher orders.  Exact Taylor reconstruction for a globally `C^(n+1)` map, the literal-iteration/kernel bridge, strict FTC, ordinary derivative recurrence, derivative cancellation, smoothness, and the additive order semigroup use completeness.  The separate `_succ_` declarations preserve the literal positive-order formulas, while their all-order companions include `n=0` without side conditions.  The four affine/base-point declarations were compiled at checkpoint `e109088ed`. | `Fabius.normalizedVolterra`, `Fabius.normalizedVolterra_zero`, `Fabius.normalizedVolterra_succ`, `Fabius.normalizedVolterra_one`, `Fabius.normalizedVolterra_succ_self`, `Fabius.normalizedVolterra_congr`, `Fabius.normalizedVolterra_smul`, `Fabius.normalizedVolterra_affine`, `Fabius.normalizedVolterra_comp_affine`, `Fabius.normalizedVolterra_finsetSum`, `Fabius.normalizedVolterra_basepoint_shift`, `Fabius.normalizedVolterra_succ_eq_taylor_of_eq_zero`, `Fabius.normalizedVolterra_succ_kernel_pow`, `Fabius.normalizedVolterra_kernel_pow`, `Fabius.normalizedVolterra_succ_polynomial`, `Fabius.normalizedVolterra_polynomial`, `Fabius.normalizedVolterra_succ_monomial`, `Fabius.normalizedVolterra_monomial`, `Fabius.normalizedVolterra_succ_iteratedDeriv_eq_sub_taylor`, `Fabius.normalizedVolterra_succ_iteratedDeriv_eq_of_zero_jet`, `Fabius.iteratedPrimitive_eq_normalizedVolterra`, `Fabius.normalizedVolterra_succ_hasStrictDerivAt`, `Fabius.deriv_normalizedVolterra_succ`, `Fabius.iteratedDeriv_normalizedVolterra_add`, `Fabius.contDiff_normalizedVolterra`, `Fabius.normalizedVolterra_add` |
| Positive-real fractional Volterra calculus (reusable real-normed-space foundation) | The Gamma-normalized operator is a total oriented interval integral for every real order, base point, and endpoint.  It vanishes at the base, depends only on the input on the closed unoriented endpoint interval, commutes with real scalars, reduces at order one to the ordinary primitive, and agrees at every positive natural order with `normalizedVolterra`, all without endpoint-order or regularity assumptions.  In the classical fractional regime `0 < α` and `a ≤ x`, a continuous input gives an interval-integrable kernel and the operator is additive on continuous inputs.  An operator-independent cutoff theorem truncates any interval-integrable kernel that vanishes on the open right tail at `min x b`, under `a ≤ b` and `a ≤ x`; its positive-order fractional specialization applies to continuous inputs with the same compact-right-support condition.  For `0 < α` and `0 < β`, the raw shifted beta kernel is interval-integrable when `s ≤ x`; when `s < x`, both its raw value `(x-s)^(α+β-1) * (Γ(α)Γ(β)/Γ(α+β))` and its Gamma-normalized value `(x-s)^(α+β-1) / Γ(α+β)` are exact.  On a complete target, a normalized shifted power of order `β` maps to the normalized power of order `α+β` when `a < x`; more generally, `ρ > -1` gives the coefficient `Γ(ρ+1)(x-a)^(α+ρ)/Γ(α+ρ+1)`, while constants have value `(x-a)^α/Γ(α+1)` and include `a=x`.  Two positive orders compose additively when `a ≤ x` and the input is continuous on `[a,x]`.  No order-zero operator law, reversed-endpoint fractional interpretation, semigroup theorem for merely interval-integrable inputs or noncomplete targets, Caputo or other fractional derivative, or complex order is asserted. | `Fabius.fractionalVolterra`, `Fabius.fractionalVolterra_self`, `Fabius.fractionalVolterra_congr`, `Fabius.fractionalVolterra_smul`, `Fabius.intervalIntegrable_fractionalVolterra_kernel`, `Fabius.intervalIntegral_eq_integral_min_of_eq_zero`, `Fabius.fractionalVolterra_eq_intervalIntegral_min_of_eq_zero`, `Fabius.fractionalVolterra_add_input`, `Fabius.fractionalVolterra_one`, `Fabius.fractionalVolterra_nat_succ`, `Fabius.intervalIntegral_fractionalVolterra_betaKernel`, `Fabius.intervalIntegrable_fractionalVolterra_betaKernel`, `Fabius.intervalIntegral_fractionalVolterra_normalizedBetaKernel`, `Fabius.fractionalVolterra_normalized_rpow_smul`, `Fabius.fractionalVolterra_rpow_smul`, `Fabius.fractionalVolterra_const`, `Fabius.fractionalVolterra_add` |
| Fractional Volterra affine/ordinary-derivative calculus, causal Rvachev primitives, and one-step Fabius--Rvachev shifts | Increasing-affine covariance holds for arbitrary real order, `0 < c`, and `a ≤ x` in every real normed codomain, with no regularity, integrability, or completeness hypothesis; an inverse-smul form is included.  For `0 < α`, a continuous Banach-valued `g` with a right-sided interior derivative `g'` that is interval-integrable satisfies `I_a^(α+1) g'(x) = I_a^α g(x) - ((x-a)^α/Γ(α+1)) • g(a)`; the boundary-free corollary assumes `g(a)=0`, and the degenerate endpoint is included.  The total `rvachevFractionalPrimitive F β x := I₋₁^β up(x)` has the exact support-truncated `min x 1` integral for `β > 0` and `x ≥ -1`, agrees with the normalized integer ladder at every positive natural order, and satisfies the additive positive-order semigroup on the same half-line.  Consequently the signed extension satisfies `I₀^(α+1) 𝓕(x) = 2^α I₀^α 𝓕(x/2)` for `x ≥ 0`, the bounded Fabius function satisfies it on `[0,1]`, and `I₋₁^(α+1) up(x) = 2^α I₀^α F((x+1)/2)` for `x ≥ -1`.  No nonpositive-scale or reversed-endpoint covariance, negative- or complex-order fractional-calculus extension, fractional derivative or Caputo theorem, shifted dyadic-lattice or endpoint-moment wrapper, transform/tail-series theorem, or inverse-function specialization is asserted. | `Fabius.fractionalVolterra_affine`, `Fabius.fractionalVolterra_comp_affine`, `Fabius.fractionalVolterra_add_one_deriv`, `Fabius.fractionalVolterra_add_one_deriv_of_eq_zero`, `Fabius.rvachevFractionalPrimitive`, `Fabius.rvachevFractionalPrimitive_eq_intervalIntegral_min`, `Fabius.rvachevFractionalPrimitive_nat_succ`, `Fabius.rvachevFractionalPrimitive_add`, `Fabius.fractionalVolterra_add_one_extendedFabius_of_nonneg`, `Fabius.fractionalVolterra_add_one_fabiusReal`, `Fabius.fractionalVolterra_add_one_rvachevUp` |
| Exact integer primitive ladders for the Fabius functions | For every order `n`, the signed extension satisfies `Jⁿ₀ 𝓕(x) = 2^(choose n 2) 𝓕(x / 2^n)` globally; the bounded function satisfies the same formula for every `x ≤ 1`.  Combining this with the generic monomial commutator gives an explicit finite dyadic sum for `Jⁿ₀(t^p 𝓕(t))(x)` for every natural `p`, globally, and its bounded counterpart for `x ≤ 1`.  All four results include order zero and both endpoint orientations.  Although the generic affine and base-point API makes further consequences available, there are not yet separately named Fabius-specific declarations for the complete folded-`up` integer ladder based at `-1`, the reflected `1-F` ladder, arbitrary affine copies of `F`, or the full piecewise polynomial tail after the support of `up`.  The separate fractional rows record the positive-real stopped-power formulas and the single order-raising Fabius/`up` bridges now available.  Iterated fractional shifts, the shifted dyadic lattice and its endpoint wrapper, complex-power weights or complex-order continuation, normal or Mellin continuation in the weight, a bounded piecewise continuation for `x > 1`, and inverse-function repeated-primitive identities remain frontier targets. | `Fabius.normalizedVolterra_extendedFabius`, `Fabius.normalizedVolterra_fabiusReal_of_le_one`, `Fabius.normalizedVolterra_pow_mul_extendedFabius`, `Fabius.normalizedVolterra_pow_mul_fabiusReal_of_le_one` |
| Generic all-order jets of a locally factored function (reusable foundation) | Over every nontrivially normed field and normed module, the `k`-th derivative at zero of `z^n • U(z)` is zero for `k<n` and otherwise is `choose k n · n! • U⁽ᵏ⁻ⁿ⁾(0)`, assuming only `C^k` regularity of `U`.  The division-free `n+r` form, scalar specialization, germ forms at zero, and vector/scalar forms at an arbitrary center are all exported.  No analyticity, completeness, characteristic-zero hypothesis, factorial division, or separately supplied regularity of the arbitrary factored function is required.  In positive characteristic the displayed coefficient may vanish, so the generic theorem identifies a potentially surviving jet rather than asserting nonvanishing. | `Fabius.iteratedDeriv_pow_smul_zero_eq_ite`, `Fabius.iteratedDeriv_pow_smul_zero_add`, `Fabius.iteratedDeriv_pow_mul_zero_add`, `Fabius.iteratedDeriv_eq_choose_factorial_mul_of_eventuallyEq_pow_mul`, `Fabius.iteratedDeriv_eq_ite_of_eventuallyEq_sub_pow_smul`, `Fabius.iteratedDeriv_eq_choose_factorial_smul_of_eventuallyEq_sub_pow_smul`, `Fabius.iteratedDeriv_eq_choose_factorial_mul_of_eventuallyEq_sub_pow_mul`, `Fabius.iteratedDeriv_pow_mul_zero`, `Fabius.iteratedDeriv_eq_factorial_mul_of_eventuallyEq_pow_mul` |
| Total integer-zero factorization and exact complex jets of the Rvachev sinc product | The denominator-cleared sinc shift laws are total, including their removable singular points, and the scaled law allows every nonzero complex scale.  They give a global local-coordinate factorization at `2^k q` without a parity hypothesis, its odd-core specialization, and canonical natural and signed-integer forms.  The sinc product is entire and nonzero at odd half-integers.  At every nonzero signed integer `m`, the total cofactor is analytic away from its totalized denominator point and is a local unit at zero; both the shifted and original-coordinate germ factorizations are exported.  The exact complex order is `d = v₂(abs m)+1`, every lower derivative vanishes, and every higher derivative satisfies `Φ⁽ᵈ⁺ʳ⁾(m) = choose (d+r) d · d! · U_m⁽ʳ⁾(0)`.  In particular the first surviving derivative is `-d! Φ(q_m/2) / m^d`, hence is nonzero. | `Fabius.mul_complexSinc_pi`, `Fabius.mul_complexSinc_pi_add_nat`, `Fabius.mul_complexSinc_pi_div_add_nat`, `Fabius.rvachevFourierProduct_two_pow_mul_add_factorization`, `Fabius.rvachevFourierProduct_two_pow_mul_odd_add_factorization`, `Fabius.rvachevFourierProduct_nat_add_factorization`, `Fabius.rvachevFourierProduct_int_add_factorization`, `Fabius.rvachevFourierProduct_differentiable`, `Fabius.rvachevFourierProduct_nat_div_two_ne_zero_of_odd`, `Fabius.integerZeroLocalCofactor`, `Fabius.rvachevFourierProduct_int_add_eq_pow_mul_cofactor`, `Fabius.rvachevFourierProduct_int_add_eventuallyEq_pow_mul_cofactor`, `Fabius.rvachevFourierProduct_int_eventuallyEq_sub_pow_mul_cofactor`, `Fabius.integerZeroLocalCofactor_analyticAt_of_add_ne`, `Fabius.integerZeroLocalCofactor_analyticAt`, `Fabius.integerZeroLocalCofactor_zero`, `Fabius.integerZeroLocalCofactor_zero_ne`, `Fabius.analyticOrderAt_rvachevFourierProduct_int`, `Fabius.iteratedDeriv_rvachevFourierProduct_int_eq_zero_of_lt`, `Fabius.iteratedDeriv_rvachevFourierProduct_int_add_order`, `Fabius.iteratedDeriv_rvachevFourierProduct_int`, `Fabius.iteratedDeriv_rvachevFourierProduct_int_ne_zero` |
| Finite base-`b` layer regrouping (reusable foundational engine) | For arbitrary `b,N,J`, the finite layer product is regrouped by its underlying multiple over every commutative monoid; the exponent is the truncated count of powers `b^j` dividing that multiple.  For `b > 1`, `m ≠ 0`, and explicit coverage `multiplicity b m ≤ J`, the count is exactly `multiplicity b m + 1`.  Additive companions hold over every additive commutative monoid.  These are finite combinatorial identities and make no infinite-product or convergence claim; the coverage hypothesis remains explicit. | `Fabius.filter_dvd_eq_image`, `Fabius.prod_multiples_eq_prod_filter`, `Fabius.sum_multiples_eq_sum_filter`, `Fabius.prod_layers_eq_prod_pow_card`, `Fabius.sum_layers_eq_sum_nsmul_card`, `Fabius.card_filter_pow_dvd`, `Fabius.prod_layers_eq_prod_pow_multiplicity`, `Fabius.sum_layers_eq_sum_nsmul_multiplicity` |
| General-base multiplicity and digit recovery | For every integer base `b > 1`, including composite bases and `N = 0`, the sharp height `log_b N + 1` contains every finite divisibility layer.  Splitting its zeroth floor term and applying the general-base digit-sum identity proves `(b-1) * ∑_(n=1)^N (1+ν_b(n)) + s_b(N) = b*N` and the exact natural-number quotient formula.  This is the exhaustive five-theorem surface of `BaseDigitMultiplicity.lean`; it establishes finite arithmetic only and does not identify analytic zero multiplicity, product convergence, or signs. | `Fabius.sum_range_weightedScaleMultiplicity_of_log_lt`, `Fabius.sum_range_weightedScaleMultiplicity_log`, `Fabius.sum_range_div_pow_log_eq_self_add_tail`, `Fabius.sub_one_mul_sum_padicValNat_succ_add_digitSum`, `Fabius.sum_range_padicValNat_succ_eq_sub_digitSum_div` |
| Denominator-free geometric principal specialization (reusable foundational engine) | Over every commutative semiring, `h_n(1,q,...,q^r) = [n+r choose r]_q`; a common scale `c` contributes exactly `c^n`, even when `c` is zero or a zero divisor.  The range-indexed form used by interpolation rows is included.  No division, distinctness, topology, ordering, or characteristic restriction is imposed. | `Fabius.completeHomogeneousEval_geometric`, `Fabius.completeHomogeneousEval_scaled_geometric`, `Fabius.completeHomogeneousEvalOn_range_pow_eq_gaussianBinomial` |
| Every moment of arbitrary exact geometric rows (reusable foundational engine) | `GeometricResidualMoments.lean` has no public definitions and exactly nine public theorems.  Over every commutative ring, any row reproducing evaluation at zero through degree `p` on the displayed nodes `c*q^k` has positive moment `c^m * (-1)^p * q^((p+1 choose 2)) * [m-1 choose p]_q`.  Repeated nodes and division-free weights are allowed, and exactness is assumed after scaling so `c` may be zero or a zero divisor.  Over a field, exactly `Set.InjOn (fun k : ℕ ↦ q ^ k) (Finset.range (p + 1))` gives the geometric Lagrange forms; the polynomial theorem reproduces `P.eval 0` for every `P` with `P.natDegree ≤ p` and every scale `c`, including zero.  Composed with the positive-power theorem, it makes `cor:scaled-geometric-moments` **Exact** and strengthens the manuscript's `c ≠ 0` hypothesis. | `Fabius.sum_weight_mul_geometric_pow_succ_add`, `Fabius.sum_weight_mul_geometric_pow_of_pos`, `Fabius.sum_weight_mul_scaled_geometric_pow_succ_add`, `Fabius.sum_weight_mul_scaled_geometric_pow_of_pos`, `Fabius.sum_geometricLagrangeWeight_mul_pow_succ_add`, `Fabius.sum_geometricLagrangeWeight_mul_pow_of_pos`, `Fabius.sum_geometricLagrangeWeight_mul_scaled_geometric_pow_of_pos`, `Fabius.sum_geometricLagrangeWeight_mul_eval_scaled_geometric`, `Fabius.sum_geometricLagrangeWeight_mul_shifted_pow_of_pos` |
| Finite-node and geometric formal-power-series filters (reusable foundational engine) | Over every commutative semiring, a finite weighted rescale filter on arbitrary nodes acts diagonally on every coefficient, commutes with further rescaling and coefficient-ring homomorphisms, and agrees through the exactness order with rescaling at the target node.  Its geometric specialization preserves the constant term and, over every commutative ring, has the exact denominator-free Gaussian residual multiplier in every positive degree; distinct geometric nodes give the Lagrange specialization over fields.  Repeated nodes, the zero ring, `p = 0`, and the mass-one `m = 0` boundary are included.  This row is formal coefficient algebra; the next row records the separate convergent-series companion. | `Fabius.finitePowerSeriesFilter`, `Fabius.coeff_finitePowerSeriesFilter`, `Fabius.finitePowerSeriesFilter_rescale`, `Fabius.map_finitePowerSeriesFilter`, `Fabius.coeff_finitePowerSeriesFilter_of_exact_of_le`, `Fabius.geometricSeriesFilter`, `Fabius.coeff_geometricSeriesFilter`, `Fabius.geometricSeriesFilter_rescale`, `Fabius.map_geometricSeriesFilter`, `Fabius.constantCoeff_geometricSeriesFilter_of_exact`, `Fabius.coeff_geometricSeriesFilter_eq_zero_of_pos_of_le`, `Fabius.coeff_geometricSeriesFilter_of_exact`, `Fabius.geometricSeriesFilter_eq_residual_mk`, `Fabius.coeff_geometricLagrangeSeriesFilter_of_pos`, `Fabius.geometricLagrangeSeriesFilter_eq_residual_mk` |
| Unconditionally summable finite-node and geometric analytic-series filters (reusable foundational engine) | A finite weighted rescale filter on a coefficient sequence in a normed additive group over a commutative semiring is evaluated as a finite sum of `tsum`s.  If each weighted sampled series is unconditionally summable, the finite and infinite sums interchange and the filter acts diagonally; exact moments split off the reproduced finite head, and exactness at target zero preserves `a 0` with an explicit tail starting at degree `p+1`.  A zero weight makes its weighted series identically zero, so that node imposes no convergence obligation.  Over a commutative ring with continuous constant scalar multiplication (each fixed scalar acts continuously on the target), an exact geometric row has the denominator-free Gaussian multiplier term by term in the tail; over a field with the same fixed-scalar continuity, finite-node injectivity supplies the geometric Lagrange and shifted-scale forms.  The geometric hypotheses ask for unweighted summability only at nonzero-weight nodes.  Mathlib's `Summable` is unconditional.  This generic module itself asserts no conditionally-only boundary theorem, formal-to-analytic power-series evaluation bridge, radius or uniform-convergence theorem, norm/sign/error bound, positivity, or asymptotic acceleration; the separate Rvachev row below records the now-proved actual sinc-product specialization. | `Fabius.finiteAnalyticSeriesFilter`, `Fabius.summable_finiteAnalyticSeriesFilter_diagonal`, `Fabius.finiteAnalyticSeriesFilter_eq_tsum`, `Fabius.finiteAnalyticSeriesFilter_eq_head_add_tail_of_exact`, `Fabius.finiteAnalyticSeriesFilter_eq_constant_add_tail_of_exact_zero`, `Fabius.geometricAnalyticSeriesFilter`, `Fabius.geometricAnalyticSeriesFilter_eq_constant_add_gaussian_tsum`, `Fabius.geometricLagrangeAnalyticSeriesFilter_eq_constant_add_gaussian_tsum`, `Fabius.geometricLagrangeAnalyticSeriesFilter_shifted` |
| Named Rvachev Fourier modes and exact Gaussian `q`-filter of the infinite sinc product | `rvachevFourierMomentTerm z n` is the exact `n`th even-moment summand.  It is one at `n=0`, scales by `(c^2)^n`, and for every bounded Fabius solution has `HasSum` equal to its actual Fourier transform; the canonical specialization sums to `rvachevFourierProduct`.  For arbitrary `c,z : ℂ` and `p : ℕ`, the geometric Lagrange row at Gaussian base `q=c^2` has the exact constant-plus-Gaussian-`tsum` residual provided only that `j ↦ (c^2)^j` is injective on `Finset.range (p+1)`.  There is no contraction, realness, positivity, or global nonvanishing hypothesis.  At `c=1/2`, hence `q=1/4`, finite-node injectivity is proved internally and the theorem is assumption-free; the same generic identity is also exported for every bounded `F` satisfying `IsFabius`.  This closes the actual infinite-product coefficient bridge.  It does not identify the report's finite prefixes `P_(b,n)` or quotient `P_(b,n)/P_b`, derive their Bell coefficients, prove a conditionally convergent boundary case, or establish the analytic error sign, bracketing or quantitative bounds, uniform/derivative convergence, or asymptotics. | Analytic moments: `Fabius.rvachevFourierMomentTerm`, `Fabius.rvachevFourierMomentTerm_zero`, `Fabius.rvachevFourierMomentTerm_scale`, `Fabius.hasSum_rvachevFourierMomentTerm`.  Product filter: `Fabius.hasSum_rvachevFourierMomentTerm_product`, `Fabius.rvachevFourierMomentTerm_pow_scale`, `Fabius.geometricLagrange_rvachevFourierProduct_eq_gaussian_tsum`, `Fabius.quarterLagrange_rvachevFourierProduct_eq_gaussian_tsum`, `Fabius.geometricLagrange_rvachevFourier_eq_gaussian_tsum` |
| Multiplicativity and monomial substitution of the formal power-series logarithm (reusable foundational engine) | Over every commutative `ℚ`-algebra, the recurrence-defined logarithm has zero constant coefficient and is characterized, for unit-constant `C`, by `C * B' = C'` and `B(0)=0`.  It sends a product of two or finitely many unit-constant series to the corresponding sum and sends `1` to `0`.  It also commutes with every nontrivial monomial substitution `X ↦ X^p` under the exact `HasSubst (φ - 1)` admissibility hypothesis; the familiar unit-constant statement is a specialization, while the general form also covers nonzero nilpotent constant defects.  Coefficient reconstruction itself needs only a commutative ring.  This is formal algebra only: no analytic logarithm, branch, convergence, or infinite-product assertion is made. | `Fabius.SaddleExpansion.logOf`, `Fabius.SaddleExpansion.massSeries_coeff`, `Fabius.SaddleExpansion.constantCoeff_logOf`, `Fabius.SaddleExpansion.mul_derivative_logOf`, `Fabius.SaddleExpansion.logOf_eq_of`, `Fabius.SaddleExpansion.logOf_mul`, `Fabius.SaddleExpansion.logOf_one`, `Fabius.SaddleExpansion.logOf_prod`, `Fabius.SaddleExpansion.logOf_expand_of_hasSubst`, `Fabius.SaddleExpansion.logOf_expand` |
| Exact Birkhoff and normalized dyadic sine-product variance | Proved at every natural length.  The zero-length case is the empty Birkhoff sum and the empty sine product `1`, so both the integral and closed form vanish exactly.  The pointwise logarithmic bridge is global off the finite zero set, without a fundamental-domain hypothesis. | `Fabius.variance_closed_form_all`, `Fabius.integral_sq_birkhoff_sum_all`, `Fabius.log_abs_prod_two_sin_global`, `Fabius.integral_sq_log_prod_two_sin_all` |
| Central-binomial valuation and the Thue--Morse sign (exhaustive companion API) | For every natural `n`, the two-adic valuation of `C(2n,n)` is exactly `binaryWeight n`, and its `(-1)`-power is `thueMorseSign n`.  The third declaration states that this valuation vanishes iff the binary weight is zero, hence only at `n = 0`; positive powers of two have valuation one.  These are the only three public declarations in `CentralBinomialValuation.lean`. | `Fabius.padicValNat_two_centralBinom`, `Fabius.thueMorseSign_eq_neg_one_pow_centralBinom`, `Fabius.padicValNat_two_centralBinom_eq_zero_iff` |
| Prime-power Pascal-row valuations and the dyadic-comb weights | `FabiusFunction.PrimePowerBinomialValuation` | Exhaustive zero-definition/six-theorem surface: `primePowerChoose_padicValNat_add`, `primePowerChoose_padicValNat`, `primePowerSubOneChoose_padicValNat`, `primePowerSubTwoChoose_padicValNat`, `twoPowChoose_padicValNat`, and `twoPowSubTwoChoose_padicValNat`.  For every prime `p`, `j ≤ p^m`, and `j ≠ 0`, the first two give the additive and natural-subtraction valuation identities, including `j = p^m` and `m = 0`.  Every column `j < p^m` of row `p^m-1` is a `p`-adic unit, including the `m = 0` boundary.  Under exactly `0 < j < p^m`, the row-`p^m-2`, column-`j-1` valuation equals `v_p(j)`; the final two theorems are the strict dyadic specializations.  The strict upper bound is essential because the companion binomial coefficient is zero at `j=p^m`; valuation-histogram counts remain open. |
| Binary digits as differences of dyadic floors (exhaustive companion API) | Dyadic natural-number division composes, `m - 2 * (m / 2) = m % 2`, and therefore the `j`-th binary digit is exactly `n / 2^j - 2 * (n / 2^(j+1))`.  The four declarations are total in natural inputs and formalize the atlas's floor-difference display; they do not assert a real-floor or analytic extension. | `Fabius.div_two_pow_succ_eq_div_div`, `Fabius.sub_two_mul_div_two`, `Fabius.div_two_pow_sub_two_mul_div_two_pow_succ`, `Fabius.testBit_toNat_eq_div_sub_two_mul_div` |
| Finite complex Hamming-weight and Thue--Morse exponential transforms | For every `m ∈ ℕ` and arbitrary `u,a,z ∈ ℂ`, the Hamming-weight sum `∑_{n<2^m} u^(binaryWeight n) exp(nz)` is the finite product `∏_{j<m} (1 + u exp(2^j z))`, and translating every exponent by `a` multiplies the product by `exp a`.  The signed versions replace the weight by `thueMorseSign n` and every plus sign in the product by a minus sign.  At `m = 0` the product is empty: the unshifted identities read `1 = 1` and the affine identities read `exp a = exp a`, including `u = 0` under the usual zeroth-power convention.  These are raw finite complex exponential identities; the separately documented complex-product bridge packages their sinc and negative-Laplace normalizations. | `Fabius.sum_range_two_pow_binaryWeight_cexp`, `Fabius.sum_range_two_pow_binaryWeight_cexp_affine`, `Fabius.sum_range_two_pow_thueMorseSign_cexp`, `Fabius.sum_range_two_pow_thueMorseSign_cexp_affine` |
| Affine Thue--Morse sine- and cosine-product algebra | Proved for every real frequency and arbitrary real phase, including the empty block.  Quarter-turn rotation gives exact affine sine and cosine polynomials; the two real lacunary product-to-sum theorems are their specializations at `x = 2w`, `φ = mπ/2 + w`, with a rescaled `z`-form for the cosine companion.  Complex half-angle and sinc normalization are supplied by the next row. | `Fabius.sum_thueMorseSign_exp_eq_sin_prod`, `Fabius.sum_thueMorseSign_mul_sin_affine`, `Fabius.prod_sin_two_pow_eq_thueMorse_sum`, `Fabius.sum_thueMorseSign_mul_cos_affine`, `Fabius.prod_sin_two_pow_mul_cos_eq_thueMorse_sum`, `Fabius.prod_sin_inv_two_pow_mul_cos_eq_thueMorse_sum` |
| Total complex finite Thue--Morse sinc and negative-Laplace bridges | The complex half-angle identity gives the exact finite sine product with no reality or nonvanishing hypothesis.  Two normalized finite prefixes then identify every dyadic block with its sinc and negative-Laplace product at every natural level, including level zero and the removable origin.  The primary statements are denominator-free; away from zero, quotient corollaries recover the atlas normalizations.  A total factor identity also proves the exact finite Fourier--Laplace rotation between the two prefixes.  These are finite identities only: no infinite-product convergence, analytic logarithm, tail estimate, or measure-specific transform assertion is made; the preceding row records the separate real affine cosine companion. | `Fabius.one_sub_cexp_mul_I`, `Fabius.mul_complexSinc`, `Fabius.sum_thueMorseSign_cexp_eq_sin_prod`, `Fabius.shiftedComplexSincPrefix`, `Fabius.shiftedComplexSincPrefix_zero`, `Fabius.shiftedComplexSincPrefix_apply_zero`, `Fabius.shiftedComplexSincPrefix_succ`, `Fabius.thueMorseBlock_cexp_eq_sincPrefix`, `Fabius.thueMorseBlock_cexp_eq_sincPrefix_of_pos`, `Fabius.shiftedComplexSincPrefix_eq_thueMorseBlock_cexp`, `Fabius.shiftedComplexSincPrefix_eq_thueMorseBlock_cexp_of_pos`, `Fabius.complexLaplacePrefix`, `Fabius.complexLaplacePrefix_zero`, `Fabius.complexLaplacePrefix_apply_zero`, `Fabius.complexLaplacePrefix_succ`, `Fabius.mul_complexExpm1Div_neg`, `Fabius.complexExpm1Div_neg_eq_exp_mul_complexSinc`, `Fabius.complexLaplacePrefix_eq_exp_mul_shiftedComplexSincPrefix`, `Fabius.thueMorseBlock_exp_neg_eq_laplacePrefix`, `Fabius.complexLaplacePrefix_eq_thueMorseBlock_exp_neg` |
| Centered mixed differences and symmetric Thue--Morse blocks | The exhaustive `ThueMorseSymmetricDifference` surface is two definitions and 11 theorems.  For arbitrary half-steps in an additive commutative group acting on the argument space, the centered operator is the ordinary mixed difference based at the upper cube corner; it includes the empty and singleton laws, an insert recurrence that permits repeated half-step values, and the exact signed powerset expansion.  Over every commutative ring, a polynomial of degree at most `s.card` is reduced to its degree-`s.card` coefficient times the sign-free factor `s.card! * ∏_(i∈s) (2*a_i)`; degree below `s.card` vanishes with the `WithBot` statement retaining the zero-polynomial/empty-support boundary, and the first surviving monomial has the displayed top factor.  The dyadic wrapper and its centered block identity need only additive commutative groups.  Over a characteristic-zero field, the increasing-grid forms are exact at every `m`, including `m=0`, with grid `x-(1-2^-m)+2*n*2^-m`.  At positive order `N=m+1`, the full point is `x-(1-2^-(m+1))+n/2^m`; its varying term is `n/2^m`, its mesh spacing is `1/2^m`, and the complement factor is `(-1)^N`.  These declarations formalize the Boolean-cube, polynomial cancellation/top-value, and exact affine-grid clauses of the continuous-chaos report's `thm:TM-corner`; the companion local-integral row closes its analytic clause. | `Fabius.symmetricMixedDifference`, `Fabius.symmetricMixedDifference_empty`, `Fabius.symmetricMixedDifference_singleton`, `Fabius.symmetricMixedDifference_insert`, `Fabius.symmetricMixedDifference_eq_sum_powerset_smul`, `Fabius.symmetricMixedDifference_polynomial_eq_coeff_card`, `Fabius.symmetricMixedDifference_polynomial_of_degree_lt`, `Fabius.symmetricMixedDifference_pow_card`, `Fabius.symmetricDyadicMixedDifference`, `Fabius.symmetricDyadicMixedDifference_zero`, `Fabius.symmetricDyadicMixedDifference_eq_sum_thueMorseSign_smul`, `Fabius.symmetricDyadicMixedDifference_inv_two_pow_eq_sum_thueMorseSign_smul`, `Fabius.symmetricDyadicMixedDifference_inv_two_pow_succ_eq_sum_thueMorseSign_smul` |
| Local centered-box integral formula (`thm:TM-corner`) | **Exact.**  `ThueMorseCornerIntegral.lean` has one public definition and exactly four public theorems.  For the first `N` nonnegative half-steps, an open order-connected set `I`, `ContDiffOn ℝ N g I`, and containment of the whole symmetric segment `[x-∑i<N,a i,x+∑i<N,a i]` in `I`, both range-indexed and `Fin N`-indexed corner sums equal the centered nested integral of `iteratedDeriv N g`.  The proof peels the last mixed difference, applies the local interval FTC at each powerset corner, interchanges the finite sum and outer integral, and inducts on the derivative.  The hypotheses are genuinely local; no global `ContDiff` assumption is substituted.  Zero half-steps and `N=0` are included, strengthening the printed positive-step domain.  Signed half-steps are not asserted.  Together with the preceding algebraic row this closes every clause of `thm:TM-corner`; the following Walsh corollary still lacks its sign--magnitude probability construction, conditioning, and `2^-N` normalization. | Definition: `Fabius.centeredBoxIntegral`.  Theorems: `Fabius.centeredBoxIntegral_zero`, `Fabius.centeredBoxIntegral_succ`, `Fabius.symmetricMixedDifference_range_eq_centeredBoxIntegral`, `Fabius.symmetricMixedDifference_univ_eq_centeredBoxIntegral` |
| Finite uniform-digit characteristic functions as Thue--Morse blocks | The characteristic function of the first `m` centered dyadic uniform digits is exactly the normalized complex sinc prefix for every natural level and every real frequency.  Substitution into the total finite block theorem gives a denominator-free measure-specific Thue--Morse identity even at frequency zero; the reverse quotient formula assumes only that the real frequency is nonzero.  This bridge is finite: it proves no convergence to the full up law, random-tail estimate, or infinite-product statement. | `Fabius.charFun_uniformDigitPrefix_eq_shiftedComplexSincPrefix`, `Fabius.thueMorseBlock_cexp_eq_charFun_uniformDigitPrefix`, `Fabius.charFun_uniformDigitPrefix_eq_thueMorseBlock_cexp` |
| Summable finite-range twists and bilateral cyclic aliasing | Proved without norm-summability.  A scalar twist that factors through a finite label type preserves summability in every complete uniform additive group with continuous distributive scalar action.  For bilateral integer families in a complete Hausdorff uniform complex module, residue fibers sum back to the original family, the unnormalized `ZMod` DFT is the character-twisted bilateral sum, and normalized inversion recovers every residue class.  The abstract engine does not itself identify any report-specific folded half-integer Fabius transform or prove an alias-error estimate. | `Fabius.summable_finite_range_smul`, `Fabius.intResidueTsum`, `Fabius.intFourierTsum`, `Fabius.intFourierSummand_summable`, `Fabius.hasSum_intResidueTsum`, `Fabius.sum_intResidueTsum`, `Fabius.dft_intResidueTsum`, `Fabius.intResidueTsum_eq_dft_average` |
| Finite odd-coset DFT and half-integer root filter | Proved for a primitive `2N`-th root in a commutative domain, with arbitrary positive `N`: the sum over odd exponents is the exact divisibility filter, and every power trace of the odd-frequency DFT is an odd-coset-filtered finite convolution.  The DFT has period `N`, without a claim that this period is minimal.  For power-of-two `N` the odd coset is the unit group and yields the classical Ramanujan form; for general `N` it need not.  No infinite Fourier series, report-specific alias identity, rationality theorem, or alias-error estimate is asserted. | `Fabius.sum_odd_powers_eq_root_filter`, `Fabius.sum_odd_powers_eq_ramanujan`, `Fabius.oddDFT_add_period`, `Fabius.oddDFTPowerTrace_eq_indexSumExpansion`, `Fabius.oddDFTPowerTrace_eq_ramanujanConvolution`, `Fabius.normalizedOddDFTPowerTrace_eq_ramanujanConvolution`, `Fabius.normalizedOddDFTPowerTrace_eq_two_mul_half` |
| Generic exponential-partition coefficient algebra (reusable infrastructure beyond the two source papers) | Proved over every commutative `ℚ`-algebra. Weighted multiplicity vectors give a finite reciprocal-factorial partition sum satisfying both marked-part and normalized successor recurrences, without cancellation in the target. Over a characteristic-zero field this agrees with the usual quotient notation, and extensionally it is the recursive `SaddleExpansion.expCoeff` family. | `Fabius.weightedPartitions`, `Fabius.mem_weightedPartitions`, `Fabius.mem_weightedPartitions_of`, `Fabius.weightedPartitions_zero`, `Fabius.partitionExpSum`, `Fabius.partitionExpSum_zero`, `Fabius.partitionExpSum_recurrence`, `Fabius.partitionExpSum_succ`, `Fabius.partitionExpSum_eq_sum_div`, `Fabius.partitionExpSum_eq_expCoeff` |
| Pairwise partition defects, sharp fixed-block bound, equality profiles, and first positive shell (exhaustive companion API) | The lexical public surface of `PartitionDefect.lean` is exactly three definitions and 33 theorems, 36 declarations total; its one private zero-sum helper is excluded.  `pairSum` is generic over an additive commutative monoid, while `blockPairDefect` and `partitionDefect` are total natural-number definitions.  The unconditional group is `pairSum_nil`, `pairSum_cons`, `pairSum_one`, `pairSum_add`, `pairSum_congr`, `pairSum_map`, `choose_add_two`, `choose_list_sum_two`, `partitionDefect_nonneg`, `pairSum_add_eq`, `pairSum_map_add_eq`, `partitionDefect_eq_linear_add_pairwise_excess`, and `pairSum_eq_zero_iff_pairwise`.  The positive-pair group is `blockPairDefect_eq_mul_sub_one`, `blockPairDefect_eq_zero_iff`, `sub_one_mul_sub_one_eq_zero_iff`, `add_sub_one_le_mul_of_pos`, and `mul_eq_add_sub_one_iff`.  Exactly `∀ x ∈ r, 0 < x` is assumed by `partitionDefect_eq_pairSum_mul_sub_one`, `choose_sum_two_eq_choose_length_add_sum_add_partitionDefect`, `partitionDefect_eq_choose_sum_sub_choose_length_sub_sum`, `length_le_sum_of_pos`, `sum_map_sub_one`, `partitionDefect_lower_bound`, `partitionDefect_eq_zero_iff`, and `partitionDefect_eq_lower_bound_iff`.  The two fixed-block versions additionally assume `r.sum = m` and `r.length = k`; the two scalar first-shell results assume `2 ≤ k < m`; the two list first-shell results assume positivity, `r.sum = m`, and `2 ≤ r.length < m`; the explicit two-block witness assumes `3 ≤ m`.  These are arbitrary positive-list statements, not labelled set-partition enumeration. | Definitions: `Fabius.pairSum`, `Fabius.blockPairDefect`, `Fabius.partitionDefect`.  Remaining theorems: `Fabius.partitionDefect_fixed_block_bound`, `Fabius.partitionDefect_fixed_block_eq_iff`, `Fabius.firstShell_le_fixedBlockProduct`, `Fabius.fixedBlockProduct_eq_firstShell_iff`, `Fabius.firstShell_le_partitionDefect`, `Fabius.partitionDefect_eq_firstShell_iff`, `Fabius.partitionDefect_twoBlock_firstShell`; all other theorem names and their hypothesis groups are enumerated in the status column. |
| Generic complete-Bell and moment--cumulant algebra (reusable infrastructure beyond the two source papers) | Proved over every commutative `ℚ`-algebra. Factorial normalization conjugates the ordinary `expCoeff` and `logCoeff` recurrences into named complete-Bell and moment--cumulant transforms, with the classical Bell recurrence, normalized all-index inverse laws, and functoriality under algebra morphisms. This is formal coefficient algebra; no probabilistic interpretation or hierarchy-specific cumulant formula is claimed. | `Fabius.factorialNormalize`, `Fabius.factorialDenormalize`, `Fabius.completeBellPolynomial`, `Fabius.momentCumulant`, `Fabius.completeBellPolynomial_succ`, `Fabius.completeBellPolynomial_momentCumulant`, `Fabius.momentCumulant_completeBellPolynomial`, `Fabius.map_completeBellPolynomial`, `Fabius.map_momentCumulant` |
| Division-free Bell-polynomial inversion and binomial convolution (exhaustive companion API) | The shift is structure-free, while the unit sequence needs only `[Zero R] [One R]`.  The convolution definition, its finite-sum formula, two distributivity laws, Pascal/product rules, and associativity hold over every `[Semiring R]`; right cancellation additionally assumes `[IsLeftCancelAdd R]`, `IsUnit (w 0)`, and coefficientwise equality against the fixed right factor `w`.  Commutativity and the complete-Bell definition, recurrence/uniqueness, shift law, addition law, and degree-one-through-three formulas use `[CommSemiring R]`; `κ 0` is unconstrained, and recurrence uniqueness assumes exactly `a 0 = 1` and `a (n+1) = binomialConv a (shift κ) n`.  The cumulant definition, its zero/successor laws, and its first three formulas are unconditional over `[CommRing R]`; `binomialConv_cumulant_shift` and `complete_cumulant` assume `m 0 = 1`, while `eq_cumulant_of_complete` assumes exactly `κ 0 = 0` and `complete κ = m`.  Thus the inversion includes positive characteristic and the zero ring and uses no factorial division.  This is finite formal coefficient algebra only: it supplies no analytic exponential generating function, convergence, probability law, or Fabius-function specialization. | Structure-free/unit layer: `Bell.shift`, `Bell.shift_apply`, `Bell.unitSeq`, `Bell.unitSeq_zero`, `Bell.unitSeq_succ`.  Semiring layer: `Bell.binomialConv`, `Bell.binomialConv_eq_sum_range`, `Bell.binomialConv_add_left`, `Bell.binomialConv_add_right`, `Bell.binomialConv_succ`, `Bell.shift_binomialConv`, `Bell.binomialConv_assoc`, `Bell.binomialConv_right_cancel`.  Commutative-semiring layer: `Bell.binomialConv_comm`, `Bell.complete`, `Bell.complete_zero`, `Bell.complete_succ`, `Bell.eq_complete_of_recurrence`, `Bell.shift_complete`, `Bell.complete_add`, `Bell.complete_one`, `Bell.complete_two`, `Bell.complete_three`.  Commutative-ring layer: `Bell.cumulant`, `Bell.cumulant_zero`, `Bell.cumulant_succ`, `Bell.binomialConv_cumulant_shift`, `Bell.complete_cumulant`, `Bell.eq_cumulant_of_complete`, `Bell.cumulant_one`, `Bell.cumulant_two`, `Bell.cumulant_three`. |
| Generic Appell sequences, affine transport, and weighted-binomial inversion (exhaustive companion API) | `AppellSequence.lean` is 3+34.  Its arbitrary-sequence polynomial, degree, derivative, evaluation, affine, and reciprocal-moment APIs remain over the stated commutative-semiring hypotheses.  The +11 translation extension makes `thm:merged-weighted-binomial-translation` **Exact** over a commutative semiring (left cancellation only for injectivity) and `cor:merged-weighted-binomial-inversion` **Exact** over a commutative ring.  These are finite coefficient identities, not analytic EGF claims. | Bell theorems: `Bell.binomialConv_map`, `Bell.binomialConv_unitSeq`, `Bell.binomialConv_pow`, `Bell.binomialConv_four_swap`, `Bell.binomialConv_pow_mul`. Appell definitions: `Appell.poly`, `Appell.translate`, `Appell.dilate`. Appell theorems: `Appell.poly_eq_binomialConv`, `Appell.coeff_poly`, `Appell.coeff_poly_self`, `Appell.poly_zero`, `Appell.poly_one`, `Appell.natDegree_poly_le`, `Appell.monic_poly`, `Appell.natDegree_poly`, `Appell.derivative_poly`, `Appell.derivative_poly_nsmul`, `Appell.eval_poly`, `Appell.eval_poly_eq_sum`, `Appell.poly_comp`, `Appell.translate_eq_binomialConv_pow`, `Appell.translate_eq_sum`, `Appell.translate_zero`, `Appell.poly_translate`, `Appell.translate_translate`, `Appell.binomialConv_translate`, `Appell.translate_injective`, `Appell.poly_dilate_comp`, `Appell.poly_affine`, `Appell.binomialConv_eval_poly`, `Appell.sum_choose_eval_poly`, `Appell.translate_neg_translate`, `Appell.translate_translate_neg`, `Appell.translate_eq_iff`, `Appell.weighted_binomial_inversion_iff`, `Appell.binomialConv_translate_neg_translate` |
| Deterministic polynomial and integer identity-certificate foundations | `GridEvaluationCertificate.lean` is 0+4: `Fabius.mvPolynomial_eq_of_eval_eq_on_grid`, `Fabius.mvPolynomial_eq_of_eval_eq_on_grid_of_degreeOf_sub_le`, `Fabius.mvPolynomial_eq_of_eval_eq_on_grid_of_degreeOf_le`, and `Fabius.mvPolynomial_grid_eval_injective`. It works over a commutative integral domain with coordinatewise grid-cardinality bounds. `IntegerCRTCertificate.lean` is 0+5: `Fabius.int_prod_dvd_of_pairwise_coprime`, `Fabius.int_eq_zero_of_modEq_zero_of_natAbs_lt_prod`, `Fabius.int_eq_of_modEq_of_natAbs_sub_lt_prod`, `Fabius.int_eq_of_modEq_of_natAbs_add_lt_prod`, and `Fabius.int_eq_of_modEq_of_two_mul_natAbs_lt_prod`. Signed or composite pairwise-coprime moduli and the empty divisibility family are allowed; equality retains strict magnitude bounds. No field, characteristic-zero, primality, reconstruction, or probabilistic claim is made. | Reusable certificate APIs; no manuscript row is independently promoted. |
| Formal power recurrence (`alg:merged-exp-log-power`) | **Exact.** `UnitSeriesPowerRecurrence.lean` is 0+3.  The general denominator-free recurrence works over every commutative ring without assuming `F(0)=1`; its falling-factorial formal-power specialization uses a commutative rational algebra and `F(0)=1`.  No analytic power or branch is asserted.  The live coefficient-calculus register is maintained in its canonical status file rather than duplicated here. | `Fabius.coeff_recurrence_of_mul_derivative_eq`, `Fabius.mul_derivative_fallingSeries_subst_sub_one`, `Fabius.coeff_fallingSeries_subst_sub_one_recurrence` |
| Reciprocal exponential-generating coefficient sequences and the rational-algebra dictionary (exhaustive companion API) | The two-sided unit laws and `complete 0 = unitSeq` hold over every `[CommSemiring R]`.  Over `[CommRing R]`, negated complete-Bell families invert one another and `Bell.reciprocal m := Bell.complete (-Bell.cumulant m)` is defined for every `m`; its normalization, cumulant-negation identity, and first three coefficients need no hypothesis on `m`.  The actual left/right inverse laws, uniqueness, backward recursion, and involution assume exactly `m 0 = 1` (uniqueness also assumes `binomialConv m b = unitSeq R`).  Only the four dictionary declarations require `[Algebra ℚ R]` in addition to `[CommRing R]`: `completeBellPolynomial_eq_complete` is unconditional, while each of the other three assumes that the displayed sequence has zeroth coefficient one.  The `Fabius` namespace there is a generic notation bridge, not a specialization to the Fabius function.  No analytic reciprocal, radius of convergence, limit, probability interpretation, or Fabius-specific coefficient formula is asserted. | Commutative-semiring unit layer: `Bell.binomialConv_unitSeq_right`, `Bell.binomialConv_unitSeq_left`, `Bell.complete_zero_eq_unitSeq`.  Commutative-ring reciprocal layer: `Bell.binomialConv_complete_neg`, `Bell.reciprocal`, `Bell.reciprocal_eq_complete_neg_cumulant`, `Bell.reciprocal_zero`, `Bell.binomialConv_reciprocal`, `Bell.binomialConv_reciprocal_left`, `Bell.eq_reciprocal_of_binomialConv`, `Bell.reciprocal_succ`, `Bell.cumulant_reciprocal`, `Bell.reciprocal_reciprocal`, `Bell.reciprocal_one`, `Bell.reciprocal_two`, `Bell.reciprocal_three`.  Commutative-`ℚ`-algebra dictionary: `Fabius.completeBellPolynomial_eq_complete`, `Fabius.momentCumulant_eq_cumulant`, `Fabius.reciprocal_eq_completeBellPolynomial`, `Fabius.binomialConv_completeBellPolynomial_neg_momentCumulant`. |
| Rvachev raw moments, reciprocal-moment Appell polynomials, and triangular injective polynomial deconvolution | In the current compiled tree, `RvachevMomentAppell` has six public definitions and thirty-three public theorems.  It constructs the full rational raw-moment sequence (the executable even moments and zero odd moments), its formal binomial-convolution reciprocal and complete-Bell description, rational and real monic Appell polynomials of exact degree, and coefficientwise deconvolution as a real linear map.  Deconvolution preserves zero, addition, scalar multiplication, finite sums, and multiplication by a constant polynomial; it sends a monomial and `X^n` to the correspondingly scaled and unscaled Rvachev--Appell polynomial.  It preserves the original top coefficient, exact natural degree, and leading coefficient, has trivial kernel, and is injective both as a linear map and as the underlying operation.  For every bounded Fabius solution, the raw moments cast to the analytic integrals, smoothing the `n`-th Appell polynomial recovers `x^n`, and smoothing the deconvolution of any real polynomial recovers that polynomial.  Reflection invariance of the even density gives the centered `x-y` counterparts `Fabius.integral_eval_rvachevDeconvolvedPolynomial_sub_mul_rvachev` and `Fabius.integral_eval_rvachevAppellPolynomial_sub_mul_rvachev`; `Fabius.integral_eval_rvachevAppellPolynomial_mul_rvachev_eq_zero` says that every positive-degree Appell polynomial has Rvachev mean zero.  The module does not prove an analytic reciprocal-MGF or Appell generating-series identity, a literal differential-operator expansion, reciprocal/deconvolution parity, or the displayed low reciprocal coefficients. | Definitions: `Fabius.rvachevRawMomentRat`, `Fabius.rvachevReciprocalMomentRat`, `Fabius.rvachevAppellPolynomialRat`, `Fabius.rvachevAppellPolynomial`, `Fabius.rvachevDeconvolvedPolynomial`, `Fabius.rvachevDeconvolutionLinearMap`.  Theorems: `Fabius.rvachevRawMomentRat_zero`, `Fabius.rvachevRawMomentRat_even`, `Fabius.rvachevRawMomentRat_odd`, `Fabius.rvachevReciprocalMomentRat_zero`, `Fabius.binomialConv_rvachevRawMomentRat_reciprocal`, `Fabius.rvachevReciprocalMomentRat_eq_completeBellPolynomial`, `Fabius.monic_rvachevAppellPolynomialRat`, `Fabius.natDegree_rvachevAppellPolynomialRat`, `Fabius.rvachevAppellPolynomial_eq_poly_cast`, `Fabius.monic_rvachevAppellPolynomial`, `Fabius.natDegree_rvachevAppellPolynomial`, `Fabius.eval_rvachevAppellPolynomial_add`, `Fabius.integral_pow_mul_rvachev_eq_rvachevRawMomentRat_cast`, `Fabius.integral_eval_rvachevAppellPolynomial_add_mul_rvachev`, `Fabius.rvachevDeconvolutionLinearMap_apply`, `Fabius.rvachevDeconvolvedPolynomial_zero`, `Fabius.rvachevDeconvolvedPolynomial_add`, `Fabius.rvachevDeconvolvedPolynomial_smul`, `Fabius.rvachevDeconvolvedPolynomial_finsetSum`, `Fabius.rvachevDeconvolvedPolynomial_C_mul`, `Fabius.rvachevDeconvolvedPolynomial_monomial`, `Fabius.rvachevDeconvolvedPolynomial_X_pow`, `Fabius.coeff_rvachevDeconvolvedPolynomial_natDegree`, `Fabius.natDegree_rvachevDeconvolvedPolynomial_le`, `Fabius.natDegree_rvachevDeconvolvedPolynomial`, `Fabius.leadingCoeff_rvachevDeconvolvedPolynomial`, `Fabius.rvachevDeconvolvedPolynomial_eq_zero_iff`, `Fabius.rvachevDeconvolutionLinearMap_injective`, `Fabius.rvachevDeconvolvedPolynomial_injective`, `Fabius.integral_eval_rvachevDeconvolvedPolynomial_add_mul_rvachev`, `Fabius.integral_eval_rvachevDeconvolvedPolynomial_sub_mul_rvachev`, `Fabius.integral_eval_rvachevAppellPolynomial_sub_mul_rvachev`, `Fabius.integral_eval_rvachevAppellPolynomial_mul_rvachev_eq_zero` |
| Nonmonic Hensel lifting and formal implicit roots (reusable foundation) | In an `I`-adically complete commutative ring, any polynomial---not assumed monic---with an approximate root modulo `I` and derivative a unit modulo `I` has a unique exact root in that residue class.  For every commutative ring `R`, if `constantCoeff (P.eval S₀) = 0` and `constantCoeff (P.derivative.eval S₀)` is a unit, the specialization to `R⟦X⟧` gives a unique root with scalar constant coefficient `constantCoeff S₀`; in particular, a polynomial whose parameter-constant constant coefficient vanishes and whose parameter-constant linear coefficient is a unit has a distinguished unique zero-constant formal root.  No field, domain, characteristic-zero, Noetherian, finite-generation, degree, monicity, or analytic-convergence hypothesis is used.  This module does not define or instantiate the dyadic Fabius jet polynomials, prove a coefficient recursion or analytic realization, enter a finite-prefix plateau, identify a flat defect, or prove an inverse/quantile result. | `FormalImplicitRoot.exists_isRoot_sub_mem`, `FormalImplicitRoot.eq_of_isRoot_of_sub_mem`, `FormalImplicitRoot.existsUnique_isRoot_sub_mem`, `PowerSeries.Implicit.existsUnique_isRoot_constantCoeff`, `PowerSeries.Implicit.existsUnique_zeroConstant_root`, `PowerSeries.Implicit.root`, `PowerSeries.Implicit.constantCoeff_root`, `PowerSeries.Implicit.eval_root`, `PowerSeries.Implicit.eq_root` |
| Division-free quadratic compositional inversion (reusable Catalan foundation) | Over every commutative ring, the signed Catalan series solves its quadratic functional equation and gives the unique zero-constant solution of `S + c S² = X`.  The API exposes every coefficient, the first four anchors, uniqueness from the functional equation, and an equivalent scalar convolution recurrence.  No square root, field, topology, characteristic hypothesis, abstract `PowerSeries.substInv` identification, or Fabius inverse-germ specialization is claimed. | `QuadraticInverse.signedCatalanSeries`, `QuadraticInverse.coeff_signedCatalanSeries`, `QuadraticInverse.sum_antidiagonal_signedCatalan`, `QuadraticInverse.coeff_signedCatalanSeries_sq`, `QuadraticInverse.signedCatalanSeries_functionalEquation`, `QuadraticInverse.inverse`, `QuadraticInverse.constantCoeff_inverse`, `QuadraticInverse.coeff_succ_inverse`, `QuadraticInverse.inverse_functionalEquation`, `QuadraticInverse.coeff_one_inverse`, `QuadraticInverse.coeff_two_inverse`, `QuadraticInverse.coeff_three_inverse`, `QuadraticInverse.coeff_four_inverse`, `QuadraticInverse.coeff_add_C_mul_sq`, `QuadraticInverse.eq_of_functionalEquation`, `QuadraticInverse.eq_inverse`, `QuadraticInverse.coeff_succ_of_functionalEquation`, `QuadraticInverse.coeff_succ_of_functionalEquation'`, `QuadraticInverse.coeff_of_functionalEquation`, `QuadraticInverse.seq_succ_of_convolution` |
| Quarter Catalan formal germ and dyadic-rescaling specialization | The module's exhaustive public surface is two definitions and thirteen theorems.  `Fabius.quarterCatalanCoefficient` and `Fabius.quarterCatalanGermSeries` give the explicit rational Catalan coefficients and the unique zero-constant solution of `D + 4D² = (4/9)X`; the coefficient, constant-term, functional-equation, injectivity, characterization, and existence-uniqueness theorems are all formalized.  The distinguished rational dyadic germ satisfies the same equation; rescaling its parameter by `9/4` identifies it exactly with the unique Catalan inverse of `X + 4X²`, with reverse rescaling and coefficient formula `(4/9)^(m+1)(-4)^m C_m`.  This closes the formal comparison between the explicit quarter series, the implicit dyadic germ, and the reusable quadratic inverse.  The module remains formal power-series algebra: the actual smooth inverse is connected only by the separate downstream `FabiusInverseQuarterJet` module, whose results identify derivative jets rather than functions on a neighborhood. | `Fabius.quarterCatalanCoefficient`, `Fabius.quarterCatalanCoefficient_zero`, `Fabius.quarterCatalanCoefficient_succ_eq_report`, `Fabius.quarterCatalanGermSeries`, `Fabius.quarterCatalanGermSeries_coeff`, `Fabius.quarterCatalanGermSeries_coeff_succ`, `Fabius.quarterCatalanGermSeries_constantCoeff`, `Fabius.quarterCatalanGermSeries_equation`, `Fabius.powerSeries_quadratic_injectiveOn_zeroConstant`, `Fabius.eq_quarterCatalanGermSeries_of_equation`, `Fabius.existsUnique_quarterCatalanGermSeries`, `Fabius.dyadicGermTwo_functionalEquation`, `Fabius.rescale_dyadicGermTwo_eq_quadraticInverse`, `Fabius.dyadicGermTwo_eq_rescale_quadraticInverse`, `Fabius.coeff_dyadicGermTwo_succ` |
| Actual Fabius inverse quarter jet | For every bounded Fabius solution, all derivatives of the centered inverse at `5/72 = F(1/4)` are the factorial-scaled coefficients of `QuadraticInverse.inverse 4`; hence `G^(m+1)(5/72) = (m+1)!(-4)^m C_m`.  The proof is smooth rather than analytic: it transfers the exact forward quarter jet through Faà di Bruno and the inverse identity, then uses the quadratic Catalan convolution.  It asserts equality of jets only, not equality on a neighborhood or a named nonzero flat-remainder decomposition. | `Fabius.iteratedDeriv_centeredFabiusInv_quarter_eq_quadraticInverse`, `Fabius.iteratedDeriv_fabiusInv_five_seventy_two_succ` |
| Finite polynomial integrals from moments and formal cumulants | Proved for an arbitrary measure and a polynomial over any semiring mapped to `ℝ`. Integrability is required only for powers indexed by the polynomial support. The raw-moment expansion has an exact Bell/cumulant rewrite with the necessary constant correction `φ(p.coeff 0) * (moment Z 0 μ - 1)`; the correction vanishes under the sole normalization `moment Z 0 μ = 1`, in particular for probability measures. No infinite expansion, independence, boundedness, or analytic remainder is asserted. | `Fabius.integral_eval₂_eq_sum_moment`, `Fabius.integral_eval₂_eq_sum_completeBell_momentCumulant_with_mass_correction`, `Fabius.integral_eval₂_eq_sum_completeBell_momentCumulant_of_moment_zero_eq_one`, `Fabius.integral_eval₂_eq_sum_completeBell_momentCumulant` |
| Full formal parity of centered Rvachev moments and cumulants | The compressed even series is expanded to a full exponential generating series: even moments, logarithmic coefficients, and cumulants recover the established compressed families, while every odd one vanishes.  The parity argument is also exposed generically: an odd-free coefficient family has an odd-free formal logarithm and odd-free moment cumulants.  Combined with the formal hyperbolic-sine logarithm, every centered cumulant at positive even order has its Bernoulli--Mersenne formula, and the associated finite moment recurrence holds at every positive index without a coefficient-identification premise.  This is coefficientwise formal algebra, with no analytic logarithm, convergence, branch choice, or newly asserted probabilistic model. | `Fabius.SaddleExpansion.logCoeff_eq_zero_of_odd_coeff`, `Fabius.momentCumulant_eq_zero_of_odd_moments`, `Fabius.centeredRvachevFullMoment_even`, `Fabius.centeredRvachevFullMoment_odd`, `Fabius.centeredRvachevFullLogCoefficient_even`, `Fabius.centeredRvachevFullLogCoefficient_odd`, `Fabius.centeredRvachevFullCumulant_even`, `Fabius.centeredRvachevFullCumulant_odd`, `Fabius.centeredRvachevEvenCumulant_eq_bernoulliMersenne`, `Fabius.moment_bernoulliMersenne_recurrence` |
| Principal specialization of complete homogeneous polynomials on geometric nodes | Proved over every commutative semiring: the complete homogeneous polynomial of degree `r` on `1,q,...,q^p` is both complementary presentations of the denominator-free Gaussian coefficient, `[p+r choose p]_q = [p+r choose r]_q`.  Comparing the two principal-specialization orientations gives a second proof of full reflection symmetry `[n choose n-k]_q = [n choose k]_q` whenever `k ≤ n`, without division, cancellation, a nonzero denominator, or any restriction on `q`; the canonical recursive proof is separately exposed by `FiniteQBinomialCore`.  Under finite-node injectivity, the shared Lagrange residual theorem therefore gives every omitted geometric moment; over `ℚ` with `0 < q < 1`, this agrees with the quotient-defined q-binomial formula.  This is finite algebra and supplies no spectral-tail expansion, convergence theorem, sign estimate, or remainder bound. | `Fabius.completeHomogeneousEvalOn_range_pow_eq_gaussianBinomial`, `Fabius.completeHomogeneousEvalOn_range_pow_eq_gaussianBinomial_degree`, `Fabius.gaussianBinomial_add_symm`, `Fabius.gaussianBinomial_symm_via_completeHomogeneous`, `Fabius.completeHomogeneousEvalOn_geometric_range`, `Fabius.sum_geometricLagrangeWeight_mul_pow_succ_add_eq_gaussianBinomial`, `Fabius.geometricLagrangeQMoment_eq_residual_gaussianBinomial`, `Fabius.completeHomogeneousEvalOn_geometric_range_eq_qBinomial`, `Fabius.geometricLagrangeQMoment_eq_residual_qBinomial_via_completeHomogeneous`, `Fabius.qBinomial_symm` |
| Formal Catalan--Gaussian quarter Richardson filter | Proved coefficientwise in `ℚ[[Q]]`.  The generic geometric rescaling filter multiplies each source coefficient by the exact geometric Lagrange moment.  At `q = 1/4`, every coefficient through degree `p` vanishes, every higher coefficient has an explicit Catalan times Gaussian q-binomial form, and degree `p+1` has the displayed closed formula.  No theorem identifies this formal series with finite-prefix quantiles, proves convergence, or supplies an analytic acceleration or error bound. | `Fabius.finiteRescaleFilter_coeff`, `Fabius.geometricRichardsonPowerSeriesFilter_coeff`, `Fabius.geometricRichardsonPowerSeriesFilter_coeff_eq_qBinomial`, `Fabius.quarterCatalanRichardsonFilter_coeff_eq_zero_of_le`, `Fabius.quarterCatalanRichardsonFilter_coeff_eq_qBinomial`, `Fabius.quarterCatalanRichardsonFilter_firstUncancelled_coeff` |
| Exact lower-Lambert phase locking and reciprocal Richardson rows | For `lambda > (log 2)⁻¹`, the lower-Lambert phase at the sampled node indexed by `j` is exactly `lambda+j`; consequently every one-periodic factor is preserved pointwise, and every real-valued one is preserved by the mass-one reciprocal-node Lagrange row.  The row has explicit factorial/binomial weights, cancels inverse powers `1,...,r`, and has complete-homogeneous formulas for every residual moment and the first omitted one.  These declarations are the finite algebraic layer.  The downstream `LambertPhaseLockedBell`, `LambertReciprocalAsymptotics`, `FabiusLambertPhaseLockedPullback`, `FabiusLambertPhaseExtraction`, and `FabiusLambertPhaseExtractionBell` modules prove the Bell/generalized-harmonic closed forms, fixed-order analytic extraction, and weighted remainder estimates recorded in the phase-extraction row below; no convergence of the literal infinite residual series or uniformity for growing row order or residual depth is claimed. | `Fabius.fabiusLambertPhase_phaseLockedNode`, `Fabius.Periodic.apply_fabiusLambertPhase_phaseLockedNode`, `Fabius.shiftedReciprocalLagrangeWeight_eq_choose`, `Fabius.sum_shiftedReciprocalLagrangeWeight_eq_one`, `Fabius.sum_shiftedReciprocalLagrangeWeight_mul_periodicPhaseLocked`, `Fabius.sum_shiftedReciprocalLagrangeWeight_mul_invPow_eq_zero`, `Fabius.sum_shiftedReciprocalLagrangeWeight_residual`, `Fabius.sum_shiftedReciprocalLagrangeWeight_firstOmitted` |
| Finite `q`-Pochhammer residue-class dissections (foundational engine) | `QPochhammerDissection.lean` has zero public definitions and exactly two public theorems.  Both are valid over an arbitrary `[CommRing R]`, including the zero ring, for arbitrary `a`, `q`, `r`, and `n`, with no division, cancellation, nonvanishing, contraction, or positivity hypothesis.  The exact-multiple formula `(a;q)_(r*n) = product_(s<r) (a*q^s;q^r)_n` is total even at `r = 0`.  The remainder formula assumes exactly `u <= r`; the first `u` residue classes have length `n+1`, the rest have length `n`, and both `u = 0` and `u = r` are included. | `Fabius.finiteQPochhammerIn_dissection`, `Fabius.finiteQPochhammerIn_dissection_remainder` |
| General infinite `q`-Pochhammer convergence, dissection, zero set, parameter regularity, and simple-zero derivatives (foundational engine) | `QPochhammerInfinite.lean` has one public definition and exactly 29 public theorems.  The total definition and its defining-product equality require only `[CommRing R] [TopologicalSpace R]`; absent convergence, `tprod` has Mathlib's junk-value convention.  The three elementary norm lemmas use exactly `[NormedRing R] [NormOneClass R]` and the displayed strict norm bound.  Finite self-product nonvanishing uses `[NormedCommRing R] [NormOneClass R] [NoZeroDivisors R]`, `‖q‖ < 1`, and no completeness.  The seven convergence/factorization theorems use `[NormedCommRing R] [NormOneClass R] [CompleteSpace R]` with `‖q‖ < 1`; only infinite dissection also assumes `0 < r`.  The three exact zero-set results add `[NormMulClass R]`, with `qPochhammerInfIn_ne_zero` additionally assuming exactly `forall j, a*q^j != 1`.  Over `[NormedField 𝕜]`, the scaled-product identity and factor Big-O estimate are unconditional, scale summability assumes `‖q‖ < 1`, and finite-symbol differentiability instead needs exactly `[NontriviallyNormedField 𝕜]`.  Over `[NormedField 𝕜] [CompleteSpace 𝕜]`, the inverse-power zero classification assumes `q != 0` and `‖q‖ < 1`; local uniform convergence and continuity assume `‖q‖ < 1` plus `[LocallyCompactSpace 𝕜]`.  The finite cofactor identity needs only `[Field 𝕜]` and `q != 0`.  Over `ℂ`, global differentiability assumes only `‖q‖ < 1`; the derivative and its nonvanishing at a displayed factor zero additionally assume exactly `a0*q^j = 1` and include `q = 0`, the inverse-power derivative formula and its coefficient nonvanishing additionally assume `q != 0`, and every zero has analytic order one under the strict contraction.  These are regularity statements in `a`, not joint regularity or continuation in `q`. | Definition: `Fabius.qPochhammerInfIn`.  Theorems: `Fabius.qPochhammerInfIn_eq_tprod`, `Fabius.summable_norm_mul_pow`, `Fabius.one_sub_ne_zero_of_norm_lt_one`, `Fabius.norm_mul_pow_self_lt_one`, `Fabius.finiteQPochhammerIn_self_ne_zero`, `Fabius.multipliable_one_sub_mul_pow_of_norm_lt_one`, `Fabius.hasProd_qPochhammerInfIn`, `Fabius.tendsto_finiteQPochhammerIn_qPochhammerInfIn`, `Fabius.qPochhammerInfIn_eq_finite_mul_shift`, `Fabius.qPochhammerInfIn_succ_shift`, `Fabius.qPochhammerInfIn_eq_factor_mul`, `Fabius.qPochhammerInfIn_dissection`, `Fabius.qPochhammerInfIn_ne_zero`, `Fabius.qPochhammerInfIn_eq_zero_iff`, `Fabius.qPochhammerInfIn_self_ne_zero`, `Fabius.qPochhammerInfIn_eq_tprod_smul`, `Fabius.summable_norm_pow_of_norm_lt_one`, `Fabius.isBigO_one_sub_sub_one`, `Fabius.differentiable_finiteQPochhammerIn`, `Fabius.qPochhammerInfIn_eq_zero_iff_exists_inv_pow`, `Fabius.hasProdLocallyUniformly_qPochhammerInfIn`, `Fabius.continuous_qPochhammerInfIn`, `Fabius.pow_sq_mul_finiteQPochhammerIn_inv_pow_self`, `Fabius.differentiable_qPochhammerInfIn`, `Fabius.hasDerivAt_qPochhammerInfIn_of_mul_pow_eq_one`, `Fabius.hasDerivAt_qPochhammerInfIn_inv_pow`, `Fabius.deriv_qPochhammerInfIn_inv_pow_ne_zero`, `Fabius.deriv_qPochhammerInfIn_ne_zero_of_mul_pow_eq_one`, `Fabius.analyticOrderAt_qPochhammerInfIn_of_eq_zero` |
| Division-free polynomial Jackson derivative (foundational engine) | `PolynomialQDerivative.lean` has two definitions and exactly 17 theorems.  `qInt` and its zero, one, successor, addition, and `q=1` laws need only a semiring; `(1-q)[n]_q=1-q^n` needs a commutative ring.  `qDerivative` is coefficientwise and linear over every commutative semiring; its application, monomial, constant, power, scaling, and both product rules are valid there for arbitrary `q`.  The difference identity `(D_q p)(x)((1-q)x)=p(x)-p(qx)` uses a commutative ring and never divides.  Thus `q=0` and `q=1` are included.  No analytic-function derivative, quotient statement, topology, or `q→1` limit is present. | Definitions: `Fabius.qInt`, `Fabius.qDerivative`.  Theorems: `Fabius.qInt_zero`, `Fabius.qInt_one`, `Fabius.qInt_succ`, `Fabius.qInt_succ'`, `Fabius.qInt_add`, `Fabius.qInt_one_left`, `Fabius.one_sub_mul_qInt`, `Fabius.qDerivative_apply`, `Fabius.qDerivative_monomial`, `Fabius.qDerivative_C`, `Fabius.qDerivative_X_pow`, `Fabius.qDerivative_X`, `Fabius.qDerivative_C_mul_X_pow`, `Fabius.eval_qDerivative_mul`, `Fabius.qDerivative_comp_C_mul_X`, `Fabius.qDerivative_mul`, `Fabius.qDerivative_mul'` |
| Iterated polynomial `q`-Leibniz rule (foundational engine) | `PolynomialQLeibniz.lean` has no public definitions and exactly four theorems.  Over every commutative semiring, for arbitrary scalars and every natural order including zero, scaling substitutions compose, `D_q` is linear over constants, its iterates obey the scaled chain law, and `D_q^n(fg)` is the finite Gaussian-coefficient Leibniz sum with `(D_q^k f)` evaluated at `q^(n-k)·`.  No division, nonzero-nome, topology, order, or convergence hypothesis occurs. | `Fabius.comp_C_mul_X_comp_C_mul_X`, `Fabius.qDerivative_C_mul`, `Fabius.qDerivative_iterate_comp_C_mul_X`, `Fabius.qDerivative_iterate_mul` |
| Finite `q`-Pochhammer parameter derivatives (foundational engine) | `QPochhammerDerivative.lean` has no public definitions and exactly three theorems, all over an arbitrary nontrivially normed field.  The cofactor-sum derivative holds for arbitrary `q,a,n`.  Its logarithmic rewrite assumes exactly `∀ j ∈ range n, 1-a*q^j ≠ 0`; the chain-rule form assumes exactly a pointwise `HasDerivAt f f' x`.  The empty product case is included.  This leaf states no infinite-product derivative, logarithm branch, multiplicity, or joint-parameter theorem. | `Fabius.hasDerivAt_finiteQPochhammerIn`, `Fabius.hasDerivAt_finiteQPochhammerIn_of_ne_zero`, `Fabius.hasDerivAt_finiteQPochhammerIn_comp` |
| Complex Lambert-series logarithm of `(a;q)_∞` | `LambertSeriesLog.lean` has no public definitions and exactly four theorems.  The natural-cast norm helper is unconditional.  The summability, principal-factor-log `HasSum`, and branch-free exponential product identity assume exactly complex `a,q` with `‖a‖<1` and `‖q‖<1`, including both zero values.  No norm-one boundary, `Complex.log` identity for the product as a whole, analytic continuation, or termwise derivative is claimed. | `Fabius.one_le_norm_natCast_add_one`, `Fabius.summable_lambert_series`, `Fabius.hasSum_lambert_log_complex`, `Fabius.exp_neg_tsum_lambert_eq_qPochhammerInfIn` |
| Real `q`-gamma recurrence, reflection product, and theta form | `QGamma.lean` has two definitions and exactly ten theorems.  `qGamma` and `qNumber` are total real definitions.  Product positivity, gamma positivity, `Γ_q(1)=1`, the functional equation, and the natural factorial product use `0<q<1`; positivity and the functional equation additionally assume `x>0`.  `qNumber_natCast` assumes only `q≠1`.  The reflection-product equality is totalized field algebra for every real `x` under only `q<1`; its bilateral theta `HasSum` form assumes `0<q<1` and `0<x<1`.  No complex continuation, pole theorem, classical-gamma limit, log-convexity/uniqueness, or digamma layer is present. | Definitions: `Fabius.qGamma`, `Fabius.qNumber`.  Theorems: `Fabius.norm_lt_one_of_pos_of_lt_one`, `Fabius.qPochhammerInfIn_rpow_pos`, `Fabius.qPochhammerInfIn_self_pos`, `Fabius.qGamma_pos`, `Fabius.qGamma_one`, `Fabius.qGamma_add_one`, `Fabius.qGamma_nat_succ`, `Fabius.qNumber_natCast`, `Fabius.qGamma_mul_qGamma_one_sub`, `Fabius.hasSum_theta_qGamma_reflection` |

| Gaussian-polynomial continuity and specialization at `q = 1` | `GaussianBinomialContinuity.lean` retains its complete 0+3 theorem inventory: polynomial continuity over a topological semiring, convergence to the ordinary binomial coefficient as `q → 1`, and the finite-q-Pochhammer quotient formula over a field under its stated nonvanishing hypotheses. | `Fabius.continuous_gaussianBinomial`, `Fabius.tendsto_gaussianBinomial_nhds_one`, `Fabius.gaussianBinomial_eq_finiteQPochhammerIn_div` |
| Gaussian-polynomial cumulants, second derivative, and cleared moments at `q = 1` | `GaussianBinomialCumulants.lean` has an exhaustive 2+24 public surface.  Its newest three theorems are exact at their displayed boundaries: `eval_one_derivative_derivative_gaussianBinomial_X` gives the explicit second derivative over a characteristic-zero field under exactly `k ≤ n`; `twelve_mul_secondMoment_gaussianBinomial_eval_one` and `twelve_mul_varianceNumerator_gaussianBinomial_eval_one` give the denominator-cleared raw second moment and variance numerator over every commutative semiring for all natural `n,k`, including `n < k`, where zero extension makes both sides vanish.  The cleared forms require no division, nonvanishing, or characteristic hypothesis. | Definitions: `Fabius.meanAtOne`, `Fabius.varAtOne`.  Theorems: `Fabius.meanAtOne_one`, `Fabius.varAtOne_one`, `Fabius.meanAtOne_mul`, `Fabius.varAtOne_mul`, `Fabius.meanAtOne_prod`, `Fabius.varAtOne_prod`, `Fabius.eval_one_derivative_X_pow`, `Fabius.eval_one_derivative_derivative_X_pow`, `Fabius.eval_one_qInt_X`, `Fabius.eval_one_derivative_qInt_X`, `Fabius.eval_one_derivative_derivative_qInt_X`, `Fabius.meanAtOne_qInt_X`, `Fabius.varAtOne_qInt_X`, `Fabius.one_sub_X_pow_succ_eq`, `Fabius.gaussianBinomial_X_mul_prod_qInt`, `Fabius.eval_one_gaussianBinomial_X`, `Fabius.sum_mean_diff`, `Fabius.sum_var_diff`, `Fabius.meanAtOne_gaussianBinomial_X`, `Fabius.varAtOne_gaussianBinomial_X`, `Fabius.eval_one_derivative_gaussianBinomial_X`, `Fabius.eval_one_derivative_derivative_gaussianBinomial_X`, `Fabius.twelve_mul_secondMoment_gaussianBinomial_eval_one`, `Fabius.twelve_mul_varianceNumerator_gaussianBinomial_eval_one` |
| Generic Gaussian-polynomial palindromicity, mean, and linear coefficient | `GaussianBinomialPalindromic.lean` has an exhaustive 0+14 inventory over a general commutative semiring.  Besides reflection, degree, boundary, coefficient-reversal, and mean results, `coeff_gaussianBinomial_one_of_pos_of_lt` proves the linear coefficient is one under exactly `0 < k` and `k < n`; the total `coeff_gaussianBinomial_one` theorem returns `if 0 < k ∧ k < n then 1 else 0`, covering `k = 0`, `k = n`, and `n < k`.  These two results need no nontriviality assumption; exact degree and monicity do. | `Fabius.reflect_add_of_natDegree_le`, `Fabius.reflect_one'`, `Fabius.gaussianBinomial_natDegree_le`, `Fabius.gaussianBinomial_zero_left`, `Fabius.gaussianBinomial_diag'`, `Fabius.reflect_gaussianBinomial`, `Fabius.coeff_gaussianBinomial_reflect`, `Fabius.coeff_gaussianBinomial_zero`, `Fabius.coeff_gaussianBinomial_top`, `Fabius.gaussianBinomial_natDegree`, `Fabius.gaussianBinomial_monic`, `Fabius.two_mul_derivative_gaussianBinomial_eval_one`, `Fabius.coeff_gaussianBinomial_one_of_pos_of_lt`, `Fabius.coeff_gaussianBinomial_one` |
| Universal Gaussian-polynomial structure | `GaussianBinomialPolynomialStructure.lean` retains its exhaustive 0+5 inventory: exact degree, monicity, constant coefficient one, exact reflection, and bounded-index coefficient symmetry for the natural-coefficient universal polynomial under `k ≤ n`.  The adjacent general-semiring `coeff_gaussianBinomial_one` theorem now supplies the complete linear-coefficient clause, including every boundary case. | `Fabius.natDegree_gaussianBinomial_universal`, `Fabius.gaussianBinomial_universal_monic`, `Fabius.coeff_zero_gaussianBinomial_universal`, `Fabius.gaussianBinomial_universal_reflect`, `Fabius.coeff_gaussianBinomial_universal_symm` |
| Central squared-base Gaussian reduction | `CentralQBinomialReduction.lean` has no definitions and exactly six theorems: sign pairing, even/odd dissection, finite-symbol naturality, the integral-polynomial certificate, the division-free commutative-ring central reduction, and its field quotient wrapper with the two displayed nonvanishing denominators. | `Fabius.finiteQPochhammerIn_mul_neg`, `Fabius.finiteQPochhammerIn_two_mul`, `Fabius.finiteQPochhammerIn_map_ringHom`, `Fabius.central_gaussianBinomial_sq_mul_int`, `Fabius.central_gaussianBinomial_sq_mul`, `Fabius.central_gaussianBinomial_sq_div` |
| Regular central q-binomial sum (`thm:regular-central-sum`) | **Exact.**  `RegularCentralQBinomialSum.lean` has exactly two definitions and one theorem.  For real `0<q<1` and complex `alpha`, it proves the displayed series as an actual `HasSum` to `qGammaC (q^2) (3/2) * qGammaC (q^2) ((alpha+1)/2) / qGammaC (q^2) ((alpha+2)/2)`, under exactly `qPochhammerInfIn (q^(alpha+1)) (q^2) ≠ 0`.  This condition is the simultaneous nonvanishing condition for the generalized q-numbers in the summand.  It includes even negative integral `alpha`, where the totalized q-Gamma quotient and its product form vanish.  No classical `q→1⁻` limit or classical hypergeometric corollary is asserted. | `Fabius.qNumberC`, `Fabius.regularCentralQBinomialTerm`, `Fabius.hasSum_regularCentralQBinomial` |
| Cyclotomic q-factorizations | `CyclotomicFactorization.lean` has no definitions and exactly seven theorems: the two floor-quotient bounds, the divisor-index exchange, the commutative-ring factorization of `(X;X)_n`, its Gaussian factorial bridge, interval-product extension, and the exact Gaussian cyclotomic product over an integral domain. | `Fabius.div_add_div_le_div`, `Fabius.div_le_div_add_div_add_one`, `Fabius.mem_range_and_mem_divisors_iff`, `Fabius.finiteQPochhammerIn_X_eq_prod_cyclotomic`, `Fabius.finiteQPochhammerIn_X_eq_gaussianBinomial_mul`, `Fabius.prod_cyclotomic_pow_div_extend`, `Fabius.gaussianBinomial_X_eq_prod_cyclotomic` |
| Jacobi triple product and Euler pentagonal theorem | `JacobiTripleProduct.lean` retains its 2-definition/25-theorem inventory, from the integer theta-exponent arithmetic and finite triple-product identities through the bilateral Jacobi `HasSum` forms and the pentagonal and paired-pentagonal `HasSum` corollaries. | Key terminal results: `Fabius.finite_triple_product_poly`, `Fabius.finite_triple_product`, `Fabius.hasSum_jacobi_triple_product`, `Fabius.hasSum_jacobi_triple_product'`, `Fabius.hasSum_pentagonal`, `Fabius.hasSum_pentagonal_pairs` |
| Infinite q-binomial and reciprocal Euler sums | `QBinomialTheoremInfinite.lean` now has a 1-definition/27-theorem q-facing inventory: the retained comparison, positivity, uniform Gaussian, Euler-product, q-binomial, and reciprocal-Euler results plus the five effective fixed-column names recorded in the foundational row. | Key terminal results: `Fabius.tendsto_gaussianBinomial_atTop`, `Fabius.tendsto_gaussianBinomial_add_const_atTop`, `Fabius.isBigO_gaussianBinomial_sub_inv`, `Fabius.isBigO_gaussianBinomial_add_sub_inv`, `Fabius.hasSum_euler_product`, `Fabius.hasSum_qBinomial_theorem`, `Fabius.hasSum_euler_reciprocal` |
| Effective fixed-column Gaussian limit and geometric rates (`thm:fixed-column-limit`) | **Exact.**  `GaussianBinomialFixedColumnRate.lean` is now 0+9 after the unprimed exponential bound and shifted-limit ownership move to `QBinomialTheoremInfinite`.  Its ring and field hypotheses and fixed/shifted relative/additive rate conclusions are unchanged. | `Fabius.norm_finiteQPochhammerIn_pow_sub_one_le_exp'`, `Fabius.norm_finiteQPochhammerIn_pow_sub_one_le`, `Fabius.norm_finiteQPochhammerIn_self_mul_gaussianBinomial_sub_one_le`, `Fabius.norm_gaussianBinomial_sub_inv_finiteQPochhammerIn_le`, `Fabius.norm_gaussianBinomial_add_sub_inv_finiteQPochhammerIn_le`, `Fabius.gaussianBinomial_fixedColumn_relativeError_isBigO`, `Fabius.gaussianBinomial_shifted_fixedColumn_relativeError_isBigO`, `Fabius.gaussianBinomial_fixedColumn_error_isBigO`, `Fabius.gaussianBinomial_shifted_fixedColumn_error_isBigO` |
| Weighted q-Pascal summation | `QPascalSummation.lean` retains its complete 0+4 theorem inventory over the stated semiring and commutation hypotheses. | `Fabius.sum_gaussianBinomial_succ_mul`, `Fabius.sum_gaussianBinomial_succ_mul'`, `Commute.gaussianBinomial_left`, `Commute.gaussianBinomial_right` |
| Quantum-plane binomial theorem | `QuantumBinomial.lean` retains its complete 0+2 theorem inventory for q-commuting variables in an associative algebra. | `Fabius.quantumPlane_mul_pow`, `Fabius.quantum_binomial` |
| Rogers--Szegő polynomial recurrences and generating function | `RogersSzegoPolynomial.lean` retains its 1-definition/9-theorem inventory: zero, row-sum, successor, dilation, and three-term laws; the Gaussian successor-factor identity; the required summability and Euler antidiagonal convolution; and the generating `HasSum`. | `Fabius.rogersSzego_zero`, `Fabius.rogersSzego_one`, `Fabius.rogersSzego_succ`, `Fabius.one_sub_pow_succ_mul_gaussianBinomial_succ_succ`, `Fabius.rogersSzego_dilation`, `Fabius.rogersSzego_three_term`, `Fabius.summable_norm_pow_div_finiteQPochhammerIn_self`, `Fabius.sum_antidiagonal_euler_mul_euler`, `Fabius.hasSum_rogersSzego_generating` |

The frontier-facing rows above deliberately separate finite or formal
identities from their analytic uses.  In particular, the finite quarter cell
is not a construction of `G_n`; the product shell is not a new
Fourier-integral theorem; the odd DFT is not an aliasing/error theorem; and the
q-binomial and Catalan rows do not close their spectral-tail or convergence
frontiers.  The downstream Lambert row does close fixed-order phase extraction
and saddle remainders, subject to the limitations stated there.

The q-series union above retains the exhaustive `QPochhammerEntire` 0+5,
`GeometricPochhammerNormalConvergence` 0+3,
`QPochhammerDissection` 0+2, `QPochhammerInfinite` 1+29,
`GaussianBinomialAtNegOneDerivative` 0+5,
`GaussianBinomialContinuity` 0+3,
`GaussianBinomialPalindromic` 0+14,
`GaussianBinomialPolynomialStructure` 0+5,
`CentralQBinomialReduction` 0+6, `CyclotomicFactorization` 0+7,
`PrimitiveRootBlock` 0+3, `QLucas` 0+7,
`CyclotomicDivisibility` 0+3, `QCatalan` 1+11,
`JacobiTripleProduct` 2+25,
`QBinomialTheoremInfinite` 1+27, `GaussianBinomialFixedColumnRate` 0+9,
`GaussianBinomialGreaterOneAsymptotics` 0+2,
`QPascalSummation` 0+4,
`QuantumBinomial` 0+2, `RogersSzegoPolynomial` 1+9,
`QPochhammerInfiniteBounds` 0+5, `HeineTransformation` 2+5,
`QGaussSummation` 0+2, `QPochhammerComplexOrder` 1+4,
`BasicHypergeometricSeries` 2+5, `TwoPhiOneReversal` 2+12,
`QChuVandermonde` 0+10, `JacobiTwoSquareCount` 0+4,
`GeometricRichardsonGenerating` 3+7, and `QMultinomial` 1+9 inventories.  In
particular, the fixed-nome row includes the reciprocal-power zero-lattice
theorem `complexQPochhammerInf_eq_zero_iff_eq_inv_pow`.

The total complex finite-product row exhausts `ThueMorseComplexProductBridge.lean`:
exactly two definitions and eighteen theorems.
The adjacent finite uniform-digit row exhausts
`UniformDigitThueMorseBridge.lean`: exactly three theorems.

The three current Gamma-jet/tower rows are exhaustive public-API inventories.
`ReciprocalGammaJets.lean` has exactly five theorems.
`ThueMorseGammaTower.lean` has two definitions and seven theorems in the current
integrated tree; at pinned commit `0ba35abd4` it had the same two definitions and six
theorems, before the master-product ratio follow-up.
`ThueMorseGammaTowerDifferential.lean` has no definitions and three theorems.

The Volterra rows above, together with the sharp kernel-integrability and
polynomial-specialization declarations
`Fabius.intervalIntegrable_normalizedVolterraKernel_add`,
`Fabius.normalizedVolterra_succ_polynomial_of_taylor_support_kernel_intervalIntegrable`,
`Fabius.normalizedVolterra_succ_polynomial_of_kernel_intervalIntegrable`,
`Fabius.normalizedVolterra_polynomial_mul_extendedFabius`,
`Fabius.normalizedVolterra_polynomial_mul_fabiusReal_of_le_one`, and
`Fabius.integral_cube_mul_fabiusReal_eq`, are an exhaustive public-API inventory
for the displayed modules.  `NormalizedVolterra.lean` and
`FabiusAntiderivatives.lean` have exactly three definitions and 47 theorems; the
private scaled-candidate implementation in the Fabius specialization is
intentionally not counted.  The four fractional Volterra rows exhaust the public API of
`FractionalVolterra.lean`, `FractionalVolterraSemigroup.lean`,
`FractionalVolterraCalculus.lean`, and `FabiusFractionalVolterra.lean`: they have,
respectively, one definition and ten theorems, no definitions and six theorems, no
definitions and four theorems, and one definition and six theorems---two definitions
and 26 theorems in total.  `AnalyticSeriesFilter.lean` has exactly two definitions and
seven theorems.  The new named-mode tranche in `AnalyticMoments.lean` is exactly one
definition and three theorems; `RvachevQBinomialFilter.lean` has exactly five public
theorems; `QPochhammerEntire.lean` has no public definitions and exactly five
public theorems; `QMultinomial.lean` has one definition and exactly nine
theorems; `BasicHypergeometricSeries.lean` has two definitions and exactly five
theorems; `FiniteQBinomialCore.lean` now also exposes exactly one signed-index
definition and four supporting theorems; `QBinomialCauchy.lean` has exactly
one definition and five public theorems; `SymmetricFunctionOrthogonality.lean`
has exactly one definition and six public theorems;
`QBinomialReciprocity.lean` has exactly four public theorems and no public
definition; `GaussianBinomialAtNegOne.lean` likewise has no public definition
and exactly five public theorems; `FiniteTriangularTransform.lean` has exactly one definition and one public
theorem; `SymmetricFunctionTransform.lean` has exactly four definitions and
five public theorems; `SymmetricFunctionGenerating.lean` has exactly two
definitions and six public theorems; and `QBinomialVandermonde.lean` has
exactly nine public theorems.
`BitPositionQBinomial.lean` has exactly eight public theorems.  The three
inversion rows below are likewise exhaustive:
`QBinomialInversion.lean` has four definitions and seven theorems,
`QBinomialTransform.lean` has four definitions and four theorems, and
`QBinomialInversionSpecializations.lean` has two definitions and four
theorems.  `QDifferenceAnnihilation.lean` has exactly four public theorems
and no public definition.  The global geometric-numerator bridge is now
owned by `GeometricQBinomialLagrange.lean`, rather than by the specialization
module.  The
affine/order-raising, causal-Rvachev, first-shift, and analytic-filter
publication was compiler-validated at checkpoint `149332f9d`.
`SubgraphFubini.lean`, `CDFLayerCake.lean`,
`FractionalCDFLayerCake.lean`, and `FabiusFractionalIntegral.lean` have,
respectively, two, four, four, and four public theorems and no definitions.
`MeasureCauchyTransform.lean`, `MeasureCauchyMomentLaurent.lean`,
`GeometricUniformCauchy.lean`, and `CauchyTransform.lean` have, respectively,
three definitions and nine theorems, one definition and fifteen theorems,
three definitions and six theorems, and six definitions and seventeen theorems; their
four rows above are exhaustive public-API inventories.  `CauchyCDF.lean`,
`CauchySurvival.lean`, `CauchyHigherPowers.lean`, and
`CauchyRenormalization.lean` contribute, respectively, three, three, four, and five
public theorems and no definitions.
`InverseLayerCake.lean` has no public definitions and eleven public theorems; the
weighted inverse-layer-cake row above is its exhaustive public-API inventory.
`InversePairIntegral.lean` has no public definitions and six public theorems; the
two-function inverse-pair row above is its exhaustive public-API inventory.
`WeightedEulerTransform.lean`, `GeneralizedSincZeta.lean`, and
`AlternatingNewtonCumulantKernel.lean` have, respectively, no public definitions
and four public theorems, one public definition and five public theorems, and no
public definitions and three public theorems; the three corresponding rows above
exhaust all thirteen public declarations.
`CentralBinomialValuation.lean` has no public definitions and three public
theorems; the central-binomial row above is its exhaustive public-API inventory.
`PrimePowerBinomialValuation.lean` has no public definitions and six public
theorems; the prime-power Pascal-row row above is its exhaustive public-API
inventory.
`BinaryDigitFloor.lean` has no public definitions and four public theorems; the
binary-digit row above is its exhaustive public-API inventory.
`BaseDigitMultiplicity.lean` has no public definitions and five public theorems,
and `RademacherSine.lean` has no public definitions and nine public theorems.
`PartitionDefect.lean` has exactly three public definitions and 33 public
theorems; its one private helper is excluded.  Those rows and the Rademacher row
exhaust their respective public surfaces.

The current New Frontiers finite Gram--Legendre inventory consists of eleven
modules and exactly 129 public declarations: twenty definitions and 109
theorems.  In dependency order the exhaustive component counts are
`PolynomialMomentGramDeterminant.lean` `2+7`,
`GramStieltjesNaturality.lean` `0+6`, `RvachevRationalJacobi.lean` `4+13`,
`LegendrePolynomialRational.lean` `2+6`,
`FabiusLegendreHankelDeterminant.lean` `2+7`,
`FabiusLegendreRationalGram.lean` `3+11`,
`FabiusLegendreRationalGramValues.lean` `0+11`, `LegendreGaunt.lean` `4+12`,
`FabiusLegendreGaunt.lean` `1+8`, `LegendreGauntClosedForm.lean` `2+25`, and
`FabiusLegendreGauntClosedForm.lean` `0+3`, where each pair is
definitions-plus-theorems.  The predecessor nine-module subtotal is the
historical `18+81=99`; the two closed-form leaves add
`(2+25)+(0+3)=30` declarations.

The three companion rows for `BellPolynomialInversion.lean`,
`AppellSequence.lean`, and `ReciprocalExponentialGenerating.lean`, together with
the normalized round-trip corollary `Bell.cumulant_complete`, are likewise
exhaustive public-API inventories: respectively five definitions and 28
theorems, three definitions and 23 theorems, and one definition and 19 theorems.
The two private recurrence helpers in `BellPolynomialInversion.lean` are
intentionally excluded.

In the current compiled tree, the extended Rvachev-specific companion
`RvachevMomentAppell.lean` has exactly six public definitions and thirty-three
public theorems; its three private support and integrability helpers are
excluded.  `RvachevPolynomialSynthesis.lean` has no
public definitions and exactly five public theorems.
`RvachevSuperconvergentSynthesis.lean` has exactly one public definition and
eight public theorems.
`LagrangeRvachevSynthesis.lean` has exactly two public definitions and seven
public theorems.  `RvachevAppellHasse.lean` and
`RvachevLagrangeNodesOnly.lean` each have exactly one public definition and
fourteen public theorems.  `LagrangeRvachevMatrix.lean` has exactly four public
definitions/abbreviations and six public theorems, while
`FabiusLegendreTranslateBlocks.lean` has exactly six public definitions and
seven public theorems.  The focused-build
`FabiusLegendreTranslateSeries.lean` has exactly five public definitions and
twenty-five public theorems; its two private mesh helpers are excluded.  The
focused-build `CompositeMeshSharpness.lean` has exactly one public definition
and seven public theorems.  In the order `RvachevMomentAppell`,
`RvachevPolynomialSynthesis`, `RvachevSuperconvergentSynthesis`,
`LagrangeRvachevSynthesis`, `RvachevAppellHasse`,
`RvachevLagrangeNodesOnly`,
`LagrangeRvachevMatrix`,
`CompositeMeshSharpness`, `FabiusLegendreTranslateBlocks`, and
`FabiusLegendreTranslateSeries`, the exact definition/theorem inventories are
`6/33`, `0/5`, `1/8`, `2/7`, `1/14`, `1/14`, `4/6`, `1/7`, `6/7`, and
`5/25`.  The relevant rows above exhaust all 153 public declarations across these ten
modules.

## Local draft: *Fabius Asymptotic*

The source draft audited for this section is not currently vendored in the
repository.  It contains **zero** formal theorem, lemma, proposition, or
corollary environments, so the table is a claim matrix rather than a
named-environment inventory.  Its public Lean aggregate is
`FabiusFunction.PaperFabiusAsymptotic`.

| Source claim | Status | Lean declaration(s) |
| --- | --- | --- |
| Definitions of `t`, `phi(t)`, and `g(t)` | Proved definitions | `Fabius.fabiusLogArgument`, `Fabius.fabiusLogPhi`, `Fabius.fabiusLogProfile` |
| Chain rule and equation (1) | Proved with the necessary large-scale hypothesis `1 ≤ t` | `Fabius.fabiusLogPhi_hasDerivAt`, `Fabius.fabiusLogProfile_hasDerivAt`, `Fabius.fabiusLogProfile_difference_eq_log_deriv` |
| Positivity used before equation (1) | Proved | `Fabius.fabiusLogPhi_pos`, `Fabius.fabiusLogSlope_pos` |
| Equation (2), both logarithm expansions | Proved | `Fabius.log_sub_one_sub_log_second_order_isBigO`, `Fabius.log_div_sub_one_second_order_isBigO` |
| Equations (3)–(8), proposed explicit main term | Formalized as a candidate and checked against the exact equation; not assumed to be the true sharp expansion | `Fabius.logMainTerm`, `Fabius.logMainDerivative`, `Fabius.logMainTerm_hasDerivAt`, `Fabius.logMainDefect_eq`, `Fabius.logMainDefect_decomposition` |
| Quadratic leading coefficient | Proved first on dyadic scales and then for all real scales | `Fabius.normalized_log_fabius_inverse_two_pow_tendsto`, `Fabius.fabiusLogProfile_normalized_tendsto` |
| Equation (9), exact remainder-difference identity | Proved with the actual remainder `g - G` and the repaired domain `1 < t` | `Fabius.fabiusLogRemainder_difference_eq` |
| Equation (9), size of the explicit main-term defect | Corrected result proved: the residual is `O((log t / t)^2)` | `Fabius.logMainDefect_isBigO_logScaleSquaredRate` |
| Equation (10)'s replacement of that residual by `O(t^-2)` | **False**; formally refuted, with its nonzero leading coefficient identified | `Fabius.logMainDefect_sub_lead_isLittleO`, `Fabius.logMainDefect_not_isBigO_one_div_sq` |
| Equation (11), bounded one-periodic-in-`t` remainder with `E(t) = O(log t / t)` | **Unsupported and not asserted in Lean.** The printed proof uses the false preceding residual estimate. | No declaration claims this particular phase/refinement. |
| Final sharp formula as derived from equations (8) and (11) | The draft's derivation and phase are invalid.  A separate rigorous saddle analysis proves the corrected Lambert-phase formula below. | `Fabius.log_fabius_sub_correctedWikipediaMain_isBigO`, `Fabius.log_fabius_sub_explicitCorrectedWikipediaMain_isBigO` |
| Rigorous coarse small-argument replacement | Proved: explicit dyadic error, eventual full-real bound, and `O(t log t)` error | `Fabius.abs_dyadicLogError_le`, `Fabius.eventually_abs_fabiusLogProfile_sub_quadratic_le`, `Fabius.fabiusLogProfile_sub_quadratic_isBigO`, `Fabius.log_fabiusLogPhi_add_quadratic_isBigO` |
| Rigorous sharp periodic correction | Proved: continuous periodic correction, exact mean, every nonzero-frequency Gamma--zeta Fourier coefficient is nonzero, absolute Fourier reconstruction, and `O(1 / (-log x))` corrected error | `Fabius.negativeLaplacePeriodicMean_eq`, `Fabius.hasSum_negativeLaplacePsi_gammaZeta_fourierSeries`, `Fabius.negativeLaplacePsiFourierCoeff_ne_zero`, `Fabius.negativeLaplacePsi_not_constant`, `Fabius.log_fabius_sub_explicitCorrectedWikipediaMain_isBigO` |
| Full sharp expansion to every order | Proved at the exact lower-Lambert phase: after the first `N` periodic saddle coefficients, the remainder is `O(lambda^-N)`; `N = 2` exposes the first explicit correction after the identically zero zeroth coefficient | `Fabius.log_fabius_sub_sharpLambertMain_hasAsymptoticExpansion`, `Fabius.log_fabius_sub_sharpLambertExpansion_isBigO`, `Fabius.fabiusSharpLambertExpansion_two` |
| Forward negative-Laplace tail at the exact lower-Lambert scale | Proved smaller than every inverse power of the phase along `x = 2^-t`: for each natural `N`, the tail is `o(lambda(2^-t)^-N)` as `t → +∞`.  The case `N = 0` states convergence to zero; this little-o result applies to the isolated forward tail, not to the complete saddle-expansion remainder. | `Fabius.negativeLaplaceTailError_dyadicLambert_isLittleO_inv_pow`, `Fabius.negativeLaplaceTailError_dyadicLambert_isBigO_inv_pow` |
| Lower-Lambert phase to every order | Proved separately in literal `-log x` and `log (-log x)` variables | `Fabius.fabiusLambertLiteralApproximation`, `Fabius.fabiusLambertPhase_sub_literalApproximation_isBigO` |
| Literal elementary formula with the periodic term omitted | **False at the claimed error scale.** | `Fabius.log_fabius_sub_WikipediaElementaryMain_not_isBigO` |

The source's intermediate coefficient matching is not a proof about an
arbitrary remainder: its equation (4) drops `R(t)-R(t-1)`, and equation (6)
uses a bound on `R'` that was never assumed.  The Lean development therefore
checks the displayed explicit term through its exact residual instead of
formalizing those circular steps as theorems.

## Linked Stack Exchange discussions

Notation in this table is intentionally disambiguated.  The Gaussian
`q`-binomial/Prouhet and normalized Toeplitz identities use the fixed base
`q = 1/2`.  A freely varying additive translation is instead written `Q`
(several corresponding Lean declarations use the local binder name `q` for
that translation).  The finite Gaussian/Richardson algebra at base `q = 1/4`
is also formalized and identified separately below; the sinc-tail
acceleration and its analytic consequences remain a research frontier.

| Web claim | Status | Lean declaration(s) |
| --- | --- | --- |
| [Recurrence sequence](https://math.stackexchange.com/questions/4354350/extracting-an-asymptotic-from-a-sequence-defined-by-a-recurrence-relation) and its connection to `F(2^-n)` | Proved with the source normalization `a_n = d_n / n!`; the resulting direct recurrence for `F(2^-n)` is proved exactly for `n ≥ 1` in rational, generic bounded, canonical bounded, generic signed-global, and canonical signed-global forms | `Fabius.fabiusRecurrenceSequence`, `Fabius.fabiusRecurrenceSequence_recurrence`, `Fabius.fabius_inverse_two_pow_eq_recurrenceSequence`, `Fabius.fabiusAtInverseTwoPow_recurrence_zpow`, `Fabius.fabiusFunction_inverse_two_pow_recurrence_zpow`, `Fabius.fabius_inverse_two_pow_recurrence_zpow`, `Fabius.extendedFabius_inverse_two_pow_recurrence`, `Fabius.globalFabius_inverse_two_pow_recurrence` |
| Nonrecursive solution of the inverse-dyadic recurrence | Proved for every natural `n`, including `n = 0`, as a finite weighted-path sum and as the explicit sum over ordered compositions `n = r₁+⋯+rₘ` with partial sums `sⱼ`; a nested version first sums over the number of blocks.  The empty composition gives `F(1)=1`.  Generic bounded, canonical, and signed-global real corollaries are exposed.  The former standalone article is consolidated into Part “Fabius Integration Research Frontiers,” especially sections `integration:sec:path-sums` and `integration:sec:fabius-composition-formula`, in the maintained [source](semi-formalized-research-frontiers/semi-formalized-research-frontiers.tex) and [PDF](semi-formalized-research-frontiers/semi-formalized-research-frontiers.pdf). | `Fabius.Composition.pathSum_eq_sum_range`, `Fabius.triangularRecurrence_eq_initial_mul_pathSum`, `Fabius.fabiusCompositionWeight`, `Fabius.fabiusCompositionSum`, `Fabius.fabiusRecurrenceSequence_eq_sum_compositions`, `Fabius.fabiusAtInverseTwoPow_eq_composition_formula`, `Fabius.fabiusAtInverseTwoPow_eq_composition_formula_by_length`, `Fabius.fabiusFunction_inverse_two_pow_eq_sum_compositions`, `Fabius.globalFabius_inverse_two_pow_eq_sum_compositions` |
| Bernoulli recurrence and generating-function equation/product | Proved in source-facing coefficient and analytic forms | `Fabius.fabiusRecurrenceSequence_bernoulli_recurrence`, `Fabius.complexGeneratingFunction_eq_fabiusRecurrenceSequence_series`, `Fabius.fabiusRecurrenceSequence_series_neg_eq_tprod` |
| [Finite q-binomial/half-shifted Thue--Morse formula for `F(m/2^n)`](https://math.stackexchange.com/questions/3283519/conjectured-formula-for-the-fabius-function) | Proved exactly for all natural `m,n`, including zero and unreduced representations.  With no bound on `m`, it computes the signed global extension; under `m ≤ 2^n`, it computes every bounded `IsFabius` function.  `qPochhammer` and `qBinomial` follow the source argument order. | `Fabius.qBinomialThueMorseDyadicHalfShiftFormula`, `Fabius.fabiusDyadic_eq_qBinomialThueMorseDyadic_halfShift_sum`, `Fabius.extendedFabius_dyadic_eq_qBinomialThueMorseDyadicHalfShiftFormula`, `Fabius.globalFabius_dyadic_eq_qBinomialThueMorseDyadicHalfShiftFormula`, `Fabius.fabiusFunction_dyadic_eq_qBinomialThueMorseDyadicHalfShiftFormula` |
| Finite polynomial functionals determined by moments, with affine transport (foundational engine; `cor:geometric-prouhet-affine`) | **Exact; exhaustive zero-definition/sixteen-theorem surface.**  After arbitrary scalar extension from a semiring to a commutative semiring, a finite weighted evaluation functional expands through its monomial moments; matching moments reproduces evaluation, while prescribed vanishing moments select any chosen coefficient, including the top or constant coefficient, and give strict-degree annihilation and coefficient congruence.  Same-ring forms require only a commutative semiring.  Nodes may repeat, the surviving moment may vanish, and no field or subtraction is required.  The `Polynomial.degree` forms correctly include `n=0` and the zero polynomial.  The affine theorem substitutes `q(X)=p(a+bX)`, applies any supplied top-coefficient extractor, and proves the transformed value `b^n*c*[X^n]p`; it requires neither `b≠0` nor distinct transformed nodes.  Composing it with the rational half-base Gaussian extractor proves `cor:geometric-prouhet-affine`, including `b=0` and `n=0`, under the established `ℚ[X]` convention. | `Fabius.sum_weight_mul_eval₂_eq_sum_coeff_mul_moment`, `Fabius.sum_weight_mul_eval₂_eq_eval₂_of_moments`, `Fabius.sum_weight_mul_eval₂_eq_coeff_mul_moment`, `Fabius.sum_weight_mul_eval₂_eq_topCoeff_mul_moment`, `Fabius.sum_weight_mul_eval₂_eq_constantCoeff_mul_sum`, `Fabius.sum_weight_mul_eval₂_eq_constantCoeff`, `Fabius.sum_weight_mul_eval₂_eq_map_coeff_mul_of_moments`, `Fabius.sum_weight_mul_eval₂_eq_map_coeff_mul_top_moment`, `Fabius.sum_weight_mul_eval₂_eq_zero_of_degree_lt`, `Fabius.sum_weight_mul_eval₂_congr_of_map_coeff_eq`, `Fabius.sum_weight_mul_eval_eq_eval_of_moments`, `Fabius.sum_weight_mul_eval_eq_coeff_mul_of_moments`, `Fabius.sum_weight_mul_eval_eq_coeff_mul_top_moment`, `Fabius.sum_weight_mul_eval_eq_zero_of_degree_lt`, `Fabius.sum_weight_mul_eval_congr_of_coeff_eq`, `Fabius.sum_weight_mul_eval_affine_of_topCoeff_extractor` |
| Denominator-free finite `q`-binomial theorem (foundational engine) | The recursive Gaussian coefficient commutes with every semiring homomorphism and has exact lower, diagonal, and above-diagonal boundaries.  It satisfies both zero-extended q-Pascal recurrences and is reflection-symmetric over every commutative semiring, without division or cancellation.  Over every commutative ring its signed generating polynomial is exactly the finite q-Pochhammer product for all `q,z`, including `q = 0` and roots of unity.  Finite q-Pochhammer products concatenate after an arbitrary prefix, and both the selected-factor identity and the full denominator-free factorial identity `(q;q)_n = (q;q)_k (q;q)_(n-k) [n choose k]_q` hold without quotient hypotheses.  Over `ℚ`, the generic product is identified with the established finite q-Pochhammer notation; at `q = 1/2` the recursive coefficient equals `halfQBinomial`. | `Fabius.finiteQPochhammerIn`, `Fabius.finiteQPochhammerIn_zero`, `Fabius.finiteQPochhammerIn_succ`, `Fabius.finiteQPochhammerIn_succ_shift`, `Fabius.finiteQPochhammerIn_add`, `Fabius.finiteQPochhammerIn_self_add`, `Fabius.gaussianBinomial`, `Fabius.map_gaussianBinomial`, `Fabius.gaussianBinomial_succ_succ`, `Fabius.gaussianBinomial_succ_succ_alt`, `Fabius.gaussianBinomial_symm`, `Fabius.gaussianBinomial_eq_zero_of_lt`, `Fabius.gaussianBinomial_self`, `Fabius.finiteQPochhammerIn_self_mul_gaussianBinomial`, `Fabius.finiteQPochhammerIn_self_eq_mul_mul_gaussianBinomial`, `Fabius.finite_qBinomial_theorem`, `Fabius.finiteQPochhammerIn_rat_eq`, `Fabius.gaussianBinomial_half_eq_halfQBinomial` |
| Central Gaussian reduction at squared base (exhaustive companion API) | In every commutative ring, the first five theorems pair `(x;q)_n` with `(-x;q)_n`, split `(q;q)_(2k)` into odd/even factors, prove naturality, and establish the integral and general division-free central reduction.  The sixth is the quotient form over a field under exactly two nonzero-denominator hypotheses. | `Fabius.finiteQPochhammerIn_mul_neg`, `Fabius.finiteQPochhammerIn_two_mul`, `Fabius.finiteQPochhammerIn_map_ringHom`, `Fabius.central_gaussianBinomial_sq_mul_int`, `Fabius.central_gaussianBinomial_sq_mul`, `Fabius.central_gaussianBinomial_sq_div` |
| Cyclotomic factorization of finite q-products and Gaussian coefficients (exhaustive companion API) | The two natural-number inequalities confine each cyclotomic exponent to `{0,1}`; a divisor-index equivalence reorganizes the product.  Over a commutative ring the module factors `(X;X)_n`, gives its Gaussian factorial bridge, and extends the cyclotomic interval product; the final Gaussian product identity adds `[IsDomain R]`. | `Fabius.div_add_div_le_div`, `Fabius.div_le_div_add_div_add_one`, `Fabius.mem_range_and_mem_divisors_iff`, `Fabius.finiteQPochhammerIn_X_eq_prod_cyclotomic`, `Fabius.finiteQPochhammerIn_X_eq_gaussianBinomial_mul`, `Fabius.prod_cyclotomic_pow_div_extend`, `Fabius.gaussianBinomial_X_eq_prod_cyclotomic` |
| Complete primitive-root Gaussian block (exhaustive companion API) | `PrimitiveRootBlock.lean` has no public definitions and exactly three public theorems.  In a commutative integral domain, a primitive `d`-th root `ζ` makes `[d,k]_ζ = 0` for `0 < k < d`; under `0 < d`, the phase is `(-1)^d ζ^(choose d 2) = -1` and `(y;ζ)_d = 1-y^d`. | `Fabius.gaussianBinomial_isPrimitiveRoot_eq_zero`, `Fabius.neg_one_pow_mul_pow_choose_two`, `Fabius.finiteQPochhammerIn_isPrimitiveRoot` |
| The q-Lucas theorem (exhaustive companion API) | `QLucas.lean` has no public definitions and exactly seven public theorems.  Two public natural-number quadratic identities support the signed coefficient and complete-block calculations.  If `0 < d`, `ζ` is a primitive `d`-th root in a commutative integral domain, and `b,s < d`, then `[a*d+b,r*d+s]_ζ = choose(a,r) * [b,s]_ζ`.  Its same-shaped `two_mul_choose_two` helper is private; the unique public `Fabius.two_mul_choose_two` is owned by `QChuVandermonde`. | `Fabius.add_mul_add_sub_one`, `Fabius.choose_two_add`, `Fabius.coeff_finiteQPochhammerIn_neg_X`, `Fabius.finiteQPochhammerIn_neg_X_block`, `Fabius.coeff_block_pow_mul`, `Fabius.pow_choose_two_add_mul_eq`, `Fabius.gaussianBinomial_q_lucas` |
| Cyclotomic carry criterion and primitive-root multiple value (exhaustive companion API) | `CyclotomicDivisibility.lean` has no public definitions and exactly three public theorems.  For `k ≤ n` and `0 < d`, the Gaussian cyclotomic exponent equals one exactly when `n % d < k % d`; over `ℚ[X]` this is equivalent to `Φ_d ∣ [n,k]_X`.  In a commutative integral domain, a primitive `n`-th root with `0 < n` gives `[a*n,b*n]_ζ = choose(a,b)`. | `Fabius.cyclotomic_exponent_eq_one_iff`, `Fabius.cyclotomic_dvd_gaussianBinomial_iff`, `Fabius.gaussianBinomial_mul_isPrimitiveRoot` |
| MacMahon's integral q-Catalan polynomial (exhaustive companion API) | `QCatalan.lean` has exactly one public definition and eleven public theorems.  Semiring q-integer naturality and commutative-ring polynomial identities yield `[n+1]_X ∣ [2*n,n]_X` over `ℚ[X]` and `ℤ[X]`.  The integral quotient `qCatalan n` has degree `n*(n-1)`, satisfies `(n+1) C_n(1) = choose(2*n,n)`, and evaluates to the ordinary Catalan number. | Definition: `Fabius.qCatalan`.  Theorems: `Fabius.map_qInt`, `Fabius.qInt_X_monic`, `Fabius.qInt_X_natDegree`, `Fabius.X_sub_one_mul_qInt`, `Fabius.qInt_X_eq_prod_cyclotomic`, `Fabius.qInt_X_dvd_gaussianBinomial_rat`, `Fabius.qInt_X_dvd_gaussianBinomial_int`, `Fabius.qInt_X_mul_qCatalan`, `Fabius.qCatalan_natDegree`, `Fabius.qCatalan_eval_one_mul`, `Fabius.qCatalan_eval_one` |
| Newton interpolation and geometric divided differences (exhaustive companion API) | `NewtonInterpolation.lean` has three public definitions and nineteen public theorems over a field.  It gives the triangular coefficients and node polynomial, interpolation and uniqueness at finite distinct nodes, the divided-difference sum, and the geometric-power-node formulas.  The node-qualified family avoids collision with the scalar `newtonPoly`; seven compatibility declarations expose the same interpolant API.  The local nonzero-product, finite-range injectivity, `q ≠ 0`, and index hypotheses remain explicit. | Definitions: `Fabius.newtonCoeff`, `Fabius.nodeNewtonPoly`, `Fabius.newtonInterpolant`.  Theorems: `Fabius.newtonCoeff_eq`, `Fabius.newtonCoeff_zero`, `Fabius.newtonCoeff_mul_prod`, `Fabius.nodeNewtonPoly_succ`, `Fabius.eval_nodeNewtonPoly`, `Fabius.degree_nodeNewtonPoly_lt`, `Fabius.nodeNewtonPoly_eq_interpolate`, `Fabius.eq_nodeNewtonPoly_of_eval_eq`, `Fabius.coeff_nodeNewtonPoly_self`, `Fabius.newtonCoeff_eq_sum`, `Fabius.nodal_range_pow`, `Fabius.prod_erase_pow_sub_pow`, `Fabius.newtonCoeff_pow_eq_sum`, `Fabius.newtonPoly_succ`, `Fabius.eval_newtonPoly`, `Fabius.degree_newtonPoly_lt`, `Fabius.newtonPoly_eq_interpolate`, `Fabius.eq_newtonPoly_of_eval_eq`, `Fabius.coeff_newtonPoly_self` |
| Jackson q-beta integral (exhaustive companion API) | `QBetaIntegral.lean` has one public definition and eight public theorems.  For `0 < q < 1`, the term identity and q-binomial summation give the product and q-Gamma evaluations; positive arguments then give symmetry, positivity, and the two recurrences. | Definition: `Fabius.qBeta`.  Theorems: `Fabius.qNumber_pos`, `Fabius.qBeta_term_eq`, `Fabius.qBeta_eq_prod`, `Fabius.qBeta_eq_qGamma`, `Fabius.qBeta_comm`, `Fabius.qBeta_pos`, `Fabius.qBeta_add_one_left`, `Fabius.qBeta_add_one_right` |
| Gaussian reciprocity and the `q = -1` parity-zero locus (foundational engine) | For a unit `q` in every commutative semiring, Gaussian reciprocity is total in `n,k`, including above-diagonal values through zero extension.  The semifield wrapper assumes exactly `q != 0`.  Over every commutative ring, odd `k*(n-k)` forces `[n choose k]_(-1) = 0`; the even-row/odd-column specialization is named separately.  No division, cancellation, characteristic, domain, or nonvanishing hypothesis enters the unit or `q = -1` forms.  This is coefficient reciprocity only: polynomial coefficient reversal and its broader generating-polynomial consequences remain outside this four-theorem tranche. | Exhaustive `QBinomialReciprocity` surface: `Fabius.gaussianBinomial_reciprocity_units`, `Fabius.gaussianBinomial_reciprocity`, `Fabius.gaussianBinomial_neg_one_eq_zero_of_odd_degree`, `Fabius.gaussianBinomial_neg_one_even_odd_eq_zero` |
| Gaussian reciprocity and dimension-dominant bounds (exhaustive companion API) | `GaussianBinomialBounds.lean` has no public definitions and exactly six public theorems.  Its field reciprocity wrapper assumes `q != 0` and `k ≤ n`; over ordered fields, coefficient nonnegativity gives `1 ≤ [n,k]_q` for `q ≥ 0`; over the reals, the stronger existing `finiteQPochhammerIn_self_pos` from `GeneralQConditionNumber` and finite-prefix comparison give `[n,k]_q ≤ (q;q)_∞⁻¹` for `0 ≤ q < 1`.  Reciprocity then gives `Q^(k*(n-k)) ≤ [n,k]_Q ≤ Q^(k*(n-k))/(Q⁻¹;Q⁻¹)_∞` for `Q > 1`.  No bound is asserted outside the displayed order and index hypotheses. | `Fabius.gaussianBinomial_inv`, `Fabius.one_le_gaussianBinomial`, `Fabius.finiteQPochhammerIn_pow_le_one`, `Fabius.gaussianBinomial_le_inv_qPochhammerInfIn`, `Fabius.pow_le_gaussianBinomial_of_one_lt`, `Fabius.gaussianBinomial_le_pow_div_of_one_lt`; reused dependency: `Fabius.finiteQPochhammerIn_self_pos` |
| Generic Gaussian-polynomial degree, palindromicity, mean, and linear coefficient (exhaustive companion API) | `GaussianBinomialPalindromic.lean` has no public definitions and exactly fourteen public theorems.  Over every `[CommSemiring R]`, its prior reflection, degree, boundary, coefficient-symmetry, and mean results remain unchanged.  The new `coeff_gaussianBinomial_one_of_pos_of_lt` gives coefficient one under exactly `0 < k` and `k < n`, and total `coeff_gaussianBinomial_one` gives `if 0 < k ∧ k < n then 1 else 0`; no `[Nontrivial R]` assumption is used for either.  `gaussianBinomial_natDegree` and `gaussianBinomial_monic` alone add `[Nontrivial R]`. | `Fabius.reflect_add_of_natDegree_le`, `Fabius.reflect_one'`, `Fabius.gaussianBinomial_natDegree_le`, `Fabius.gaussianBinomial_zero_left`, `Fabius.gaussianBinomial_diag'`, `Fabius.reflect_gaussianBinomial`, `Fabius.coeff_gaussianBinomial_reflect`, `Fabius.coeff_gaussianBinomial_zero`, `Fabius.coeff_gaussianBinomial_top`, `Fabius.gaussianBinomial_natDegree`, `Fabius.gaussianBinomial_monic`, `Fabius.two_mul_derivative_gaussianBinomial_eval_one`, `Fabius.coeff_gaussianBinomial_one_of_pos_of_lt`, `Fabius.coeff_gaussianBinomial_one` |
| Universal Gaussian-polynomial degree, monicity, and palindromicity (exhaustive companion API) | `GaussianBinomialPolynomialStructure.lean` has no public definitions and exactly five public theorems.  If `k ≤ n`, the universal polynomial `gaussianBinomial (X : ℕ[X]) n k` has exact natural degree `k*(n-k)`, is monic, has constant coefficient one, and equals its reflection in that exact degree.  If also `d ≤ k*(n-k)`, its coefficients in degrees `d` and `k*(n-k)-d` agree.  The neighboring general-semiring theorem `coeff_gaussianBinomial_one` completes the linear-coefficient clause with value one exactly when `0 < k ∧ k < n` and zero otherwise. | `Fabius.natDegree_gaussianBinomial_universal`, `Fabius.gaussianBinomial_universal_monic`, `Fabius.coeff_zero_gaussianBinomial_universal`, `Fabius.gaussianBinomial_universal_reflect`, `Fabius.coeff_gaussianBinomial_universal_symm` |
| Gaussian coefficients and finite `q`-Pochhammer products at `q = -1` (exhaustive companion API) | `GaussianBinomialAtNegOne.lean` has no public definitions and exactly five public theorems.  All five are total over every `[CommRing R]`, with no domain, characteristic, cancellation, division, or nonvanishing hypothesis.  The three Gaussian identities evaluate the even/even, odd/even, and odd/odd parity classes as ordinary binomial coefficients, including above-row columns where both sides vanish.  The even-row/odd-column zero is the pre-existing `gaussianBinomial_neg_one_even_odd_eq_zero` in `QBinomialReciprocity`, not a sixth declaration here; together the two modules give the four-case table.  The two finite-product pairings give `(z;-1)_(2a) = (1-z^2)^a` and `(z;-1)_(2a+1) = (1-z^2)^a(1-z)`.  The derivative and root-multiplicity layer is recorded in the adjacent row. | `Fabius.gaussianBinomial_neg_one_even_even`, `Fabius.gaussianBinomial_neg_one_odd_even`, `Fabius.gaussianBinomial_neg_one_odd_odd`, `Fabius.finiteQPochhammerIn_neg_one_even`, `Fabius.finiteQPochhammerIn_neg_one_odd` |
| Gaussian-polynomial evaluation, derivatives, and simple alternating roots at `q = -1` (exhaustive companion API) | `GaussianBinomialAtNegOneDerivative.lean` has no public definitions and exactly five public theorems.  `gaussianBinomial_X_eval` evaluates `[n,k]_X` at every point of every commutative semiring.  Over every `[CommRing R]`, `gaussianBinomial_derivative_eval_neg_one_of_even_degree` gives `G'_(n,k)(-1) = -(k*(n-k)/2) G_(n,k)(-1)` whenever `k*(n-k)` is even, including degree zero and above-row columns, and `gaussianBinomial_derivative_eval_neg_one_even_odd` gives `G'_(2a,2b+1)(-1) = (a-b) * choose(a,b)` for every `a,b`, using natural subtraction and zero extension.  Under exactly `b < a`, `gaussianBinomial_even_odd_rootMultiplicity_int` proves multiplicity one over `ℤ`, and `gaussianBinomial_even_odd_rootMultiplicity` transports it to every `[CommRing K] [CharZero K]`.  Thus simplicity is proved on the admissible alternating locus, not over arbitrary characteristic and not for every cyclotomic zero. | `Fabius.gaussianBinomial_X_eval`, `Fabius.gaussianBinomial_derivative_eval_neg_one_of_even_degree`, `Fabius.gaussianBinomial_derivative_eval_neg_one_even_odd`, `Fabius.gaussianBinomial_even_odd_rootMultiplicity_int`, `Fabius.gaussianBinomial_even_odd_rootMultiplicity` |
| Simple dyadic roots of the half-base q-binomial polynomial | `HalfQBinomialRootSimplicity.lean` has no public definitions and exactly one public theorem.  For every `j < n`, the exact rational coefficient polynomial `∑_{k≤n} (-1)^k (1/2)^(k.choose 2) halfQBinomial n k · X^k` has root multiplicity exactly one at `2^j`.  Together with the existing complete rational root classification, this proves that every root in the half-base locus is simple.  It does not assert simplicity for an arbitrary q-base, coefficient field, or cyclotomic zero. | `Fabius.halfQBinomial_sum_rootMultiplicity_two_pow` |
| Elementary finite `q`-Pochhammer reversal, terminating factors, and adjacent Gaussian ratios (foundational engine) | The two base-reversal orientations are proved over every commutative ring for unit parameters and over every field under exactly `a != 0` and `q != 0`.  A denominator-free product identity gives the root-safe numerator of the terminating specialization; over a field, its cleared form and its `k > N` zero clause require only `q != 0`, while the usual quotient for `k <= N` additionally assumes `(q;q)_(N-k) != 0`.  The first Gaussian column and both adjacent cross-multiplied identities hold over every commutative ring, including roots of unity.  Both cross identities are total in all `n,k`; zero extension makes their two sides vanish on and above the row boundary.  Their quotient corollaries remain restricted to `k < n` and require exactly the displayed denominators: respectively `[n choose k]_q != 0` with `1-q^(k+1) != 0`, and `[n-1 choose k]_q != 0` with `1-q^(n-k) != 0`; neither ratio assumes `q != 0`. | Exhaustive `QPochhammerElementaryIdentities` surface (13 theorems): `Fabius.finiteQPochhammerIn_base_reversal_units`, `Fabius.finiteQPochhammerIn_inv_base_reversal_units`, `Fabius.finiteQPochhammerIn_base_reversal`, `Fabius.finiteQPochhammerIn_inv_base_reversal`, `Fabius.prod_pow_sub_pow_eq_finiteQPochhammerIn`, `Fabius.pow_mul_finiteQPochhammerIn_inv_pow_eq`, `Fabius.finiteQPochhammerIn_inv_pow_eq_self_div`, `Fabius.finiteQPochhammerIn_inv_pow_eq_zero_of_lt`, `Fabius.one_sub_mul_gaussianBinomial_one`, `Fabius.gaussianBinomial_adjacent_mul`, `Fabius.gaussianBinomial_row_adjacent_mul`, `Fabius.gaussianBinomial_adjacent_div`, `Fabius.gaussianBinomial_row_adjacent_div` |
| Finite q-Cauchy identities and q-Bernstein partition of unity (foundational engine) | Over every commutative ring and for arbitrary `q,u,v,n`, the primary finite q-Cauchy convolution proves `(uv;q)_n = sum_(k=0)^n [n choose k]_q (u;q)_k v^k (v;q)_(n-k)`.  A reflected theorem exposes the complementary orientation `(v;q)_k v^(n-k) (u;q)_(n-k)`, and the `u = 0` specialization packages a denominator-free q-Bernstein basis whose complete row sums to one.  The second finite Cauchy identity proves `(ac;q)_n(bc;q)_n = sum_(k=0)^n [n choose k]_q (a;q)_k(b;q)_k c^k(c;q)_(n-k)(abcq^k;q)_(n-k)`.  All statements include degree zero, `q = 0`, roots of unity, positive characteristic, and zero divisors, with no division, cancellation, domain, or nonvanishing hypothesis. | Exhaustive `QBinomialCauchy` surface (one definition, five theorems): `Fabius.finite_qCauchy_identity`, compatibility spelling `Fabius.finiteQPochhammerIn_mul_eq_sum_gaussianBinomial`, `Fabius.finite_qCauchy_identity_reflected`, `Fabius.qBernsteinBasis`, `Fabius.sum_qBernsteinBasis`, `Fabius.finite_qCauchy_second_identity` |
| Reflected terminating two-phi-one algebra and q-Chu--Vandermonde (exhaustive companion APIs) | `TwoPhiOneReversal.lean` has two public definitions and exactly twelve public theorems.  It supplies the finite normalization and reflected parameter map, the cleared finite-symbol identities and nonvanishing transfer, reflection involutivity, finite and actual-tsum reversal (including double reversal), and the degree-zero bridge, which includes `q = 0`.  The general reversals retain `q,A,C,z != 0` and the three displayed finite-product nonvanishing hypotheses.  `QChuVandermonde.lean` has no public definitions and exactly ten public theorems: exponent arithmetic, denominator-cleared first and second sums, finite quotient evaluations, a separately named reversal derivation, and actual-tsum wrappers.  Both displayed q-Chu evaluations are exact on the full rational domain `q != 0`, `A != 0`, `(q;q)_n != 0`, `(C;q)_n != 0`; the second needs neither `C != 0` nor `(A;q)_n != 0`.  The stronger claim that reversal alone proves the second evaluation on that full domain remains partial: `q_chu_vandermonde_second_by_reversal` additionally assumes those two nonvanishing conditions, whereas the full-domain theorem uses the denominator-cleared q-Cauchy route.  Rational continuation and a cleared commutative-ring extension remain unformalized. | Definitions: `Fabius.twoPhiOneFinite`, `Fabius.twoPhiOneReflection`.  Reversal theorems: `Fabius.choose_two_add_succ_choose_two`, `Fabius.finiteQPochhammerIn_sub_eq`, `Fabius.finiteQPochhammerIn_reversal_ne_zero`, `Fabius.finiteQPochhammerIn_inv_pow_self`, `Fabius.twoPhiOneReflection_involutive`, `Fabius.twoPhiOneFinite_reversal`, `Fabius.twoPhiOneFinite_reversal_twice`, `Fabius.twoPhiOneFinite_eq_sum_twoPhiOneTerm`, `Fabius.twoPhiOne_eq_twoPhiOneFinite_inv_pow`, `Fabius.twoPhiOne_reversal`, `Fabius.twoPhiOne_reversal_twice`, `Fabius.twoPhiOne_one_eq_twoPhiOneFinite_zero`.  Q-Chu theorems: `Fabius.two_mul_choose_two`, `Fabius.mul_sub_one_eq_mul_sub_add`, `Fabius.finiteQPochhammerIn_div_eq_sum_chu`, `Fabius.q_chu_vandermonde_first`, `Fabius.finiteQPochhammerIn_div_eq_sum_chu_second`, `Fabius.twoPhiOneFinite_mul_finiteQPochhammerIn_eq_chu_second`, `Fabius.q_chu_vandermonde_second`, `Fabius.q_chu_vandermonde_second_by_reversal`, `Fabius.twoPhiOne_q_chu_vandermonde_first`, `Fabius.twoPhiOne_q_chu_vandermonde_second` |
| Elementary/complete symmetric-function orthogonality and weighted Pascal (foundational engine) | For every finite family over a commutative semiring, `elementarySymmetricEval` is Mathlib's evaluated `MvPolynomial.esymm`, is invariant under reindexing, has degree-zero value one, and satisfies exact adjoining-variable recurrences for `Option` and `Fin`.  Its `Option` recurrence and the existing `completeHomogeneousEval_option_succ` are the two weighted-Pascal laws.  Over every commutative ring, the total convolution `sum_(k=0)^n (-1)^k e_k(a) h_(n-k)(a)` is one at `n = 0` and zero otherwise.  This includes the empty family and every degree and uses no division, characteristic, domain, or nonvanishing assumption. | Exhaustive `SymmetricFunctionOrthogonality` surface (one definition, six theorems): `Fabius.elementarySymmetricEval`, `Fabius.elementarySymmetricEval_eq_eval_esymm`, `Fabius.elementarySymmetricEval_zero`, `Fabius.elementarySymmetricEval_comp_equiv`, `Fabius.elementarySymmetricEval_option_succ`, `Fabius.elementarySymmetricEval_fin_succ`, `Fabius.sum_elementarySymmetricEval_mul_completeHomogeneousEval`; companion existing theorem: `Fabius.completeHomogeneousEval_option_succ` |
| Generic finite lower-triangular transforms (foundational engine) | For `[Semiring R] [AddCommMonoid M] [Module R M]`, a scalar kernel acts on a module-valued sequence by a finite sum over `Icc 0 n`.  If the ordered convolution `sum_(k in Icc j n) K n k * L k j` is the Kronecker delta for every `n,j`, applying the `L`-transform and then the `K`-transform gives equality of whole sequence functions.  The total hypothesis includes the empty interval when `n < j`; no commutativity of `R`, subtraction, topology, or infinite summability is required. | Exhaustive `FiniteTriangularTransform` surface (one definition, one theorem): `Fabius.lowerTriangularTransform`, `Fabius.lowerTriangularTransform_comp` |
| Weighted elementary--complete transforms and inversion (foundational engine) | For a finite alphabet `w`, the complete-homogeneous kernel and transform already exist over `[CommSemiring R]`; their signed-elementary counterparts, both total kernel orthogonalities, the two whole-function inverse compositions, and the exact inversion iff require `[CommRing R]`.  Every transform acts on `[AddCommMonoid M] [Module R M]`.  Both kernels are extended by zero above the diagonal, so all statements are total in the outer indices and reuse the generic finite triangular-transform theorem.  This proves the weighted inversion theorem exactly, for module-valued rather than only scalar-valued sequences, with no division, topology, domain, characteristic, or nonvanishing assumption. | Exhaustive `SymmetricFunctionTransform` surface (four definitions, five theorems): `Fabius.completeHomogeneousKernel`, `Fabius.signedElementaryKernel`, `Fabius.completeHomogeneousKernel_left_orthogonality`, `Fabius.completeHomogeneousKernel_right_orthogonality`, `Fabius.completeHomogeneousTransform`, `Fabius.signedElementaryTransform`, `Fabius.signedElementaryTransform_completeHomogeneousTransform`, `Fabius.completeHomogeneousTransform_signedElementaryTransform`, `Fabius.weightedSymmetricFunction_inversion` |
| Formal elementary/complete generating series and finite Gaussian reciprocity (foundational engine) | For a finite alphabet over `[CommSemiring R]`, the two series definitions, their coefficient theorems, and `E_a(X) = prod_i (1 + a_i X)` are formal power-series identities.  Over `[CommRing R]`, `E_{-a}(X) H_a(X) = 1`, `H_a` is the canonical `PowerSeries.invOfUnit` of `E_{-a}`, and the specialization to `1,q,...,q^n` gives `prod_(j=0)^n (1-q^j X) * sum_(k>=0) [n+k choose k]_q X^k = 1`.  These results are total at `q = 0`, roots of unity, zero divisors, positive characteristic, and the zero ring.  They close the formal-series halves of the weighted-generating and reciprocal-finite claims only: analytic evaluation and convergence under conditions such as `|w_i z| < 1` or `|z| < 1` remain unproved. | Exhaustive `SymmetricFunctionGenerating` surface (two definitions, six theorems): `Fabius.elementarySymmetricGeneratingSeries`, `Fabius.completeHomogeneousGeneratingSeries`, `Fabius.coeff_elementarySymmetricGeneratingSeries`, `Fabius.coeff_completeHomogeneousGeneratingSeries`, `Fabius.elementarySymmetricGeneratingSeries_eq_prod`, `Fabius.elementarySymmetricGeneratingSeries_neg_mul_completeHomogeneousGeneratingSeries`, `Fabius.completeHomogeneousGeneratingSeries_eq_invOfUnit_elementarySymmetricGeneratingSeries_neg`, `Fabius.prod_one_sub_qPow_X_mul_gaussianBinomialGeneratingSeries` |
| Total integer-index Gaussian coefficients and denominator-free `q`-Vandermonde, central, and shifted-central convolutions (foundational engine) | `gaussianBinomialInt` extends the natural lower index by zero to all integers, vanishes below zero and above the row, agrees with the natural coefficient on casts, and satisfies total row symmetry.  Over every commutative semiring and arbitrary `q`, both zero-extended Vandermonde orientations and both central supports are total.  The three natural shifted forms remain available, while the report's stronger single identity is proved for every `k : ℤ`, including `|k|>N`, both as a sum over `0,…,N` and literally as a finite-support `finsum` over `ℤ`; no quotient, cancellation, nonvanishing, characteristic, or admissibility hypothesis is used. | `Fabius.gaussianBinomialInt`, `Fabius.gaussianBinomialInt_ofNat`, `Fabius.gaussianBinomialInt_eq_zero_of_neg`, `Fabius.gaussianBinomialInt_eq_zero_of_lt`, `Fabius.gaussianBinomialInt_symm`, `Fabius.gaussianBinomial_add_vandermonde`, `Fabius.gaussianBinomial_add_vandermonde'`, `Fabius.gaussianBinomial_add_central`, `Fabius.gaussianBinomial_add_central_min`, `Fabius.gaussianBinomial_two_mul_add_shifted_central`, `Fabius.gaussianBinomial_two_mul_sub_shifted_central`, `Fabius.gaussianBinomial_two_mul_sub_shifted_central_Icc`, `Fabius.gaussianBinomial_two_mul_int_shifted_central`, `Fabius.gaussianBinomial_two_mul_int_shifted_central_finsum` |
| Bit-position and weighted-subset Gaussian enumeration (foundational engine) | Over every commutative ring, Gauss's positive finite product is expanded without division, then reindexed through binary bit positions and through fixed-cardinality powersets.  The zero-based identity sums `q^(sum T)` over `r`-subsets of `range N` and gives `q^(choose r 2) * [N choose r]_q`; the literal one-based identity sums over `Icc 1 N` and gives `q^(choose (r+1) 2) * [N choose r]_q`.  Both are total in `N,r`; the `q = 1` shadow is the ordinary binomial count. | Exhaustive `BitPositionQBinomial` surface: `Fabius.prod_one_add_mul_pow_eq_gaussianBinomial`, `Fabius.prod_one_add_pow_eq_sum_gaussianBinomial`, `Fabius.sum_powersetCard_two_pow`, `Fabius.sum_pow_bitPositionSum_filter_eq_gaussianBinomial`, `Fabius.sum_pow_bitPositionSum_filter_eq_gaussianBinomial'`, `Fabius.sum_pow_sum_powersetCard_eq_gaussianBinomial`, `Fabius.sum_pow_sum_powersetCard_Icc_eq_gaussianBinomial`, `Fabius.gaussianBinomial_one_eq_choose` |
| Division-free Gaussian chain, alternating rows, and scalar kernel orthogonality (foundational engine) | The flag identity `[n choose k]_q [k choose j]_q = [n choose j]_q [n-j choose k-j]_q` is proved over every commutative semiring.  Over every commutative ring the general alternating row at `q^m` is `(q^m;q)_n`, its `m = 0` face is the exact row-zero Kronecker value, and both multiplication orders of the signed Gaussian lower-triangular kernel are the delta on the total interval `Icc j n`.  An arbitrary independent scale `s` may be inserted in both kernels with the same two identities; no inverse, nonzero, cancellation, characteristic, or domain hypothesis is imposed on `q` or `s`. | Exhaustive `QBinomialInversion` surface: `Fabius.gaussianBinomial_mul`, `Fabius.sum_gaussianBinomial_alternating_mul_pow`, `Fabius.sum_gaussianBinomial_alternating`, `Fabius.gaussianBinomialKernel`, `Fabius.gaussianBinomialInverseKernel`, `Fabius.scaledGaussianBinomialKernel`, `Fabius.scaledGaussianBinomialInverseKernel`, `Fabius.gaussianBinomialKernel_left_orthogonality`, `Fabius.gaussianBinomialKernel_right_orthogonality`, `Fabius.scaledGaussianBinomialKernel_left_orthogonality`, `Fabius.scaledGaussianBinomialKernel_right_orthogonality` |
| Scaled and classical q-binomial inversion for module-valued sequences (foundational engine) | The forward transform definitions require only `[Semiring R] [AddCommMonoid M] [Module R M]`; the signed inverse definitions require `[Ring R] [AddCommMonoid M] [Module R M]`.  All four inversion theorems use `[CommRing R] [AddCommMonoid M] [Module R M]`: both compositions are equalities of whole sequence functions, and the two triangular relations are packaged as an exact iff.  The refactored proofs reuse `lowerTriangularTransform_comp`; Gaussian kernels are zero-extended above the row, so their total convolution hypotheses include `n < j`.  The scale is arbitrary and need not be a unit; no topology, convergence, division, or order is present. | Exhaustive `QBinomialTransform` surface (four definitions, four theorems): `Fabius.scaledGaussianBinomialTransform`, `Fabius.scaledGaussianBinomialInverseTransform`, `Fabius.scaledGaussianBinomialInverseTransform_transform`, `Fabius.scaledGaussianBinomialTransform_inverseTransform`, `Fabius.scaledGaussianBinomial_inversion`, `Fabius.gaussianBinomialTransform`, `Fabius.gaussianBinomialInverseTransform`, `Fabius.gaussianBinomial_inversion` |
| Scaled Gaussian characteristic polynomial, monomial moments, and exact q-difference annihilation (foundational engine) | Over every commutative ring and for arbitrary `q,s,z,n`, `sum_(k=0)^n (-s)^(n-k) q^(choose (n-k) 2) [n choose k]_q z^k = prod_(j<n) (z-s q^j)`.  Its `s = 1`, `z = q^d` specialization evaluates the complete monomial moment as `prod_(j<n) (q^d-q^j)`, which vanishes for `d < n`.  If `p` has coefficients in any semiring `A`, `φ : A →+* R`, and `degree p ≤ n`, the same row on `p.eval₂ φ (q^k)` is exactly `φ(p.coeff n) * prod_(j<n) (q^n-q^j)`; strict degree below `n` is annihilated.  These statements include `n = 0` and the zero polynomial.  Nodes may repeat and the top surviving product may itself be zero; there is no division, distinctness, nonzero/invertible-base, characteristic, domain, topology, or convergence assumption. | Exhaustive `QDifferenceAnnihilation` surface (four theorems): `Fabius.sum_scaledGaussianBinomialInverseKernel_mul_pow`, `Fabius.sum_gaussianBinomialInverseKernel_mul_geometric_pow`, `Fabius.qDifference_sum_eval₂_eq_map_coeff_mul`, `Fabius.qDifference_sum_eval₂_eq_zero_of_degree_lt` |
| Exact q-Gaussian coefficient inversions (foundational engine) | The residual and reconstruction definitions and their two pointwise closed-form theorems require only an arbitrary, possibly noncommutative ring.  At Gaussian base `q^2` and scale `-q`, the coefficients are `(-q)^(n-k) [n choose k]_(q^2)` and `q^((n-k)^2) [n choose k]_(q^2)`.  Commutativity is required exactly for the two total-`Icc` convolution theorems, which give the Kronecker delta in both orders. | Exhaustive `QBinomialInversionSpecializations` surface (two definitions, four theorems): `Fabius.qGaussianResidualCoeff`, `Fabius.qGaussianReconstructionCoeff`, `Fabius.qGaussianResidualCoeff_eq`, `Fabius.qGaussianReconstructionCoeff_eq`, `Fabius.qGaussianReconstructionCoeff_residualCoeff_delta`, `Fabius.qGaussianResidualCoeff_reconstructionCoeff_delta` |
| Gaussian closed form for geometric Lagrange/Richardson rows (foundational engine) | Proved over every commutative ring at the numerator level and over every field at the normalized level.  The zero-extended numerator has both the defining reversed-index form and the standard forward-index form; division-free Gaussian symmetry proves their equality, and the global forward theorem remains literal above the row because both sides vanish.  It is globally the scaled inverse Gaussian kernel at base and scale `q`, including above-diagonal indices and without `k ≤ n`; this is the `s = q` specialization of the general characteristic polynomial.  Its generating polynomial is `prod_(j<n) (z-q^(j+1))`, and its moments through degree `n` are `(q;q)_n * 0^d`.  Injectivity of `1,q,...,q^n` proves `(q;q)_n ≠ 0` and yields quotient and cleared-denominator pointwise Lagrange formulas.  Every positive residual degree is `(-1)^n q^(n(n+1)/2) [d-1 choose n]_q`.  All statements include `n = 0`; no separate exclusion of `q = 0` or roots of unity is imposed beyond actual node distinctness.  The global numerator identity does not alter the separate `Field`, finite-node `InjOn`, and in-range hypotheses of normalized quotient formulas. | `Fabius.geometricQBinomialWeightNumerator`, `Fabius.geometricQBinomialWeightNumerator_eq_of_le`, `Fabius.geometricQBinomialWeightNumerator_eq_forward_of_le`, `Fabius.geometricQBinomialWeightNumerator_eq_forward`, `Fabius.geometricQBinomialWeightNumerator_eq_scaledGaussianBinomialInverseKernel`, `Fabius.geometricQBinomialWeightNumerator_eq_zero_of_lt`, `Fabius.reversed_finite_qBinomial_theorem`, `Fabius.sum_geometricQBinomialWeightNumerator_mul_pow`, `Fabius.finiteQPochhammerIn_self_ne_zero_of_injOn`, `Fabius.geometricLagrangeWeight_eq_gaussianBinomial_div`, `Fabius.finiteQPochhammerIn_mul_geometricLagrangeWeight_eq`, `Fabius.sum_geometricLagrangeWeight_mul_pow_eq_gaussianBinomial` |
| Lagrange evaluation and geometric-grid Richardson exactness (foundational engine) | Proved first for arbitrary finite distinct nodes over a field: Lagrange evaluation weights reproduce every polynomial below the node count, are characterized by those moments, act coefficientwise on arbitrary module-valued finite power sums, and have the exact nodal-polynomial defect at the first omitted degree (including the empty family).  On nodes `1,q,...,q^n`, an explicit `InjOn` hypothesis gives mass one, vanishing positive moments through `n`, the first surviving moment, and module-valued Richardson exactness on a block beginning at arbitrary `start`.  The direct and local sequence forms include `n = 0` and `start = 0`; no positivity or nonzeroness assumption on the base is imposed beyond finite-node distinctness. | `Fabius.lagrangeEvalWeight`, `Fabius.sum_lagrangeEvalWeight_mul_eval`, `Fabius.eq_lagrangeEvalWeight_of_moments`, `Fabius.sum_lagrangeEvalWeight_mul_pow`, `Fabius.sum_lagrangeEvalWeight_mul_pow_card`, `Fabius.sum_lagrangeEvalWeight_smul_sum_pow`, `Fabius.geometricLagrangeWeight`, `Fabius.sum_geometricLagrangeWeight_mul_pow`, `Fabius.sum_geometricLagrangeWeight`, `Fabius.sum_geometricLagrangeWeight_mul_pow_eq_zero`, `Fabius.sum_geometricLagrangeWeight_mul_shifted_pow_eq_zero`, `Fabius.sum_geometricLagrangeWeight_mul_pow_succ_product`, `Fabius.sum_geometricLagrangeWeight_mul_pow_succ`, `Fabius.geometricLagrange_richardson_exact`, `Fabius.geometricLagrange_richardson_exact_of_eq` |
| Complete-homogeneous all-order interpolation residuals, finite formal generating series, Bell/power-sum conversion, and homogeneous asymptotic bound (foundational engine) | Proved first for arbitrary polynomially exact finite rows over a commutative ring: nodes may repeat, no division is used, and every moment above the exactness range is the universal nodal-product residual with a complete homogeneous factor.  The corresponding monomial quotient and remainder by Mathlib's monic nodal polynomial are identified exactly.  For distinct field-valued nodes this specializes to all higher Lagrange moments; geometric grids inherit the signed triangular nodal factor times the complete homogeneous polynomial in the geometric nodes.  Independently, the complete homogeneous evaluations are packaged as a `PowerSeries`; adjoining a variable gives the geometric recurrence, the finite product of linear denominators clears the series, and `PowerSeries.invOfUnit` identifies its canonical reciprocal over every commutative ring, including zero divisors.  The Bell layer defines the power sums and cumulant input `κ_0=0`, `κ_(k+1)=k! p_(k+1)`, and proves division-free over every commutative semiring that `Bell.complete κ n = n! h_n`; a commutative `ℚ`-algebra then gives the factorially normalized complete-Bell formula.  Empty alphabets, repetitions, zero divisors, positive characteristic, and the zero ring remain included.  The asymptotic wrapper proves that a fixed degree-`n` evaluation of coordinate functions that are each `O(g)` is `O(g^n)`.  Only analytic evaluation or convergence of the formal generating series remains open. | `Fabius.completeHomogeneousEval_eq_eval_hsymm`, `Fabius.completeHomogeneousEvalAt_zero`, `Fabius.X_pow_card_add_divByMonic_nodal`, `Fabius.X_pow_card_add_modByMonic_nodal`, `Fabius.sum_weight_mul_pow_card_add`, `Fabius.sum_lagrangeEvalWeight_mul_pow_card_add`, `Fabius.sum_geometricLagrangeWeight_mul_pow_card_add`, `Fabius.completeHomogeneousGeneratingSeriesOn`, `Fabius.completeHomogeneousGeneratingSeriesOn_insert`, `Fabius.one_sub_mul_completeHomogeneousGeneratingSeriesOn_insert`, `Fabius.prod_one_sub_mul_completeHomogeneousGeneratingSeriesOn`, `Fabius.completeHomogeneousGeneratingSeriesOn_eq_invOfUnit_prod`, `Fabius.completeHomogeneousPowerSum`, `Fabius.completeHomogeneousBellInput`, `Fabius.completeHomogeneousEvalOn_insert_eq_sum`, `Fabius.bellComplete_completeHomogeneousBellInput`, `Fabius.factorialNormalize_completeBellPolynomial_completeHomogeneousBellInput`, `Fabius.completeHomogeneousEvalOn_eq_factorialNormalize_completeBellPolynomial`, `Fabius.completeHomogeneousEvalOn_isBigO_pow` |
| Arbitrary-scalar translation `Q` and dyadic-representation invariance of the finite formula | The complete normalized finite expression is packaged as a polynomial over `ℚ` and proved equal to the constant centered formula, then evaluated in every field over `ℚ`.  Consequently the fully displayed sum with inner power `(j - m * 2^k + Q)^(n+k)` computes the same value for every real or complex additive translation `Q`, with an explicit Gaussian-rational endpoint.  This `Q` is not the Gaussian base, which remains `q = 1/2`.  Equal nonnegative dyadic rationals give equal formula values even when both scalar translations are changed simultaneously; binary refinement is the corresponding special case. | `Fabius.qBinomialThueMorseDyadicTranslatedFormulaPolynomial_eq_const`, `Fabius.qBinomialThueMorseDyadicTranslatedFormulaIn_eq_centered`, `Fabius.qBinomialThueMorseDyadicTranslatedFormulaIn_eq_of_rat_eq`, `Fabius.qBinomialThueMorseDyadicTranslatedFormulaIn_refine`, `Fabius.fabiusDyadic_algebraMap_eq_qBinomialThueMorseDyadic_translated_sum`, `Fabius.globalFabius_dyadic_eq_qBinomialThueMorseDyadic_translated_sum_real`, `Fabius.globalFabius_dyadic_eq_qBinomialThueMorseDyadic_translated_sum_complex`, `Fabius.globalFabius_dyadic_eq_qBinomialThueMorseDyadicTranslatedFormulaIn_gaussianRat`, `Fabius.qBinomialThueMorseDyadicTranslatedFormula_eq_of_rat_eq`, `Fabius.qBinomialThueMorseDyadicTranslatedFormula_refine`, `Fabius.qBinomialThueMorseDyadicFormula_eq_of_rat_eq`, `Fabius.qBinomialThueMorseDyadicHalfShiftFormula_refine`, `Fabius.qBinomialThueMorseDyadicHalfShiftFormula_eq_of_rat_eq` |
| Gaussian/dyadic Prouhet coefficient extractor at base `q = 1/2` | The signed half-`q`-binomial weights at the nodes `-2^k` annihilate every rational polynomial of degree below `n` and, in degree at most `n`, extract its top coefficient times the exact half-`q`-Pochhammer/Mersenne factor.  The `Polynomial.degree` formulation includes `n = 0, p = 0`. | `Fabius.halfQBinomialDyadicWeight`, `Fabius.negativeDyadicNode`, `Fabius.halfQBinomialDyadicWeight_nodes_zero`, `Fabius.halfQBinomialDyadicWeight_nodes_self`, `Fabius.halfQBinomial_negativeDyadic_polynomial_sum_eq_coeff`, `Fabius.halfQBinomial_negativeDyadic_polynomial_sum_eq_mersenne`, `Fabius.halfQBinomial_negativeDyadic_polynomial_sum_eq_zero_of_degree_lt` |
| Inverse-power specialization `F(2^-n)` | Recovered for every natural `n`, including `n = 0`; both the centered and `+1/2` literal sums are exposed.  The arbitrary-numerator formula is proved to specialize exactly to the centered inverse-power formula at numerator one. | `Fabius.qBinomialThueMorseDyadicFormula_one`, `Fabius.fabiusAtInverseTwoPow_eq_qBinomialThueMorse_sum`, `Fabius.fabiusFunction_inverse_two_pow_eq_qBinomialThueMorseFormula`, `Fabius.fabiusAtInverseTwoPow_eq_qBinomialThueMorse_halfShift_sum`, `Fabius.fabius_inverse_two_pow_eq_qBinomialThueMorseHalfShiftFormula` |
| Centered and raw-coordinate inverse-power formulas with arbitrary additive shifts `Q` | Proved over every field over `ℚ`, hence for every real or complex `Q`; explicit Gaussian-rational and fully literal real/complex endpoints are exposed.  The centered inner power is `(r - 2^k + Q)^(n+k)`, while dyadic reflection gives the raw power `(r+Q)^(n+k)` with denominator `(-2)^(n^2)`.  The translation `Q` is independent of the fixed Gaussian base `q = 1/2`.  The zero-one `thueMorseBit` is used literally, and `n = Q = 0` follows Lean's `0^0 = 1` convention. | `Fabius.qBinomialThueMorseTranslatedFormulaIn_eq_centered`, `Fabius.fabiusAtInverseTwoPow_cast_eq_qBinomialThueMorse_translated_sum`, `Fabius.fabius_inverse_two_pow_eq_qBinomialThueMorse_translated_sum_complex`, `Fabius.qBinomialThueMorseRawTranslatedNumeratorPolynomial_eq_const`, `Fabius.qBinomialThueMorseRawTranslatedFormulaIn_eq_fabiusAtInverseTwoPow`, `Fabius.fabiusAtInverseTwoPow_cast_eq_qBinomialThueMorse_rawTranslated_sum`, `Fabius.fabius_inverse_two_pow_eq_qBinomialThueMorse_rawTranslated_sum_real`, `Fabius.fabius_inverse_two_pow_eq_qBinomialThueMorse_rawTranslated_sum_complex`, `Fabius.fabius_inverse_two_pow_eq_qBinomialThueMorseRawTranslatedFormula_gaussianRat` |
| Infinite binary-reduction half-`q`-binomial/Thue--Morse series with translation `Q` | Corrected and proved with outer index `m = 0`: for every real `x` and every real or complex additive translation `Q`, the literal nested summands are absolutely summable and their series sums to the signed global Fabius extension; on `[0,1]` it equals the bounded Fabius function.  For `N ≥ 1`, the finite analytic telescope through scale `N` has uniform error at most `2 * 2^-N`; with the natural-floor zero extension on `x ≤ 0`, its partial sums converge uniformly on all of `ℝ`.  The finite half-`q`-binomial expression (base `q = 1/2`) is a constant polynomial in `Q`.  At `x = 1`, the binary prefix at scale `m` is `2^m`, the scale-zero summand is one, and every positive-scale coefficient and summand vanishes. | `Fabius.qBinomialThueMorseTranslatedNumeratorPolynomial_eq_const`, `Fabius.qBinomialFabiusReductionPolynomial_rclike_eq_fabiusReductionSum`, `Fabius.binaryPreviousPrefix_eq_floor_zpow`, `Fabius.binaryPrefix_one`, `Fabius.globalBinaryReductionSummand_one_zero`, `Fabius.globalBinaryReductionCoefficient_one_eq_zero_of_one_le`, `Fabius.globalBinaryReductionSummand_one_eq_zero_of_one_le`, `Fabius.norm_globalBinaryReductionSum_sub_extendedFabius_le`, `Fabius.globalBinaryReductionSum_tendstoUniformly_extendedFabius`, `Fabius.hasSum_globalBinaryReductionSummand_all`, `Fabius.qBinomialFabiusGlobalSummand`, `Fabius.qBinomialFabiusGlobalSummand_eq`, `Fabius.qBinomialFabiusGlobalSummand_independent`, `Fabius.summable_norm_qBinomialFabiusGlobalSummand_all`, `Fabius.summable_qBinomialFabiusGlobalSummand_all`, `Fabius.hasSum_qBinomialFabiusGlobalSummand_all`, `Fabius.extendedFabius_eq_tsum_qBinomialFabiusGlobalSummand_all`, `Fabius.globalFabius_eq_tsum_qBinomialFabiusGlobalSummand_all`, `Fabius.fabiusReal_eq_tsum_qBinomialFabiusGlobalSummand` |
| Parity-power Fabius series | Corrected and proved with outer index `m = 0` for every real `x`; its summands are absolutely summable and the sum is the signed global Fabius extension.  On `x ≤ 0` every summand vanishes.  The source exponent is interpreted in `ℤ`, so its `n = 0` value is `-1`.  At `x = 1`, the zeroth summand is one and every positive-scale summand vanishes.  On `[0,1]` the sum equals the bounded Fabius function; the original `m = 1` indexing is proved on the half-open domain `0 ≤ x < 1`. | `Fabius.fabiusParityPowerExponent_eq_choose_sub_one`, `Fabius.fabiusParityPowerInner_eq_half_reductionSum`, `Fabius.fabiusParityPowerSummand_eq_globalBinaryReductionSummand`, `Fabius.fabiusParityPowerSummand_eq_zero_of_nonpos`, `Fabius.fabiusParityPowerSummand_one_zero`, `Fabius.fabiusParityPowerSummand_one_eq_zero_of_one_le`, `Fabius.summable_norm_fabiusParityPowerSummand_all`, `Fabius.hasSum_fabiusParityPowerSummand_all`, `Fabius.globalFabius_eq_tsum_fabiusParityPower_literal_all`, `Fabius.fabiusReal_eq_tsum_fabiusParityPowerSummand`, `Fabius.globalFabius_eq_tsum_fabiusParityPowerSummand_succ` |
| Generalized Wolfram `DiscreteLimit` half-`q`-binomial/Thue--Morse formula with translation `Q` | Proved for every real `x` and every additive translation `Q : ℂ`, including Gaussian-rational shifts and arbitrary real shifts (hence irrational ones); the Gaussian base remains `q = 1/2`.  The limit is the signed global Fabius extension; on `x ≤ 0` every finite approximant and the target vanish exactly, while on `[0,1]` the target is the bounded Fabius function.  For `x ≥ 0`, the safe natural range length `⌊2^(n+k)x+1/2⌋₊` equals one plus the inclusive upper cutoff `Floor[2^(n+k)x-1/2]`, including the empty case.  Finite rows may depend on `Q`; their limit does not.  Exact reindexing makes each row a Toeplitz average of convergent complex-shift finite splines.  The exact finite half-`q`-binomial telescope retains its natural `0 ≤ x` hypothesis, but convergence of the binary telescope and both infinite-`tsum` identifications hold for all real `x`; none of these statements asserts termwise equality with a finite `DiscreteLimit` row.  The former nonnegative-domain convergence declarations remain as compatibility forms. | `Fabius.fabiusDiscreteLimitRangeLength_eq_floor_add_one`, `Fabius.fabiusDiscreteLimitApproximationComplex_eq_weighted_shiftSpline`, `Fabius.fabiusDiscreteLimitApproximation_eq_zero_of_nonpos`, `Fabius.extendedFabius_eq_zero_of_nonpos`, `Fabius.fabiusDiscreteLimitApproximationComplex_tendsto_globalFabius_all`, `Fabius.fabiusDiscreteLimitApproximationGaussianRat_tendsto_globalFabius_all`, `Fabius.fabiusDiscreteLimitApproximationReal_tendsto_globalFabius_all`, `Fabius.fabiusDiscreteLimitApproximationRat_tendsto_globalFabius_all`, `Fabius.fabiusDiscreteLimit_literal_complex_tendsto_globalFabius_all`, `Fabius.fabiusDiscreteLimitApproximationComplex_tendsto_globalFabius`, `Fabius.fabiusDiscreteLimitApproximationGaussianRat_tendsto_globalFabius`, `Fabius.fabiusDiscreteLimitApproximationReal_tendsto_globalFabius`, `Fabius.fabiusDiscreteLimitApproximationRat_tendsto_globalFabius`, `Fabius.fabiusDiscreteLimit_literal_complex_tendsto_globalFabius`, `Fabius.fabiusDiscreteLimitApproximationComplex_tendsto_fabiusReal`, `Fabius.extendedFabius_eq_qBinomial_telescope_add_remainder`, `Fabius.globalFabius_eq_qBinomialThueMorse_telescope_add_remainder_complex`, `Fabius.hasSum_globalBinaryReductionSummand_all`, `Fabius.extendedFabius_eq_tsum_globalBinaryReductionSummand_all`, `Fabius.binary_telescope_tendsto_globalFabius_all`, `Fabius.binary_telescope_tendsto_globalFabius`, `Fabius.hasSum_qBinomialFabiusGlobalSummand_all`, `Fabius.extendedFabius_eq_tsum_qBinomialFabiusGlobalSummand_all`, `Fabius.fabiusDiscreteLimitApproximationComplex_tendsto_binary_tsum_all`, `Fabius.fabiusDiscreteLimitApproximationComplex_tendsto_binary_tsum`, `Fabius.fabiusDiscreteLimitApproximationComplex_tendsto_literal_tsum_all`, `Fabius.fabiusDiscreteLimitApproximationComplex_tendsto_literal_tsum` |
| Normalized dyadic Toeplitz row at Gaussian base `q = 1/2` | The complete rational generating polynomial is the normalized finite half-`q`-Pochhammer product.  Its rational roots are exactly `2, 4, ..., 2^n`, including the empty root set at `n = 0`; mass one is the value at `z = 1`.  Reversing the row gives exactly the evaluation-at-zero moments on `1,(1/2),...,(1/2)^n`; pointwise, `discreteLimitWeight n j` is the geometric Lagrange weight at the reversed node `n-j`, and a module-valued sum identity performs the same reindexing on any sample block.  The row annihilates each of the first `n` dyadic geometric modes after their rational coefficients act on an arbitrary vector.  Exact q-Richardson cancellation holds for sequences in every `ℚ`-module both globally and from only the `n+1` samples in a block beginning at arbitrary `start`; these algebraic statements include `n = 0` and need no multiplication, topology, order, or norm on the target.  The existing rational theorem is the `M = ℚ` specialization.  Coefficient absolute values have an exact formula, the exact row variation is `finiteQPochhammer (-1/2) (1/2) n / halfQPochhammer n`, and it is uniformly at most `16`.  A generic normalized-row theorem transfers convergence in any normed ring under uniform variation and escaping-index hypotheses; after casting the rational weights, the `RCLike` specialization proves convergence of the rows sampled at `2n-j`.  No root classification at arbitrary scalar arguments is claimed. | `Fabius.sum_range_discreteLimitWeight_mul_pow`, `Fabius.sum_range_discreteLimitWeight_mul_pow_eq_zero_iff`, `Fabius.sum_range_discreteLimitWeight`, `Fabius.sum_range_discreteLimitWeight_reverse_mul_half_pow`, `Fabius.sum_range_discreteLimitWeight_reverse_moment`, `Fabius.discreteLimitWeight_eq_geometricLagrangeWeight`, `Fabius.geometricLagrangeWeight_half_eq_discreteLimitWeight`, `Fabius.sum_range_discreteLimitWeight_smul_eq_geometricLagrangeWeight`, `Fabius.sum_range_discreteLimitWeight_mul_geometricMode_smul_eq_zero`, `Fabius.discreteLimitWeight_qRichardson_exact_module_at_of_eq`, `Fabius.discreteLimitWeight_qRichardson_exact_module`, `Fabius.discreteLimitWeight_qRichardson_exact`, `Fabius.one_fourth_le_halfQPochhammer`, `Fabius.abs_discreteLimitWeight`, `Fabius.sum_abs_discreteLimitWeight`, `Fabius.sum_abs_discreteLimitWeight_le`, `Fabius.tendsto_weighted_rows_of_tendsto`, `Fabius.discreteLimitWeightIn`, `Fabius.sum_range_discreteLimitWeightIn`, `Fabius.sum_norm_discreteLimitWeightIn_le`, `Fabius.discreteLimit_index_ge`, `Fabius.tendsto_discreteLimitWeightIn_sum` |
| Complete residual moments of the reversed Toeplitz row at Gaussian base `q = 1/2` | The reverse pointwise bridge identifies the half-base geometric Lagrange row with `discreteLimitWeight n (n-k)`.  For every positive degree `d`, its exact residual moment is `(-1)^n (1/2)^(n(n+1)/2) * halfQBinomial (d-1) n`; this includes `n = 0`, recovers cancellation from the above-diagonal Gaussian boundary, and recovers the first surviving moment from the diagonal boundary. | `Fabius.geometricLagrangeWeight_half_eq_discreteLimitWeight`, `Fabius.sum_range_discreteLimitWeight_reverse_moment_eq_halfQBinomial`, `Fabius.gaussianBinomial_eq_zero_of_lt`, `Fabius.gaussianBinomial_self` |
| Gaussian-binomial Richardson filter at base `q = 1/4` and proposed spectral-tail acceleration | **The complete finite filter algebra, its generic unconditionally summable series transform, and its exact specialization to the actual infinite Rvachev sinc product are proved; the report's finite-prefix acceleration and analytic estimates remain a research frontier.**  The denominator-free numerator and every positive residual moment are first proved over arbitrary commutative rings and fields.  Over rational `0 < q < 1`, the geometric Lagrange weights are additionally identified with the established q-Pochhammer/q-binomial notation; every power moment, its residual q-Pochhammer and q-binomial forms, its exact algebraic sign, and the finite exact `l1` norm are proved.  Dedicated `q = 1/4` corollaries expose the report-facing weights, cancellations, residual moments, first uncancelled moment, and finite condition-number quotient.  For every real `0 ≤ q < 1`, the finite row variation converges to `(-q;q)_∞ / (q;q)_∞`; the denominator is strictly positive, the limit is at least `1 / (1-q)`, tends to `+∞` as `q → 1⁻`, and equals one exactly at `q = 0`.  The generic analytic row preserves the constant and has the Gaussian `tsum` tail under unconditional summability, with no convergence obligation at zero-weight nodes.  The named Rvachev even modes are unconditionally summable, so the generic row now gives an exact entire identity for the actual sinc product at arbitrary complex `c,z`, Gaussian base `q=c^2`, and exactly the finite-power injectivity hypothesis; the `c=1/2`, `q=1/4` theorem is assumption-free.  The Euler--zeta exponential sinc expansions hold on their natural disks (including zero), the real logarithmic expansion holds on the punctured disk `0 < abs x < 1`, and the dyadic infinite product has an exact prefix-times-exponential tail with a certified remainder.  Lean still does not identify the report's finite prefixes `P_(b,n)` or quotient `P_(b,n)/P_b` with this filter, derive the Bell coefficients of that quotient, prove a formal-to-analytic boundary bridge, or establish the report's analytic error sign, alternating brackets, quantitative bounds, uniform or derivative convergence, asymptotics, or Thue--Morse transfer. | Generic algebra: `Fabius.reversed_finite_qBinomial_theorem`, `Fabius.geometricLagrangeWeight_eq_gaussianBinomial_div`, `Fabius.sum_geometricLagrangeWeight_mul_pow_eq_gaussianBinomial`.  Generic analytic series: `Fabius.geometricAnalyticSeriesFilter_eq_constant_add_gaussian_tsum`, `Fabius.geometricLagrangeAnalyticSeriesFilter_eq_constant_add_gaussian_tsum`, `Fabius.geometricLagrangeAnalyticSeriesFilter_shifted`.  Actual Rvachev product: `Fabius.hasSum_rvachevFourierMomentTerm_product`, `Fabius.rvachevFourierMomentTerm_pow_scale`, `Fabius.geometricLagrange_rvachevFourierProduct_eq_gaussian_tsum`, `Fabius.quarterLagrange_rvachevFourierProduct_eq_gaussian_tsum`, `Fabius.geometricLagrange_rvachevFourier_eq_gaussian_tsum`.  Sinc analytic input: `Fabius.complexSinc_pi_mul_eq_cexp`, `Fabius.complexSinc_eq_cexp`, `Fabius.log_sin_div_pi_mul`, `Fabius.rvachevFourierProduct_eq_cexp`, `Fabius.rvachevFourierProduct_eq_prefix_mul_cexp`, `Fabius.norm_sincZeta_tail_le`.  Report-facing rational layer: `Fabius.quarterLagrangeWeight_eq_qBinomial`, `Fabius.quarterForwardRichardsonPolynomial_coeff_eq_qBinomial`, `Fabius.quarterGeometricLagrangeQMoment_eq_qPochhammer`, `Fabius.quarterGeometricLagrangeQMoment_eq_zero`, `Fabius.quarterGeometricLagrangeQMoment_eq_residual_qPochhammer`, `Fabius.quarterGeometricLagrangeQMoment_eq_residual_qBinomial`, `Fabius.quarterGeometricLagrangeQMoment_firstUncancelled`, `Fabius.negOnePow_mul_geometricLagrangeQMoment_pos`, `Fabius.sum_abs_quarterGeometricLagrangeWeight_eq_qPochhammer_ratio`, `Fabius.sum_abs_geometricLagrangeWeight_eq_prod`.  Infinite-product and limit layer: `Fabius.qPochhammerInf`, `Fabius.tendsto_finiteQPochhammerIn`, `Fabius.qPochhammerInf_self_pos`, `Fabius.qConditionNumberLimit`, `Fabius.tendsto_sum_abs_qToeplitzWeight`, `Fabius.one_div_one_sub_le_qConditionNumberLimit`, `Fabius.tendsto_qConditionNumberLimit_atTop_at_one_left`, `Fabius.one_lt_qConditionNumberLimit`, `Fabius.qConditionNumberLimit_zero` |
| Phase-locked Lambert reciprocal-grid extraction of the periodic Fabius endpoint term | **The phase locking, complete finite interpolation algebra, finite Bell/generalized-harmonic conversion, growing-weight control, and analytic phase-extraction theorem are proved.**  On the real lower-Lambert branch, the nodes `(lambda+j) * 2^(-(lambda+j))` have exact phase `lambda+j`, so every one-periodic coefficient is frozen.  The fixed-node pullback of the full Fabius expansion is valid to every order.  Reciprocal weights grow at most as `lambda^r`, while their exact higher moments supply the additional nodal-product cancellation.  The reciprocal alphabet now has named power sums and Bell input, and every higher moment, individual Fabius residual, and finite residual partial sum has the exact factorially normalized Bell form from the report.  The direct normalized specialization uses a field with rational-algebra structure, and the weighted moment forms assume characteristic zero; within that setting the definitions are total in `lambda`, and field inversion handles zero shifts without an added nonvanishing hypothesis.  Consequently, for every fixed row order `r` and residual depth `S`, the estimator minus `negativeLaplacePsi` and its first `S` exact residual terms is `O(lambda^(-(r+1+S)))`; the unsubtracted error is `O(lambda^(-(r+1)))`, subtracting the first omitted term improves this to `O(lambda^(-(r+2)))`, and sampling `lambda=a+n` converges to `negativeLaplacePsi a`.  No convergence of the literal infinite residual series, exponentiated Bell relative-error hierarchy, higher derivative extractor, residual sign/bracketing theorem, or uniformity for `r` or `S` growing with `lambda` is claimed. | `Fabius.fabiusLambertPhase_phaseLockedNode`, `Fabius.sum_shiftedReciprocalLagrangeWeight_mul_invPow_card_add`, `Fabius.shiftedReciprocalPowerSum`, `Fabius.shiftedReciprocalBellInput`, `Fabius.shiftedReciprocalPowerSum_eq_sum`, `Fabius.shiftedReciprocalBellInput_zero`, `Fabius.shiftedReciprocalBellInput_succ`, `Fabius.completeHomogeneousEvalOn_shiftedReciprocal_eq_bell`, `Fabius.sum_shiftedReciprocalLagrangeWeight_mul_invPow_card_add_eq_bell`, `Fabius.sum_shiftedReciprocalLagrangeWeight_mul_invPow_eq_bell`, `Fabius.completeHomogeneousEvalOn_isBigO_pow`, `Fabius.shiftedReciprocalLagrangeWeight_mul_invPow_isBigO_atTop`, `Fabius.tendsto_lambertPhaseLockedNode_smallArgument`, `Fabius.log_fabius_phaseLockedNode_sub_WikipediaLambertExpansion_isBigO`, `Fabius.fabiusPhaseLockedPeriodicEstimator`, `Fabius.fabiusPhaseLockedResidualTerm`, `Fabius.fabiusPhaseLockedResidualTerm_eq_bell`, `Fabius.fabiusPhaseLockedResidualPartialSum_eq_sum_bell`, `Fabius.fabiusPhaseLockedPeriodicEstimator_sub_residual_isBigO`, `Fabius.fabiusPhaseLockedPeriodicEstimator_sub_firstOmitted_isBigO`, `Fabius.fabiusPhaseLockedPeriodicEstimator_tendsto_periodicAlong` |
| Fixed-constant asymptotic conjecture for the recurrence sequence | Corrected: the sharp dyadic formula has a genuine nonconstant periodic term and `O(1/n)` error | `Fabius.log_fabius_dyadic_sub_lambertMain_isBigO`, `Fabius.negativeLaplacePsi_not_constant` |
| [Explicit logarithmic small-`x` formula](https://math.stackexchange.com/a/3925650/19661) | Transcribed exactly, but corrected by adding the nonconstant periodic term at the exact lower-Lambert phase | `Fabius.fabiusWikipediaElementaryMain`, `Fabius.fabiusExplicitCorrectedWikipediaMain` |
| Corrected general formula with `O(1 / (-log x))`, and the resulting asymptotic equivalent | Proved unconditionally by the quantitative Bromwich saddle argument | `Fabius.log_fabius_sub_explicitCorrectedWikipediaMain_isBigO`, `Fabius.fabius_isEquivalent_exp_explicitCorrectedWikipediaMain` |
| Complete corrected asymptotic expansion | Proved for every order `N`: `log F - main - sum_{j<N} lambda^{-j} c_j(lambda) = O(lambda^{-N})`, where `c_j` is continuous, bounded, and one-periodic | `Fabius.fabiusSaddleLogCoefficient`, `Fabius.log_fabius_sub_sharpLambertMain_hasAsymptoticExpansion`, `Fabius.log_fabius_sub_sharpLambertExpansion_isBigO` |
| First term beyond the linked formula | Identified explicitly as `fabiusFirstSaddleCorrection(lambda) / lambda`, with an `O(lambda^-2)` remainder after insertion | `Fabius.fabiusSaddleLogPartialSum_two`, `Fabius.fabiusSharpLambertExpansion_two`, `Fabius.log_fabius_sub_sharpLambertExpansion_isBigO` |
| All-orders elementary expansion of the exact phase | Proved independently; the oscillatory saddle coefficients in the full formula remain evaluated at the exact phase | `Fabius.fabiusLambertLiteralApproximation`, `Fabius.fabiusLambertPhase_sub_literalApproximation_isBigO` |
| Uncorrected formula with the same error | **False** because the omitted periodic correction cannot be absorbed into a vanishing remainder | `Fabius.negativeLaplacePsi_comp_fabiusLambertPhase_not_isBigO`, `Fabius.log_fabius_sub_WikipediaElementaryMain_not_isBigO` |
| [Quotient-of-exponentials approximation](https://mathematica.stackexchange.com/questions/285919/approximation-of-the-fabius-function-with-a-quotient-of-exponentials) | Formally refuted as an endpoint asymptotic: it is little-o of the true displaced bump | `Fabius.mathematicaFabiusQuotientCandidate_isLittleO_fabiusLogPhi`, `Fabius.mathematicaFabiusQuotientCandidate_not_isEquivalent_fabiusLogPhi` |

## Local draft: *K-fold summation over the signed Thue--Morse sequence*

The source draft audited for this section is likewise not currently vendored
in the repository.  It contains **zero** formal theorem, lemma, proposition,
or corollary environments.  Its public Lean aggregate is
`FabiusFunction.PaperKFoldThueMorse`.

| Source claim | Status | Lean declaration(s) |
| --- | --- | --- |
| Thue--Morse sign definition and self-similarity | Proved | `Fabius.binaryWeight`, `Fabius.thueMorseSign`, `Fabius.thueMorseSign_two_mul`, `Fabius.thueMorseSign_two_mul_add_one` |
| Canonical binary-support dictionary and Boolean-cube reindexing (foundational engine) | `bitSupport` is exactly Mathlib's canonical finite set of binary indices.  Summing its distinct two-powers reconstructs the natural number, taking the support of any finite two-power sum reconstructs the exponent set, and binary weight/sign become cardinality/parity with no boundedness hypothesis.  Restriction to subsets of `range m` gives an equivalence with `Fin (2^m)` and an additive-commutative-monoid-valued reindexing from the powerset to the dyadic range.  The empty support and the singleton level `m = 0` are included. | `Fabius.bitSupport`, `Fabius.bitSupport_eq_toFinset_bitIndices`, `Fabius.mem_bitSupport`, `Fabius.sum_two_pow_bitSupport`, `Fabius.bitSupport_sum_two_pow`, `Fabius.sum_two_pow_injective`, `Fabius.binaryWeight_sum_two_pow_eq_card`, `Fabius.thueMorseSign_sum_two_pow`, `Fabius.bitSupport_subset_range_iff_lt_two_pow`, `Fabius.powersetRangeEquivFin`, `Fabius.powersetRangeEquivFin_zero_apply`, `Fabius.image_sum_two_pow_powerset_range`, `Fabius.sum_powerset_two_pow` |
| Degree-valued Boolean-cube and sparse Prouhet annihilation | Proved over every commutative ring using `Polynomial.degree`.  Unlike the legacy `natDegree` interfaces, the public degree-valued theorems include the genuine boundary where the support is empty: the strict bound then admits exactly the zero polynomial.  Consequently the sparse theorem is valid also at submask parameter `n = 0`; the sharp moment remains valid there under the `0^0 = 1` convention.  Both powerset parametrizations and the literal odd-Pascal-row indices are exposed. | `Fabius.sum_powerset_neg_one_pow_eval_of_degree_lt`, `Fabius.sum_powerset_neg_one_pow_pow_card`, `Fabius.sum_thueMorseSign_mul_affine_eval_of_degree_lt`, `Fabius.sum_submask_neg_one_pow_eval_of_degree_lt`, `Fabius.sum_submask_neg_one_pow_pow`, `Fabius.sum_oddBinomialIndices_thueMorseSign_mul_affine_eval_of_degree_lt`, `Fabius.sum_oddBinomialIndices_thueMorseSign_mul_affine_pow_binaryWeight` |
| Even/odd binary-weight decomposition and Prouhet partition (foundational engine) | Proved over every commutative ring.  On an arbitrary finite set, a Thue--Morse signed sum is exactly its even-binary-weight part minus its odd-binary-weight part.  On the full block below `2^m`, the affine power sums of the two parts agree in every degree strictly below `m`; the classical raw-power partition is the specialization at zero translation and unit step.  The strict degree bound makes the level-zero partition statement vacuous, while the generic finite-set decomposition remains nonvacuous there. | `Fabius.sum_thueMorseSign_mul_eq_sum_even_binaryWeight_sub_sum_odd_binaryWeight`, `Fabius.sum_even_binaryWeight_affine_pow_eq_sum_odd_binaryWeight_affine_pow`, `Fabius.sum_even_binaryWeight_pow_eq_sum_odd_binaryWeight_pow` |
| Additional requested identity (not in the draft): zero-one `ThueMorse[n]` binomial/`Log2` formula | Proved for every natural `n`, with the integer inside `Log2` first identified exactly as `2^(binaryWeight n + 1)` and the final division interpreted in `ℚ` | `Fabius.thueMorseBit`, `Fabius.signedBinomialParitySum`, `Fabius.thueMorseLogIntegerArgument_eq_two_pow`, `Fabius.log2_thueMorseLog2Argument`, `Fabius.thueMorseBit_eq_log2_binomialParity_formula` |
| Inclusive iterated prefix sums and odd first-prefix zeros | Proved | `Fabius.iteratedPrefix`, `Fabius.iteratedPrefix_succ`, `Fabius.iteratedPrefix_succ_sub`, `Fabius.iteratedPrefix_one_two_mul_add_one` |
| Equation (1), literal normalized grid | Defined exactly | `Fabius.paperPrefixGridValue`, `Fabius.prefixGridPoint` |
| Equation (1), instruction to join consecutive grid points | Defined as the intended real polygon (with a rational companion), distinct from the contradictory floor/step wording | `Fabius.paperPrefixPolygonReal`, `Fabius.paperPrefixPolygonReal_grid`, `Fabius.paperPrefixPolygon`, `Fabius.paperPrefixPolygon_grid` |
| Equation (2), discrete functional equation | Proved with the omitted unit-interval index condition made explicit | `Fabius.paperPrefixGridValue_equation`, `Fabius.paperPrefixGridValue_equation_of_pos`, `Fabius.prefixGridPoint_lower_argument_mem` |
| Additional library generalization: arbitrary-shift prefix-grid recurrence | **Proved and compiler-validated.**  Source checkpoint `00ff41a5e` is validated at immutable coordinator merge `ae16882d5`.  For `V_s(k,j) = S^(k+s)_j / 2^(k.choose 2)`, the unscaled and denominator-cleared recurrences hold for every natural shift `s`.  Shift zero is definitionally the literal equation-(1) grid, and shift one is the inclusive-prefix correction.  The exact equation records `j < 2^q` so that its lower-level abscissa lies in `[0,1]`; the positive-level form assumes `0 < k` and `j < 2^(k-1)`.  Endpoint, polygon, and convergence semantics are deliberately not generalized because they depend on the shift. | `Fabius.shiftedPrefixGridValue`, `Fabius.shiftedPrefixGridValue_zero`, `Fabius.shiftedPrefixGridValue_one`, `Fabius.shiftedPrefixGridValue_succ_sub`, `Fabius.shiftedPrefixGridValue_scaledDifference`, `Fabius.shiftedPrefixGridValue_equation`, `Fabius.shiftedPrefixGridValue_equation_of_pos` |
| Equation (3), Fabius characterization | Repaired: the bounded derivative equation holds only on `[0,1/2]` and symmetry is required | `Fabius.IsFabius`, `Fabius.existsUnique_fabius` |
| Claimed convergence of the literal equation-(1) normalization | **False**, even pointwise at `x = 1`; the intended real polygon has the same obstruction | `Fabius.paperPrefixGridValue_endpoint`, `Fabius.paperPrefixGridValue_endpoint_not_tendsto_one`, `Fabius.paperPrefixPolygonReal_endpoint_not_tendsto_one` |
| Corrected prefix approximation | Proved pointwise, with one prefix-order shift and the centered-cell interpretation; no uniform rate is claimed | `Fabius.correctedPrefixCoefficient_eq_stepApproximant`, `Fabius.correctedPrefixGridSample_tendsto_rvachevUp`, `Fabius.correctedPrefixGridSample_tendsto_fabius` |
| Repeated-derivative scaling | Proved for the signed global extension; the bounded global wording is domain-invalid | `Fabius.iteratedDeriv_extendedFabius` |
| Signed dyadic pre-run reciprocity and reflected zero locus; separate terminal zero run, left boundary, and right boundary | Proved for the literal hypotheses `k ≤ r` and `k + d < 2 ^ r`; the zero equivalence pairs any zeros and does not assert a zero-free pre-run window | Base sign reflection: `Fabius.thueMorseSign_dyadic_complement`; signed prefix reflection: `Fabius.iteratedPrefix_dyadic_reverse_window`; reflected zero iff: `Fabius.iteratedPrefix_dyadic_reverse_window_eq_zero_iff`; value at zero used by the `d = 0` specialization: `Fabius.iteratedPrefix_at_zero`; terminal endpoint/run: `Fabius.iteratedPrefix_dyadic_endpoint`, `Fabius.iteratedPrefix_dyadic_zero_reverse`, `Fabius.iteratedPrefix_dyadic_zero_run`; left boundary: `Fabius.iteratedPrefix_before_dyadic_run`; right boundary: `Fabius.iteratedPrefix_at_dyadic` |
| Equation (4), local `4h` error and global uniform error | **False** for the printed normalization | `Fabius.literalGridSlope_two_one`, `Fabius.literal_local_error_bound_false`, `Fabius.paperPrefixGridValue_endpoint_not_tendsto_one` |
| Equation (5), discrete B-spline kernel and binomial convolution | Proved in exact source-facing form, with a shifted recurrence also available | `Fabius.iteratedPrefixKernel`, `Fabius.iteratedPrefix_eq_sum_kernel`, `Fabius.iteratedPrefix_convolution` |
| Equation (6), both generating-series identities | Proved as formal power-series identities, with finite coefficient stabilization for the infinite product | `Fabius.thueMorseBlockPolynomial_eq_product`, `Fabius.coeff_finite_thueMorse_product`, `Fabius.iteratedPrefixSeries_eq`, `Fabius.one_sub_X_pow_mul_iteratedPrefixSeries`, `Fabius.coeff_eq6_finite` |
| Block-polynomial derivatives at one | Proved through the exact derivative/binomial-moment dictionary, including every lower-order zero and the first nonzero derivative | `Fabius.iterate_derivative_thueMorseBlockPolynomial_eval_one`, `Fabius.iterate_derivative_thueMorseBlockPolynomial_eval_one_of_lt`, `Fabius.iterate_derivative_thueMorseBlockPolynomial_eval_one_self` |
| Equation (7), maximum proxy | **False**: for every `x > 0` its terms are unbounded and no maximum exists | `Fabius.paperProxyTerm_succ`, `Fabius.paperProxyRatio_tendsto_atTop`, `Fabius.paperProxyTerm_unbounded`, `Fabius.paperProxyTerm_has_no_maximum` |
| Stirling estimate invoked after equation (7) | Proved exactly as `log(n!) = n log n - n + O(log n)` | `Fabius.log_factorial_sub_main_isBigO_log` |
| Equation (8), continuous stationary equation | Repaired positive-real calculus proved after making the draft's integer-to-real change explicit | `Fabius.paperStirlingPhi_hasDerivAt`, `Fabius.paper_stationary_iff_mul_two_rpow_neg` |
| Equation (9), Lambert `W_{-1}` solution | Proved for `0 < x` with `(log 2)x ≤ exp(-1)`, equivalently for the Lambert argument `z = -(log 2)x` in the full closed-left lower-branch domain `[-exp(-1),0)`.  On that domain, the branch has endpoint value `-1`, stays at or below `-1`, satisfies its defining equation, is the unique solution at or below `-1`, is strictly decreasing, has image `(-∞,-1]`, and is continuous through the finite branch point.  The stationary value solves equation (9) at the branch point as well, satisfies `1 / log 2 ≤ paperLambertN(x)`, attains equality exactly at the branch point, and is strictly larger in the interior.  The former strict-domain declarations remain as compatibility forms; zero remains excluded from the lower branch. | `Fabius.lowerLambertW_branchPoint`, `Fabius.lowerLambertW_le_neg_one`, `Fabius.lowerLambertW_mul_exp_of_mem_Ico`, `Fabius.lowerLambertW_unique_of_mem_Ico`, `Fabius.lowerLambertW_strictAntiOn_Ico`, `Fabius.lowerLambertW_image_Ico`, `Fabius.lowerLambertW_continuousWithinAt_branchPoint`, `Fabius.lowerLambertW_continuousAt`, `Fabius.lowerLambertW_continuousOn`, `Fabius.lowerLambertW_continuousOn_Ico`, `Fabius.paperLambertN_eq9_of_le`, `Fabius.one_div_log_two_le_paperLambertN`, `Fabius.paperLambertN_eq_one_div_log_two`, `Fabius.paperLambertN_eq_one_div_log_two_iff`, `Fabius.one_div_log_two_lt_paperLambertN`, `Fabius.lowerLambertW_lt_neg_one`, `Fabius.lowerLambertW_mul_exp`, `Fabius.lowerLambertW_unique`, `Fabius.lowerLambertW_strictAntiOn`, `Fabius.lowerLambertW_image`, `Fabius.paperLambertN_eq9` |
| Principal real Lambert branch `W_0` | The companion branch is constructed on `[-exp(-1),∞)`, solves `W_0(z) exp(W_0(z)) = z`, is the unique solution at or above `-1`, is strictly increasing, and is continuous on its full closed natural domain.  Its first derivative is formalized for `z > -exp(-1)`; at zero it equals one and `W_0(z) ~ z`.  The exact images of `(-exp(-1),∞)` and `[-exp(-1),0]` are `(-1,∞)` and `[-1,0]`.  The raw branch-point derivative/secant limits and leading signed square-root laws are covered in the separate rows below; no finite endpoint derivative is asserted. | `Fabius.principalLambertArg`, `Fabius.principalLambertW`, `Fabius.principalLambertW_mul_exp`, `Fabius.neg_one_le_principalLambertW`, `Fabius.principalLambertW_unique`, `Fabius.principalLambertW_branchPoint`, `Fabius.principalLambertW_zero`, `Fabius.principalLambertW_exp_one`, `Fabius.mul_exp_strictMonoOn`, `Fabius.principalLambertW_strictMonoOn`, `Fabius.principalLambertW_nonpos`, `Fabius.neg_one_lt_principalLambertW`, `Fabius.principalLambertW_image_Ioi`, `Fabius.principalLambertW_image_Icc`, `Fabius.principalLambertW_continuousWithinAt_branchPoint`, `Fabius.principalLambertW_continuousAt`, `Fabius.principalLambertW_continuousOn`, `Fabius.principalLambertW_continuousOn_Ici`, `Fabius.principalLambertW_hasDerivAt`, `Fabius.deriv_principalLambertW_pos`, `Fabius.deriv_principalLambertW_zero`, `Fabius.principalLambertW_isEquivalent_zero` |
| Exact pairing of the two real Lambert branches, the converse gap coordinate, and symmetric corollaries | **Proved on the exact strict domains.**  `LambertWBranchPairing` is 0+7, `LambertWGapBijection` is 4+16, and `LambertWBranchSymmetry` is 0+9, so the deduplicated union is four definitions plus 32 theorems, 36 declarations.  For `x ∈ (-exp(-1),0)`, `Δ=W₀(x)-W₋₁(x)>0`; the two branch values and their common argument have the exact exponential-rational gap forms.  Conversely, `gapArg` and `branchGap` are two-sided inverses between `(-exp(-1),0)` and `(0,∞)`, proving the gap map is a bijection.  For every `t>1`, the explicit `t=exp Δ` formulas recover `W₀`, `W₋₁`, and the argument.  The quotient is `W₋₁/W₀=exp Δ`; both exponential and hyperbolic sum/product forms are proved, together with `W₀+W₋₁<-2` and `0<W₀W₋₁<1`.  The branch point and zero endpoint are excluded, exactly as the divisions require.  This is a finite exact package: it does not claim the guide's Bernoulli-number series for the gap factors, its convergence or remainder theory, or any branch-point/small-input asymptotic. | Pairing 0+7: `Fabius.principalLambertW_sub_lowerLambertW_pos`, `Fabius.lowerLambertW_eq_principalLambertW_mul_exp_gap`, `Fabius.principalLambertW_eq_neg_gap_div`, `Fabius.lowerLambertW_eq_neg_gap_mul_exp_div`, `Fabius.lowerLambertW_eq_neg_gap_div_one_sub_exp_neg`, `Fabius.eq_neg_gap_div_mul_exp`, `Fabius.principalLambertW_lowerLambertW_eq_of_exp_gap`.  Gap bijection definitions: `Fabius.gapPrincipal`, `Fabius.gapLower`, `Fabius.gapArg`, `Fabius.branchGap`; its 16 theorems: `Fabius.gap_denominator_pos`, `Fabius.gapPrincipal_mem_Ioo`, `Fabius.gapLower_eq_mul_exp`, `Fabius.gapLower_eq_sub`, `Fabius.gapLower_lt_neg_one`, `Fabius.gapLower_mul_exp`, `Fabius.gapArg_mem_Ioo`, `Fabius.principalLambertW_gapArg`, `Fabius.lowerLambertW_gapArg`, `Fabius.branchGap_gapArg`, `Fabius.gapArg_branchGap`, `Fabius.branchGap_invOn`, `Fabius.branchGap_bijOn`, `Fabius.principalLambertW_gapArg_log`, `Fabius.lowerLambertW_gapArg_log`, `Fabius.gapArg_log`.  Symmetry 0+9: `Fabius.lowerLambertW_div_principalLambertW_eq_exp_branchGap`, `Fabius.principalLambertW_add_lowerLambertW_eq_exp_branchGap`, `Fabius.principalLambertW_add_lowerLambertW_eq_cosh_div_sinh_branchGap`, `Fabius.principalLambertW_mul_lowerLambertW_eq_exp_branchGap`, `Fabius.principalLambertW_mul_lowerLambertW_eq_sinh_sq_branchGap`, `Fabius.principalLambertW_add_lowerLambertW_lt_neg_two`, `Fabius.principalLambertW_mul_lowerLambertW_pos`, `Fabius.principalLambertW_mul_lowerLambertW_lt_one`, `Fabius.principalLambertW_mul_lowerLambertW_mem_Ioo`. |
| Lambert Guide `eq:bernoulli-gen` and `eq:pair-Bernoulli-general` | **Exact with the removable-origin representation made explicit.**  The zero-definition/five-theorem `LambertWBranchGapBernoulli` leaf proves absolute convergence of `∑ B_n z^n/n!` for every real `|z|<2π`; for every complex `z`, it proves summability exactly on `‖z‖<2π`, hence divergence on the boundary and throughout the exterior.  It evaluates the real series as an actual `HasSum` to `z/(exp z-1)` when `z≠0`, and its complex theorem states that the series has sum `(complexExpm1Div z)⁻¹` exactly when `‖z‖<2π`, including value `1` at `z=0`.  Therefore `eq:bernoulli-gen` is Exact only when its quotient is interpreted by this canonical removable extension; no equality to Lean's literal totalized quotient at zero and no holomorphy theorem is claimed.  On `x∈(-exp(-1),0)`, `Δ=branchGap x<2π`, the branch theorem proves the two displayed identities `W₀(x)=-∑ B_nΔ^n/n!` and `W₋₁(x)=-Δ-∑ B_nΔ^n/n!`, making `eq:pair-Bernoulli-general` Exact.  Together with the three finite branch-coordinate modules, the four-module union has four definitions and 37 theorems, 41 declarations.  The real quotient statement excludes `z=0`, the branch statement excludes both real-branch endpoints, and no remainder bound or higher/convergent Puiseux expansion is proved. | `Fabius.summable_norm_bernoulli_mul_pow_div_factorial`, `Fabius.summable_bernoulli_mul_pow_div_factorial_iff`, `Fabius.hasSum_bernoulli_mul_pow_div_factorial`, `Fabius.hasSum_bernoulli_mul_pow_div_factorial_complex_iff`, `Fabius.principalLambertW_lowerLambertW_eq_bernoulliSeries` |
| Curvature of both real Lambert branches | For `W_0` when `z > -exp(-1)`, and for `W_{-1}` when `-exp(-1) < z < 0`, the exact nonsingular second derivative is `-exp(-2W)(W+2)/(W+1)^3`.  Thus `W_0''(0) = -2` and `W_0` is strictly concave on `[-exp(-1),∞)`.  On the lower smooth domain the sign changes exactly at `z = -2 exp(-2)`: the branch is strictly convex on `[-exp(-1),-2 exp(-2)]` and strictly concave on `[-2 exp(-2),0)`.  The finite-endpoint shape statements use continuity and do not assert a derivative limit there. | `Fabius.deriv_principalLambertW`, `Fabius.deriv_principalLambertW_hasDerivAt`, `Fabius.deriv_deriv_principalLambertW`, `Fabius.deriv_deriv_principalLambertW_zero`, `Fabius.deriv_deriv_principalLambertW_neg`, `Fabius.strictConcaveOn_principalLambertW`, `Fabius.deriv_lowerLambertW_hasDerivAt`, `Fabius.deriv_deriv_lowerLambertW`, `Fabius.deriv_deriv_lowerLambertW_pos_iff`, `Fabius.deriv_deriv_lowerLambertW_neg_iff`, `Fabius.deriv_deriv_lowerLambertW_eq_zero_iff`, `Fabius.strictConvexOn_lowerLambertW_left`, `Fabius.strictConcaveOn_lowerLambertW_right` |
| One-sided Lambert branch-point vertical tangents | From the right of `-exp(-1)`, `deriv W_0` and the endpoint secant slope `(W_0(z)+1)/(z+exp(-1))` tend to `+∞`; the corresponding lower-branch quantities tend to `-∞`.  Neither branch therefore has a finite right derivative within `(-exp(-1),∞)`, and neither totalized real function is differentiable at the branch point.  This geometry package does not use a square-root expansion. | `Fabius.tendsto_deriv_principalLambertW_branchPoint_atTop`, `Fabius.tendsto_deriv_lowerLambertW_branchPoint_atBot`, `Fabius.tendsto_principalLambertW_secantSlope_branchPoint_atTop`, `Fabius.tendsto_lowerLambertW_secantSlope_branchPoint_atBot`, `Fabius.principalLambertW_not_differentiableWithinAt_branchPoint`, `Fabius.lowerLambertW_not_differentiableWithinAt_branchPoint`, `Fabius.principalLambertW_not_differentiableAt_branchPoint`, `Fabius.lowerLambertW_not_differentiableAt_branchPoint` |
| Leading signed Lambert branch-point scale | Define `scale(z) = sqrt(2 exp(1) (z+exp(-1)))`.  It is positive strictly to the right of the branch point and its square is exactly `2 exp(1) (z+exp(-1))`.  For both branches, `(W(z)+1)^2/(z+exp(-1)) → 2 exp(1)`, equivalently the squared displacements are asymptotic to the same linear normalization; the signed first-order laws are `W_0(z)+1 ~ scale(z)` and `W_{-1}(z)+1 ~ -scale(z)`.  This is only the leading equivalent: an `O(z+exp(-1))` remainder, a convergent signed Puiseux expansion, and higher coefficients remain open. | `Fabius.lambertWBranchPointScale`, `Fabius.lambertWBranchPointScale_pos`, `Fabius.lambertWBranchPointScale_sq`, `Fabius.tendsto_principalLambertW_add_one_sq_div_branchPoint`, `Fabius.tendsto_lowerLambertW_add_one_sq_div_branchPoint`, `Fabius.principalLambertW_add_one_sq_isEquivalent_branchPoint`, `Fabius.lowerLambertW_add_one_sq_isEquivalent_branchPoint`, `Fabius.principalLambertW_add_one_isEquivalent_branchPoint`, `Fabius.lowerLambertW_add_one_isEquivalent_branchPoint` |
| Scaled power--exponential saddle `A lambda^m exp(-beta lambda)` | For `m : ℕ` with `m ≠ 0`, `A > 0`, and `beta > 0`, both nonnegative inverse branches are formalized.  Their solve laws include the common peak, their images are `[0,m/beta]` and `[m/beta,∞)`, and each phase is packaged with the saddle as a two-sided `InvOn`.  The principal phase is continuous and strictly increasing on `[0,peak]`; the lower phase is continuous and strictly decreasing on `(0,peak]`.  The first derivative `lambda / (x * (m - beta*lambda))` and its sign hold on `(0,peak)`.  For `x ∈ (0,peak]` and `lambda ≥ 0`, the saddle equation holds iff `lambda` is the principal or lower phase; the roots are distinct on the open interval. | `Fabius.powerExponentialSaddle`, `Fabius.powerExponentialPeak`, `Fabius.powerExponentialLambertArgument_mem_Icc`, `Fabius.powerExponentialLambertArgument_mem_Ico`, `Fabius.powerExponentialLambertArgument_mem_Ioo`, `Fabius.principalPowerExponentialPhase_solves`, `Fabius.lowerPowerExponentialPhase_solves`, `Fabius.principalPowerExponentialPhase_mem_Icc`, `Fabius.lowerPowerExponentialPhase_mem_Ici`, `Fabius.powerExponentialLambertArgument_strictAntiOn`, `Fabius.principalPowerExponentialPhase_strictMonoOn`, `Fabius.lowerPowerExponentialPhase_strictAntiOn`, `Fabius.principalPowerExponentialPhase_hasDerivAt`, `Fabius.lowerPowerExponentialPhase_hasDerivAt`, `Fabius.deriv_principalPowerExponentialPhase`, `Fabius.deriv_lowerPowerExponentialPhase`, `Fabius.deriv_principalPowerExponentialPhase_pos`, `Fabius.deriv_lowerPowerExponentialPhase_neg`, `Fabius.principalPowerExponentialPhase_continuousOn`, `Fabius.lowerPowerExponentialPhase_continuousOn`, `Fabius.principalPowerExponentialPhase_continuousOn_Icc`, `Fabius.lowerPowerExponentialPhase_continuousOn_Ioc`, `Fabius.powerExponentialLambertArgument_image_Icc`, `Fabius.powerExponentialLambertArgument_image_Ioc`, `Fabius.principalPowerExponentialPhase_image_Icc`, `Fabius.lowerPowerExponentialPhase_image_Ioc`, `Fabius.principalPowerExponentialPhase_invOn`, `Fabius.lowerPowerExponentialPhase_invOn`, `Fabius.powerExponentialSaddle_eq_iff_eq_principal_or_eq_lower`, `Fabius.principalPowerExponentialPhase_ne_lowerPowerExponentialPhase` |
| Second derivatives of the scaled phases | On `(0,peak)`, both branches satisfy `lambda'' = lambda * (m - (m - beta*lambda)^2) / (x^2 * (m - beta*lambda)^3)` under the same three generic hypotheses.  The module intentionally stops at this exact formula: it does not yet export the general square-root threshold, inflection, or strict-shape classification. | `Fabius.deriv_principalPowerExponentialPhase_hasDerivAt`, `Fabius.deriv_deriv_principalPowerExponentialPhase`, `Fabius.deriv_lowerPowerExponentialPhase_hasDerivAt`, `Fabius.deriv_deriv_lowerPowerExponentialPhase` |
| Small-input scaled Lambert phases and the Fabius specialization | For `m : ℕ` with `m ≠ 0`, `A > 0`, and `beta > 0`, the intrinsic positive scale `epsilon(x) = (beta/m)(x/A)^(1/m)` tends to zero as `x ↓ 0`; the principal phase is equivalent to `(x/A)^(1/m)`, while the lower phase tends to `+∞` and differs by `o(1)` from its two-term intrinsic logarithmic main term.  This does not provide a cleaned `L = log(A/x)` form or generic complete series.  At `(m,A,beta) = (1,1,log 2)`, the exact root dichotomy, distinctness, full-domain continuity, and principal equivalent specialize to the classical Fabius phases.  Named generic/Fabius phase wrappers for the raw branch-point derivative, secant, and leading square-root laws remain open. | `Fabius.powerExponentialLambertEpsilon`, `Fabius.powerExponentialLambertArgument_eq_neg_epsilon`, `Fabius.powerExponentialLambertEpsilon_pos`, `Fabius.tendsto_powerExponentialLambertEpsilon_nhdsGT_zero`, `Fabius.principalPowerExponentialPhase_isEquivalent_rpow`, `Fabius.tendsto_lowerPowerExponentialPhase_nhdsGT_zero_atTop`, `Fabius.lowerPowerExponentialPhaseIntrinsicMain`, `Fabius.lowerPowerExponentialPhase_sub_intrinsicMain_tendsto_zero`, `Fabius.lowerPowerExponentialPhase_rate_one`, `Fabius.generalizedLambertCoordinate_argument_mem_Ioo`, `Fabius.generalizedLambertCoordinate_argument_mem_Ico`, `Fabius.generalizedLambertCoordinate_solves_saddle_of_mem`, `Fabius.powerExponentialSaddle_one_one_log_two`, `Fabius.powerExponentialPeak_one_one_log_two`, `Fabius.lowerPowerExponentialPhase_one_one_log_two`, `Fabius.fabiusPrincipalLambertPhase`, `Fabius.fabiusSaddle_eq_iff_eq_principal_or_eq_lower`, `Fabius.fabiusPrincipalLambertPhase_ne_fabiusLambertPhase`, `Fabius.fabiusPrincipalLambertPhase_continuousOn_Icc`, `Fabius.fabiusLambertPhase_continuousOn_Ioc`, `Fabius.fabiusPrincipalLambertPhase_isEquivalent_id` |
| Exact Fabius Lambert phase geometry | On the smooth interval `0 < x < peak`, the lower phase has unique inflection input `x* = 2 exp(-2)/log 2`, phase value `2/log 2`, and positive/zero/negative second derivative exactly before/at/after `x*`; it is strictly convex on `(0,x*]` and strictly concave on `[x*,peak]`.  The principal phase is represented globally by the affine principal-Lambert coordinate, is continuous on `(-∞,peak]`, has positive second derivative for every input below the peak (including value `2 log 2` at zero), and is strictly convex on the whole closed half-line. | `Fabius.fabiusLambertInflectionInput`, `Fabius.fabiusLambertInflectionInput_mem_Ioo`, `Fabius.fabiusLambertPhase_inflectionInput`, `Fabius.deriv_deriv_fabiusLambertPhase`, `Fabius.deriv_deriv_fabiusLambertPhase_pos_iff`, `Fabius.deriv_deriv_fabiusLambertPhase_neg_iff`, `Fabius.deriv_deriv_fabiusLambertPhase_eq_zero_iff`, `Fabius.deriv_deriv_fabiusLambertPhase_inflectionInput`, `Fabius.strictConvexOn_fabiusLambertPhase_left`, `Fabius.strictConcaveOn_fabiusLambertPhase_right`, `Fabius.fabiusPrincipalLambertPhase_eq_principalLambertW`, `Fabius.fabiusPrincipalLambertPhase_continuousOn_Iic`, `Fabius.fabiusPrincipalLambertPhase_hasDerivAt`, `Fabius.deriv_fabiusPrincipalLambertPhase`, `Fabius.deriv_fabiusPrincipalLambertPhase_hasDerivAt`, `Fabius.deriv_deriv_fabiusPrincipalLambertPhase`, `Fabius.deriv_deriv_fabiusPrincipalLambertPhase_pos`, `Fabius.deriv_deriv_fabiusPrincipalLambertPhase_zero`, `Fabius.strictConvexOn_fabiusPrincipalLambertPhase` |
| Equation (10), claimed `O(log n)` error after stationary substitution | **False for the displayed Stirling proxy**: substitution leaves an omitted linear `+n`, which is not `O(log n)` | `Fabius.paperStirlingPhi_of_stationary`, `Fabius.paperStirlingOmittedTerm_not_isBigO_log` |
| Standard lower-branch expansion used before equation (11) | Proved as a positive-side limit: `W₋₁(-ε) - (log ε - log |log ε|) → 0` | `Fabius.tendsto_lowerLambertW_expansion` |
| Equation (11), coarse log-squared decay | Proved rigorously without the false maximum/Lambert-W chain | `Fabius.log_fabiusLogPhi_add_quadratic_isBigO`, `Fabius.fabiusLogProfile_normalized_tendsto` |
| “Faster than every power” | Proved from smooth flatness | `Fabius.extendedFabius_isLittleO_pow_at_zero`, `Fabius.fabius_isLittleO_pow_at_zero` (two-sided), `Fabius.fabius_isLittleO_pow_at_zero_right` |
| “Slower than `exp(-c/x)`” | Proved precisely on the dyadic logarithmic scale: for every `c > 0`, `exp (-c 2^t)` is little-o of `F(2^-t)` | `Fabius.exp_neg_two_rpow_isLittleO_fabiusLogPhi`, `Fabius.exp_neg_two_rpow_isLittleO_fabius` |

The corrected approximation is a separate theorem, not a reinterpretation
that makes the literal source normalization true.  In particular, the
development does not claim the draft's suggested quantitative uniform rate.

## Corrections to the printed sources

The Lean declarations do not assert formulas known to be false.  The relevant
corrections are documented in their source modules:

- arXiv:1702.05442 equation (12) has a finite upper index;
- the step functions use half-weighted shared endpoints;
- equation (25) includes `t`, equation (26) assumes `n > 0`, and equation
  (32) uses consistent scaling;
- equations (33), (36), and (38) expose their necessary positivity or
  removable-singularity handling;
- arXiv:1702.06487 Lemma 1 assumes `0 ≤ scale + order`;
- `R_n` uses the positive exponent required by equation (27) and the examples;
- the proof-internal naturality assertion in Theorem 20 is stated for
  `2 * d_k * N_n`, since the printed assertion without the factor two is
  false already at small indices.
