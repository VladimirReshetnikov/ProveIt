# Coordinator board for `Analysis/FabiusFunction`

> **Status: INACTIVE.**  The multi-agent campaign that used this board
> concluded on 2026-08-26.  Its feature branches were merged or archived and
> then deleted, the worktrees were removed, and the per-branch registry files
> were cleared.  No lease, freeze, token, queue entry, or branch-specific
> instruction recorded in any earlier revision of this file is in effect.
> The complete campaign record remains available through
> `git log -- Analysis/FabiusFunction/docs/registry/`.
>
> **To re-activate:** designate a coordinator, replace this status block with
> `Status: ACTIVE` plus the date and the coordinator's branch, pin the
> integration SHA below, and start filling in the live sections.  Workers then
> follow the "Live coordination protocol" in [`../../AGENTS.md`](../../AGENTS.md)
> and the `SYNC Fabius` reply template in
> [`../COLLABORATION.md`](../COLLABORATION.md), each advertising its claims in
> its own new `docs/registry/<branch-with-slashes-replaced-by-dashes>.md` file.

## Board skeleton (empty while inactive)

### Pinned integration SHA

None — the board is inactive; `main` is advanced directly.

### Immediate shared instructions

None.

### Active path map and branch-specific instructions

No branches are registered.  No path is hot, frozen, or single-owner.

### Collision and integration queue

Empty.

### Build-token log

No Lean/Lake or TeX/PDF token is assigned.  Outside a campaign the
single-`lean`-process rule in `AGENTS.md` ("Building Lean") still applies on
this machine, for memory rather than coordination reasons.

### Worktree maintenance log

Empty.
