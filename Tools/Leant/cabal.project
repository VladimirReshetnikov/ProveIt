-- Leant builds together with the vendored Djex synthesis library
-- (lib/Djex, a read-only git submodule) so that :synth can link the
-- Djinn engine in-process. Both packages target GHC 9.12.4.
packages:
  .
  ../../lib/Djex
