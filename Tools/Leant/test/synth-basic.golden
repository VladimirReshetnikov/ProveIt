
   __                  __
  / /  ___ ___ ____   / /_
 / /__/ -_) _ `/ _ \_/ __/
/____/\__/\_,_/_//_/ \__/
Leant (Haskell) - a GHCi-style REPL for Lean 4.  Type :help for help, :quit to exit.
λ> :synth (A × (B ⊕ C) → (A × B) ⊕ (A × C))
preparing synthesis environment (session imports + Lean)...
  1  fun ⟨x, y⟩ => match y with | .inl z => .inl ⟨x, z⟩ | .inr w => .inr ⟨x, w⟩
(1 verified candidate)
:synth N binds candidate N as `it`
λ> :synth ((A → B → C) → (A → B) → A → C)
  1  fun f g x => f x (g x)
(1 verified candidate)
:synth N binds candidate N as `it`
λ> :synth (((A → B) → A) → A)
constructively unprovable — but classically:
  1  Classical.byContradiction (fun k => k (fun f => absurd (fun _ => f (fun a => absurd (fun _ => a) k)) k))
:synth N binds candidate N as `it`
λ> :synth (∀ a b : Type, a → b)
provably uninhabited — no closed term of this polymorphic type exists
λ> :synth (a → a → a)
  1  fun _ x => x
  2  fun x _ => x
(2 verified candidates)
:synth N binds candidate N as `it`
λ> :synth (Nat → Nat)
  1  Nat.succ
  2  fun x => x
  3  fun _ => Nat.zero
  4  fun _ => Nat.succ Nat.zero
  5  fun x => Nat.succ x
(5 verified candidates)
:synth N binds candidate N as `it`
λ> :synth (∀ p q : Prop, (p ↔ q) → ¬p → ¬q)
  1  fun _ _ ⟨_, f⟩ k x => k (f x)
(1 verified candidate)
:synth N binds candidate N as `it`
λ> :synth (((A → B) → A) → (A → A))
  1  fun _ x => x
(1 verified candidate)
:synth N binds candidate N as `it`
λ> :quit
