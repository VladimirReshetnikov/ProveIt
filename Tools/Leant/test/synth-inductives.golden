
   __                  __
  / /  ___ ___ ____   / /_
 / /__/ -_) _ `/ _ \_/ __/
/____/\__/\_,_/_//_/ \__/
Leant (Haskell) - a GHCi-style REPL for Lean 4.  Type :help for help, :quit to exit.
λ> :synth Bool
preparing synthesis environment (session imports + Lean)...
  1  .false
  2  .true
(2 verified candidates)
:synth N binds candidate N as `it`
λ> :synth Ordering
  1  .lt
  2  .eq
  3  .gt
(3 verified candidates)
:synth N binds candidate N as `it`
λ> :synth (∀ a : Type, Option a → a ⊕ Unit)
  1  fun _ _ => .inr ⟨⟩
  2  fun _ a => match a with | .none => .inr ⟨⟩ | .some b => .inl b
(2 verified candidates)
:synth N binds candidate N as `it`
λ> :synth (∀ a b : Type, a → b → a × Option b)
  1  fun _ _ a _ => ⟨a, .none⟩
  2  fun _ _ a b => ⟨a, .some b⟩
(2 verified candidates)
:synth N binds candidate N as `it`
λ> :synth (∀ a : Type, Option (Option a) → Option a)
  1  fun _ _ => .none
  2  fun _ a => match a with | .none => (.none) | .some b => b
(2 verified candidates)
:synth N binds candidate N as `it`
λ> :synth (∀ e a b : Type, Except e a → (a → b) → Except e b)
  1  fun _ _ _ a b => match a with | .error c => .error c | .ok d => .ok (b d)
(1 verified candidate)
:synth N binds candidate N as `it`
λ> :synth (∀ p : Prop, Decidable p → Decidable (¬ p))
  1  fun _ a => match a with | .isFalse b => .isTrue b | .isTrue c => .isFalse (fun d => d c)
(1 verified candidate)
:synth N binds candidate N as `it`
λ> :synth (∀ a b : Type, Option a → b)
provably uninhabited — no closed term of this polymorphic type exists
λ> inductive Traffic | red | amber | green
λ> 
λ> :synth Traffic
  1  .red
  2  .amber
  3  .green
(3 verified candidates)
:synth N binds candidate N as `it`
λ> structure Pair (A B : Type) where
…>   fst : A
…>   snd : B
…> 
λ> :synth (∀ a b : Type, a → b → Pair a b)
  1  fun _ _ a b => ⟨a, b⟩
(1 verified candidate)
:synth N binds candidate N as `it`
λ> :synth (∀ a b : Type, Pair a b → a)
  1  fun _ _ a => match a with | ⟨b, _⟩ => b
(1 verified candidate)
:synth N binds candidate N as `it`
λ> :synth (∀ a b : Type, Pair a b → Empty)
provably uninhabited — no closed term of this polymorphic type exists
λ> :synth (∀ a : Type, (Bool → a) → a)
  1  fun _ a => a (.false)
  2  fun _ a => a (.true)
(2 verified candidates)
:synth N binds candidate N as `it`
λ> :quit
